# 논문 포지셔닝 확정안 — 2026 한국자동차공학회 추계학술대회

2026-09-10 검증 결과를 반영한 개정판. 이전 확정안(비대칭 시간 맥락 증류 중심)은
학위논문 3장으로 이관하고, 학술대회 논문은 **활성 오버플로와 저정밀 배포 강건성** 축으로
바꾼다. 근거는 `thesis_topic_survey_20260910.md` §3.

---

## 1. 확정 제목

### 학술대회 (한국자동차공학회 추계학술대회)

**「지식 증류가 비전-언어 모델의 fp16 표현 범위 여유에 미치는 영향:
자율주행 VLM 사례 연구」**

### 석사 학위 논문

*Feature Alignment Shapes Representable-Range Headroom
in Distilled Vision-Language Models*

> **2026-09-11 1차 확정 → 2026-09-14 어감 하향 확정.** INT8 실험 결과(§3)로
> "표현 범위(절대 크기)"와 "동적 범위(이상치 비율)"가 분리되는 축이 데이터로 확인되어
> 후보 B로 확정했다. 이전 제목(「…활성 오버플로와 특징 정렬 증류를 통한 완화」)은
> 폐기한다 — 오버플로 현상 자체는 vLLM Issue #40290·PASA에 선례가 있어 전면에
> 세울 수 없다.
>
> **09-14 하향 이유** — 원래 제목의 *Governs*(「손실 설계가 **좌우하는**」)는 세 결과와
> 충돌한다. (i) **fixA**: 사후 가중치 재스케일만으로 붕괴가 94.4% → 0.0%가 되므로
> `L_align`이 유일한 경로가 아니다. (ii) **B(정렬 대상 ablation)**: 정렬 대상을 7B 교사
> 대신 3B 자기 자신으로 바꿔도 효과의 51%가 재현되므로, 효과의 절반은 `L_align`의 특정
> 설계가 아니라 정렬 제약 일반에서 온다. (iii) 5-way 정확도 순서는 통계적으로 뒷받침되지
> 않는다. *Shapes*는 "형태를 만든다"이지 "혼자 결정한다"가 아니므로 세 결과와 모두
> 양립한다. 학회 제목의 「미치는 영향」과 「사례 연구」도 같은 이유로 범위를 명시한다.

> **"아무도"라고 쓰지 않는 이유 (2026-09-15).** 근거가 둘인데 둘 다 절대 주장을
> 받치지 못한다. (i) 공개 저장소 197개 **config** 조사 — config는 애초에 활성 통계를
> 적는 곳이 아니므로 거기 없는 것은 증거가 아니다. (ii) arXiv 전문 검색 몇 건이 0 반환 —
> 검색어 몇 개의 부재는 문헌 전체의 부재가 아니다. 따라서 **"조사한 범위에서 보고 사례를
> 찾지 못했다"**로 쓴다. 또한 TensorRT 등 배포 도구가 fp16 빌드에서 자동으로 오버플로를
> 회피할 가능성은 **아직 조사하지 않았다** — 7장 7.2의 정밀도 3조건 비교가 답할 질문이다.

**선정 근거**
- **"표현 범위 여유"가 본 연구가 여는 축이다.** 압축 문헌은 전부 스케일 불변량(이상치
  비율, 양자화 SNR)으로 모델을 평가하는데, 그 지표는 붕괴하는 모델과 무사한 모델을
  구별하지 못한다(§3의 blk31 측정). 제목이 그 구분을 담아야 한다.
- 현상(오버플로)이 아니라 **영향 요인(손실 설계)**을 전면에 세워 선행 연구와 충돌하지
  않는다. ("결정 요인"이라고 쓰지 않는 이유는 §1의 하향 사유 참조 — fixA와 B가
  `L_align`이 유일한 경로가 아님을 보였다.) CrAFT는 optimizer 축, 이상치 억제 정규화 계열은 설계된 처방, PASA·vLLM은
  아키텍처 기인 현상이다.
- 정확도 이득이 작다(~1.4pp)는 약점이 논지에 영향을 주지 않는다. 주장하는 것이
  정확도가 아니라 배포 여유이기 때문이다.

---

## 2. 손실 명칭 (이전 결정 유지)

| 기존 | 변경 | 사유 |
|---|---|---|
| `L_spatial` | `L_align` (특징 정렬 손실) | ICLR 2026 논문의 `L_spatial`과 명칭·역할이 거의 동일 |
| `L_temporal` | `L_atc` (Asymmetric Temporal Context) | 동명 손실이 존재하나 방향이 반대 |

```
L_total = L_task + λ_align · L_align + λ_atc · L_atc
```

본 논문의 주역은 `L_align`이고 `L_atc`는 변형 비교의 한 축으로만 등장한다.

---

## 3. 기여 주장 수위

> **2026-09-10 선행연구 재검토 반영.** 아래 등급은 §4의 검토 결과에 맞춰 하향 조정했다.
> "미세조정 방식이 압축 친화성을 좌우한다"는 명제 자체는 CrAFT 계열이 선점하고 있으므로
> **발견이 아니라 축 확장과 정량화**로 서술한다. 대신 arXiv 전문 검색에서
> `"activation magnitude" AND "fp16"` 0건, `"knowledge distillation" AND "activation outliers"`
> 0건으로, **증류 손실의 종류를 통제 비교해 활성 크기를 재는 축은 비어 있다.**

### [중—핵심] 증류 손실의 종류가 비전 인코더 활성 크기를 좌우한다

동일 아키텍처·데이터·LoRA 구성·최적화 설정·학습 스텝에서 **손실 항만 바꿨을 때**
비전 인코더 중간 계층(blk24)의 최대 활성값이 8,096 대 130으로 **약 60배** 갈린다.
INT4 체크포인트뿐 아니라 FP16 체크포인트에서도 동일하게 나타나므로 양자화의 산물이
아니라 학습된 가중치 자체의 성질이다.

주의: 베이스 모델(미세조정 없음)이 8,256이므로 `L_align`은 "값을 억제"하고 과제 손실
단독 미세조정은 베이스 수준을 유지한다. 마지막 블록에서는 반대로 과제 손실 단독이
33,792(베이스) → 50,176으로 **키운다**.

### [중—핵심] 그 크기가 저정밀 배포의 성패를 가른다 (단, 사후 수정도 가능하다)

> **2026-09-11 수정.** fixA 실험(§ 아래)에서 **학습 없이 가중치 편집만으로 붕괴를 0%로
> 만들 수 있음**이 확인되었다. 따라서 "손실 설계가 배포 가능성을 **결정한다**"는 표현을
> 쓰지 않는다. 정확한 진술은 **"손실 설계가 그 여유를 자동으로 주며, 그렇지 않은 모델도
> 사후 수정이 가능하지만 **조사한 범위에서 그 여유를 보고한 사례가 없어** 방치되어 있다"**이다.

마지막 블록 활성이 fp16 표현 한계(65,504)를 넘는 학생은 입력 해상도에 따라 최대
**94.8%**의 이미지에서 수치 발산 → 생성 붕괴를 일으킨다. `L_align`을 쓴 학생은 모든
해상도에서 **0%**.

**해상도 × 붕괴율 (2026-09-10 측정, 250장, 그림 2)**

| 모델 | 128 | 256 | 512 | 1,024 | 1,836 tok | 원본 해상도 fp16 여유 |
|---|---|---|---|---|---|---|
| task CE only | 0.8% | 3.2% | 10.4% | 25.6% | **94.8%** | 0.88× (한계 초과) |
| 사전학습 (미세조정 없음) | 0 | 0 | 0 | 0 | **69.6%** | 0.95× |
| task + `L_atc` | 0 | 0.4% | 0 | 0 | **28.8%** | 1.06× |
| task + 출력 KD | 0 | 0 | 0 | 0 | **20.4%** | 1.10× |
| **task + `L_align`** | 0 | 0 | 0 | 0 | **0.0%** | **2.89×** |
| **task + `L_align` + `L_atc`** | 0 | 0 | 0 | 0 | **0.0%** | **2.92×** |

원본 해상도(1,836 토큰)에서 `L_align`을 쓴 둘만 붕괴하지 않는다. 따라서 "여유가 크다"가
아니라 **"이 조건에서는 `L_align`이 붕괴 회피의 필요조건"**으로 서술할 수 있다.

**단, "미세조정이 안전한 모델을 망친다"고 쓰지 않는다** — 사전학습 모델도 69.6% 붕괴한다.
정확한 서술은 여유 비율 기준이다: 사전학습 0.95배(이미 여유 없음) → 과제 손실 단독
0.88배(남은 여유마저 소진) → 증류 항 하나라도 있으면 1.06~1.10배 → `L_align` 2.89~2.92배.
논지는 **"사전학습 상태부터 여유가 없는데 보고하는 곳이 없으며, 손실 설계만이 그 여유를
만든다"**이다.
붕괴는 1,024 → 1,836 구간에서 급격히 일어나 **안전 해상도에 절벽이 존재**한다.
 붕괴는 이미지 단위로 갈리며 같은 장면의 연속 키프레임에서 뭉치는 성질이 있어,
흩어진 오류보다 주행 시스템에 불리하다.

주의: **fp16 비전 타워 오버플로 붕괴라는 현상 자체는 새롭지 않다** — vLLM의 Gemma 4
이슈(#40290)가 동일 메커니즘(비전 타워 fp16 캐스팅 → −inf → NaN → 전 이미지에 `<pad>`)을
이미 보고했다. 다만 그 사례는 SigLIP의 **고정 상수**(`std_bias`)가 원인이라 모든
체크포인트에 동일하게 적용된다. 본 연구가 보이는 것은 **같은 아키텍처·같은 base에서
학습 손실만 다른 두 체크포인트 중 하나만 붕괴한다**는, 학습이 만든 성질이다.


### [중—핵심] 표현 범위와 동적 범위는 분리된다 — 기존 지표가 이 축을 보지 못한 이유

INT8 활성 양자화 오차를 함께 측정한 결과(200장, `eval_results/int8_quant_error.json`),
`L_align`의 효과가 **층 깊이에 따라 성질이 다르다**는 것이 드러났다.

| 층 | 모델 | max | max/rms | INT8 SNR (per-channel) |
|---|---|---|---|---|
| blk24 | task CE only | 8,128 | **316** | 31.5 dB |
| blk24 | task+align+atc | 130 | **32** | **44.0 dB** |
| blk31 | task CE only | 48,640 | **153** | **40.3 dB** |
| blk31 | task+align+atc | 18,048 | **168** | 39.4 dB |

- **중간층(blk24)**: 절대 크기(62배)와 이상치 비율(10배)을 함께 낮추고 per-channel INT8
  SNR이 12.5 dB 개선된다. 기존 이상치 문헌(CrAFT, Colinearity Decay)과 일관된 결과다.
- **마지막 블록(blk31)**: 절대 크기는 2.7배 다른데 **이상치 비율은 사실상 같고
  (153 대 168) INT8 SNR은 오히려 baseline이 근소 우위**(40.3 대 39.4 dB)다.

**fp16 붕괴를 결정하는 것은 blk31이다.** 즉 그 층에서 `L_align`의 효과는 순수하게 절대
크기에만 있으며, **INT8 지표(max/rms, SNR)는 붕괴하는 모델과 무사한 모델을 구별하지
못한다.** 이것이 "왜 선행 연구가 이 축을 찾지 못했는가"에 대한 답이다 — 문헌 전체가
스케일 불변량으로 모델을 평가하기 때문이다.

따라서 **INT8 벤치마크를 통과한 모델이 fp16 캐스팅 한 번에 죽을 수 있다**는 예측이
성립하며, `student_baseline_v2`가 그 존재 증명이다(blk31 INT8 SNR 최상위, fp16 붕괴율 94.8%).

부수 결과: 중간층 per-channel INT8 SNR 12.5 dB 개선은 실무적으로 유의미하다.
본 논문의 주 논지는 아니나 `L_align`의 이점으로 한 문단 보고할 가치가 있다.

### [중] 정밀도 비교 실험의 교란 변수 보고

저장 dtype과 입력 해상도만으로 정밀도 비교의 결론이 뒤집힌다. 본 연구에서 실제로
INT4 정확도 1.39%가 관측되었으나, 원인은 양자화가 아니라 (i) AWQ 저장본 config의
`torch_dtype: float16` 때문에 양자화 대상이 아닌 비전 타워가 fp16으로 실행된 것과
(ii) 저장본에 딸린 processor의 기본 해상도가 평가 해상도보다 훨씬 컸던 것이었다
(`max_pixels` 명목 64배이나 원본 1600×900에서 포화하므로 유효 배수는 약 7.2배,
vision token 256 → 1,836).
재현 가능한 실무 경고로서 보고 가치가 있다.

**일반성 확인 (2026-09-10)**: 이것이 본 연구의 아티팩트인지 도구 사슬의 성질인지 가리기 위해
HuggingFace 공개 양자화 VLM 체크포인트 197개의 config를 조사했다. `torch_dtype: float16`과
비전 타워 양자화 제외를 **동시에** 만족하는 — 즉 비전 타워가 fp16으로 실행되는 — 구성이
**27개(14%)**였고, 여기에 제작사 공식 저장소 `Qwen/Qwen2-VL-{2B,7B,72B}-Instruct-AWQ`가
포함된다. 특히 7B는 `max_pixels`까지 12,845,056으로 본 연구의 조건과 동일하다.
또한 조사 대상의 28%가 `max_pixels > 4M`(대부분 12,845,056)로, 학습 시 해상도와 무관한
기본값을 그대로 저장하고 있었다.

다만 붕괴는 활성 크기가 큰 체크포인트에서만 발생하므로 **"27개가 모두 붕괴한다"고 주장하지
않는다.**

**실측 확인 (2026-09-14).** 위험 구성으로 분류된 공식 저장소 `Qwen/Qwen2-VL-2B-Instruct-AWQ`를
직접 프로파일한 결과 fp16 여유가 **3.88배로 안전**했다. 다른 계열인 SmolVLM(Idefics3)은
**50.86배**였다. 반면 본 연구의 `base_3b`(Qwen2.5-VL-3B)는 같은 조건에서 0.95배다.
즉 **여유는 계열은 물론 같은 계열의 세대·크기에 따라서도 크게 갈리며, 위험 구성은
필요조건이지 충분조건이 아니다.**

따라서 주장하는 것은 **"위험 구성이 드물지 않고 공식 배포본에도 존재하지만, 실제 위험
여부는 측정해야만 알 수 있는데 그 측정을 아무도 하지 않는다"**이다. 이는 본 논문의 처방
("배포 전에 표현 범위 여유를 측정하라")과 정확히 일치한다.

### [중] 통제된 다중 변형 비교 인프라

손실 항만 다르고 데이터·LoRA·optimizer·스텝 수·평가 프로토콜을 전부 고정한 비교 설계.
위 결론은 이 통제가 없으면 성립하지 않는다.

### [약 — 이번 논문에서 참신성 주장 안 함] 비대칭 시간 맥락 증류 `L_atc`

교사가 K프레임, 학생이 현재 1프레임만 보는 압축 방향의 증류.
**SCPNet [2303.06884, CVPR 2023]이 자율주행에서 동일 발상(멀티프레임 교사 → 단일프레임
학생, 배포 시 추론 비용 불변)을 이미 제시했다** — 모달리티(LiDAR)와 과제(시맨틱 장면
완성)만 다르다. 따라서 발상 자체의 참신성은 주장할 수 없고, 카메라 기반 생성형 VLM으로의
이식으로만 서술한다. 학위논문 3장의 실험 기반이며 본 논문에서는 변형 비교의 한 축으로만 쓴다. 마찬가지로 `L_align`의 구조(projection +
거리 손실) 자체도 FitNets 계열의 확립된 방법이므로 참신성을 주장하지 않는다.
**주장하는 것은 그 손실이 활성 크기에 미치는 부수 효과와 그 배포상 함의다.**

---

## 4. 관련연구 차별화 문단 (본문 삽입 초안)

2026-09-10 전면 재검토 결과를 반영한 개정판. **초판은 "미세조정 방식이 저정밀 강건성을
좌우한다"를 본 연구의 발견처럼 서술했으나, 이는 선행 연구가 있어 삭제했다.**

### 4.1 검토로 확인한 선행 연구

| 계열 | 대표 문헌 | 본 연구와의 관계 |
|---|---|---|
| 미세조정 방식 → 압축 친화성 | CrAFT (arXiv:2305.04526) | **선점.** sharpness 최소화로 미세조정하면 PTQ·pruning 손실이 크게 준다. 변수가 **optimizer 기하**이고 대상은 ViT 분류. |
| 학습 시 이상치 억제 | Colinearity Decay (2605.01330), RoLoRA (2407.08044), RoSTE (2502.09003), QuantTune (2403.06497) | **양자화를 목적으로 설계한 정규화.** 본 연구의 `L_align`은 지식 전이용이며 활성 억제는 부수 효과. |
| 거대 활성 현상 | Massive Activations (2402.17762) 및 후속 25편 이상 | 현상·원인·완화를 다루나 **미세조정/증류가 크기를 바꾸는지는 측정하지 않음**. |
| 활성 크기의 모델 간 편차 | Measuring Maximum Activations (2605.15572) | 모델 계열별로 네 자릿수 차이를 **문서화**했으나 원인을 학습에서 찾지 않음. |
| 비전 인코더 이상치 양자화 | RegCache (2510.04547), Small VLM Quantization (2607.08029) | 비전 인코더 중간층 이상치를 다루나 **서로 다르게 미세조정한 체크포인트를 비교하지 않음**. |
| fp16 추론 오버플로 | PASA (2503.01873), vLLM Issue #40290 | **현상 선점.** 원인을 아키텍처·사전학습(고정 상수, attention bias)에서 찾으며 알고리즘적 완화를 제안. |
| 증류 → 양자화 강건성 (자동차) | Edge AI for VRU Safety (2604.26857) | 증류 student가 INT8에서 teacher보다 강건함을 관측. **손실 종류를 비교하지 않고 메커니즘 분석 없음**. 객체 검출. |
| 멀티프레임 교사 → 단일프레임 학생 | **SCPNet (2303.06884, CVPR 2023)** | **`L_atc`의 선행 연구.** 자율주행에서 멀티프레임 교사의 시간 지식을 단일프레임 학생에 증류(Dense-to-Sparse KD). 모달리티(LiDAR)와 과제(시맨틱 장면 완성)만 다르다. |
| 주행 VLM 증류 | Drive-KD | 동일 교사–학생 조합(Qwen2.5-VL-7B→3B)이나 attention 정렬에 초점, 배포 정밀도 미고려. |

### 4.2 비어 있는 축 (arXiv 전문 검색으로 확인)

- `"activation magnitude"` AND `"fp16"` → **0건**
- `"knowledge distillation"` AND `"activation outliers"` → **0건**

따라서 본 연구가 주장하는 것은 다음 셋으로 한정한다.

1. **증류 손실의 종류**를 통제 비교해 활성 크기를 정량화한 것
2. **절대 크기(표현 범위 여유)**와 **이상치 비율(동적 범위)**을 분리해 측정한 것 —
   기존 문헌은 전부 후자만 본다
3. 그 크기가 **생성형 주행 VLM에서 출력 전면 붕괴**로 이어짐을 이미지 단위로 정량화한 것

### 4.3 본문 삽입 문단 초안

> 미세조정 방식이 사후 압축의 성패에 영향을 준다는 점은 CrAFT [인용]가 sharpness 최소화
> 목적함수를 통해 이미 보였으며, 학습 단계에서 활성 이상치를 억제하는 정규화 계열
> [인용: Colinearity Decay, RoLoRA, RoSTE]도 제안되어 왔다. 다만 이들은 모두 **양자화
> 친화성을 목적으로 설계된 기법**이며, 대상은 영상 분류용 ViT 또는 언어모델이다. 본
> 연구는 지식 전이를 목적으로 선택한 **증류 손실이 부수적으로** 학생 비전 인코더의 활성
> 크기를 좌우함을 보인다는 점에서 구별된다.
>
> 거대 활성(massive activation) 자체는 널리 보고된 현상이며 [인용: 2402.17762 외],
> 모델 계열에 따라 활성 크기가 네 자릿수까지 차이 난다는 측정도 존재한다 [인용:
> 2605.15572]. 그러나 이들 연구는 활성 크기를 아키텍처와 사전학습의 산물로 취급하며,
> **동일 아키텍처를 서로 다른 목적함수로 미세조정했을 때 크기가 달라지는지는 측정하지
> 않았다.**
>
> 반정밀도 추론에서의 수치 오버플로 역시 알려진 문제로, PASA [인용]는 attention 입력의
> 큰 진폭을 원인으로 지목하고 알고리즘적 완화를 제안하였다. 그러나 해당 연구가 다루는
> 원인은 아키텍처와 사전학습에 기인한 고정된 성질이며, 본 연구가 보이는 것은 **같은
> base 모델을 서로 다른 손실로 학습한 두 체크포인트 중 하나만 붕괴한다**는, 학습이 만든
> 차이다.
>
> 시간 정보의 비대칭 증류 자체도 선행 사례가 있다. SCPNet [인용]은 멀티프레임 교사의
> 시간적 의미 지식을 단일프레임 학생에 전이하여 배포 시 추론 비용을 늘리지 않는 구조를
> 제시하였다. 본 연구의 `L_atc`는 그 구조를 LiDAR 기반 시맨틱 장면 완성이 아니라 카메라
> 기반 생성형 VQA로 옮긴 것이며, 본 논문에서 참신성을 주장하지 않는다.
>
> 자율주행 영역에서는 증류한 소형 모델이 교사보다 INT8 양자화에 강건하다는 관측이
> 보고된 바 있으나 [인용: 2604.26857], 손실 종류를 통제 비교하지 않았고 메커니즘 분석도
> 제시하지 않았다. 주행 VLM 증류에서 Drive-KD [인용]는 본 연구와 동일한 교사–학생
> 조합을 사용하나 텍스트–비전 attention 정렬에 초점을 두며 배포 정밀도는 고려하지 않는다.

---

## 5. 초록 (확정)

### 국문

> 자율주행용 시각-언어 모델(VLM)은 장면 이해와 자연어 설명을 함께 제공하지만, 차량
> 탑재를 위해서는 모델 경량화와 저정밀 연산이 함께 요구된다. 본 연구는 저정밀 배포의
> 성패가 **순전파 한 번으로 계산되는 값 하나 — 표현 범위 여유 `format_max ÷
> max|activation|` — 로 사전에 갈리는데도, 압축 평가의 표준 지표에는 그 값이 없다**는
> 점을 보인다.
> 데이터, LoRA 구성, 최적화 설정, 학습 스텝을 모두 고정하고 손실 항만 달리한 학생
> 모델들을 DriveLM 검증 키프레임 799장에 대해 계층별로 측정하였다. 그 결과 과제 손실
> 만으로 미세조정한 학생은 비전 인코더 중간 계층의 최대 활성값이 수천 단위에 이른 반면,
> 교사 특징과의 방향 정렬 손실을 함께 사용한 학생은 같은 계층에서 약 60분의 1 수준에
> 머물렀다. 이 차이는 양자화 여부와 무관하게 학습된 가중치 자체에서 나타났다. 마지막
> 계층에서는 정렬 손실을 쓰지 않은 학생의 활성값이 사전학습 모델 대비 1.4배까지 증가하여
> fp16 표현 한계를 넘어섰고, 입력 해상도에 따라 최대 94.8%의 이미지에서 수치 발산과 생성
> 붕괴가 발생하였다. 원본 해상도에서는 정렬 손실을 사용하지 않은 모든 변형이 20~95%의
> 붕괴율을 보인 반면, 정렬 손실을 사용한 학생은 모든 해상도에서 붕괴가 관찰되지 않았다.
> 아울러 INT8 활성 양자화 오차를 함께 측정한 결과, 붕괴를 결정하는 최종 계층에서는 두
> 모델의 이상치 비율과 양자화 신호대잡음비가 사실상 동일하여 **기존 압축 지표가 이 차이를
> 구별하지 못함**을 확인하였다. 즉 본 연구가 다루는 것은 동적 범위가 아니라 **표현 범위
> 여유**라는 별개의 축이다.
> 아울러 해당 계층의 곱셈 이전 지점에 가중치 스케일링을 적용하면 학습 없이도 붕괴를
> 제거할 수 있음을 보였다. 즉 이 활성값은 **기능에 기여하지 않으면서 배포 위험만 만드는
> 값**이며, 증류 손실은 그 여유를 부수적으로 확보해 준다.
> 본 결과는 경량화 VLM의 배포 강건성이 양자화 기법만의 문제가 아니라 그 앞 단계인 증류
> 손실 설계와 밀접함을 시사한다. 아울러 정밀도 비교 실험에서 저장 dtype과 입력
> 해상도가 그 자체로 결론을 뒤집을 수 있음을 함께 보고한다.

```
Key Words : Activation magnitude(활성 크기), Autonomous driving(자율주행),
            Feature-alignment distillation(특징 정렬 증류), Knowledge distillation(지식증류),
            Low-precision inference(저정밀 추론), Representable-range headroom(표현 범위 여유),
            Vision-language model(시각-언어 모델)
```

알파벳순 6개. 「모델 경량화」는 제목에 이미 있어 제외했고, 7개까지 허용되면
`Model lightweighting(모델 경량화)`를 추가한다. `Low-precision inference` 대신
`Quantization(양자화)`을 쓸 수도 있으나, 본 논문의 결론이 "양자화 기법의 문제가 아니다"
이므로 저정밀 추론 쪽이 논지와 일치한다(검색 노출을 우선하면 교체 가능).

### 영문

> Vision-language models (VLMs) for autonomous driving provide scene understanding
> together with natural-language explanation, but on-vehicle deployment requires both
> model compression and reduced-precision arithmetic. This study shows that, when
> distilling a Qwen2.5-VL-7B teacher into a 3B student, **the choice of training loss
> governs the magnitude of the student's vision-encoder activations, and this magnitude
> determines whether the model survives reduced-precision deployment**. Holding the data,
> LoRA configuration, optimizer settings, and number of training steps fixed while varying
> only the loss terms, we measured layer-wise activations over 799 DriveLM validation
> keyframes. A student fine-tuned with the task loss alone reached maximum activations on
> the order of thousands in the intermediate layers of the vision encoder, whereas a
> student additionally trained with a directional feature-alignment loss against the
> teacher stayed roughly sixty times lower at the same layer. This gap appears in the
> trained weights themselves, independently of quantization. In the final block, the
> task-only student's activations grew to 1.5x those of the pre-trained model, approaching
> the fp16 representable limit, and produced numerical divergence and generation collapse
> on up to 94.8% of images depending on input resolution. At native input resolution every
> variant trained without the alignment loss collapsed on 20-95% of images, whereas the
> alignment-trained students showed no collapse at any resolution. These
> We further show that scaling the weights **before** the elementwise product inside that
> block removes the collapse entirely without any training (94.4% to 0.0% of images, with
> accuracy unchanged), whereas scaling the block output does not. The activation is
> therefore a quantity that **contributes nothing to function while consuming the entire
> representable-range budget**. These results indicate that the deployment robustness of
> compressed driving VLMs is closely tied to the distillation stage rather than to the
> quantization method alone.
> Measuring INT8 activation-quantization error alongside, we find that at the final block
> — the layer that governs the collapse — the outlier ratio and quantization SNR of the two
> students are effectively identical, so **existing compression metrics cannot distinguish
> them**: the axis at stake is representable-range headroom, not dynamic range. We
> further report that stored dtype and input resolution can by themselves reverse the
> conclusions of a precision-comparison experiment.

**Key Words** (제출 서식은 국문 항목의 `Key Words :` 줄을 그대로 사용):
Activation overflow, Autonomous driving, Feature-alignment distillation,
Knowledge distillation, Low-precision inference, Vision-language model

### λ 스윕 완료 시 삽입할 문장 (2026-09-12 예정, 현재 미관측)

초록에서 "이 차이는 양자화 여부와 무관하게 학습된 가중치 자체에서 나타났다." 뒤에 넣는다.
`_orchestrate_lambda_sweep.sh`가 λ_align ∈ {0, 0.5, 1.0, 2.0} × 5,000스텝을 돌고 있으며,
결과에 따라 둘 중 하나를 쓴다. **결과 확인 전에는 어느 쪽도 넣지 않는다.**

**실측 확정 (2026-09-12).** 아래 문장을 사용한다.

> 정렬 손실 가중치를 0, 0.5, 1.0, 2.0으로 변화시키고 시드와 학습률 궤적을 고정한 통제
> 실험에서, 표현 범위 여유를 결정하는 최종 계층의 활성값이 36,608에서 14,208로 **단조
> 감소**하였다(fp16 여유 1.79배 → 4.61배). 구간별 감소율이 28.7 / 27.0 / 25.5%로 일정하여
> 뚜렷한 용량-반응을 보인다. 다만 중간 계층은 가중치 1.0에서 포화하여, 동일한 손실이
> 계층에 따라 다르게 작용함을 함께 관찰하였다.

영문:

> In a controlled sweep of the alignment-loss weight (0, 0.5, 1.0, 2.0) with the seed and
> learning-rate trajectory held fixed, activation magnitude at the final block — the layer
> that governs representable-range headroom — decreased **monotonically** from 36,608 to
> 14,208 (fp16 headroom 1.79x to 4.61x), with per-interval reductions of 28.7 / 27.0 /
> 25.5%. Intermediate layers, by contrast, saturate at a weight of 1.0, so the same loss
> acts differently at different depths.

### 분량 압축본 (초록 입력란이 짧을 경우)

> 자율주행용 시각-언어 모델의 차량 탑재에는 경량화와 저정밀 연산이 함께 요구된다. 본
> 연구는 저정밀 배포의 성패가 **순전파 한 번으로 계산되는 표현 범위 여유
> (`format_max ÷ max|activation|`)로 사전에 갈리는데도, 압축 평가의 표준 지표에는 그 값이
> 없다**는 점을 보인다. 데이터·LoRA·최적화
> 설정을 고정하고 손실 항만 달리한 학생들을 DriveLM 검증 키프레임 799장에서 계층별로
> 측정한 결과, 과제 손실만 쓴 학생은 비전 인코더 중간 계층 최대 활성값이 수천 단위인
> 반면 교사 특징과의 방향 정렬 손실을 함께 쓴 학생은 약 60분의 1이었다. 전자는 마지막
> 계층에서 fp16 표현 한계를 넘어 원본 해상도에서 94.8%의 이미지에서 생성이 붕괴하였고,
> 후자는 모든 해상도에서 붕괴하지 않았다. 아울러 해당 계층의 곱셈 이전 지점에 가중치
> 스케일링을 적용하면 학습 없이도 붕괴가 제거되며 정확도 비용은 0.59%p에 그쳐, 이 활성값이
> **기능에 기여하지 않으면서 표현 범위를 소모하는 값**임을 확인하였다. 경량화 VLM의 배포
> 강건성이 양자화 기법이 아니라 그 앞 단계의 증류 손실 설계와 밀접함을 시사한다.

---

## 6. 예상 질문 대응

**Q. 결국 fp16 버그 아닌가? bf16을 쓰면 해결되는데 왜 연구인가?**
> bf16이 항상 선택지는 아닙니다. 일부 추론 엔진과 차량용 가속기, 그리고 양자화 툴체인이
> fp16을 기본 또는 고정으로 쓰며, 실제로 본 연구가 쓴 AWQ 저장 경로가 config에
> `torch_dtype: float16`을 기록합니다. 더 중요한 것은 dtype 선택과 무관하게 **활성 크기
> 자체가 목적함수로 60배 갈린다**는 사실이며, 이는 fp16뿐 아니라 활성 양자화(W8A8 등)와
> 이상치 처리 전반의 난이도를 좌우합니다.

**Q. massive activation은 이미 알려진 현상 아닌가?**
> 현상 자체는 알려져 있습니다. 본 연구가 새로 보이는 것은 동일 아키텍처에서 **학습
> 목적함수만으로 그 크기가 갈린다**는 점과, 그 차이가 배포 정밀도에서 성패로 직결된다는
> 점입니다.

**Q. 교사와 정렬했다면 교사 크기가 되어야 하는데, 교사가 오히려 더 높지 않은가?**
> 맞습니다. 교사의 같은 계층 활성값은 9,024로 오히려 더 큽니다. `L_align`은 코사인
> 손실이라 **방향만** 제약하고 크기는 제약하지 않습니다. 따라서 활성 감소는 "교사에 맞춘
> 결과"가 아니라 정렬 학습의 부수 효과이며, 본문에서도 그렇게 서술합니다.

**Q. 상관 아닌가?**
> 인과를 두 링크로 나눠 각각 다른 실험이 담당합니다.
> **① λ → 활성 크기**: 정렬 손실 가중치를 0 / 0.5 / 1.0 / 2.0으로 두고 시드와 학습률
> 궤적을 고정한 채 각각 5,000 스텝 학습하는 통제 실험. 결과 변수는 **연속량인 활성 크기**입니다.
> **② 활성 크기 → 붕괴**: 완주 모델 6개 × 5해상도에서 이미 확립했습니다(그림 2).
> 두 링크가 같은 모델에서 동시에 측정될 필요는 없습니다.
>
> ①의 실측(2026-09-12): λ = 0 / 0.5 / 1.0 / 2.0에서 최종 블록 활성이
> 36,608 → 26,112 → 19,072 → 14,208로 **단조 감소**하며, fp16 여유가 1.79 → 4.61배로
> 늘어납니다. 구간별 감소율이 28.7 / 27.0 / 25.5%로 일정해 포화 조짐이 없습니다.
> 중간 계층은 λ=1.0에서 포화하므로, 층에 따라 반응이 다르다는 점을 함께 보고합니다.

**Q. CrAFT가 이미 미세조정 방식이 압축 강건성을 바꾼다고 했는데?**
> 맞습니다. 그 명제 자체를 본 연구의 발견으로 주장하지 않습니다. CrAFT의 변수는
> optimizer 기하(sharpness)이고 대상은 ViT 분류입니다. 본 연구의 변수는 **증류 손실의
> 종류**이며, 그 효과가 양자화를 겨냥해 설계된 것이 아니라 **부수 효과**라는 점이
> 다릅니다. 실무 함의도 다릅니다 — "친화 기법을 추가로 쓰라"가 아니라 "정확도만 보고
> 고른 증류 설계가 배포 가능성을 이미 결정하고 있다"입니다.

**Q. fp16 비전 타워 오버플로는 vLLM 이슈로 이미 알려진 버그 아닌가?**
> 현상은 알려져 있습니다. 다만 그 사례의 원인은 SigLIP의 고정 상수라 모든 체크포인트에
> 동일하게 적용됩니다. 본 연구는 **같은 아키텍처·같은 base에서 손실만 다른 두 체크포인트
> 중 하나만 붕괴한다**는 것을 보입니다. 즉 아키텍처의 성질이 아니라 학습이 만든 성질입니다.

**Q. 그냥 bf16으로 내보내면 되는 것 아닌가?** (위 첫 문항의 보강)
> 위 답변에 더해 두 가지. (i) 현재 압축 관행이 절대 활성 크기를 **아예 측정하지 않습니다**
> — 문헌은 전부 스케일 불변량인 이상치 비율만 봅니다. (ii) 따라서 INT8 벤치마크를 모두
> 통과한 모델도 fp16 캐스팅 한 번에 죽을 수 있으며, `student_baseline_v2`가 그 존재
> 증명입니다(blk31 INT8 SNR 최상위 40.3 dB, fp16 붕괴율 94.8%).

**Q. `L_align`은 결국 이상치 억제 기법 아닌가? Colinearity Decay 계열과 뭐가 다른가?**
> **작동하는 층이 다릅니다.** `L_align`이 이상치를 억제하는 것은 중간층입니다 —
> blk24에서 max/rms가 316에서 32로 낮아지고 per-channel INT8 SNR이 12.5 dB 개선됩니다.
> 그러나 그 층의 최대 활성은 **8,128로 fp16 한계 65,504의 12%**에 불과해, 애초에
> 오버플로와 무관합니다. NaN이 처음 발생하는 곳은 blk31이고, **그 층에서는 이상치 비율이
> 153 대 168로 사실상 같으며 INT8 SNR은 오히려 baseline이 근소 우위**(40.3 대 39.4 dB)입니다.
> 즉 `L_align`이 이상치 억제기로 작동하는 층과 fp16이 붕괴하는 층이 다릅니다.
> 이상치 억제 계열의 지표로는 이 차이를 볼 수 없습니다.
>
> **"그렇다면 중간층 억제가 하류로 전파되어 blk31을 낮춘 것 아닌가"**에 대해서는
> 전파를 두 형태로 나눠 답합니다.
>
> **크기 매개 전파는 기각됩니다.** ① 중간층 격차 33.6배가 blk31에서 2.8배로 줄어 92%가
> 소실되고, ② blk31 최댓값 좌표에서 잔차 기여는 0.2~0.9%뿐이며 나머지는 **마지막 블록
> MLP의 생성분**입니다(−46,848 대 −17,152). ③ blk30 출력을 1/34로 줄이는 개입에서
> blk31은 오히려 **커집니다** — 부호가 반대입니다.
>
> **다만 방향 매개 전파는 기각하지 않습니다.** `L_align`은 코사인 손실이라 방향을 바꾸고,
> `norm2` 출력 방향이 달라지면(cos 0.64) MLP 생성량이 5.7배 변합니다. 따라서 가장
> 그럴듯한 경로는 **중간층 표현의 방향 변화 → 마지막 블록 MLP 생성량 감소**입니다.
> 확실한 것은 **blk31의 값이 운반된 것이 아니라 그 블록에서 생성된다**는 점이며,
> "중간층과 무관하다"고는 주장하지 않습니다.

**Q. 사전학습 모델도 69.6% 붕괴하는데, 그러면 미세조정 탓이 아니지 않나?**
> 맞습니다. 그래서 "미세조정이 안전한 모델을 망친다"고 쓰지 않습니다. 정확한 서술은
> 여유 비율입니다 — 사전학습 모델이 **0.95배로 이미 여유가 없고**, 과제 손실 단독
> 미세조정이 **0.88배로 남은 여유마저 없애며**, `L_align`만이 **2.9배 여유를 만듭니다.**
> 논지는 "사전학습 상태부터 여유가 없는데 보고하는 곳이 없으며, 손실 설계만이 그 여유를
> 만든다"입니다.

**Q. λ 스윕에서 붕괴율 차이가 안 나오면 인과를 주장할 수 없는 것 아닌가?**
> 인과를 하나의 실험으로 세우지 않습니다. **① λ → 활성 크기**는 λ 스윕이(연속 변수),
> **② 활성 크기 → 붕괴**는 해상도 스윕이(완주 모델 6개 × 5해상도) 각각 담당합니다.
> 두 링크가 같은 모델에서 동시에 측정될 필요는 없습니다. 오히려 붕괴율은 30점 중 24점이
> 0%인 포화된 이진 변수라, 그것으로 용량-반응을 재는 것이 설계 오류입니다.

**Q. 활성이 크면 그냥 사후에 스케일을 줄이면 되지 않나?**
> **개입 지점에 따라 갈립니다. 올바른 지점을 고르면 됩니다.**
>
> | 개입 | 붕괴율 | 정확도 |
> |---|---|---|
> | 없음 | 94.4% | 53.33% |
> | MLP **출력**을 1/3 | 94.8% | 54.00% |
> | **gate·up를 각각 1/√3** (= 곱을 1/3) | **0.0%** | **49.96%** |
>
> 출력 스케일링은 실패합니다 — 오버플로가 `down_proj` 계산에서 이미 일어나므로 NaN을
> 나누는 셈입니다. 반면 **곱이 만들어지기 전에** 줄이면(gate·up의 **weight와 bias를 함께**
> 1/√3로 나누면 등가입니다) 곱이 4,588 → 1,530, `down_proj` 출력이 63,200 → 24,800이
> 되어 **붕괴가 0%가 됩니다.** 정확도는 50.55% → 49.96%로 **0.59%p 낮아집니다.**
>
> > **정정 (2026-09-14).** 이전 판은 300문항 결과(53.33% → 54.67%)를 근거로
> > "정확도는 변하지 않습니다"라고 적었습니다. **전체 11,309문항으로 재측정하니 부호가
> > 뒤집혔습니다.** 300문항에서는 노이즈가 ±0.7pp라 1.34pp 차이가 "개선"으로 보였던
> > 것입니다. 위 표의 정확도 열도 전체 측정값으로 교체했습니다(무개입 50.55%).
> > 이 0.59%p는 시드 노이즈가 아닙니다 — 같은 가중치에 결정론적 편집만 가한 짝지은
> > 비교라 학습 무작위성이 없습니다.
> >
> > **측정 조건**: 정확도는 평가 해상도(200,704)에서 쟀습니다. 그 해상도에서는 무개입도
> > 거의 붕괴하지 않으므로(3.2%) 이 수치는 **개입의 순수 비용**입니다. 원본 해상도에서는
> > 무개입이 94.4%의 이미지에서 출력을 내지 못하므로 비용은 상쇄되고도 남습니다.
>
> > **정정 (2026-09-14)**: 이전 판은 "bias가 없어"라고 적었으나 **틀렸다**. `Qwen2_5_VLVisionBlock`은 `Qwen2_5_VLMLP(config, bias=True)`로 생성하므로 비전 MLP에는 bias가 있다(`gate_proj.bias` 등이 실제 가중치에 존재한다). 훅은 `gate_proj`의 **출력**(`Wx+b`)을 스케일하므로, 등가인 가중치 편집은 **weight와 bias를 함께** 나누는 것이다. weight만 나누면 bias가 남아 다른 연산이 된다. 실험 결과 자체는 훅으로 측정했으므로 영향이 없고, **처방 서술만 정정**한다.
>
> 따라서 본 연구는 **"사후 수정이 불가능하다"고 주장하지 않습니다.** 주장하는 것은
> (i) 이 값이 **기능에 기여하지 않으면서 배포 위험만 만든다**는 것, (ii) 손실 설계가
> 그 여유를 **자동으로** 준다는 것, (iii) 그럼에도 **조사한 범위에서 보고 사례가 없어** 공개 체크포인트의
> 14%가 위험 구성으로 방치되어 있다는 것입니다. 수정법이 있다는 사실은 오히려 실무
> 기여를 강화합니다 — 위험 구성 27개에 바로 적용 가능한 처방이 됩니다.

> **실측 후속 (2026-09-14 21:22, `scripts/probe_fixa_weight_equiv.py`, 250장 fp16)** —
> 네 조건을 직접 재서 위 정정의 실제 영향을 확인했다.
>
> | 조건 | 붕괴율 |
> |---|---|
> | 무개입 | 95.2% |
> | 훅 fixA (원래 실험) | **0.0%** |
> | 가중치 fixA (weight+bias) | **0.0%** ← ONNX로 내보낸 판본 |
> | 가중치 fixA (weight만) | **0.0%** |
>
> **`weight`만 편집해도 결과가 같다.** 이 스케일에서는 bias 기여가 붕괴 여부를 바꾸지
> 않는다. 따라서 정정 전 서술의 **결론**("가중치를 1/√3로 나누면 된다")은 틀리지 않았고,
> 틀린 것은 **근거**였다 — "bias가 없어서"가 아니라 "이 스케일에서 bias 기여가 작아서"다.
> 논문에는 근거를 이렇게 정확히 쓰고, **일반적으로는 weight와 bias를 함께 나눠야 훅과
> 등가**임을 함께 적는다(다른 스케일·다른 층에서는 갈릴 수 있다).
>
> 부수 소득: 이 실험이 **보드 측정의 전제를 닫았다.** Orin에 보낸 fixA ONNX가 A100에서
> 잰 것과 같은 개입임이 확인됐다.

---

### Q. 7B 교사의 표현이 특별해서 여유가 생기는 것인가? (B: 정렬 대상 ablation, 09-14)

> **아니요. 절반만 그렇습니다.** `L_align`의 정렬 대상을 7B 교사 대신 **3B 학생 자기
> 자신**으로 바꿔 다른 조건(5,000 step, λ=1.0, seed 1234)을 모두 고정하고 학습했습니다.
> 동일 250장 짝지어 비교(fp16, blk31 p50):
>
> | 변형 | 정렬 대상 | p50 | λ=0 대비 |
> |---|---|---|---|
> | `sweep_align0.0` | — | 36,480 | — |
> | **`aligntgt_base3b`** | **3B 자기 자신** | **27,664** | **−24.2%** |
> | `sweep_align0.5` | 7B 교사 | 25,560 | −29.9% |
> | `sweep_align1.0` | 7B 교사 | 19,232 | −47.3% |
> | `sweep_align2.0` | 7B 교사 | 14,464 | −60.4% |
>
> 같은 λ=1.0에서 **자기 정렬이 교사 정렬 효과의 51%를 재현**합니다. 사전 등록한 두 분기
> ("정렬 제약 자체가 원인" / "교사 표현이 필요")가 **모두 빗나갔고**, 정직한 해석은
> **두 요인이 대략 절반씩 기여한다**는 것입니다. 어느 한쪽으로 밀어 서술하지 않습니다.
>
> 부수 관찰: 3B 자기 정렬 λ=1.0이 7B 교사 정렬 λ=0.5와 거의 같은 자리에 옵니다. 교사
> 표현이 "특별한 무언가"라기보다 **더 강한 정렬 신호**로 작동함을 시사하나, 두 지점의
> 일치라 근거가 약하므로 **관찰로만 적고 주장으로 쓰지 않습니다**.
>
> 이 결과는 제목에서 `Governs`를 뺀 두 번째 근거입니다(§1). 효과의 절반이 `L_align`의
> 특정 설계가 아니라 **정렬 제약 일반**에서 오므로, "이 손실이 여유를 결정한다"고 쓸 수
> 없습니다. 결과: `eval_results/align_target_profile.json`

**Q. 그건 여러분이 양자화를 잘못한 것 아닌가?**
> 공개 양자화 VLM 체크포인트 197개를 조사한 결과, 비전 타워가 fp16으로 실행되는 구성이
> 27개(14%)였고 그중 셋은 제작사 공식 저장소입니다(`Qwen/Qwen2-VL-{2B,7B,72B}-Instruct-AWQ`).
> 사용자 설정 실수가 아니라 도구 사슬의 기본 동작입니다.

**Q. 정확도는 얼마나 개선되었나?**
> 증류 미적용 기준선 대비 1.4%p 이내로 작습니다. 더 정확히 말하면 **변형 간 우열을
> 주장하지 않습니다** — 5개 변형을 시드 고정 없이 한 번씩만 학습했으므로 전체 정확도의
> 0.35%p 차이는 시드 노이즈와 구별되지 않습니다. 본 논문의 주장은 정확도 개선이 아니라
> **저정밀 배포에서의 생존 여부**이며, 그 차이는 94.8% 대 0%로 노이즈 수준이 아닙니다.

---

## 7. 체크리스트

- [ ] **λ 스윕 결과 삽입** — `_orchestrate_lambda_sweep.sh` 진행 중(2026-09-10 06:30 시작,
      실측 6.98 s/step × 5,000스텝 × 4회 ≈ 39시간, 완료 예정 09-12). 초록에서 "단조 변화"
      문장은 **미관측이므로 일단 삭제**했고, 결과가 나오면 §5의 "λ 스윕 완료 시 삽입할
      문장" 블록에서 해당하는 쪽을 골라 넣는다.
- [ ] **5-way 정밀도 재평가** — dtype(bf16)과 해상도를 고정한 조건에서 다시 측정.
      현재 INT4 표는 교란이 섞여 있어 논문에 그대로 실을 수 없다.
- [ ] 그림 1: 계층별 활성 프로파일 + fp16 한계선 (`eval_results/vision_activation_profile.json`)
- [ ] 그림 2: 입력 해상도 × 붕괴율
- [ ] 표 1: 손실 구성별 blk24 / blk31 / 붕괴율
- [ ] 관련연구 인용 추가 — §4.1 표의 전체 목록:
      CrAFT(2305.04526), Colinearity Decay(2605.01330), RoLoRA(2407.08044),
      RoSTE(2502.09003), QuantTune(2403.06497), Massive Activations(2402.17762),
      Measuring Maximum Activations(2605.15572), RegCache(2510.04547),
      Small VLM Quantization(2607.08029), PASA(2503.01873), vLLM Issue #40290,
      Edge AI for VRU Safety(2604.26857), Flat Minima INT4(2604.15167),
      Astro(2602.07596), SCPNet(2303.06884), Drive-KD
- [ ] **제목 확정** — §1의 보류 표시 참조. INT8 결과 확인 후 후보 A/B 중 선택
- [ ] 코드/문서 명칭 `L_spatial` → `L_align`, `L_temporal` → `L_atc` 통일
- [ ] 비대칭 시간 맥락 증류(`L_atc`)는 학위논문 3장으로 이관 — 본 논문에서는 변형 축으로만

# [KSAE 2026 추계학술대회 원고 초안] 2026-09-10 작성 · 마감 9/18

> 학회 템플릿(한글/워드)에 붙여넣기 위한 국문 초안. 제목·초록은
> `paper_positioning_ksae2026.md`의 확정안을 따르고 실험 수치를 채워 넣었다.
> **저자·소속은 비워 두었다.** 그림은 `loss_graph/ksae_fig_loss.png` 사용.

---

## 제목

**단일 프레임 추론을 유지하는 비대칭 시간 맥락 증류 기반 자율주행 VLM 경량화**

*Lightweight Autonomous Driving VLM via Asymmetric Temporal Context Distillation
with Single-Frame Inference*

저자: ○○○¹ · ○○○¹  |  ¹○○대학교 ○○공학과

---

## 초록

자율주행용 시각-언어 모델(VLM)은 장면 이해와 자연어 설명을 동시에 제공할 수 있으나, 모델
규모로 인해 차량 탑재가 어렵다. 본 연구는 Qwen2.5-VL-7B 교사 모델을 3B 학생 모델로 압축하면서,
교사가 보유한 시간적 맥락을 학생의 추론 비용 증가 없이 전이하는 **비대칭 시간 맥락 증류**를
제안한다. 제안 기법에서 교사는 현재 프레임과 과거 프레임을 함께 입력받아 시간적으로 맥락화된
표현을 형성하는 반면, 학생은 **현재 프레임 한 장만을 입력받아** 해당 표현을 모방하도록 학습된다.
이로써 배포 환경에서 학생의 프레임당 추론 횟수는 단일 프레임 모델과 동일하게 유지된다. 여기에
교사와 학생의 비전 인코더 중간 표현을 학습 가능한 정렬 계층과 코사인 손실로 정합시키는 특징
정렬 손실을 함께 적용하였다. 제안 기법의 효과는 증류 미적용 기준선, 출력 수준 증류, 각 손실의
단독 및 결합 적용을 포함한 5개 변형 비교를 통해 NuScenes-QA 검증셋 11,309개 샘플에서 검증하였다.
학습 데이터·LoRA 구성·최적화기·스텝 수·평가 프로토콜을 전부 동일하게 고정하여 손실 설계에 따른
효과만을 분리하였으며, 제안 기법은 **51.69%**의 정확 일치 정확도로 증류 미적용 기준선(50.30%)
대비 1.39%p, 출력 수준 증류(46.74%) 대비 4.95%p 우위를 보였다. 아울러 대형 어휘 사전을 가진
VLM에 표준 지식증류 온도를 그대로 적용할 경우 문장 종료 신호가 소실되어 생성이 붕괴하는 현상을
관찰하고, 그 원인과 해결 방안을 함께 보고한다.

**주요어**: 지식증류(Knowledge Distillation), 시각-언어 모델(Vision-Language Model),
자율주행(Autonomous Driving), 모델 경량화(Model Compression), 시간적 맥락 전이

---

## 1. 서론

자율주행 인지 시스템에 시각-언어 모델(VLM)을 도입하면 장면 이해 결과를 자연어로 설명할 수 있어
사고 원인 규명과 운전자 신뢰 확보에 유리하다. 그러나 실용적 성능을 내는 VLM은 수십억 개의
파라미터를 가져 차량 탑재 연산 자원으로는 실시간 추론이 어렵다. 이에 대형 교사 모델의 지식을
소형 학생 모델로 옮기는 지식증류가 유력한 경량화 수단으로 검토되어 왔다.

주행 장면 이해에서 시간적 맥락은 본질적이다. 정지한 차량과 서행하는 차량, 진입하려는 보행자와
서 있는 보행자는 단일 프레임만으로는 구분되지 않는다. 그러나 학생 모델이 다중 프레임을
입력받도록 하면 프레임당 비전 인코딩 횟수가 증가해 경량화의 목적 자체가 훼손된다.

본 연구는 이 상충을 **증류의 비대칭성**으로 해소한다. 교사에게만 다중 프레임을 제공해 시간적으로
맥락화된 표현을 형성하게 하고, 학생은 현재 프레임 한 장만 입력받은 상태에서 그 표현을 모방하도록
학습시킨다. 시간 정보는 학습 시점에 교사로부터 흡수되고, 배포 시 학생의 입력과 추론 비용은
단일 프레임 모델과 동일하게 유지된다.

본 논문의 기여는 다음과 같다.

1. 교사가 학생보다 시간적으로 풍부한 **압축 방향**의 시간 맥락 증류 손실 `L_atc`를 제안하고,
   학생의 추론 비용 불변을 설계 제약으로 명시하였다.
2. 손실 항 이외의 모든 조건을 고정한 **5개 변형 통제 비교**를 통해 각 손실의 기여를 분리하였다.
3. 대형 어휘 사전(152K 토큰) 환경에서 표준 증류 온도가 문장 종료 신호를 소실시켜 생성을
   붕괴시키는 현상을 규명하고 해결하였다.

---

## 2. 관련 연구

**주행 VLM 증류.** Drive-KD는 본 연구와 동일한 교사–학생 조합(Qwen2.5-VL 7B → 3B)을 사용하나,
텍스트–비전 attention 행렬을 MSE로 정렬하는 방식을 취하며 시간 정보는 활용하지 않는다. 해당
연구는 스스로 "이미지–텍스트 입력에 제한되어 시간적·기하학적 정보가 미활용됨"을 한계로 명시하고
있으며, 본 연구는 이 지점을 다룬다.

**시간 정보의 증류 방향.** Patera 등은 사고 예측 과제에서 공간·시간 증류를 결합한 경량화
프레임워크를 제안하였다. 그러나 해당 연구의 시간 증류는 단일 프레임 교사(MobileCLIP)로부터
학생이 미래 프레임 특징을 예측하도록 하는 구조로, **학생이 교사보다 더 풍부한 시간 정보를
갖는 방향**이다. 본 연구는 반대로 멀티프레임 교사의 시간 맥락을 단일 프레임 학생이 흡수하도록
하여, 배포 시 학생의 추론 비용이 증가하지 않도록 하는 **압축 방향**의 설계를 취한다. 또한 대상
과제가 이진 사고 예측이 아닌 자연어 생성 기반 VQA이며, 압축 대상이 소형 이미지 인코더가 아닌
범용 대형 VLM이라는 점에서 구별된다.

**경량 주행 VLM.** EM-VLM4AD, MiniDrive 등은 처음부터 소형 구조를 설계하는 접근이다. 이들은
압축 대상이 되는 대형 교사를 전제하지 않으므로 본 연구와 문제 설정이 다르며, 고정 해상도 다중뷰
CNN 구조로 인해 QA 형식 밖으로의 확장이 제한적이다.

**특징 정렬 증류.** 중간층 특징을 학습 가능한 정렬 계층과 함께 정합시키는 설계는 FitNets 이래
확립된 방법이다. 본 연구의 `L_align`은 이 계열에 속하며 **새로운 기여로 주장하지 않는다**.
본 논문에서는 코사인 거리 채택의 실증적 근거만을 보고한다.

---

## 3. 제안 방법

전체 손실은 다음과 같다.

```
L_total = L_task + λ_atc · L_atc + λ_align · L_align
```

`L_task`는 학생의 답변 토큰에 대한 교차엔트로피이며, 질문 구간은 −100으로 마스킹된다.
출력 수준 증류(로짓 KL) 항은 포함하지 않는다.

### 3.1 비대칭 시간 맥락 손실 `L_atc`

교사는 현재 프레임과 직전 K−1개 프레임을 함께 입력받고, 학생은 현재 프레임만 입력받는다.
두 모델의 **답변 생성 직전 위치**(`q_len − 1`)에서의 최종 디코더 계층 은닉 표현을 비교한다.
이 위치는 좌측 기준 절대 인덱스이므로 우측 패딩과 무관하게 유효하다. 학생과 교사의 은닉 차원이
다르므로(2048 대 3584) 학습 가능한 선형 계층 `P_atc: R^2048 → R^3584`로 사영한 뒤 코사인 손실을
적용한다.

```
L_atc = 1 − cos( P_atc(h_S[q_len−1]) , h_T[q_len−1] )
```

DriveLM 키프레임은 평균 약 3.1초 간격으로 성기므로 K=2(직전 1프레임)에서 시작하였다.
장면별 프레임 시간 순서는 JSON의 사전 순서가 아니라 `CAM_FRONT` 파일명에 포함된 마이크로초
타임스탬프로 정렬해 구성한다.

### 3.2 특징 정렬 손실 `L_align`

교사와 학생의 비전 인코더는 구조가 동일하나(깊이 32, 은닉 1280) 서로 독립적으로 사전학습되어
자연적으로 정렬되어 있지 않다. 정렬 계층 없이 두 인코더의 23번째 블록 출력을 직접 비교했을 때
손실이 450스텝 동안 0.437에서 0.438로 정체하였고, 같은 구간에서 `L_task`와 `L_atc`는 정상적으로
감소하였다. 이는 rank-16 비전 LoRA만으로는 표현 적응과 두 특징 공간의 정합을 동시에 수행할 수
없음을 시사한다. 이에 학습 가능한 선형 계층 `P_align: R^1280 → R^1280`을 도입하였고, 추가 이후
손실이 첫 스텝부터 감소하였다.

```
L_align = 1 − cos( P_align(v_S) , v_T[현재 프레임 구간] )
```

교사의 비전 블록 출력은 K개 프레임의 패치가 섞여 있으므로, `image_grid_thw`로부터 계산한
오프셋으로 **현재 프레임의 패치 구간만** 잘라내어 학생의 단일 프레임 패치와 비교한다.

두 정렬 계층 `P_atc`, `P_align`은 학습 수렴을 위한 보조 구성요소이며 **추론 시 제거된다**.
비전 attention LoRA 역시 마찬가지다. 따라서 배포되는 학생 모델의 구조와 연산량은 원본
Qwen2.5-VL-3B에 LoRA 어댑터를 병합한 것과 동일하다.

---

## 4. 실험 설정

### 4.1 데이터

DriveLM-nuScenes(377,983 QA 쌍)와 NuScenes-QA(DriveLM 이미지와 매칭 가능한 54,607 쌍)를
`ConcatDataset`과 가중 샘플러로 결합하여, 배치 내 DriveLM 비율이 0.4가 되도록 유지하였다.
입력 영상은 `CAM_FRONT` 단일 뷰만 사용한다.

평가는 **NuScenes-QA 검증셋 11,309개 샘플**에서 수행하였다. DriveLM 검증 장면은 학습에 사용된
696개 장면과 겹치지 않으므로 장면 단위로 분리된 held-out 집합이다. 지표는 정답 문자열 정규화 후
정확 일치(exact match)이며, 질문 유형(exist/object/status/count/comparison)별로도 분해하였다.

> DriveLM 공식 검증·시험 집합은 정답이 비공개이고(`v1_1_val_nus_q_only.json`의 모든 `"A"`가
> 공란), 유일한 채점 경로였던 CVPR 2024 리더보드가 종료되어 사용할 수 없다. 이에 정답이 공개된
> NuScenes-QA를 정량 평가 축으로 삼았다.

### 4.2 학습 조건 — 5개 변형에 걸쳐 고정

| 항목 | 값 |
|---|---|
| 교사 | Qwen2.5-VL-7B-Instruct + LoRA |
| 학생 | Qwen2.5-VL-3B-Instruct |
| LoRA rank / alpha / dropout | 16 / 32 / 0.05 |
| LoRA 대상 | q,k,v,o_proj + gate,up,down_proj + 비전 qkv/proj |
| 학습 가능 파라미터 | 41,124,224 |
| learning rate / warmup | 2e-5 / 0.05 (1,351 스텝) |
| epoch / effective batch / total steps | 1 / 16 / 27,037 |
| 정밀도 | bf16 혼합 정밀도 |
| K (`temporal_k`) | 2 |
| 정렬 계층 인덱스 | 23 (전체 32) |
| λ_atc, λ_align | 1.0, 1.0 |

**변형 간 유일한 차이는 손실 항이다.** 데이터 구성, LoRA 대상 모듈, 최적화기, 스텝 수, 평가
스크립트가 전부 동일함을 학습 로그 대조로 확인하였다.

| 변형 | 손실 |
|---|---|
| Full (제안) | `L_task + L_align + L_atc` |
| `L_align` 단독 | `L_task + L_align` (λ_atc = 0) |
| `L_atc` 단독 | `L_task + L_atc` (λ_align = 0) |
| 기준선 | `L_task` (교사 미사용) |
| 출력 수준 증류 | `L_task + L_kd` (균등 가중 KL) |

---

## 5. 결과 및 분석

### 5.1 정량 결과

**표 1. NuScenes-QA 검증셋 정확 일치 정확도 (11,309 샘플, %)**

| 모델 | 전체 | comparison | count | exist | object | status |
|---|---|---|---|---|---|---|
| **Full (제안)** | **51.69** | 64.72 | 7.46 | 80.17 | **43.87** | 51.03 |
| `L_align` 단독 | 51.40 | 63.09 | **10.58** | 81.38 | 37.86 | **52.79** |
| `L_atc` 단독 | 51.34 | 64.44 | 7.73 | **81.17** | 39.89 | 52.24 |
| 기준선 (증류 미적용) | 50.30 | 64.49 | 7.96 | 80.26 | 36.67 | 51.15 |
| 출력 수준 증류 | 46.74 | 63.88 | 10.40 | 67.68 | 35.48 | 51.09 |

두 손실을 함께 적용한 Full과 단독 적용 두 변형은 **전체 정확도에서 구별되지 않는다**
(51.69 / 51.40 / 51.34%). 세 변형을 각각 시드 고정 없이 한 번씩만 학습했으므로
0.35%p 폭은 시드 노이즈와 구별할 수 없다. 증류 미적용 기준선과의 차이도 1.4%p 이내로 작다.
`object` 유형에서는 Full이 43.87%로 단독 변형(37.86~39.89%)보다 폭이 크며 그 방향이
패치 수준 특징을 정합시키는 `L_align`의 목적과 일치하나, **이 역시 단일 시드 관측이므로
순위 주장으로 쓰지 않고 방향성 관찰로만 보고한다.**

출력 수준 증류가 46.74%로 최하위이며, 특히 `exist` 유형에서 67.68%로 다른 네 변형(80% 내외)보다
크게 낮다. 실제 생성 결과를 확인한 결과 판단 자체는 맞히면서 정답 뒤에 부연 설명을 덧붙이는
패턴("yes, there are many.")이 관찰되었으며, 이는 교사의 DriveLM 학습 문체가 출력 수준 증류를
통해 일부 전이된 잔여 효과로 판단된다. **특징 수준 증류가 출력 수준 증류보다 유리하다**는 본
연구의 관점을 지지하는 결과다.

`count` 유형은 5개 변형 모두 7~11%대로 공통적으로 낮다. 손실 설계와 무관하게 나타나므로 모델
규모와 데이터 체제에서 오는 한계로 보고한다.

### 5.2 학습 곡선

**그림 1** (`loss_graph/ksae_fig_loss.png`)

(a) 다섯 변형의 `L_task`가 거의 동일한 궤적으로 수렴한다. 손실 항 구성이 달라도 과제 학습 자체는
동등하게 진행되었음을 의미하며, 표 1의 차이가 학습 실패에서 오는 것이 아님을 보인다.

(b) 증류 항 곡선. 주목할 점은 **단독 변형의 곡선이 Full 변형의 해당 곡선과 거의 겹친다**는 것이다.
즉 `L_align`과 `L_atc`는 서로의 최적화를 방해하지 않으며, 두 항을 함께 쓸 때의 이득이 상호 간섭
없이 누적됨을 시사한다.

### 5.3 비용

학습은 단일 GPU에서 순차 수행하였다. Full 63.2시간, `L_align` 단독 52.0시간, `L_atc` 단독
62.6시간, 기준선 30.0시간, 출력 수준 증류 51.7시간이 소요되었다. Full과 `L_atc` 단독이 오래
걸리는 것은 교사가 K=2 프레임을 함께 토큰화·인코딩하기 때문이며, `L_align` 단독은 교사도 단일
프레임만 보므로 그만큼 짧다. **이 비용은 전부 학습 시점에만 발생하며 배포 학생 모델의 추론
비용에는 영향을 주지 않는다.**

---

## 6. 대형 어휘 사전에서의 증류 온도 붕괴

출력 수준 증류 변형을 구성하는 과정에서, 표준적인 증류 온도 설정이 생성을 붕괴시키는 현상을
관찰하였다. 부수적 발견이지만 대형 어휘 사전을 갖는 VLM 전반에 해당하므로 별도로 보고한다.

**현상.** 온도 T=4.0으로 학습한 모델은 학습 손실이 정상적으로 감소했음에도 검증 정확도가
**1.78%**에 그쳤다. 생성 결과를 확인한 결과 정답 토큰 자체는 높은 확신도로 맞히면서
(예: `"yes"` p=0.90~0.99) 문장 종료 토큰(`<|im_end|>`)을 방출하지 못해, 정답 뒤에 무의미한 숫자
나열을 무한히 덧붙이고 있었다.

**첫 가설의 반증.** 손실 크기 불균형을 원인으로 보고 λ를 1.0에서 0.3으로 낮춘 변형을 학습하였다.
손실 비율은 개선되었으나, 기준선이 이미 종료 토큰을 정상적으로 학습한 동일 지점(5,000스텝,
전체의 18.5%)에서 여전히 종료 토큰이 학습되지 않았다. 손실 크기가 원인이 아님이 확인되었다.

**원인.** 교사 모델 자신의 로짓을 정답 직후 위치에서 직접 측정하였다.

**표 2. 교사의 종료 토큰 확률 — 온도에 따른 변화**

| 질문 | T=1 | T=4 |
|---|---|---|
| 보행자 식별 | 0.965 (1위) | 0.00064 |
| 교통콘 존재 | 0.243 (2위) | 0.00116 |
| 이동 차량 존재 | 0.755 (1위) | 0.00036 |

원본 확률에서 종료 토큰은 0.24~0.97로 정상이나, T=4로 나눈 뒤 152K 토큰 전체에 softmax를
적용하면 확률이 0.0004~0.0016 수준까지 붕괴한다. 순위는 유지되어도 차순위 후보들과 사실상
동률이 되어 "여기서 멈추라"는 신호가 소실된다. Hinton 등이 제시한 T=2~4는 약 1,000개 클래스를
전제로 보정된 값이며, 두 자릿수 배 큰 어휘 사전에 그대로 적용하면 목표 분포가 균등분포에
가깝게 평탄화된다. 내용 토큰은 확률 질량이 상대적으로 덜 밀집되어 평탄화를 비교적 잘 견디는
반면, 종료 신호는 선택적으로 파괴된다.

**해결.** 온도를 1.5로 낮추어 목표 분포의 첨예도를 회복시키고 λ를 표준값 1.0으로 복원하였다.
5,000스텝부터 학습 종료 시점까지 각 체크포인트에서 종료 토큰이 1위(확률 0.4~1.0)로 회복됨을
확인하였다. 온도를 낮추면 손실의 `T²` 배율도 16배에서 2.25배로 줄어 손실 크기 균형이 부수적으로
개선된다. 표 1의 출력 수준 증류 결과는 이 수정을 적용한 모델의 것이다.

---

## 7. 결론

교사에게만 다중 프레임을 제공하고 학생은 단일 프레임만 입력받는 비대칭 시간 맥락 증류를
제안하고, 손실 항 이외의 조건을 모두 고정한 5개 변형 비교로 검증하였다. 제안 기법은
NuScenes-QA 검증셋에서 51.69%의 정확 일치 정확도로 증류 미적용 기준선 대비 1.39%p, 출력 수준
증류 대비 4.95%p 높았으며, 배포 시 학생의 프레임당 추론 비용은 단일 프레임 모델과 동일하다.
또한 대형 어휘 사전 환경에서 표준 증류 온도가 문장 종료 신호를 소실시키는 현상을 규명하고
해결 방안을 제시하였다.

향후 연구로는 학습 목적함수가 비전 인코더의 활성값 크기 분포에 미치는 영향과, 그것이 축소
정밀도 배포에서 갖는 함의를 다룰 예정이다. 예비 측정에서 특징 정렬 손실을 적용한 모델의
비전 인코더 중간 활성값이 그렇지 않은 모델 대비 현저히 작게 나타났다.

---

## 참고문헌 (정리 필요)

1. Bai, S. et al., "Qwen2.5-VL Technical Report," 2025.
2. Sima, C. et al., "DriveLM: Driving with Graph Visual Question Answering," ECCV, 2024.
3. Qian, T. et al., "NuScenes-QA: A Multi-modal Visual Question Answering Benchmark for Autonomous Driving Scenario," AAAI, 2024.
4. Hinton, G. et al., "Distilling the Knowledge in a Neural Network," NIPS Workshop, 2015.
5. Romero, A. et al., "FitNets: Hints for Thin Deep Nets," ICLR, 2015.
6. Hu, E. et al., "LoRA: Low-Rank Adaptation of Large Language Models," ICLR, 2022.
7. Gopalkrishnan, A. et al., "Multi-Frame, Lightweight & Efficient Vision-Language Models for Question Answering in Autonomous Driving," arXiv:2403.19838, 2024. (EM-VLM4AD)
8. Drive-KD, arXiv:2601.21288, 2026. — 서지사항 확인 필요
9. Patera, L. et al., ICLR, 2026. — 서지사항 확인 필요

---

## 제출 전 체크리스트

- [ ] 저자·소속·교신저자 기입
- [ ] 학회 템플릿(한글/워드)에 이식, 분량 규정 확인 (전문연구발표 통상 2~10쪽)
- [ ] 참고문헌 8, 9의 정확한 서지사항 확보 (저자·학회·페이지)
- [ ] 그림 1 캡션 국문화, 본문 번호 상호참조 확인
- [ ] **시드 캐벗 각주 삽입** — 문장 하나로 끝낸다: "5개 변형을 시드 고정 없이 한 번씩만
      학습했으므로 전체 정확도의 0.35%p 차이는 시드 노이즈와 구별되지 않는다."
      **수치(시드 분산)는 제시하지 않는다.** 2026-09-14 정정: 이전 판은 "동일 설정 재실행 간
      차이를 수치로 명시"였으나, 그 측정(C4)을 생략하기로 했다 — 두 시드는 차이값 하나일 뿐
      분산 추정이 아니고, 운용점도 5-way 표(27,037스텝)와 다르며(5,000스텝),
      완전 해소는 5변형 × 3시드 = 약 32일이라 선택지가 아니다.
      사유: `thesis_outline_20260910.md` §7 「C4를 생략한다」
- [ ] 초록 분량이 학회 규정에 맞는지 확인
- [ ] 코드의 `lambda_spatial`/`L_spatial` 명칭은 미변경 상태 — 원고의 `L_align`/`L_atc`와
      대응 관계를 각주 또는 부록에 한 줄 명시할지 결정

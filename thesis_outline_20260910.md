# 학위논문 구성안 — 기여 하나, 사례 둘 (2026-09-10)

이전 판(발견 셋을 나열하는 구성)을 폐기하고 **단일 기여 구조**로 다시 쓴다.
근거 데이터는 `thesis_topic_survey_20260910.md` §3, 선행 연구 위치는
`paper_positioning_ksae2026.md` §4.

---

## 1. 왜 다시 쓰는가

이전 구성은 4장(EOS 붕괴)·5장(활성 크기)·6장(정밀도 교란)을 **서로 다른 세 기여**처럼
나열했다. 이 형태의 문제는 둘이다.

- 심사위원에게 **디버깅 노트 모음**으로 읽힌다. 각각은 흥미롭지만 "그래서 이 논문의
  주장이 무엇인가"에 답하지 못한다.
- 방어할 명제가 셋이 되어, 하나가 흔들리면 전체가 흔들리는 것처럼 보인다.

실제로는 세 발견이 **하나의 구조**를 공유한다. 그것을 논지로 세우고 나머지를 증거로
배치하면, 기여는 하나가 되고 증거는 셋이 된다.

---

## 2. 단일 기여 진술

> **압축된 주행 VLM의 저정밀 배포 안전성에 대해 표준 압축 지표는 역방향 신호를 준다.
> 실패는 정확도의 점진적 저하가 아니라 기능의 이산적 상실로 오는데, 그 상실을 예고하는
> 값은 순전파 한 번으로 계산되며 지표를 움직이는 수단도 여럿이다. 부족한 것은 수단이
> 아니라 측정이다.**

네 부분으로 읽는다. **순서가 곧 근거의 강도**다 — 가장 방어하기 쉬운 것을 앞에 둔다.

| # | 주장 | 근거 | 강도 |
|---|---|---|---|
| 1 | **표준 지표가 역방향을 가리킨다** | 붕괴하는 `baseline_v2`가 이상치 비율(153.1 대 168.0)도 낮고 INT8 SNR(31.17 대 23.10)도 높은데 붕괴율은 94.8% 대 0.0% | **가장 강함** — 논증이 아니라 산술 |
| 2 | **이산성** — 기능이 통째로 죽는다 | EOS 순위 1위 ↔ 수십만 위 / 붕괴가 이미지 단위로 0% ↔ 94.8%, 혼재 0건 | 강함 |
| 3 | **예고하는 값은 순전파 한 번** | 사례 1: 운용 온도에서 교사의 EOS 확률. 사례 2: `format_max ÷ max\|activation\|` | **약함 — 아래 참조** |
| 4 | **움직이는 수단은 여럿** | 사례 1: 온도 조정. 사례 2: `L_align`(교사 정렬), 정렬 일반(자기 정렬로도 51%), λ 조정, **사후 가중치 편집(fixA)**, 그리고 반대 방향으로 LoRA 확장 단계 | 강함 |

> **3번을 앞세우지 말 것 (2026-09-15).** `format_max ÷ max|activation|`은 **나눗셈 하나**다.
> "최대 활성값을 재라"가 기여냐는 질문에 답하기 어렵다. **기여의 실체는 지표 제안이 아니라
> 기존 지표가 틀렸다는 발견(1번)**이므로, 이전 판처럼 "예고 지표가 있다"를 앞에 놓으면
> 약한 쪽으로 논문을 소개하는 셈이 된다. 순서를 1↔3으로 바꿨다.
> 3번은 **"그래서 무엇을 대신 재야 하는가"라는 실무 답**으로만 쓴다.

> **2026-09-14 전면 재구성.** 이전 진술의 셋째 절은 "상실 여부는 학습 설계와 밀접하다"였다.
> **두 실험이 그 절을 무너뜨렸다.**
>
> - **fixA**: 학습을 전혀 하지 않고 마지막 블록 gate·up의 weight·bias를 1/√3로 나누는
>   것만으로 붕괴가 94.4% → **0.0%**가 되고 정확도는 유지된다. 학습 설계가 유일한 경로가 아니다.
> - **B (정렬 대상 ablation)**: 정렬 대상을 7B 교사가 아니라 **3B 학생 자기 자신**으로 바꿔도
>   효과의 **51%**가 재현된다(−24.2% 대 −47.3%). 효과의 절반은 `L_align`의 특정 설계가 아니라
>   **정렬 제약 일반**에서 온다.
>
> 여기서 두 갈래가 있었다. (i) 셋째 절을 계속 약화시켜 붙들거나, (ii) **논지의 축을 옮기는** 것.
> (i)은 "밀접하다" → "관련이 있다"로 계속 물러나다 결국 아무 말도 아니게 된다.
> (ii)를 택했다.
>
> **핵심은 이것이다.** 두 실험은 `L_align`의 지위를 낮추는 대신, **여유를 움직이는 것이
> 쉽다**는 사실을 보여준다. 교사가 특별할 필요도 없고(자기 정렬로 절반), 학습이 필요하지도
> 않다(fixA). 그렇다면 진짜 질문은 "왜 안 움직이는가"이고, 답은 **"재지 않기 때문"**이다.
>
> > **근거의 한계를 정확히 (2026-09-15).** "**아무도** 재지 않는다"는 쓰지 않는다.
> > 근거가 둘인데 둘 다 절대 주장을 받치지 못한다.
> > (i) 공개 저장소 197개 **config** 조사 — 그러나 **config는 애초에 활성 통계를 적는
> > 곳이 아니다.** 거기 여유가 없는 것은 당연하며 증거가 되지 못한다.
> > (ii) arXiv 전문 검색 몇 건이 0을 반환 — 검색어 몇 개의 부재는 문헌 전체의 부재가 아니다.
> >
> > **정확한 진술**: "우리가 조사한 범위(공개 저장소 197개의 config, arXiv 전문 검색)에서
> > 표현 범위 여유를 보고한 사례를 찾지 못했다." 절대 주장 대신 **조사 범위를 명시**한다.
> >
> > **미조사 위협**: TensorRT 등 배포 도구가 fp16 빌드에서 레이어별 정밀도를 자동
> > 선택하며 오버플로를 회피할 가능성이 있다. 그렇다면 "재지 않는다"는 문제 제기 자체가
> > 약해진다. **7장 7.2의 정밀도 3조건 비교가 이 질문에 답한다** — 그 전까지 이 위협을
> > 해소된 것으로 쓰지 않는다.
>
> 이 재구성으로 **B와 fixA가 약점에서 근거로 바뀐다.** 둘은 4번 절(수단은 여럿)의 직접
> 증거이며, 그 절이 있어야 "부족한 것은 측정"이라는 결론이 성립한다. 이전 구조에서는
> 두 결과가 논지를 갉아먹었다.

**무엇을 포기했는가.** "이 손실이 여유를 만든다"는 주장을 포기했다. 남는 것은
"`L_align`은 여유를 **부수적으로, 자동으로** 확보해 주는 여러 수단 중 하나"다.
정확도 이득이 작다는 약점(~1.4pp)이 여기서 오히려 정합적이 된다 — `L_align`의 가치를
정확도로도 여유로도 단독 주장하지 않고, **여유가 움직인다는 사실의 한 사례**로 쓴다.

**무엇을 얻었는가.** 기여가 인과 주장에서 **방법 제안**으로 바뀐다. 인과 주장은 반례
하나에 무너지지만(실제로 B가 절반을 무너뜨렸다), "이 두 스칼라를 재라"는 제안은 반례가
없다. 그리고 재는 비용이 순전파 한 번이라 실무자가 실제로 채택할 수 있다.

**사례 1에 대한 정직한 단서.** 우리는 교사의 EOS 확률을 **사후에** 쟀다. 미리 재고
시작한 것이 아니다. 다만 그 값은 학생을 학습시키기 **전에** 계산 가능하며, 쟀다면
`kd_only_v2`와 `v3` **두 번의 전체 학습**을 아꼈을 것이다. 논문에는 이 순서를 그대로
적는다 — 사후에 찾았고, 그래서 더 확실히 "표준 절차에 없다"는 증거가 된다.

**왜 지표가 필요한가 — 계열 간 편차가 근거다.** 3번 절(표준 평가에 지표가 없다)이
성립하려면, 지표 없이는 판단할 수 없다는 것을 보여야 한다. 같은 조건(원본 해상도,
동일 nuScenes CAM_FRONT)에서 잰 여유는 이렇게 갈린다.

| 모델 | fp16 여유 | 붕괴 |
|---|---|---|
| Qwen2.5-VL-3B | **1.05배** | 69.6% |
| Qwen2-VL-2B-AWQ | 3.88배 | — |
| SmolVLM-Instruct (Idefics3) | **50.86배** | — |

**이 편차가 처방을 필요하게 만든다.** 모두가 1.0배 근처였다면 처방은 "비전 타워에 fp16을
쓰지 마라" 한 줄로 끝나고 지표가 필요 없다. 50배까지 갈리기 때문에 **모델마다 재야 한다**가
성립한다. 같은 Qwen 계열 안에서도 세대·크기에 따라 갈린다는 점이 이를 강화한다.

> **서술 위치에 대한 메모 (2026-09-14).** 이 결과는 이전 판에서 8장 한계 목록에
> "계열 일반성 없음"으로 들어가 있었다. 거기 두면 "우리 결과가 좁다"로 읽힌다.
> **한계가 아니라 근거이므로** 여기로 옮긴다. 8장에서는 "사례 연구 범위"로만 짧게
> 재확인한다.

**범위 — 사례가 둘이므로 패턴이지 법칙이 아니다.** 본 논문은 일반 법칙을 주장하지 않는다.
서로 다른 메커니즘(확률분포 평탄화 / 수치 범위 초과)에서 같은 구조의 실패가 나온다는 것을
두 사례로 보이는 것까지가 범위다. 하나였다면 특정 설정의 일화이고, 둘이면 패턴이다.
제목의 「사례 연구」가 이 범위를 명시한다.

자동차공학회 청중에게 이 구분은 익숙하다. **완만히 성능이 떨어지는 시스템과 갑자기
출력을 잃는 시스템**은 안전 논의에서 전혀 다른 대상이며, 후자에는 **사전 지표**가 있어야 한다.

---

## 3. 두 사례의 공통 구조

| | **사례 1** (4장) | **사례 2** (5장) |
|---|---|---|
| 상실되는 기능 | **정지 기능** — 문장 종료 신호 | **수치 표현** — 활성값의 표현 범위 |
| 증상 | 정답을 맞히고도 멈추지 못해 생성 폭주 | 출력이 토큰 id 0 반복으로 전면 붕괴 |
| 나머지 능력 | 정상 (`"yes"` p=0.90~0.99) | 정상 (여유 있는 이미지에서는 완전 동작) |
| 이산성의 근거 | EOS 순위가 1위 ↔ 수십만 위로 갈림 | 이미지 단위로 0% ↔ 94.8% 붕괴, 혼재 0건 |
| **① 예고 지표** | **운용 온도 T에서 교사의 EOS 확률** | **`format_max ÷ max\|activation\|`** |
| 그 값 | T=1: 0.24~0.97 (정상) → **T=4: 0.0004~0.0016** | `full` 2.90배 (정상) → **`baseline_v2` 1.03배** |
| 계산 비용 | 교사 순전파 1회 (학생 학습 **전**) | 학생 순전파 1회 |
| **② 표준 평가에 없는 이유** | KD 문헌의 온도 논의가 ~1,000 클래스 기준이라 어휘 크기 민감도를 다루지 않음 | 압축 지표가 전부 **스케일 불변량**이라 절대 크기를 볼 수 없음 |
| 지표가 대신 말하는 것 | 정확도 1.78% — 실패는 보이나 **"출력 KD가 나쁜 방법"으로 오독**하게 만듦 | INT8 SNR 31.2 대 23.1 — **붕괴하는 쪽을 더 안전하다고** 순위 매김 |
| **③ 움직이는 수단** | 온도 하향 (T=4 → 1.5) | `L_align`(교사 정렬) / **정렬 일반**(자기 정렬로 51%) / λ 조정 / **사후 가중치 편집(fixA)** |
| 수단이 여럿인 증거 | — (온도 하나만 확인) | **B**: 자기 정렬 −24.2% 대 교사 정렬 −47.3%. **fixA**: 학습 없이 94.4% → 0.0% |
| 메커니즘 | 대형 어휘에서 높은 KD 온도가 target을 균등분포로 뭉갬 | 미세조정이 활성 크기를 키워 fp16 표현 한계 초과 |

**표의 ①②③이 §2 진술의 2·3·4번 절에 그대로 대응한다.** 사례 1은 ③의 증거가 하나뿐이라
(온도만 확인) 사례 2보다 약하다 — 이 비대칭을 숨기지 않고 4장에 적는다.

**메커니즘이 서로 무관하다는 점이 중요하다.** 하나는 확률분포의 평탄화, 다른 하나는
수치 범위 초과다. 서로 다른 원인에서 같은 구조의 실패가 나온다는 것이 논지가 일반적
현상을 가리킨다는 근거가 된다.

**사례가 왜 둘이어야 하는가.** 하나면 특정 모델·특정 설정의 일화로 축소된다. 둘이면
패턴이다. 부수적으로 위험도 나뉜다 — 사례 2의 강도는 진행 중인 실험(INT8, λ 스윕)에
달려 있으나 **사례 1은 그와 완전히 독립**이며 이미 데이터가 모두 확보돼 있다.

---

## 4. 장 구성

각 장은 **독립 기여가 아니라 §2 논지를 세우는 단계**다. 서론에서 이를 명시한다.

### 1장 서론
문제 제기(차량 탑재를 위한 경량화와 저정밀 연산) → §2의 논지 제시 →
**"본 논문의 기여는 하나이며, 두 사례와 하나의 따름정리로 이를 입증한다"**를 명시.

### 2장 관련연구
네 계열. 각 계열 끝에 본 연구와의 차이를 한 문장씩 (§`paper_positioning_ksae2026.md` §4.3 참조).

1. VLM 지식증류·주행 VLM 경량화 — Drive-KD, **SCPNet**, EM-VLM4AD, MiniDrive
2. 저정밀 배포와 활성 이상치 — CrAFT, Colinearity Decay, RoLoRA, RoSTE, QuantTune,
   Massive Activations 계열, Measuring Maximum Activations, RegCache, PASA
3. 자율주행 VLM 효율화 서베이 — 2608.30144, 2510.24795, 2506.24044, 2512.16760
4. 평가 프로토콜 — DriveLM, NuScenes-QA, CODA-LM

### 3장 실험 기반 — 통제된 다중 변형 증류 파이프라인
**기여 장이 아님을 서두에 명시한다.** 논지를 검증하려면 "손실만 다른 학생들"이
필요하고, 이 장은 그것을 만든다.

- 교사(7B)/학생(3B), LoRA 설정, 데이터 파이프라인
- `L_align`(특징 정렬, FitNets 계열)과 `L_atc`(비대칭 시간 맥락) 정의 —
  **`L_atc`는 SCPNet의 구조를 카메라 기반 생성형 VQA로 이식한 것**이며 참신성을
  주장하지 않는다고 명시
- 5개 변형과 공정성 통제(데이터·LoRA·optimizer·스텝·평가 프로토콜 고정) 및 로그 대조 감사
- NuScenes-QA val 5-way 결과 — **여기서 "정확도로는 변형을 구별할 수 없다"를 먼저 인정한다.**
  뒤 두 장이 "그런데 정확도가 못 보는 실패가 있다"로 이어지므로, 약점을 앞에 두는 것이
  논지를 강화한다.

  **통계 관련 서술 규칙 (2026-09-11 확정)**: 5개 변형을 각각 **시드 고정 없이 한 번씩만**
  학습했다. 따라서 Full / spatial / temporal의 전체 정확도(51.69 / 51.40 / 51.34%)
  **0.35%p 차이는 시드 노이즈와 구별되지 않는다.**
  - **금지**: "Full이 단일 손실 ablation보다 우위", "α가 β보다 낫다" 류의 순위 주장
  - **사용**: "세 변형은 전체 정확도에서 구별되지 않는다", "증류 미적용 기준선 대비
    차이도 1.4%p 이내로 작다"
  - **`object` 유형(43.87% 대 37.86~39.89%)도 같은 제약을 받는다 (2026-09-14 추가).**
    폭이 6pp로 0.35pp보다 훨씬 크지만 **반복 측정한 적이 없다** — n=1에서는 6pp에도
    구간을 붙일 수 없다. "뚜렷한 우위"라고 쓰지 말고 **"폭이 크며 방향이 `L_align`의
    목적과 일치한다"는 방향성 관찰**로만 적는다.
    (이 항목이 없어서 `ksae2026_manuscript_draft.md`에 "가장 높았으며 … 0.29~0.35%p
    높았다"는 금지 위반 문장이 들어가 있었다. 09-14 정정.
    `CLAUDE.md`의 "repeatable"이라는 표현도 근거가 없어 함께 고쳤다.)
  - 이 서술이 오히려 논지와 맞는다 — **정확도로는 안 보이는 차이를 다루는 논문**이기 때문이다.
  - 활성 크기 차이(62배)와 붕괴율 차이(94.8% 대 0%)는 규모가 커서 이 제약을 받지 않는다.

### 4장 사례 1 — 정지 기능의 상실
- 현상: `kd_only_v2`가 val 1.78%. 정답 내용은 맞히나 멈추지 못함
- 진단: 교사 forced-position 로짓 프로빙 — T=1에서 EOS 0.24~0.97(1~2위) →
  T=4에서 0.0004~0.0016
- **반증**: 손실 크기 불균형 가설(v3, λ 축소)을 세우고 실험으로 기각
- 원인: Hinton식 T=2~4는 ~1,000클래스 기준. 152K 어휘에서는 target이 균등분포에
  수렴하며 **종료 신호만 선택적으로** 소실 (내용 토큰은 상대적으로 생존)
- 수정·검증: T=1.5, 체크포인트별 EOS 순위 회복
- **보강 완료 (09-10)**: 어휘를 상위 K개로 제한해 K × T 스윕
  (`eval_results/kd_temperature_vocab_sweep.json`, 샘플 40개). 교사의 정답 직후 위치
  EOS 목표 질량 중앙값:

  | 어휘 크기 | T=1 | T=1.5 | T=2 | **T=4** | T=8 |
  |---|---|---|---|---|---|
  | 1,000 | 0.947 | 0.508 | 0.176 | **0.0164** | 0.0041 |
  | 10,000 | 0.933 | 0.349 | 0.070 | 0.0029 | 0.0005 |
  | 100,000 | 0.923 | 0.259 | 0.031 | 0.0006 | 0.0001 |
  | full 152,064 | 0.923 | 0.255 | 0.029 | **0.0005** | 0.00006 |
  | **1K→full 감소 배수** | 1.0× | 2.0× | 6.0× | **30.7×** | 66.1× |

  **T=1에서는 어휘 크기가 무관하고(2.6% 차이), 온도가 오를수록 영향이 급격히 커진다.**
  Hinton 기본값 T=4에서 1,000클래스는 EOS에 1.64%를 남기지만 152K 어휘는 0.053%만
  남긴다(31배). KD 손실이 목표 질량으로 가중하므로 "멈춰라" 신호의 압력이 31분의 1이 된다.
  → 변수는 온도가 아니라 **온도 × 어휘 크기**임이 논증에서 측정으로 승격됐다.

  **캐벗**: 상위 K 절단은 실제로 작은 어휘를 가진 모델과 동일하지 않다(로짓 기하가 다름).
  꼬리가 softmax 분모에 기여하지 못하게 막는다는 점에서 메커니즘은 같으나,
  **시뮬레이션임을 본문에 명시한다.**
- **집계 지표가 원인을 가린 실례 — 본 연구 자신의 기록.** val 1.78%를 보고 "출력 수준
  KD는 이 과제에 통하지 않는다"고 결론 내릴 뻔했다(5-way 표에서 최하위로 기록될 뻔했다).
  실제 원인은 방법이 아니라 **온도 설정 하나**였고, 생성 결과를 직접 열어보고 나서야
  드러났다. 지표가 실패를 숨긴 것이 아니라 **원인을 숨겨 잘못된 결론으로 이끌었다**는
  점을 자기 사례로 보고한다.
- 선행 연구: `"distillation temperature"` AND `"vocabulary"` 0건,
  `"knowledge distillation"` AND `"end-of-sequence"` 관련 0건

### 5장 사례 2 — 수치 표현의 상실
- 활성 크기 프로파일: DriveLM val 799장 × 7모델, 층별 max|activation| (**그림 1**)
  - 중간층(blk16~28): `L_align`이 있는 둘만 약 62배 낮음 (8,096 대 130)
  - 마지막 블록: 과제 손실 단독이 베이스보다 **키움** (33,792 → 50,176)
  - 교사가 오히려 높다(9,024) — 코사인 손실은 방향만 제약하므로 크기 감소는 **부수 효과**
- 이산적 실패: fp16 표현 한계(65,504) 초과 → NaN → 토큰 0 반복.
  이미지 단위로 갈리고 같은 장면의 연속 키프레임에서 뭉친다
- 해상도 × 붕괴율 (**그림 2**, 완료)

  **해상도 × 붕괴율 (2026-09-10 측정, 250장, 그림 2)**

| 모델 | 128 | 256 | 512 | 1,024 | 1,836 tok | 원본 해상도 fp16 여유 |
|---|---|---|---|---|---|---|
| task CE only | 0.8% | 3.2% | 10.4% | 25.6% | **94.8%** | 0.88× (한계 초과) |
| 사전학습 (미세조정 없음) | 0 | 0 | 0 | 0 | **69.6%** | 0.95× |
| task + `L_atc` | 0 | 0.4% | 0 | 0 | **28.8%** | 1.06× |
| task + 출력 KD | 0 | 0 | 0 | 0 | **20.4%** | 1.10× |
| **task + `L_align`** | 0 | 0 | 0 | 0 | **0.0%** | **2.89×** |
| **task + `L_align` + `L_atc`** | 0 | 0 | 0 | 0 | **0.0%** | **2.92×** |

원본 해상도(1,836 토큰)에서 `L_align`을 쓴 둘만 붕괴하지 않는다. 따라서 "여유가 크다"가
아니라 **"이 조건에서는 `L_align`이 붕괴 회피의 필요조건"**으로 서술할 수 있다.
붕괴는 1,024 → 1,836 구간에서 급격히 일어나 **안전 해상도에 절벽이 존재**한다.

**서술 주의 — "미세조정이 안전한 모델을 망친다"고 쓰지 않는다.** 사전학습 모델
`base_3b`도 원본 해상도에서 69.6% 붕괴한다. 정확한 서술은 **여유 비율** 기준이다.

| 모델 | 원본 해상도 fp16 여유 | 해석 |
|---|---|---|
| base_3b (미세조정 없음) | **0.95×** | 사전학습 상태에서 이미 여유가 없다 |
| task CE only | 0.88× | 남은 여유마저 없앤다 |
| task + `L_atc` / 출력 KD | 1.06× / 1.10× | 증류 항이 하나라도 있으면 한계선 위로 올린다 |
| **task + `L_align`(±`L_atc`)** | **2.89× / 2.92×** | 3배 여유를 만든다 |

즉 논지는 "미세조정이 망친다"가 아니라 **"사전학습 모델부터 여유가 없는데 아무도 이를
측정하지 않으며, 손실 설계만이 그 여유를 만든다"**이다. 후자가 본 논문의 주장과 더
정확히 맞고, 반박 여지도 적다.

- **절대 크기(표현 범위) 대 이상치 비율(동적 범위)의 구분 — 독립 절로 승격 (09-10 확정)**

  | 층 | 모델 | max | max/rms | INT8 SNR (per-ch) |
  |---|---|---|---|---|
  | blk24 | task CE only | 8,128 | 316 | 31.5 dB |
  | blk24 | task+align+atc | 130 | 32 | **44.0 dB** |
  | blk31 | task CE only | 48,640 | **153** | **40.3 dB** |
  | blk31 | task+align+atc | 18,048 | **168** | 39.4 dB |

  중간층에서는 두 축이 함께 움직이나(기존 문헌과 일관), **붕괴를 결정하는 마지막
  블록에서는 절대 크기만 2.7배 다르고 이상치 비율·INT8 SNR은 사실상 동일**하다.
  기존 압축 지표가 이 차이를 구별하지 못한다는 것이 본 논문의 핵심 근거다.
  부수 결과로 중간층 per-channel INT8 SNR이 12.5 dB 개선된다.

- **학습 길이 축 (09-10 발견)**: blk31 활성은 학습이 진행될수록 자란다
  (spatial λ=1: step_5000에서 13,376 → 완주 18,432, +38%). 따라서 λ 스윕은 **동일
  5,000스텝 시점끼리만** 비교해야 하며, 완주 체크포인트와 직접 비교해서는 안 된다.
  동시에 "오래 학습할수록 배포 여유가 줄어든다"는 실무적 함의가 생긴다.

- **메커니즘 규명 — massive activation은 마지막 블록 MLP가 생성한다 (09-11)**

  `L_align`의 fp16 이득이 중간층 이상치 억제의 하류 결과인지(전파 가설) 별개 현상인지를
  개입 실험으로 가렸다 (`scripts/probe_blk31_intervention.py`,
  `scripts/probe_blk31_decompose.py`, 30장 × 5변형).

  **(a) 성분 분해** — 비전 블록은 `h = h + attn(norm1(h))` → `h = h + mlp(norm2(h))`.
  blk31 최댓값 좌표에서 각 성분의 부호 있는 값:

  | 변형 | 잔차 | attn | **mlp** | 합 |
  |---|---|---|---|---|
  | baseline_v2 | −88 | 7 | **−46,848** | −46,848 |
  | kd_only_v4 | −98 | −9 | **−28,544** | −28,672 |
  | temporal | −94 | −10 | **−33,536** | −33,536 |
  | spatial | −148 | −20 | **−17,024** | −17,280 |
  | full | −139 | −29 | **−17,152** | −17,408 |

  **잔차 기여가 전체의 0.2~0.9%에 불과하다.** massive activation은 앞 블록에서 운반된
  것이 아니라 **마지막 블록 MLP가 생성**하며, `L_align`을 쓴 둘은 그 **생성량 자체가
  절반 이하**다(−46,848 → −17,152). 이것이 fp16 여유의 직접적 원인이다.

  **(b) 잔차 개입** — blk30 출력에 스칼라를 곱해 잔차 운반분만 조절:

  | 변형 | 무개입 | 잔차 1/34배 | 잔차 34배 |
  |---|---|---|---|
  | baseline_v2 | 46,592 | 68,608 (147%) | 266,240 (5.7×) |
  | kd_only_v4 | 28,416 | 47,616 (168%) | 268,288 (9.4×) |
  | temporal | 33,280 | 54,016 (162%) | 276,480 (8.3×) |
  | spatial | 17,152 | 20,736 (121%) | 21,248 (1.2×) |
  | full | 16,320 | 12,544 (77%) | 22,016 (1.3×) |

  **잔차를 줄이면 blk31이 오히려 커진다.** 전파 가설이 예측하는 방향과 **반대**다.

  **(c) 간접 경로의 메커니즘 — RMSNorm은 크기는 고정하되 방향은 고정하지 않는다**
  (`scripts/probe_norm2_direction.py`, 20장)

  비전 블록은 `h1 = h + attn(norm1(h))`, `out = h1 + mlp(norm2(h1))` 구조다.
  `norm1`의 입력은 **순수 잔차**이므로 스케일이 완전히 흡수되지만, `norm2`의 입력은
  `잔차 + attn출력` 혼합이라 잔차만 스케일하면 **두 항의 비율이 바뀐다.**

  | 조건 | cos(norm1) | cos(norm2) | cos(mlp) |
  |---|---|---|---|
  | baseline_v2 × 1/34 | **1.00000** | **0.638** | 0.99936 |
  | baseline_v2 × 34 | **1.00000** | **0.914** | 0.99963 |
  | full × 1/34 | **1.00000** | **0.652** | 0.98720 |
  | full × 34 | **1.00000** | **0.918** | 0.99929 |

  `norm1`은 소수점 다섯 자리까지 1.00000 — 개입이 "잔차 운반분만 조절한다"는 전제가
  실증된다. `norm2`는 0.64까지 떨어져 **방향이 바뀌고**, 그래서 최댓값은 거의 그대로인데
  (31.00 → 31.25) MLP 생성량이 5.7배 변한다. `cos(mlp)`가 0.99 이상인 것도 일관된다 —
  **방향은 유지되고 크기만 바뀐다.**

  무개입 시 잔차/attn 최댓값 비가 **baseline 60.5, full 3.8**인 것이 민감도 차이를
  설명한다. baseline은 잔차가 압도적이라 축소 시 구성비가 급변하고(cos 0.638),
  full은 원래 둘이 비슷해 덜 민감하다.

  → **결론 (2026-09-14 정정). 전파 가설을 두 형태로 나눠야 한다.**

  | 형태 | 상태 |
  |---|---|
  | **크기 매개 전파** — "중간층 활성이 크므로 blk31도 크다" | **기각.** 잔차 기여 0.2~0.9%, 잔차를 34배 줄여도 blk31이 오히려 증가(부호 반대), 중간층 격차 33.6배가 blk31에서 2.8배로 축소(92% 소실) |
  | **방향 매개 전파** — "중간층 표현의 방향이 바뀌어 MLP 생성량이 준다" | **기각되지 않았다.** 오히려 (c)의 `norm2` 실험이 지지한다 — 방향이 바뀌면(cos 0.64) MLP 출력이 5.7배 달라진다 |

  **확실한 것**: blk31의 값은 앞 블록에서 **운반된 것이 아니라 마지막 블록 MLP가
  생성**한다(생성 위치). **주장하면 안 되는 것**: "중간층 변화가 blk31에 영향을 주지
  않는다". `L_align`의 개입이 코사인 손실이라 **방향을 바꾸므로**, 가장 그럴듯한 그림은
  **중간층 표현의 방향 변화 → 마지막 블록 MLP 생성량 감소**다.

  이 정정은 5장의 논지를 약화시키지 않는다 — 논지는 "표현 범위 여유가 기존 지표에
  보이지 않으며 학습 설계로 조절된다"이고, 그 조절이 어느 경로를 타는지와는 무관하다.

- **사후 개입 실험 — 어디서 줄이느냐가 전부다 (09-11)**

  `scripts/probe_mlp_overflow.py`, `scripts/probe_posthoc_rescale.py` (250장 + 300문항).
  비전 MLP는 `down_proj(act(gate_proj(x)) * up_proj(x))`이다. 아래 hook 개입은
  **weight와 bias를 함께 스케일하는 가중치 편집과 등가**다.

  > **정정 (2026-09-14)**: 이전 판은 "bias가 없어"라고 적었으나 **틀렸다**. `Qwen2_5_VLVisionBlock`은 `Qwen2_5_VLMLP(config, bias=True)`로 생성하므로 비전 MLP에는 bias가 있다(`gate_proj.bias` 등이 실제 가중치에 존재한다). 훅은 `gate_proj`의 **출력**(`Wx+b`)을 스케일하므로, 등가인 가중치 편집은 **weight와 bias를 함께** 나누는 것이다. weight만 나누면 bias가 남아 다른 연산이 된다. 실험 결과 자체는 훅으로 측정했으므로 영향이 없고, **처방 서술만 정정**한다.

  > **실측 후속 (2026-09-14 21:22, `scripts/probe_fixa_weight_equiv.py`, 250장 fp16)** —
  > 네 조건을 직접 재서 위 정정의 실제 영향을 확인했다.
  >
  > | 조건 | 붕괴율 |
  > |---|---|
  > | 무개입 | 95.2% |
  > | 훅 fixA (원래 실험) | **0.0%** |
  > | 가중치 fixA (weight+bias) | **0.0%** ← ONNX로 내보낸 판본 |
  > | 가중치 fixA (weight만) | **0.0%** |
  >
  > **`weight`만 편집해도 결과가 같다.** 이 스케일에서는 bias 기여가 붕괴 여부를 바꾸지
  > 않는다. 따라서 정정 전 서술의 **결론**("가중치를 1/√3로 나누면 된다")은 틀리지 않았고,
  > 틀린 것은 **근거**였다 — "bias가 없어서"가 아니라 "이 스케일에서 bias 기여가 작아서"다.
  > 논문에는 근거를 이렇게 정확히 쓰고, **일반적으로는 weight와 bias를 함께 나눠야 훅과
  > 등가**임을 함께 적는다(다른 스케일·다른 층에서는 갈릴 수 있다).
  >
  > 부수 소득: 이 실험이 **보드 측정의 전제를 닫았다.** Orin에 보낸 fixA ONNX가 A100에서
  > 잰 것과 같은 개입임이 확인됐다.

  | 개입 | 곱 | down 출력 | 붕괴율 (원본 해상도) | 정확도 (평가 해상도, 11,309문항) |
  |---|---|---|---|---|
  | 없음 | 4,588 | 63,200 | **94.4%** | 50.55% |
  | MLP 출력 1/3 (fixB) | 4,588 | 63,360 | 94.8% | — |
  | **gate·up 각 1/√3 (fixA)** | **1,530** | **24,800** | **0.0%** | **49.96% (−0.59pp)** |

  최초 inf 발생 지점은 세 조건 모두 `down_proj`다. fixB는 **이미 inf가 된 값을 나누므로**
  실패하고, fixA는 **곱이 만들어지기 전에** 줄이므로 성공한다.

  > **정정 (2026-09-14, 전체 11,309문항 재측정).** 이전 판은 300문항 결과
  > (53.33% → 54.67%, **+1.34pp**)를 근거로 **"정확도 변화 없음"**이라 적었다.
  > **틀렸다 — 전체로 올리니 부호가 뒤집혔다**(50.55% → 49.96%, **−0.59pp**).
  > 300문항에서는 노이즈가 ±0.7pp라 1.34pp가 "개선"으로 보였던 것이다.
  > 전체 평가를 돌린 이유가 정확히 이것이고, 결과가 그 필요성을 입증했다.
  > 결과: `eval_results/posthoc_rescale_full.json`
  >
  > **이 0.59pp는 시드 노이즈가 아니다.** 5-way의 0.35pp와 성격이 다르다 — 같은
  > 가중치에 결정론적 편집만 가한 짝지은 비교라 **학습 무작위성이 전혀 없다.**
  > (엄밀히 하려면 문항별 덤프로 McNemar가 필요하나 이 실행에는 덤프가 없다.)
  >
  > **측정 조건을 정확히 읽을 것.** 정확도는 **평가 해상도(200,704)**에서 bf16으로 쟀다.
  > 그 해상도에서는 무개입도 거의 붕괴하지 않으므로(3.2%), 이 수치는 **개입의 순수
  > 비용**이다. 원본 해상도에서는 무개입이 94.4%의 이미지에서 출력을 내지 못하므로
  > 이 비용은 상쇄되고도 남는다.

> **McNemar 검정 결과 — "비용 0.59%p"를 철회한다 (2026-09-15).**
  > 문항별 덤프로 짝지어 검정하니 **p = 0.2503으로 유의하지 않다**
  > (불일치 681쌍: 무개입만 356 / fixA만 325, χ²(1)=1.32).
  > **평가 표본 잡음과 구별되지 않는다.**
  >
  > **더 중요한 것은 측정 자체가 흔들린다는 점이다.** 같은 무개입 조건을 두 번
  > 돌렸더니 50.55% → 50.30%로 **0.26%p 달랐다**(`do_sample=False`인데도).
  > cuDNN/cuBLAS 비결정성으로 보이며, 실행 중 `cudnnException` 폴백 경고가 있었다.
  > 즉 **관측된 효과(0.27%p)와 같은 크기로 측정이 흔들린다.**
  >
  > 이는 앞서 적은 "이 0.59pp는 시드 노이즈가 아니다 — 결정론적 편집이라 학습
  > 무작위성이 없다"는 서술을 **직접 반증한다.** 학습 무작위성이 없는 것은 맞지만
  > **추론 자체가 결정론적이 아니었다.**
  >
  > **정확한 진술**: "fixA는 원본 해상도에서 붕괴를 94.4% → 0.0%로 제거하며,
  > 정확도 변화는 **측정 한계 이하**다(11,309문항 McNemar p=0.25, 동일 조건
  > 재실행 변동 0.26%p)."
  >
  > 유형별로는 `count`만 −1.04%p에 p=0.0122였으나 **다중 비교 보정을 하지 않았고**
  > (5개 유형, Bonferroni 임계 0.01), `count`는 모든 변형에서 7~11%로 원래 약한
  > 유형이다. **유형별 주장은 하지 않는다.**
  > 결과: `eval_results/mcnemar_fixa.json`

  **서술 방침**: **"원본 해상도에서 붕괴를 94.4% → 0.0%로 제거하며, 정확도 변화는
  측정 한계 이하다"**로 쓴다. "공짜"라고 단정하지도, "0.59%p 비용"이라 쓰지도 않는다 —
  전자는 동등성을 증명한 것이 아니고(비유의는 동등성의 증거가 아니다), 후자는 반증됐다.

  **이 결과가 5장의 강조점을 바꾼다 (사전 등록 방침 적용).**
  - "손실 설계가 배포 가능성을 **결정한다**"는 표현을 **쓰지 않는다.**
  - 정확한 진술: **"이 활성값은 기능에 기여하지 않으면서 표현 범위 예산을 전부 소모한다.
    손실 설계는 그 여유를 자동으로 확보해 주고, 그렇지 않은 모델도 사후 수정이 가능하지만
    아무도 측정하지 않아 방치되어 있다."**
  - 실무 기여는 오히려 강해진다 — 6장의 위험 구성 27개에 **바로 적용 가능한 처방**이 생긴다.

- **가중치 분포 — 증폭기는 사전학습에 박혀 있다 (09-11)**

  `scripts/probe_weight_distribution.py` (LoRA 병합 후 행 노름, CPU).

  | 모델 | blk31 gate | up | **down** | down max/p50 | top 행 |
  |---|---|---|---|---|---|
  | base_3b ~ student_full (6개 전부) | 1.7 | 1.7 | **13.3** | **9.3** | **849** |

  **6개 변형이 완전히 동일하다.** LoRA가 비전 MLP에 걸려 있으나 델타가 base 대비
  무시할 수준이다(lora_A absmax 0.027, lora_B 0.007). 따라서 현상은 **가중치 이상치가
  아니라 활성**이며, 변형 간 차이는 `down_proj`에 **들어가는 입력**에서 온다.

  → 메커니즘 4단계: ① 사전학습 가중치의 `down_proj` 849행 이상치(중앙값의 9.3배)가
  증폭기다 → ② 미세조정은 이 증폭기를 바꾸지 않는다(가중치 이상치는 6개 변형 동일)
  → ③ 변형 간 차이는 증폭기로 가는
  입력(곱)의 크기다 → ④ `L_align`은 중간층 표현을 바꿔 그 입력을 줄인다.

  **활성 증가의 귀속 — "미세조정이 키운다"는 부정확하다 (2026-09-15 실측).**
  v1(LLM LoRA만)과 v2를 동일 250장에서 짝지어 재니 분해가 달라졌다.

  | 단계 | blk31 p50 | 여유 | 직전 대비 |
  |---|---|---|---|
  | 사전학습 `base_3b` | 33,312 | 1.97배 | — |
  | + 과제 손실 미세조정 (`baseline_v1`, LLM LoRA만) | 30,024 | **2.18배** | **0.90배** |
  | + 비전-어텐션 LoRA & hazard 오버샘플 (`baseline_v2`) | 47,296 | **1.38배** | **1.58배** |

  **과제 손실 미세조정 자체는 활성을 키우지 않는다 — 오히려 10% 줄인다.**
  `base_3b` → `baseline_v2`의 1.42배 증가는 `0.90배 × 1.58배`이고, 키운 쪽은
  두 번째 단계다. 붕괴도 v1은 0/250, v2는 8/250이다.

  > **다만 귀속을 확정할 수 없다 (2026-09-15).** v1 → v2는 `training_state.pt` 대조
  > 결과 **두 변수가 바뀐다** — `lora_target_vision_attn`(False→True)과
  > **`hazard_oversample`(False→True)**. 후자는 DriveLM 부분집합 내 샘플러 가중치를
  > 바꾸므로 **데이터 분포 변경**이다. 따라서 1.58배를 "비전-어텐션 LoRA 용량 때문"으로
  > 쓸 수 없다. (`CLAUDE.md`가 "exactly one variable changes"라고 적고 있었는데
  > 사실과 달라 함께 정정했다.)
  >
  > 분리하려면 `hazard_oversample=True` + `lora_target_vision_attn=False`인 세 번째
  > 실행이 필요하며 약 51.6시간이다.

  **그래도 5장 서술은 바꿔야 한다.** 지금까지 "과제 손실만으로 미세조정한 학생이 활성을
  1.5배 키웠다"고 써 왔는데, **그 귀속이 틀렸다.** 정확한 진술은 이렇다.

  > 사전학습 모델 대비 활성이 커진 것은 과제 손실 미세조정 때문이 아니다. LLM LoRA만
  > 쓴 변형은 오히려 활성이 10% 낮다. 증가는 **비전 인코더까지 LoRA를 확장하고 데이터
  > 샘플링을 바꾼 단계**에서 나타나며(1.58배), 두 요인은 본 연구에서 분리되지 않았다.

  **이것이 §2 넷째 절(수단은 여럿)의 세 번째 증거이자 방향이 반대인 첫 사례다** —
  `L_align`과 fixA는 여유를 늘리는 쪽, 이것은 줄이는 쪽이다. 실무적으로는
  **"LoRA를 비전 인코더까지 확장하는 흔한 선택이 fp16 여유를 2.18배 → 1.38배로 줄이고
  붕괴를 0/250 → 8/250으로 만드는데 아무도 재지 않는다"**가 된다.
  결과: `eval_results/v1_vs_v2_profile.json`

  **849는 잔차 스트림의 전용 채널이다 (2026-09-14 실측, `scripts/probe_channel849.py`).**

  두 가지를 함께 쟀다. 하나는 32개 블록 전부의 `down_proj` 849행 노름(가중치만),
  다른 하나는 마지막 블록 출력에서 **실제로 최대 활성이 나오는 채널**(원본 해상도 50장)이다.
  후자는 5장 서술의 전제 검증이다 — 오버플로가 다른 채널에서 일어난다면 "849행이
  증폭기"라는 서술이 틀리기 때문이다.

  | blk | 배율 | blk | 배율 | blk | 배율 |
  |---|---|---|---|---|---|
  | 0 | 2.79 | 16 | 2.21 | 28 | 1.99 |
  | 4 | 1.90 | 20 | 2.03 | 30 | 2.52 |
  | 8 | 1.58 | 23 | 2.42 | **31** | **9.33** |
  | 12 | 1.83 | 24 | 2.25 | | |

  - **849행이 32/32 블록 전부에서 최상위**다. 예외가 없다.
  - **점진적 증폭이 아니라 마지막 블록의 불연속이다.** blk0~30은 1.58~2.79배로 평탄하고
    blk31만 9.33배다(blk30 대비 3.7배 도약). **"깊이에 따라 증폭된다"고 쓰지 말 것** —
    "모든 블록이 쓰는 전용 채널인데 마지막 블록만 특별히 크게 쓴다"가 정확하다.
  - **활성 최대 채널이 50/50장에서 849번**이고, 849번/전역 최대 비율 중앙값이 1.0000이다.
    그 지점의 활성 중앙값은 **73,448**로 fp16 한계 65,504를 넘는다.
    → 가중치 이상치와 활성 이상치가 **같은 채널**임이 확정됐다. 5장 서술의 전제가 성립한다.

  결과: `eval_results/channel849.json`

  **이 행은 이 체크포인트 고유가 아니다 (2026-09-14 실측).**
  `scripts/probe_downproj_outlier_across_models.py`로 세 모델의 마지막 비전 블록
  출력 선형층 행 노름을 비교했다(가중치만, GPU 불필요).

  | 모델 | 최대 배율 | 행 번호 | 2위 |
  |---|---|---|---|
  | Qwen2.5-VL-3B (본 연구) | **9.32배** | **849** | 1.06배 |
  | Qwen2.5-VL-7B (교사) | **9.55배** | **849** | 1.25배 |
  | Qwen2-VL-2B (이전 세대, `fc2`) | 14.80배 | 759 | 1.12배 |

  세 모델 모두 **단 하나의 행만** 튄다 — 2~5위가 전부 1.05~1.25배에 몰려 있어
  점진적 분포의 꼬리가 아니라 별개의 것이다.

  **3B와 7B가 같은 849행인데 가중치는 다르다.** 두 모델은 비전 config가 완전히 동일해
  (depth 32 / hidden 1280 / intermediate 3420) "같은 ViT를 물려받았을 뿐"일 가능성이
  있었으므로, 노름 벡터를 직접 대조했다 — 최대 차이 0.949(중앙값 대비 66%)로 **다른
  가중치**다. 즉 독립적으로 학습된 두 모델이 같은 채널 번호에 이상치를 만들었다.
  이 대조를 넣지 않았다면 결과를 "같은 ViT를 썼을 뿐"으로 오독했을 것이다.

  → **현상은 Qwen 비전 타워 계열 공통이고, 위치는 아키텍처가 같으면 같다.**
  결과: `eval_results/downproj_outlier_across_models.json`

  **여전히 답하지 못하는 것 — 왜 849인가.** 사전학습 데이터·과정에 접근해야 하므로 본
  연구 범위 밖이다(중간 체크포인트도 공개되지 않았고, 630M ViT를 직접 사전학습해 재현하는
  것은 학위논문 규모를 넘는다). 다만 **위 논증은 그 답을 필요로 하지 않는다**: 증폭기가
  존재하고, 6개 변형에서 동일하며, 달라지는 것은 거기 들어가는 입력이라는 세 사실만으로
  닫힌다. 표본이 셋뿐이라는 점도 함께 적는다.

- **인과 논증의 구조 — 2단계 사슬 (중요)**

  인과를 하나의 실험으로 세우려 하지 않는다. 두 링크를 각각 다른 실험이 담당한다.

  | 링크 | 담당 실험 | 결과 변수 | 상태 |
  |---|---|---|---|
  | ① **λ → 활성 크기** | λ 스윕 (0 / 0.5 / 1.0 / 2.0 × 5,000스텝, 시드 고정) | 연속 (max\|act\|) | 진행 중 |
  | ② **활성 크기 → 붕괴** | 해상도 스윕 (완주 모델 6개 × 5해상도) | 이산 (붕괴율) | **완료** |

  **두 링크가 같은 모델에서 동시에 측정될 필요가 없다.** ①은 손실 가중치가 활성 크기를
  좌우함을, ②는 활성 크기가 배포 성패를 좌우함을 각각 보인다.

  이 분리가 중요한 이유: λ 스윕은 5,000스텝이라 blk31 활성이 완주 모델보다 낮고,
  따라서 **붕괴율이 전 구간 0%로 나올 가능성이 높다.** 붕괴율을 ①의 결과 변수로 삼으면
  포화된 이진 변수를 쓰는 설계 오류가 된다(②에서도 30점 중 24점이 0%였다).
  ①은 반드시 **연속 변수인 활성 크기**로 측정한다.

- **λ 용량-반응 (그림 3) — 완료 (2026-09-12)**

  시드 1234·학습률 궤적 고정, 각 5,000스텝. 250장 프로파일.

  | λ | blk16 | blk24 | blk28 | **blk31** | **fp16 여유** | 붕괴율 |
  |---|---|---|---|---|---|---|
  | 0.0 | 896 | 8,512 | 8,512 | 36,608 | 1.79× | 0% |
  | 0.5 | 167 | 5,856 | 5,856 | 26,112 | 2.51× | 0% |
  | 1.0 | 55 | 124 | 187 | 19,072 | 3.43× | 0% |
  | 2.0 | 55 | 128 | 212 | **14,208** | **4.61×** | 0% |

  **층마다 거동이 다르다.**
  - **최종 블록(blk31)은 단조 감소하며 구간별 감소율이 28.7 / 27.0 / 25.5%로 일정하다.**
    fp16 여유가 1.79 → 4.61배로 늘어난다. **배포 여유를 결정하는 층에서 뚜렷한 용량-반응**이며,
    λ=2.0까지 포화 조짐이 없다. → **링크 ①(λ → 활성 크기)의 인과 근거**
  - 중간층(blk24·blk28)은 λ=1.0에서 급락한 뒤 **포화**한다(124 → 128). blk16도 55에서 멈춘다.

  이 층별 차이는 개입 실험에서 얻은 결론(중간층 억제와 최종 블록 생성량 억제가 독립)과
  일관된다.

  **붕괴율은 4점 모두 0%다.** 5,000스텝 모델은 blk31이 14,000~36,000이라 fp16 상한에 여유가
  있다. 예상된 결과이며, **결과 변수를 연속량(활성 크기)으로 잡은 2단계 사슬 설계가
  옳았음을 확인**한다 — 붕괴율로 쟀다면 4점 모두 0%라 아무것도 보이지 않았을 것이다.
- 배포 설계 지표 제안: `format_max / p99(max|act|)`
- 선행 연구 위치: CrAFT는 optimizer 축, 이상치 억제 정규화 계열은 설계된 처방,
  PASA·vLLM Issue #40290은 현상 선점이나 아키텍처 기인. **증류 손실 축은 미측정**
  (`"activation magnitude"` AND `"fp16"` 0건)

### 6장 따름정리 — 이산적 실패는 집계 지표를 무력화한다
별개 기여가 아니라 §2 논지의 직접적 귀결이다. 실패가 이산적·파국적이기 때문에
교란 변수 하나가 표 전체를 뒤집는다.

- INT4 정확도 1.39% 관측 → 원인은 양자화가 아니라 (i) 저장 dtype, (ii) 입력 해상도

  **두 변수를 고정한 재측정 (2026-09-15 완료).** 같은 AWQ 체크포인트를
  `--dtype bfloat16`으로 로드하고 processor를 base에서 만들어(max_pixels 200,704)
  전체 11,309문항을 다시 쟀다. 태그에 `_bfloat16`이 붙어 **교란값 파일을 덮지 않으므로
  둘을 나란히 실을 수 있다.**

  | 변형 | INT4 교란 | **INT4 정정** | bf16 원본 | 정정−원본 |
  |---|---|---|---|---|
  | `baseline_v2` | **1.39%** | **48.30%** | 50.30% | −2.00pp |
  | `full` | 51.60% | 51.86% | 51.69% | +0.17pp |
  | `spatial` | 50.82% | 50.64% | 51.40% | −0.76pp |
  | `temporal` | **30.49%** | **50.31%** | 51.34% | −1.03pp |
  | `kd_only_v4` | **32.99%** | **47.96%** | 46.74% | **+1.22pp** |

  **`baseline_v2` 1.39% → 48.30%, `temporal` 30.49% → 50.31%.** 두 변수만 고정했는데
  회복됐다. **양자화 때문이 아니었음이 수치로 확정된다** — 6장의 핵심 주장이 이제
  말이 아니라 표다.

  **서술 규칙.** 교란을 걷어내면 INT4 자체의 손실이 보이지만(정정−원본 열),
  **이것을 순위 주장으로 쓰지 않는다.** 각 변형이 n=1이고 `full`의 +0.17pp는 노이즈
  범위다. 쓸 수 있는 진술은 **"교란을 제거하면 다섯 변형이 모두 48~52%대로 모이며
  1.39% 같은 파국적 값은 사라진다"**까지다. `baseline_v2`의 −2.00pp가 다른 것보다
  크다는 점은 **관찰로만** 적고, "정렬 손실이 INT4 강건성을 준다"로 확대하지 않는다
  (3장 통계 서술 규칙 적용).

  **마지막 값이 이 규칙의 필요성을 입증했다.** `kd_only_v4`가 **+1.22pp**로 `full`
  (+0.17pp)보다 크게 올랐다 — **정렬 손실이 없는 변형이 가장 많이 회복한 것이다.**
  "정정−원본" 열에는 일관된 방향이 없다(−2.00 ~ +1.22pp). 만약 `kd_only_v4`를 재기 전에
  네 개만 보고 "정렬 손실이 INT4에서 덜 잃는다"로 썼다면 틀린 서술이 됐을 것이다.

  결과: `eval_results/*_4bit_awq_bfloat16_nuscenesqa_val.json`
- **일반성 검증 (2026-09-10 완료)**: HuggingFace 공개 양자화 VLM 체크포인트 **197개**의
  config를 수집해(`scripts/survey_quantized_vlm_configs.py`) 같은 패턴의 빈도를 셌다.

  | 패턴 | 비율 |
  |---|---|
  | `torch_dtype == float16` | 55/197 (28%) |
  | 비전 타워를 양자화에서 제외 | 67/197 (34%) |
  | **둘 다 — 비전 타워가 fp16으로 실행** | **27/197 (14%)** |
  | processor `max_pixels` > 4M | 56/197 (28%, 대부분 12,845,056) |

  위험 구성 27개에는 **모델 제작사 공식 저장소 3개**가 포함된다 —
  `Qwen/Qwen2-VL-{2B,7B,72B}-Instruct-AWQ`. 그중 7B는 `max_pixels`까지
  12,845,056으로, **본 연구가 겪은 것과 완전히 동일한 구성**이다.

  → 6장은 "우리가 실수했다"가 아니라 **"공개 양자화 VLM의 14%가 같은 조건에 놓여 있고,
  우리가 조사한 범위에서 그 여유를 보고한 저장소는 없었다"**가 된다.

  > **실측이 이 서술을 더 좁힌다 (2026-09-15).** 위험 구성 중 실제로 잰 셋이
  > **전부 안전했다** — Qwen2-VL-2B 3.88배, Qwen2-VL-7B 4.49배, SmolVLM 50.86배.
  > 따라서 **"공개 체크포인트가 위험하다"고 쓸 수 없다.** 실측으로 위험이 확인된 것은
  > **Qwen2.5-VL-3B 계열뿐**이며, 그중에는 미세조정 전 `base_3b`도 포함된다
  > (원본 해상도 여유 1.05배, 붕괴 69.6%).
  >
  > **그래서 실무 주장의 범위는 이렇다**: "위험 구성은 드물지 않다(13.7%). 그러나 구성만으로는
  > 알 수 없고 재봐야 한다 — 실제로 우리가 잰 셋은 안전했고, 위험했던 것은
  > **사전학습 상태의 Qwen2.5-VL-3B**였다." 범위는 좁지만 실측으로 뒷받침된다.
- **위험 구성의 실측 — 27개 중 4개만 잴 수 있었다 (2026-09-15 완료).**
  config 집계를 실측 표로 격상하려 했으나, **생태계의 버전 파편화 때문에 대부분
  로드되지 않았다.** 그 실패 자체가 6장의 논지를 보강한다.

  | 모델 | fp16 여유 | 비고 |
  |---|---|---|
  | **Qwen2.5-VL-3B** (본 연구 대상) | **1.05배** | 원본 해상도에서 69.6% 붕괴 |
  | Qwen2-VL-2B-AWQ | 3.88배 | 공식 위험 구성 |
  | Qwen2-VL-7B-AWQ | 4.49배 | 공식 위험 구성 |
  | SmolVLM-Instruct (Idefics3) | 50.86배 | 비-Qwen 계열 |

  **가중치가 실제로 로드됐는지 별도로 검증했다**(`scripts/verify_family_weights.py`,
  결과 `eval_results/family_weight_verification.json`). 세 모델 모두 비전 타워의
  norm 파라미터 24개 중 정확히 1.0인 것이 **0개**이고 "newly initialized" 경고도
  없다 — 랜덤 초기화가 아니다.

  > **이 검증이 왜 필요했나.** 체크포인트 키 규약이 이 `transformers` 버전과 다르면
  > `from_pretrained`가 **경고만 찍고 랜덤 초기화 모델을 돌려준다.** 로드는 "성공"하고
  > 비전 타워도 찾아지며 활성값도 나온다 — 그 값이 난수일 뿐이다. 실제로
  > `AngelSlim/Qwen2.5-VL-3B-INT4-AWQ`에서 이 일이 일어났고, 검증을 넣지 않았다면
  > **난수를 논문에 실었을 것이다.**

  **측정하지 못한 것과 그 이유** — "그냥 빠진 것"과 구분해 적는다.

  | 사유 | 해당 | 내용 |
  |---|---|---|
  | config 구조 불일치 | Qwen2.5-VL AWQ 4종 | 저장본이 **새 `transformers`의 중첩 config**(`text_config`)로 쓰여 4.49의 평면 구조와 충돌 |
  | 가중치 키 규약 불일치 | 위와 동일 | 저장본은 `model.visual.*`, 4.49는 `visual.*` → 전 가중치 랜덤 초기화 |
  | 아키텍처 미인식 | Qwen3-VL 30B·32B | 4.49가 `qwen3_vl` / `qwen3_vl_moe`를 모름 |
  | 커스텀 코드 의존성 | InternVL3-8B, Phi-3.5-vision | `timm` 등 미설치, config 클래스 미인식 |
  | 디스크 | Qwen2-VL-72B | 4bit로도 40GB |
  | 원인 미상 | LLaVA-1.5-7B-AWQ | 프로세서 경로에서 `TypeError` |

  **`transformers`를 올리지 않기로 했다.** 15개 스크립트가 4.49 내부 API에 묶여 있고
  (`visual.blocks`, `rot_pos_emb`, `get_window_index`, `fullatt_block_indexes`,
  `spatial_merge_unit`, ONNX export의 원숭이 패치 9곳), **지금까지의 모든 측정이
  4.49에서 나왔다.** 업그레이드하면 한 논문에 두 버전의 수치가 섞인다. 얻는 것(모델
  6~7개 추가)이 잃는 것(전체 측정의 일관성)보다 작다. autoawq 0.2.9도 공식 폐기됐고
  마지막 검증 조합이 transformers 4.51.3이다.

  → **6장에 이렇게 쓴다.** "공개 양자화 VLM 27개 중 실제로 여유를 측정할 수 있었던 것은
  4개뿐이었다. 나머지는 config 구조·가중치 키 규약·아키텍처 인식이 서로 달라 단일
  `transformers` 버전에서 로드되지 않았다. **'재면 된다'는 처방조차 생태계의 버전
  파편화 때문에 실행이 쉽지 않으며, 이는 아무도 재지 않는 이유의 일부다.**"
  이 서술은 논지를 약화시키지 않고 **"왜 안 재는가"에 대한 답을 추가한다.**

- 실무 권고: 정밀도 비교 시 고정해야 할 항목 체크리스트

### 7장 저정밀 배포 검증 파이프라인과 실기기 확인

> **2026-09-15 위상 변경.** 원래 "Orin에서 붕괴가 재현되는가"만 확인하는 장이었으나,
> **논문이 방법 기여가 약하다는 지적**에 따라 이 장을 **검증된 배포 절차(시스템 기여)**로
> 격상한다. 만든 것이 셋이다 — (i) 고정 형상 ONNX export 경로, (ii) 정밀도 조건별 엔진
> 빌드·측정 절차, (iii) 해상도 상한 결정 방법. 여기에 8장의 `headroom_guard` 도구가
> 붙으면 "진단 → 교정 → 배포 검증"이 하나의 경로로 닫힌다.

**이 장이 기여로 성립하는 근거.** VLM 비전 타워를 엣지 보드에 fp16으로 올리는 절차는
공개된 것이 없다. 실제로 막히는 지점이 넷이었고 전부 해결했다.

| 막힌 지점 | 해결 | 근거 |
|---|---|---|
| `grid_thw` 의존 동적 로직(회전 위치 임베딩·윈도우 인덱스·`cu_seqlens`)이 ONNX로 안 나감 | 배포 해상도 고정 전제로 **상수화 래퍼** | 원본 대비 오차 0.000e+00 |
| 어텐션 마스크 채움값 `finfo.min`이 fp16에서 −inf가 되어 무관한 NaN 생성 | 채움값을 **−1e4**로 (exp(−1e4)는 fp32에서도 0) | 상수화 대조 통과 |
| 트레이서가 큰 상수를 블록마다 복제해 파일이 3배로 부풂 | 저장 전 **`dedup_constants()`** — 8.32 GiB → 2.69 GiB | 코사인 0.999920 동일 |
| 외부 데이터(`.onnx.data`) 그래프를 TensorRT가 파싱 못 함 | `parse(bytes)`가 아니라 **`parse_from_file`** | 9,177 레이어 정상 파싱 |

**정밀도 조건을 나눠 재는 절차**도 이 장의 산출물이다. `--fp16`은 fp16을 *허용*하는
것이지 강제가 아니므로, 관행·엄격·fp32 셋을 나눠 재야 "런타임이 회피하는가"에 답할 수
있다(7.2에서 실제로 답했다).


**왜 새 장인가.** 5장까지의 붕괴 측정은 전부 A100·PyTorch에서 이루어졌다. 「실차 배포 시
위험」을 주장하려면 **실제 엣지 하드웨어의 fp16 산술**에서 같은 일이 일어나는지 보여야
한다. 부수적으로 이 장은 현재 논문의 빈 자리 하나를 메운다 — **경량화를 표방하면서
지연시간·메모리 수치가 하나도 없다.**

**5장에만 붙는다.** 사례 1(정지 기능 상실)은 학습 산물이라 어떤 런타임에서도 동일하게
나타나므로 실기기 검증이 필요 없다. 사례 2만 배포 시점의 수치 표현 문제이므로 이 장은
**사례 2의 검증 장**이다. 이 비대칭을 장 서두에 명시한다.

#### 7.1 대상과 준비 (완료)

비전 타워만 ONNX로 내보낸다. 검증 대상이 비전 인코더 마지막 블록의 활성 크기이므로
LLM은 필요 없고, 630M 파라미터(fp16 1.17 GiB)라 8GB 보드에 올라간다.
`grid_thw`에 의존하는 동적 로직(회전 위치 임베딩·윈도우 인덱스·`cu_seqlens`·어텐션 마스크)을
배포 해상도 기준으로 상수화한 래퍼를 쓴다. `scripts/export_vision_tower_onnx.py`.

| 파일 | `max_pixels` | 패치 | 상수화 대조 | ONNX 코사인 |
|---|---|---|---|---|
| `tower_baseline_v2_native.onnx` | 1,440,000 | 7,296 | 0.000e+00 | 0.999920 |
| `tower_full_native.onnx` | 1,440,000 | 7,296 | 0.000e+00 | 0.999999 |
| `tower_baseline_v2.onnx` | 200,704 | 924 | 0.000e+00 | 0.999978 |
| `tower_full.onnx` | 200,704 | 924 | 0.000e+00 | 1.000000 |

검증을 두 단계로 나눈 이유를 본문에 적는다. **절대오차로 판정하면 안 된다** — 블록 32개를
지나며 누적되는 fp32 재정렬 오차만으로 최대 절대오차가 수 단위까지 벌어지고, 이는 **같은
PyTorch 코드를 CPU와 CUDA에서 돌려도 동일하게 나타난다**(실측 0.8394). 따라서 토큰별
코사인 유사도로 판정하고 CPU/CUDA 차이를 기준선으로 함께 보고한다.

#### 7.2 측정 프로토콜

A100과 **같은 250장·같은 해상도**를 넣어 출력의 NaN/Inf 발생 이미지 비율을 센다.
A100 기준값(원본 해상도): `student_baseline_v2` 94.8% / `student_full` 0.0%.

**정밀도 조건을 둘로 나눈다.** `trtexec --fp16`은 "fp16을 **허용**"이지 "강제"가 아니며,
TensorRT는 레이어별로 fp32를 남길 수 있다. 두 조건 모두 의미가 있다.

| 조건 | 명령 | 무엇을 답하는가 |
|---|---|---|
| **관행** | `--fp16` | 실제 배포 관행에서 무슨 일이 일어나는가 |
| **엄격** | `--fp16 --precisionConstraints=obey --noTF32` | 우리 주장의 수치적 조건이 성립하는가 |
| 대조군 | (기본 fp32) | 붕괴가 정밀도 때문임을 확인 |

레이어별 실제 정밀도는 `--verbose` 빌드 로그로 확인해 함께 싣는다.

### 7.2 실측 — TensorRT는 이 오버플로를 자동 회피하지 않는다 (2026-09-15, A100)

**보드 없이 답할 수 있는 질문이었다.** "런타임이 알아서 막아주지 않나"는 TensorRT의
정밀도 배치 정책에 대한 질문이지 Orin에 대한 질문이 아니다. A100에 TensorRT 10.13을
별도 가상환경(`/venv/trt`)에 설치해 확인했다 — 기존 파이프라인(transformers 4.49)은
건드리지 않았다.

| 조건 | 붕괴율 (50장, 원본 해상도) | 가중치 메모리 | 지연 p50 | 대비 fp16 |
|---|---|---|---|---|
| fp32 대조군 | **0.0%** | 2,760 MiB | 441.0 ms | 3.16배 |
| **fp16 (관행, `--fp16` 상당)** | **94.0%** | **1,385 MiB** | **139.7 ms** | 1.00배 |
| **bf16** | **0.0%** | 1,385 MiB | 197.4 ms | **1.41배** |
| fp16 엄격 | 빌드 실패 (아래) | — | — | — |
| *참고: A100 PyTorch fp16* | *94.8%* | — | — | — |

**관행 설정에서 그대로 붕괴한다.** 가중치 메모리가 정확히 절반(2,760 → 1,385 MiB)이므로
네트워크가 실제로 fp16으로 변환됐고, 빌드 로그의 Engine Layer Information에서도 텐서
대부분이 `Half`로 배치됐음이 확인된다. **TensorRT의 레이어별 정밀도 선택은 이 오버플로를
막지 못한다.**

PyTorch 94.8%와 TensorRT 94.0%가 거의 일치한다. **"PyTorch 특유의 아티팩트"라는 반론도
함께 막힌다.**

**bf16 대조 (2026-09-15 추가) — 형식 선택 자체가 교환이다.** "그냥 bf16으로 빌드하면
되지 않나"는 정당한 반론이므로 같은 그래프·같은 50장으로 재봤다. bf16 엔진은 가중치가
fp16과 동일한 1,385 MiB이면서 **붕괴가 0.0%**로 사라진다 — 표현 범위 가설의 독립적
확인이다(활성 크기는 그대로인데 형식 상한만 3.4e38로 올렸더니 붕괴가 없어졌다).
그러나 지연은 **139.7 ms → 197.4 ms로 1.41배** 늘어난다. A100 기준이므로 Orin에서의
비율은 다를 수 있으나, **"공짜 해법"이 아니라는 점은 남는다.**

> **사전 등록한 두 해석 중 "bf16이 느리다" 쪽이 나왔다 (등록: 결과 보기 전).**
> 등록한 문장 그대로 쓴다 — *형식 선택 자체가 교환이며, 그 판단도 이 값을 재야
> 내릴 수 있다.* bf16을 "가장 단순한 처방"이라고 쓰지 않는다. 실시간 제약이 있는
> 탑재 환경에서 41% 지연 증가는 무시할 수 없고, 더구나 공개 저장소 상당수가 이미
> fp16으로 가중치를 저장해 배포한다.

**세 형식을 나란히 놓으면 선택지가 보인다.** fp32는 안전하지만 3.16배 느리고 메모리가
2배다. fp16은 가장 빠르지만 이 체크포인트에서 94% 붕괴한다. bf16은 안전하고 메모리도
fp16과 같지만 1.41배 느리다. 가중치 교정(fixA/`headroom_guard`)은 fp16의 속도를 유지한
채 붕괴를 없애지만 완전 등가가 아니다(§8). **어느 쪽을 고르든 먼저 여유를 재야 선택지가
생긴다는 것이 이 표의 요지다.**

> **사전 등록한 예상이 빗나갔다.** 7.3 분기표에서 "엄격에서만 재현, 관행에서는 안전"이
> 가장 가능성 높다고 적었으나, 실제로는 **관행에서 재현**됐다. 등록된 대로 결과를
> 그대로 적는다 — 이 경우 해당 분기의 서술("주장을 축소하지 말고 조건을 특정한다")은
> 적용되지 않으며, 주장이 실제 배포 도구에서 성립한다.

**엄격 조건 빌드 실패는 결론에 영향이 없다.** 원인은 모델이 아니라 스크립트다 —
모든 레이어에 일괄로 fp16을 강제했는데 인덱스를 계산하는 레이어에는 적용할 수 없다
(`cannot use precision Half for layer that computes indices`). 애초에 엄격 조건은
**관행에서 안전했을 때만** 필요했으므로 없어도 결론이 선다.

**부수 관찰 — 런타임이 대신 막아줄 여지가 줄고 있다.** TensorRT 10.12부터
`layer.precision`이 deprecated(강타입으로 대체)이고, **11.x는 `FP16`·
`OBEY_PRECISION_CONSTRAINTS` 플래그를 아예 제거**해 정밀도가 ONNX 그래프의 dtype으로
결정된다. 즉 "fp16을 허용하고 빌더가 고르는" 방식에서 **"그래프가 말하는 대로 하는"**
방식으로 이행 중이며, 이는 여유를 사용자가 직접 확인해야 할 이유를 강화한다.

결과: `eval_results/trt_precision_probe.json`, `trt_precision_bf16.json`,
빌드 로그 `logs/trt_build_*.log`

#### 7.3 해석 사전 등록 (결과 보기 전 작성)

| 결과 | 해석 | 논문 반영 |
|---|---|---|
| **엄격·관행 모두 재현** (baseline 높은 붕괴, full 0%) | 주장이 실기기까지 확장된다 | 5장을 "A100 시뮬레이션"에서 **"실제 배포 하드웨어에서 확인"**으로 승격. 7장이 논문의 실용적 정점이 된다 |
| **엄격에서만 재현, 관행에서는 안전** | TensorRT가 해당 레이어를 fp32로 남긴 것 | **가장 가능성 높은 결과.** 주장을 축소하지 말고 **조건을 특정**한다: "순수 fp16 경로에서 발생하며, TensorRT 기본 정책은 이를 우연히 회피한다." 실용적 함의는 **런타임 의존성**으로 옮겨간다 — 같은 가중치가 런타임 설정에 따라 붕괴하거나 안 하므로 **여유 측정의 필요성은 오히려 커진다** |
| **둘 다 안전** | fp16 경로가 아예 안 만들어졌거나 전처리 불일치 | 빌드 로그로 원인 확인. **결과가 아니라 디버깅 대상**이다. 끝내 재현 못 하면 "PyTorch fp16에서 관찰, TensorRT에서는 미재현"으로 **그대로 보고**하고 5장의 주장 범위를 런타임 한정으로 좁힌다 |
| **둘 다 붕괴** (full도) | 엔진 빌드·전처리 오류 | 결과 아님. 코사인 대조부터 다시 |
| **OOM으로 원본 해상도 불가** | 8GB 공유 메모리 한계 | 802,816으로 내린다(A100 기준 25.6% 대 0.0%). 판정력은 줄지만 유효하며, **그 사실 자체가 7.5의 배포 파라미터 결과가 된다** |

**유리하게 재해석하지 않는다.** 특히 "관행에서 안전"이 나오면 그것을 숨기지 않는다 —
그 결과는 **본 논문의 처방을 약화시키지 않고 정밀화한다.**

#### 7.4 처방의 실기기 검증 (fixA)

5장에서 fixA(gate·up 가중치를 각각 1/√3, 곱이 만들어지기 전에 축소)가 A100에서
붕괴를 94.4% → 0.0%로 없앴다. **가중치 편집이므로 그대로 export할 수 있다.**
fixA를 적용한 타워를 추가로 내보내 같은 엔진 절차를 밟으면, 처방이
**"A100에서 되는 것"에서 "배포 하드웨어에서 검증된 것"으로** 올라간다.
추가 비용은 export 약 10분과 엔진 하나뿐이다.

#### 7.5 이 기기에서 안전한 최대 해상도 — 새 배포 파라미터

해상도를 올리면 여유가 줄고(5장) 동시에 메모리가 는다. 두 곡선을 겹치면
**"이 보드에서 이 체크포인트가 안전하게 처리 가능한 최대 해상도"**라는 배포 파라미터가
나온다. 해상도별로 다음을 잰다.

- fp16 붕괴율 (A100 기준값과 대조)
- 지연시간 (중앙값·p95)
- 피크 메모리
- 최종 블록 여유 = 65,504 / max\|activation\|

**메모리 타당성 사전 계산.** 내보낸 그래프는 HF 원본과 같은 eager attention이라
어텐션 행렬 `(head 16, S, S)`를 통째로 만든다. 원본 해상도(S=7,296)에서 fp16 QK^T가
1.59 GiB, softmax가 fp32로 승격되면 3.17 GiB가 더 붙고, 가중치 1.17 GiB와 마스크
0.2 GiB를 더하면 피크가 **6 GiB 안팎**이다. 8GB를 OS와 공유하는 Orin Nano에서는
아슬아슬하다. 안 되면 (i) 802,816으로 내리거나 (ii) 윈도우 어텐션을 창 단위로
배치화한다(어텐션 3.17 GiB → 30 MiB).

#### 7.6 하드웨어를 확보하지 못할 경우

**실기기 없이도 절반은 메울 수 있다.** A100에 TensorRT를 설치해 fp16 엔진을 빌드하면
7.2의 정밀도 조건 비교와 7.4의 fixA 검증을 그대로 수행할 수 있다. Orin은 아니지만
**"PyTorch 특유의 아티팩트"라는 반론을 제거**하고 내보낸 ONNX를 끝까지 검증한다.
이 경우 장 제목을 「런타임 검증」으로 바꾸고 **엣지 보드 미검증을 한계로 명시**한다.
보드를 확보하면 같은 절차를 그대로 옮기면 된다.

---

### 8장 도구 — `headroom_guard`: 배포 전 진단·교정 패스

> **2026-09-15 신설.** 논문의 주장은 "부족한 것은 수단이 아니라 측정"인데, 그렇게
> 말하면서 **재는 도구를 주지 않으면 주장이 불완전하다.** 이 장은 앞 장들의 진단을
> 실행 가능한 하나의 패스로 묶는다. 공학 석사 논문에 기대되는 "만든 것"이 여기에 해당한다.

**입력** 모델(체크포인트 또는 저장소), 목표 dtype, 목표 해상도, 대표 이미지 N장
**출력** 층별 여유 표 · 위험 층 목록 · (선택) 교정된 가중치 · 교정 전후 붕괴율

| 단계 | 하는 일 |
|---|---|
| 1. 진단 | 목표 dtype·해상도에서 블록별 `max\|activation\|`을 재고 `format_max ÷ max`로 여유를 계산, **임계 미만 블록을 자동 검출** |
| 2. 계산 | 각 위험 블록에 필요한 축소 배수를 **목표 여유로부터 자동 산출** |
| 3. 교정 | 오버플로 지점 **이전에** 적용 — 게이트형 MLP면 곱 이전 선형층들을 `(1/scale)^(1/n)`로. **bias가 있으면 함께** 나눈다 |
| 4. 검증 | 재측정해 붕괴율·여유 변화를 보고 |

**기본값이 중요하다.** `--max_pixels`를 지정하지 않으면 **모델 자신의 기본 설정**을
쓴다. Qwen2.5-VL-3B의 기본 `max_pixels`는 12,845,056으로 nuScenes 원본(1,440,000)보다
커서 축소가 일어나지 않는다. 즉 **"모델을 받아 그대로 쓰는 경로"에서의 위험을 기본으로
보여준다** — 인위적 고해상도 조건이 아니다.

**계열 일반화.** MLP 구조를 이름 패턴으로 찾는다 — `(gate_proj, up_proj) → down_proj`
(Qwen2.5-VL·LLaMA 계열)와 `(fc1) → fc2`(Qwen2-VL·CLIP 계열). 인식하지 못하면 해당 블록을
건너뛰고 그 사실을 기록한다.

**정직하게 적을 한계 — 교정은 완전 등가가 아니다.** 게이트형 MLP에서 곱이 만들어지기
전에 줄이면 오버플로는 사라지지만 **잔차 분기와 MLP 분기의 비율이 바뀐다.** 뒤따르는
정규화가 대부분 흡수하나 전부는 아니며, 실측 비용은 11,309문항에서 **−0.59%p**였다.
따라서 도구는 **교정을 권하지 않고 비용과 함께 보고**하며, 채택은 사용자가 정한다.
이 설계 선택 자체를 논문에 적는다 — "자동으로 고쳐 준다"고 주장하지 않는다.

**설계 판단 — 측정 dtype과 배포 dtype을 분리한다.** 활성 **크기**는 넘치지 않는
dtype(bf16)에서 재고 **붕괴**는 목표 dtype(fp16)에서 잰다. 여유 정의
`format_max ÷ max|activation|`의 분모는 **참값**이어야 하는데, 목표 dtype에서 재면
이미 발산한 표본의 값을 알 수 없어 필요한 교정량을 **과소평가**한다.

> **이 원칙을 빠뜨렸다가 실측으로 잡았다 (2026-09-15).** 처음에는 두 측정을 모두
> fp16에서 했고, 그 결과 10장 중 8장이 발산한 상태로 풀려 **목표 2.0배에 1.74배밖에
> 도달하지 못했다.** bf16으로 크기를 재자 진단값 자체가 1.03배가 아니라 **0.90배**
> (이미 한계 초과)였고, 필요한 축소가 ×0.50에서 **×0.39**로 바뀌어 목표를 달성했다.
> 논문 본체는 처음부터 bf16/fp16 두 경로로 재고 있었는데 도구를 만들며 그 원칙을
> 놓친 것이다. **설계 판단으로 본문에 적는다.**

**잔차를 분리해 축소량을 푼다.** 블록 출력은 `잔차 + MLP(...)`이므로 MLP 분기만
f배로 줄여도 잔차는 남는다. 따라서 블록 출력의 최대 지점에서 두 분기를 분리해 재고
`|r + f·m| ≤ format_max / target`을 f에 대해 푼다. 모든 이미지에 대한 f 중 최솟값을
취해 보수적으로 잡으므로 **목표를 약간 넘겨 달성**한다(실측: 목표 2.0 → 2.28배).
잔차만으로 이미 목표를 넘는 표본이 있으면 **"MLP 축소만으로는 도달 불가"를 경고**한다.

**실측 (2026-09-15, `student_baseline_v2`, 기본 설정, 10장)**

| | 교정 전 | 교정 후 |
|---|---|---|
| blk31 여유 (bf16 참값 기준) | **0.90배** | **2.28배** |
| fp16 붕괴율 | **80.0%** | **0.0%** |
| MLP 분기 축소 | — | ×0.3901 (선형층 2개 각 ×0.6245, bias 포함) |

**검증 결과 (2026-09-15) — 3개 계열 4개 모델.** 도구를 우리 체크포인트와 외부 공개
모델에 그대로 적용했다. 각 모델의 **자기 기본 설정**(`--max_pixels` 미지정 또는 해당
저장소 설정)에서 잰 값이다.

| 모델 | 계열 | 블록 구조 | 마지막 블록 여유(fp16) | 판정 |
|---|---|---|---|---|
| `student_baseline_v2` | Qwen2.5-VL | `blocks`, gate·up→down | **0.90배** | **위험 → 교정** |
| Qwen2-VL-2B-Instruct-AWQ | Qwen2-VL | `blocks`, fc1→fc2 | 3.83배 | 교정 불필요 |
| Qwen2-VL-7B-Instruct-AWQ | Qwen2-VL | `blocks`, fc1→fc2 | 4.52배 | 교정 불필요 |
| SmolVLM-Instruct | Idefics3 | `encoder.layers`, fc1→fc2 | 51.34배 | 교정 불필요 |

세 안전 모델은 모두 "위험 블록 0개"로 판정되어 가중치를 건드리지 않았고(붕괴율 0.0%),
위험 모델 하나에서만 교정이 발동해 붕괴가 제거됐다. **오탐도 미탐도 없었다.**

> **6장 표와 소수점이 다른 이유를 적어 둔다.** 6장은 같은 세 모델을 3.88 / 4.49 / 50.86배로
> 적었는데 여기서는 3.83 / 4.52 / 51.34배다. 원인은 **측정 dtype**이다 — 6장은 크기를
> fp16에서, 도구는 bf16(참값)에서 잰다(위 설계 판단). 안전한 모델에서는 fp16이 발산하지
> 않으므로 차이가 1% 남짓이지만, **위험한 모델에서는 이 차이가 1.03배 대 0.90배로 벌어져
> 판정을 뒤집는다.** 두 표를 나란히 두고 이 문장을 붙인다.

> **일반화의 범위를 정확히 적는다.** 3개 계열에서 돌아간 것은 (i) 블록 리스트 탐색과
> (ii) MLP 패턴 매칭이 계열 차이를 흡수한다는 뜻이지, **모든 VLM에서 동작한다는 뜻이
> 아니다.** 실제로 두 군데를 실측으로 고쳤다 — `tower.blocks` 하드코딩이 Idefics3에서
> 깨졌고(`encoder.layers`), SmolVLM 프로세서가 내놓는 5-D `pixel_values`
> (B, 타일, C, H, W)를 타워가 4-D로 기대해 깨졌다. 인식하지 못하는 구조는 건너뛰고
> 그 사실을 기록하는 것이 현재 설계이며, **"미인식=안전"이 아님을 출력에 명시**한다.

**형식 우선 권고가 실제로 값을 갖는다.** 도구는 목표 dtype만이 아니라 모든 형식의 여유를
함께 보고한다. 위 네 모델 모두 bf16 여유는 10³⁴배 이상으로, 표현 범위만 보면 bf16이
문제를 즉시 없앤다. 다만 그 선택에는 비용이 있다 — §7.2의 TensorRT 실측에서 bf16 엔진은
붕괴를 0.0%로 없애지만 지연이 fp16 대비 1.41배였다. 따라서 도구는 bf16을 **권고**하되
자동 선택하지 않고, 지연 비용은 사용자가 재도록 남긴다.

산출물: `scripts/headroom_guard.py`
결과: `eval_results/headroom_guard_baseline_v2.json`, `guard_qwen2vl_2b_awq.json`,
`guard_qwen2vl_7b_awq.json`, `headroom_guard_smolvlm.json`

---

### 9장 종합 논의와 한계
- 두 사례의 공통 구조 재확인 → §2 논지의 일반성

**한계를 먼저 적되, 넷으로 줄인다.**

> **2026-09-14 정리.** 이전 판은 여섯 항목을 나열했다. **한계 목록이 길수록 좋은 논문이
> 아니다** — 여섯을 늘어놓으면 그중 진짜 중요한 하나(단일 계열)가 상투구처럼 읽힌다.
> 판정 기준은 **"이걸 모르는 독자가 본문으로부터 틀린 결론을 내리는가"**이지
> "포함하면 불리한가"가 아니다. 후자를 기준으로 삼으면 은폐다.
>
> 이 기준으로 둘을 뺐고, 하나는 **한계가 아니라 근거**임을 확인해 위치를 옮겼다.

1. **단일 모델 계열 (Qwen2.5-VL).** 가장 중요한 한계다. 다른 계열에 `L_align`을 적용해
   같은 효과가 나는지는 학습이 필요해 범위 밖이다. 현상의 프로파일은 보였으나
   **손실 설계 축은 이 계열에서만 검증했다.**
2. **실기기 검증의 범위 (한 줄).** 7장이 어디까지 갔는지에 따라 문장이 달라진다.
   보드에서 돌렸으면 "단일 보드(Orin Nano 8GB)·단일 런타임(TensorRT)", A100 TensorRT까지만
   갔으면 "엣지 보드 미검증". 어느 쪽이든 **"실차 배포 시 붕괴한다"고 단정하지 않는다.**
3. **λ 스윕의 학습 길이 (5,000스텝).** 곡선의 **모양**(0 / 0.5 / 1.0 / 2.0 네 점)은
   5,000스텝에서만 측정했다. 다만 **두 끝점은 전체 에폭에서도 확인된다** — 아래.

   **끝점 대조 (2026-09-14, 추가 실험 없이 기존 데이터에서 확인).**
   `student_baseline_v2`(전체 에폭, L_align 없음)와 `student_spatial`(전체 에폭,
   λ_align=1.0)이 바로 λ=0과 λ=1.0 지점이다. 동일 250장 짝지어 비교(fp16, blk31 p50):

   | 조건 | 학습 길이 | λ_align | p50 |
   |---|---|---|---|
   | `sweep_align0.0` | 5,000 | 0 | 36,480 |
   | `sweep_align1.0` | 5,000 | 1.0 | 19,232 |
   | `student_baseline_v2` | **27,037** | 0 | 35,776 |
   | `student_spatial` | **27,037** | 1.0 | 18,304 |

   **학습 길이에 따른 효과 — 5개 지점 (`eval_results/training_trajectory_profile.json`,
   150장, max_pixels 200704).** 위 두 점 대조보다 이쪽이 정확하다. 같은 조건에서
   `student_baseline_v2`와 `student_spatial`의 중간 체크포인트를 모두 갖고 있다.

   | 스텝 | `baseline_v2` | `spatial` | 감소율 |
   |---|---|---|---|
   | 5,000 | 42,016 | 13,376 | **68.2%** |
   | 10,000 | 44,224 | 15,304 | 65.4% |
   | 15,000 | 47,392 | 17,024 | 64.1% |
   | 20,000 | 48,480 | 18,272 | 62.3% |
   | 25,000 | 48,288 | 18,256 | **62.2%** |

   > **정정 (2026-09-14).** 처음에는 위 두 점(5,000스텝 47.3% → 전체 에폭 48.8%)만 보고
   > "포화도 역전도 없고 오히려 크다"고 적었다. **틀렸다** — 그 둘은 해상도와 표본이
   > 다른 측정이라 직접 비교할 수 없었다. 같은 조건 5개 지점으로 보면 **완만히 감소하다
   > 20,000스텝 부근에서 62%로 포화**한다.

   따라서 한계 3의 정확한 진술은 이렇다.

   - **역전은 없다** — 5개 지점 전부 62~68%로 같은 방향이다.
   - **포화는 있다** — 20,000스텝 부근에서 62%로 수렴한다. 다만 **효과가 사라지는 것이
     아니라 안정화**되는 것이므로, 배포 관점에서는 학습을 길게 해도 여유가 유지된다는
     뜻이다.
   - **남는 한계는 곡선의 모양뿐이다** — λ 4점(0/0.5/1.0/2.0)의 형태는 5,000스텝에서만
     쟀다. 중간점을 전체 에폭에서 재려면 2회 × 51.6시간 = 약 4.3일이며, 끝점의 방향과
     크기가 이미 5개 지점에서 확인됐으므로 수행하지 않는다.
4. **데이터 축.** nuScenes 기반이 주축이고 CODA-LM으로 한 축을 더했다. CODA의 약 9%가
   nuScenes 유래이나, **학습 이미지와 실제로 겹치는 것은 없음을 실측으로 확인했다**
   (아래). 남는 한계는 "데이터 출처가 둘뿐"이라는 것이지 중첩 우려가 아니다.

**뺀 것 둘 — 무엇을, 왜.**

- **849번 행 이상치의 기원.** 논증에 구멍이 없다. 5장이 보이는 것은 "증폭기가 존재하고,
  6개 변형에서 동일하며, 달라지는 것은 거기 들어가는 입력"이다. 기원은 **더 나아간 질문**이지
  이 논증의 결손이 아니며, 몰라도 독자가 틀린 결론에 이르지 않는다.
  → **5장 말미에 한 문장**("왜 그 행인지는 사전학습 접근이 필요해 범위 밖")으로 옮기고
  한계 절에서 삭제.
- **다중 시드 5-way를 못 한 비용 사유.** 독자에게 필요한 것은 **결론**("정확도 순서를
  주장하지 않는다")이지 "15회 × 9.5시간이라 못 했다"가 아니다. 후자는 판단에 아무것도
  더하지 않는다. → 비용 사유 삭제, **결론은 반드시 유지**(안 쓰면 51.69 > 51.40을
  유의미한 차이로 읽는다).

**옮긴 것 하나 — 계열 일반성은 한계가 아니라 근거다.**

> SmolVLM 50.86배·Qwen2-VL-2B 3.88배라는 반례를 한계 목록에 두면 "우리 결과가 좁다"로
> 읽힌다. **그러나 이 반례들이야말로 처방의 필요성을 만든다.** 모든 모델이 여유 1.0배
> 근처였다면 처방은 "비전 타워에 fp16을 쓰지 마라" 한 줄로 끝나고 지표가 필요 없다.
> 갈리기 때문에 **모델마다 재야 한다**가 성립한다.
> → **§2 진술 3번 절(표준 평가에 그 지표가 없다)의 근거로 이동**하고, 8장에서는
> "사례 연구 범위"로만 짧게 재확인한다.

**사례가 둘이라 패턴 제시 수준**이라는 항목도 한계가 아니라 **범위 설정**이므로
§2로 옮긴다.

**데이터 축 일반성 (2026-09-12 완료)** — 한계 4번을 좁히는 근거로 여기 둔다.

  | 조건 | 토큰 | baseline_v2 여유/붕괴 | spatial | full | 판정 |
  |---|---|---|---|---|---|
  | CAM_FRONT (기준) | 1,836 | 0.88× / 94.8% | 2.89× / 0% | 2.92× / 0% | — |
  | **②a CAM_BACK** | 1,824 | 0.86× / **96.8%** | 2.92× / **0%** | 2.99× / **0%** | **순서 유지** |
  | **②b CODA-LM** | 1,196 | 0.94× / **72.0%** | 2.64× / **0%** | 2.60× / **0%** | **순서 유지** |

  - **②a**: 토큰 수를 고정한 채 장면 내용만 바꿔도 순서·크기가 그대로다 →
    **활성 크기는 입력 내용이 아니라 모델 성질**이다.
  - **②b**: nuScenes 밖(CODA-LM)에서도 align 둘만 0%다. 토큰이 1,196개로 적어 활성이
    전반적으로 낮지만, nuScenes의 1,024토큰(25.6%)과 1,836토큰(94.8%) 사이에 72.0%로
    놓여 **같은 "토큰 수 → 붕괴율" 곡선 위**에 있다.

  **CODA-LM은 학습 이미지와 겹치지 않는다 (2026-09-14 실측).**
  CODA는 KITTI 309 / nuScenes 134 / ONCE 1,057에서 구성돼 약 9%가 nuScenes 유래이므로,
  그 장면이 학습에 쓴 696개 DriveLM 장면과 겹치는지 확인이 필요했다.
  **메타데이터로는 불가능했다** — CODA-LM llava-format의 id가 `Mini_general_0` 같은
  일련번호뿐이라 원출처가 남아 있지 않다. 그래서 **이미지 자체를 대조했다**
  (`scripts/probe_codalm_overlap.py`): CODA-LM 143장 × DriveLM 학습 **전 카메라
  24,432장**, dHash+aHash로 후보를 추리고 32×32 정규화 상관으로 확정.

  | 항목 | 값 |
  |---|---|
  | 확정 중복 | **0건** |
  | 후보 중 최고 상관 | 0.9077 (임계 0.92) |
  | 상관 중앙값 | 0.7880 |
  | **양성 대조**(720p 축소 + JPEG q95 재인코딩본을 원본과 대조) | 해밍 **0/0**, 상관 **1.0000**, 5/5 검출 |

  **양성 대조가 이 결과의 핵심이다.** 그것이 없으면 "0건"이 정말 없어서인지
  **검출기가 작동하지 않아서인지** 구별할 수 없다. CODA-LM이 가한 것과 같은 변환
  (720p 축소 + 재인코딩)을 거쳐도 검출기가 상관 1.0000으로 완벽히 잡으므로,
  0건은 실제로 겹치지 않는다는 뜻이다.

  상위 유사 쌍들은 `aHash` 거리 1~2(밝기 분포가 닮음)인데 `dHash` 거리는 21~26
  (구조가 다름)이다 — **도로 장면끼리 비슷한 것**이지 같은 사진이 아니다.
  진짜 중복(1.0000)과 최고 유사 쌍(0.9077) 사이에 뚜렷한 간격이 있다.

  → 서술을 **"largely domain-disjoint"에서 "학습 이미지와 겹치지 않음을 실측 확인"**으로
  올린다. 다만 "CODA 자체가 nuScenes를 일부 포함한다"는 사실은 그대로 적는다 —
  우리가 확인한 것은 **우리 학습 집합과의 중첩 부재**이지 데이터셋 간 독립성이 아니다.
  결과: `eval_results/codalm_overlap.json`

  **계열 간 편차(SmolVLM 50.86배 등)는 여기서 반복하지 않는다** — §2로 옮겼다.
  8장에서는 "본 논문이 사례 연구 범위임"을 한 줄로 재확인하는 데 그친다.
  이유: 그 결과는 한계가 아니라 **처방의 필요성 근거**이며, 한계 목록에 두면
  "우리 결과가 좁다"로 오독된다.

  **데이터 일반성 실험의 구분 (서술 주의)**
  - `CAM_BACK` / `CAM_FRONT_LEFT` 측정은 **데이터셋 일반성이 아니다.** 같은 nuScenes,
    같은 도시·센서이며 시점만 다르다. 이 측정이 보이는 것은 **활성 크기가 입력 내용에
    둔감한 모델 성질인지**(도메인 내 입력 분포 불변성)뿐이다.
  - nuScenes 밖 검증은 **CODA-LM 193장**이 담당한다. 단 CODA-LM 이미지는 720p로
    전처리되어 vision token 수가 nuScenes(1,836)와 다르므로(≈1,175) 직접 비교할 수 없다.
    nuScenes에서 이미 측정한 **"토큰 수 → 활성 크기" 곡선 위에 놓이는지**로 비교한다.
  - **CODA 출처 캐벗**: CODA는 KITTI·nuScenes·ONCE에서 구성된 데이터셋이라 nuScenes와
    완전히 분리되지 않는다. 받은 193장은 전부 720p로 재처리되어 원출처를 판별할 수
    없었다(해상도 1280~1355×720, nuScenes 원본 1600×900 없음). 따라서
    **"완전히 도메인 분리"라고 쓰지 않는다.**

  *(제외된 항목)* "간접 경로 미규명"은 5장 (c)에서 규명되어 한계가 아니다.
  "상위 K 절단이 시뮬레이션"도 한계가 아니라 **통제**다 — 검증 대상이 softmax 분모에
  꼬리가 기여하는 효과이므로 상위 K 절단이 그 효과만 정확히 분리하며, 다른 어휘 크기의
  실제 모델을 쓰면 사전학습·아키텍처가 전부 교란된다. 4장 본문에 그렇게 서술한다.

### 10장 결론

---

## 5. 서론·결론 기여 진술 초안

> 본 논문의 기여는 하나다. 압축된 주행 시각-언어 모델의 실패가 정확도의 점진적 저하가
> 아니라 **기능의 이산적 상실**로 나타나고, 각 상실에는 **순전파 한 번으로 계산되는 예고
> 지표**가 있으나 **표준 평가에는 그 지표가 없으며**, 지표를 움직이는 수단은 여럿이므로
> **부족한 것은 수단이 아니라 측정**임을 보인 것이다.
>
> 서로 다른 메커니즘을 갖는 두 사례로 입증한다. 대형 어휘 환경에서 증류 온도가 문장 종료
> 신호를 소실시키는 경우 — 예고 지표는 **운용 온도에서 교사가 부여하는 EOS 확률**이며,
> T=1에서 0.24~0.97이던 값이 T=4에서 0.0004~0.0016으로 무너진다. 그리고 미세조정이 비전
> 인코더 활성 크기를 키워 저정밀 표현 범위를 초과시키는 경우 — 예고 지표는
> **`format_max ÷ max|activation|`**이며, 1.03배인 모델은 원본 해상도에서 94.8%의 이미지에서
> 붕괴하고 2.90배인 모델은 0%다. 두 지표 모두 압축·배포 문헌의 표준 평가 항목에 없다.
>
> 후자에서는 지표를 움직이는 수단이 하나가 아님을 함께 보인다. 특징 정렬 손실이 여유를
> 부수적으로 확보해 주고, 정렬 대상을 교사가 아닌 학생 자신으로 두어도 그 효과의 절반이
> 재현되며, 학습 없이 마지막 블록 가중치를 재조정하는 것만으로도 붕괴가 제거된다.
> **수단이 이렇게 여럿인데도 공개 양자화 체크포인트의 13.7%가 위험 구성에 방치되어 있고
> 여유를 보고한 저장소는 하나도 없다** — 이것이 본 논문이 지목하는 공백이다.

---

## 6. 진행 중 실험 결과에 따른 분기

논지(§2)는 **어느 결과에서도 유지된다.** 달라지는 것은 5장의 강도뿐이다.

| 실험 | 최선 | 그 외 | 5장 서술 조정 |
|---|---|---|---|
| ~~해상도 스윕 최종점~~ | **최선 확정 (09-10)** — align만 0%, 나머지 20~95% 붕괴 | — | **"필요조건"으로 서술 확정** |
| ~~INT8~~ | **최선 확정 (09-10)** — blk31에서 `max/rms`·SNR 모델 간 동일 | — | **독립 절로 승격 확정** |
| λ 스윕 | 0→0.5→1.0→2.0 단조 감소 | 계단 함수 | "조절 가능" → "존재 여부가 결정" |

**λ=0이 baseline 수준(5만대)으로 나오는지는 결과가 아니라 파이프라인 검증이다.**
gradient 상 동일해야 하므로, 벗어나면 결과 해석 전에 버그를 의심한다.

---

### 사후 리스케일 실험 — 해석 사전 등록 (2026-09-11, 결과 보기 전 작성)

**동기.** Qwen2.5-VL의 patch merger는 앞에 RMSNorm(`ln_q`)이 있어 blk31 출력이 토큰별로
정규화된 뒤 LLM에 전달된다. **균일 스케일은 정규화가 흡수하므로** blk31 MLP 출력을 일정
배수로 나눠도 표현이 수학적으로 동일할 수 있다. 사실이면 fp16 여유를 **학습 없이** 얻을 수
있다는 뜻이라, range 정규화 학습 실험(5.6 GPU-시간)을 하기 전에 먼저 가려야 한다.

**방법.** `student_baseline_v2`의 blk31 MLP 출력에 1/3을 곱하는 forward hook만 걸고
(i) 원본 해상도 fp16 붕괴율, (ii) NuScenes-QA 정확도(bf16 기준 대조)를 잰다. 학습 없음.

**예상.** 결과 1이 가장 유력하다고 본다(주관적 55%). 근거: align 모델이 blk31 18,000에서
정확도가 오히려 높고(51.69% 대 50.30%), 비전 MLP에 이미 LoRA가 걸려 스케일 조정 용량이 있다.

| 결과 | 서술 방침 (**지금 확정**) |
|---|---|
| **1. 붕괴 0% + 정확도 유지** | 여유는 **사후에 무료로 얻어진다.** 5장의 강조점을 "손실 설계가 결정한다"에서 **"기능적으로 무의미한 스케일이 배포 위험을 만들고, 아무도 측정하지 않아 방치된다"**로 옮긴다. 실무 기여는 오히려 강해진다(위험 구성 27개를 가중치 나누기로 수정 가능). `L_align`은 "그것을 자동으로 주는 손실"로 서술 수위를 낮춘다. **range 정규화 학습 실험은 불필요해지므로 취소한다.** |
| **2. 붕괴 0% + 정확도 하락** | 스케일이 기능적으로 유의미하다 → 여유는 학습 중에 벌어야 한다. **5장의 현재 서술을 그대로 유지**하고, `L_align`을 "정확도 대가 없이 여유를 얻는 방법"으로 정량 비교한다. range 정규화 실험을 예정대로 진행한다. |
| **3. 붕괴 유지** | 흡수 논리가 틀렸다 → 오버플로가 MLP 출력이 아니라 내부 계산(gate×up)이나 merger 내부에서 발생한다는 뜻. 5장 (a) 성분 분해를 MLP 내부까지 확장해 재규명한다. range 실험은 보류. |

**주의.** 예상(1번)과 논문에 유리한 결과(2번)가 어긋난다. 그래서 해석을 결과 전에 고정한다.
1번이 나와도 4장·6장과 관통 논지는 영향을 받지 않는다 — 흔들리는 것은 5장의 강조점 하나다.

#### 실행 결과 (2026-09-11) — **3번**. 예상(1번) 빗나감

`scripts/probe_posthoc_rescale.py`, 원본 해상도 250장 + NuScenes-QA val 300문항.

| 조건 | fp16 붕괴율 | blk31 p50 | 정확도(bf16) |
|---|---|---|---|
| 무개입 | **94.4%** | 63,264 | 53.33% |
| 1/3 리스케일 | **94.8%** | 21,184 | **54.00%** (+0.67pp) |

무개입 94.4%는 해상도 스윕의 94.8%와 일치해 **재현성이 확인**된다.

**두 가지가 동시에 드러났다.**

1. **스케일은 기능적으로 대체로 무의미하다** — 3배 줄여도 정확도가 크게 흔들리지 않는다.
   merger의 RMSNorm이 흡수한다는 예측은 **맞았다.**
   (2026-09-14: 여기 적힌 +0.67pp는 300문항 값이다. fixA를 전체 11,309문항으로 재측정하니
   −0.59pp였으므로 **"무손실"이라는 표현은 쓰지 않는다.** fixB는 전체 재측정을 하지 않았다.)
2. **그럼에도 붕괴는 전혀 고쳐지지 않는다** — 오버플로가 hook보다 **먼저** 일어나기 때문이다.
   원본 해상도에서 baseline_v2의 blk31은 bf16에서 이미 74,752로 fp16 상한(65,504)을 넘는다.
   즉 MLP **내부 계산**에서 inf가 되고, NaN을 3으로 나눠도 NaN이다.
   (리스케일 조건의 21,184는 붕괴하지 않은 5%만 집계한 값이라 착시다.)

**서술 방침 (사전 등록 3번에 따름)**
- 5장의 "손실 설계가 결정한다"를 **그대로 유지**한다.
- **"그냥 사후 리스케일하면 되지 않나"라는 반론이 실험으로 막혔다** — §6 예상 질문에 추가.
- 동시에 **"이 크기는 배포 위험에 비해 기능 기여가 작다"**는 진술은 살아남았다
  (3배 축소의 대가가 0.59pp). 5장의 논지를 강화하되 **"무의미"·"무손실"로 쓰지 않는다**
  — 2026-09-14 전체 재측정에서 비용이 확인됐다.
- 사전 등록대로 **성분 분해를 MLP 내부까지 확장**해 오버플로 발생 지점을 특정한다(후속).
- range 정규화 학습 실험은 **보류**한다(사전 등록 3번 방침).

#### 후속: MLP 내부 개입(fixA) — 해석 사전 등록 (2026-09-11, 결과 보기 전 작성)

**동기.** 위 실험이 막은 것은 blk31 **출력** 스케일링뿐이다. 심사에서 나올 다음 수는
**"가중치를 줄이면 되지 않나"**이므로, 우리가 먼저 시험한다.

**방법** (`scripts/probe_mlp_overflow.py`, 원본 해상도 250장, fp16).
비전 MLP는 `down_proj( act_fn(gate_proj(x)) * up_proj(x) )`이다. gate/up 출력을
스케일하는 hook은 **weight와 bias를 함께 나누는** 가중치 편집과 등가이므로,
hook 개입이 가중치 편집을 대신한다. (bias 관련 정정은 위 §5 각주 참조.)
- **진단**: norm2 / gate / up / 곱 / down / 블록출력 중 inf가 **처음** 나타나는 지점
- **fixA**: gate·up 출력을 각각 1/√3 → 곱이 1/3 (down_proj 계산 **전에** 축소)
- **fixB**: down 출력만 1/3 (이미 실패한 조건, 대조군)

| 결과 | 서술 방침 (**지금 확정**) |
|---|---|
| **fixA 실패** (붕괴 유지) | **사후 수정 불가가 확정**된다. 5장의 "여유는 학습 단계에서만 얻을 수 있다"가 완성되고, "그냥 리스케일하면 되지 않나"라는 반론이 두 층위(출력·가중치) 모두에서 막힌다. 논문에 가장 유리하지만 **그래서 더 조심해서 서술한다** — "우리가 시도한 두 가지 사후 개입이 실패했다"이지 "사후 수정이 원리상 불가능하다"가 아니다. |
| **fixA 성공** (붕괴 0% + 정확도 유지) | **사후 가중치 편집으로 고쳐진다.** 5장의 강조점을 옮긴다: "손실 설계가 **결정한다**" → "손실 설계가 여유를 **자동으로** 주며, 그렇지 않은 모델도 사후 수정이 가능하다 — 다만 **아무도 측정하지 않아 방치되어 있다**". 실무 기여는 오히려 강해진다(위험 구성 27개에 적용 가능한 구체적 처방). **이 결과가 나오면 5장 제목·초록의 '결정한다'를 약화된 표현으로 고친다.** 유리하게 재해석하지 않는다. |
| **fixA 성공 + 정확도 하락** | 여유와 정확도의 교환 관계가 사후 개입에도 존재. `L_align`이 그 대가 없이 얻는다는 비교 우위가 정량화된다. 5장 현행 서술 유지. |

**주의.** fixA 성공은 논문의 현재 프레이밍에 **불리**하다. 그럼에도 시험하는 이유는
심사에서 반드시 제기될 반론이고, 우리가 먼저 재지 않으면 심사위원이 재기 때문이다.
결과가 어느 쪽이든 **그대로 보고한다.**

**실행 결과 (2026-09-11 → 2026-09-14 정정): fixA 성공, 다만 정확도 비용이 있다.**
붕괴 94.4% → **0.0%**. 정확도는 300문항에서 53.33% → 54.67%(+1.34pp)로 보였으나,
**전체 11,309문항에서 50.55% → 49.96%(−0.59pp)로 부호가 뒤집혔다.**
사전 등록표의 세 번째 분기("fixA 성공 + 정확도 하락")가 실제 결과다 —
"여유와 정확도의 교환 관계가 사후 개입에도 존재"한다.
최초 inf 지점은 세 조건 모두 `down_proj`.
등록된 방침대로 5장 강조점을 이동하고 §3·초록의 "결정한다"를 약화했다
(`paper_positioning_ksae2026.md` §3 상단 수정 표시 참조). 유리하게 재해석하지 않았다.

---

## 7. 남은 할 일

### 보류 — 자원이 확보되면 진행 (2026-09-14 결정)

큐에 걸지 **않았다.** 지금 자원으로는 돌릴 수 없거나, 돌려도 앞 작업을 밀어내기만 한다.
조건이 충족되면 아래 그대로 실행한다.

| 항목 | 조건 | 명령 | 얻는 것 |
|---|---|---|---|
| **C3 대형 위험 구성** | 여유 디스크 **25GB 이상** | `python scripts/profile_other_vlm_families.py --limit 100 --cleanup --min_free_gb 25` | 위험 구성 27개 중 30B·32B·72B의 실측 여유. 목록과 디스크 가드는 이미 스크립트에 있다 — 가드가 부족분을 "디스크 부족"으로 결과에 기록하므로 **그냥 빠진 것과 구분된다** |
| ~~C4 시드 잡음~~ | — | — | **2026-09-14 생략 결정.** 아래 참조 |

**디스크를 25GB로 만드는 법** — 6장 INT4 재측정(큐에 있음)이 끝나면
`checkpoints_awq/`(16GB)를 지울 수 있다. 그것이 그 체크포인트의 유일한 용도였다.
**단, 지우기 전에 재측정 결과가 실제로 저장됐는지 확인할 것.**

**C5의 잔여(849행 기원)에는 큐를 만들지 않는다.** 사전학습 데이터·과정 접근이 필요해
자원 확보로 풀리는 종류가 아니다. 구조성 확인만 별도로 실행 중이다
(`scripts/probe_downproj_outlier_across_models.py`).

### C4(시드 잡음)를 생략한다 — 2026-09-14 결정

**완전 해소 비용을 이전 판에서 5배 낮게 적었다.** "15회 × 9.5시간 ≈ 6일"이라 썼는데,
9.5시간은 **5,000스텝** 기준이고 5-way 표는 **27,037스텝**(전체 에폭)이다.
실측 속도(0.1145분/스텝)로 다시 계산하면:

| | |
|---|---|
| 전체 에폭 1회 | **51.6시간** |
| 5변형 × 3시드 = 15회 | **774시간 ≈ 32일** |
| 시드 2개만 추가 (10회) | 516시간 ≈ 21일 |

**부분 측정(두 시드)도 하지 않는다.** 이유 셋:

1. 두 시드는 **차이값 하나**일 뿐 분산 추정이 아니다. 구간을 붙일 수 없다.
2. **운용점이 다르다.** 5,000스텝 대 27,037스텝, 단일 변형(`--variant spatial`,
   λ_align=1.0) 대 5변형. 학습이 길수록 시드 민감도가 줄어드는 것이 통념이나
   그건 측정이 아니므로, 유리한 방향(차이 ≥ 0.35pp)이 나와도 단서를 달아야 한다.
3. **논문의 결론이 바뀌지 않는다.** 처음부터 "정확도 순서를 주장하지 않는다"이고,
   C4는 그 주장을 뒷받침할 뿐 필요조건이 아니다.

**논문에 쓸 문장은 이것뿐이다** — "5-way 정확도 차이(0.35pp)는 단일 시드 관측이므로
순서를 주장하지 않는다." 시드 분산을 수치로 제시하지 않는다.


**2026-09-14 결정 — Orin Nano 배포를 논문에 포함한다.** 7장을 신설했고 기존 7·8장을
8·9장으로 옮겼다. 이로써 「실기기 검증」이 선택 항목에서 **필수 항목**이 되었으므로,
아래 목록의 우선순위가 바뀐다.

- [x] 4장 보강 — 어휘 크기 × 온도 스윕 **완료** (T=4에서 1K 대 152K가 31배 차이)
      결과: `eval_results/kd_temperature_vocab_sweep.json`, 스크립트: `scripts/probe_kd_temperature_vocab.py`
- [x] 6장 보강 — 공개 양자화 VLM 체크포인트 config 조사 **완료** (197개 중 위험 구성 27개=14%, 공식 저장소 3개 포함)
      결과: `eval_results/quantized_vlm_config_survey.json`, 스크립트: `scripts/survey_quantized_vlm_configs.py`
- [x] 그림 2 생성 **완료** — `figures/fig2_resolution_collapse.{png,pdf}` (+`_single`)
- [x] INT8 결과 반영 **완료** — 5장 독립 절로 승격 (`eval_results/int8_quant_error.json`)
- [x] blk31 메커니즘 규명 **완료 (09-11)** — 전파 가설 기각. 개입 실험 + 성분 분해
      `scripts/probe_blk31_intervention.py`, `scripts/probe_blk31_decompose.py`,
      `eval_results/blk31_{intervention,decompose}.json`
- [x] λ 스윕 **완료 (09-12)** — 4점 전부. 최종 블록 단조 감소(여유 1.79→4.61배),
      중간층은 λ=1.0에서 포화. `eval_results/lambda_sweep_activation_profile.json`
- [x] 일반성 실험 **완료 (09-12)** — ②a CAM_BACK / ②b CODA-LM 순서 유지,
      ① SmolVLM은 여유 50.86배로 **계열 일반이 아님**
- [x] 그림 3 **완료 (09-12)** — `figures/fig3_lambda_dose_response.{png,pdf}` (+`_single`)
- [x] `Qwen2-VL-2B-AWQ` 재측정 **완료 (09-14)** — fp16 여유 **3.88배로 안전**.
      SmolVLM은 50.86배. → **위험 구성은 필요조건이지 충분조건이 아니다.**
      6장 서술을 "14%가 위험하다"가 아니라 **"위험 구성이 드물지 않은데 실제 위험 여부는
      측정해야만 알 수 있고, 아무도 측정하지 않는다"**로 쓴다. 논문의 처방과 일치한다.
      (같은 Qwen 계열에서도 세대·크기에 따라 base_3b 0.95배 대 Qwen2-VL-2B 3.88배로 갈린다.)
- [x] B 정렬 대상 ablation **완료 (09-14 19:28 KST)** — 사전 등록한 두 분기 **모두 빗나갔다**.
      `L_align`의 정렬 대상을 7B 교사 대신 **3B 자기 자신**으로 바꿔 5,000 step·λ=1.0으로
      학습(`checkpoints/aligntgt_base3b/step_5000`). 동일 250장 짝지어 비교(fp16, blk31 p50):

      | 변형 | 정렬 대상 | p50 | λ=0 대비 |
      |---|---|---|---|
      | `sweep_align0.0` | — | 36,480 | — |
      | **`aligntgt_base3b`** | **3B 자기 자신** | **27,664** | **−24.2%** |
      | `sweep_align0.5` | 7B 교사 | 25,560 | −29.9% |
      | `sweep_align1.0` | 7B 교사 | 19,232 | −47.3% |
      | `sweep_align2.0` | 7B 교사 | 14,464 | −60.4% |

      같은 λ=1.0에서 **자기 정렬이 교사 정렬 효과의 51%를 재현**한다. 즉 "정렬 제약 자체가
      원인"도 "교사 표현이 필요"도 아니고 **두 요인이 대략 절반씩 기여**한다. 어느 한쪽
      분기로 밀지 말 것. 부수 관찰: 3B 자기 정렬 λ=1.0이 7B 교사 정렬 λ=0.5와 거의 같은
      자리에 온다 — 교사 표현이 "특별한 무언가"라기보다 **더 강한 정렬 신호**로 작동함을
      시사하나, 두 지점의 일치라 근거가 약하므로 **관찰로만 적고 주장으로 쓰지 않는다**.
      결과: `eval_results/align_target_profile.json`, `eval_results/align_target_per_image.jsonl`
      **제목에 미치는 영향**: fixA(사후 재스케일만으로 붕괴 94.4%→0.0%)와 함께,
      "`L_align`이 여유를 **결정한다**"는 과하다. "좌우한다/영향을 준다" 수준까지 낮출 것.
- [x] 두 제목 **확정 (09-14)** — fixA·B 결과를 반영해 09-11 안에서 어감을 낮췄다.
      - 학술대회: 「지식 증류가 비전-언어 모델의 fp16 표현 범위 여유에 미치는 영향:
        자율주행 VLM 사례 연구」
      - 학위 논문: *Feature Alignment Shapes Representable-Range Headroom in
        Distilled Vision-Language Models*
      폐기: *Distillation Loss Design **Governs** …* — `Governs`는 (i) fixA(사후
      재스케일로 붕괴 94.4%→0.0%), (ii) B(자기 정렬이 효과의 51% 재현), (iii) 통계적으로
      뒷받침되지 않는 정확도 순서와 충돌한다. `Shapes`는 "형태를 만든다"이지 "혼자
      결정한다"가 아니라 셋 모두와 양립한다. 근거: `paper_positioning_ksae2026.md` §1
- [x] 코드/문서 명칭 통일 **완료 (09-14)** — `train_distillation.py`의 클래스·변수·
      CONFIG 키·CLI·로그 키까지. 구 이름은 alias와 `_normalize_config()`로 호환 유지.
      `--variant spatial`과 `spatial_proj.pt`는 기존 산출물 연결 때문에 의도적으로 유지.
      상세: `RENAME_PLAN.md`
- [x] **`headroom_guard` 3계열 검증 완료 (09-15)** — Qwen2.5-VL(`student_baseline_v2`,
      0.90배·위험) / Qwen2-VL(2B·7B AWQ, 3.83·4.52배·안전) / Idefics3(SmolVLM, 51.34배·안전).
      오탐 0, 미탐 0. 도구를 만들며 두 군데를 실측으로 고쳤다 — `tower.blocks` 하드코딩이
      Idefics3(`encoder.layers`)에서, 5-D `pixel_values`(B,타일,C,H,W)가 4-D를 기대하는
      타워에서. **"3계열에서 동작"이지 "모든 VLM에서 동작"이 아니라고 적을 것.** §8장.
      결과: `eval_results/headroom_guard_{baseline_v2,smolvlm}.json`,
      `guard_qwen2vl_{2b,7b}_awq.json`
- [x] **bf16 TensorRT 대조 완료 (09-15)** — fp32 0.0%/441.0 ms, fp16 94.0%/139.7 ms,
      **bf16 0.0%/197.4 ms(1.41배)**. 사전 등록한 두 해석 중 **"bf16이 느리다"** 쪽.
      등록 문장 그대로: *형식 선택 자체가 교환이며, 그 판단도 이 값을 재야 내릴 수 있다.*
      부수 소득: 활성 크기는 그대로인데 형식 상한만 올려 붕괴가 사라졌으므로 **표현 범위
      가설의 독립적 확인**이 된다. §7.2. 결과: `eval_results/trt_precision_bf16.json`

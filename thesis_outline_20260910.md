# 학위논문 구성안 — 기여 하나, 사례 둘 (2026-09-10)

이전 판(발견 셋을 나열하는 구성)을 폐기하고 **단일 기여 구조**로 다시 쓴다.
근거 데이터는 `thesis_topic_survey_20260910.md` §3, 선행 연구 위치는
`paper_positioning_ksae2026.md` §4.

---

## 1. 왜 다시 쓰는가

이전 구성은 4장(EOS 붕괴)·5장(활성 크기)·6장(정밀도 교란)을 **서로 다른 세 기여**처럼
나열했다. 이 형태의 문제는 둘이다.

- 심사위원에게 **디버깅 노트 모음**으로 읽힌다. 각각은 흥미롭지만 "그래서 이 논문의
  주장이 무엇인가"에 답하지 못한다.
- 방어할 명제가 셋이 되어, 하나가 흔들리면 전체가 흔들리는 것처럼 보인다.

실제로는 세 발견이 **하나의 구조**를 공유한다. 그것을 논지로 세우고 나머지를 증거로
배치하면, 기여는 하나가 되고 증거는 셋이 된다.

---

## 1.5 확정 제목 (2026-09-17)

**「자율주행 비전-언어 모델의 저정밀 배포를 위한 표현 범위 여유 측정과 진단」**

*Measuring and Diagnosing Representable-Range Headroom for Low-Precision
Deployment of Autonomous-Driving Vision-Language Models*

**학술대회와의 역할 분담** — 같은 연구의 두 얼굴이며 제목이 그 차이를 말한다.

| | 제목이 말하는 것 | 범위 |
|---|---|---|
| 학술대회 | 「…표현 범위 여유와 **표준 압축 지표의 역전**」 | 발견 (4장 일부) |
| **학위 논문** | 「…**측정과 진단**」 | 체계 (4~7장 전체) |

근거·폐기 이력: `paper_positioning_ksae2026.md` §1

---

## 2. 단일 기여 진술

> **압축된 주행 VLM을 저정밀로 배포할 때 표현 범위 초과로 인한 기능 상실이 일어나며,
> 표준 압축 지표는 이 위험을 역방향으로 가리킨다. 이 실패는 정확도의 점진적 저하가
> 아니라 이산적이고, 그것을 예고하는 값은 순전파 한 번으로 계산되며 그 값을 움직이는
> 수단도 여럿이다. 부족한 것은 수단이 아니라 측정이다.**

> **범위를 명시한다 (2026-09-16).** 이 진술은 **fp16 표현 범위 초과라는 한 실패 양식**에
> 대한 것이다. 이전 판은 *"실패는 … 이산적 상실로 온다"*처럼 일반형으로 썼으나,
> 사례를 하나로 좁히기로 했으므로(§3) **일반형 표현을 걷어냈다.**
> "모든 저정밀 실패가 이산적이다"를 주장하지 않는다.

네 부분으로 읽는다. **순서가 곧 근거의 강도**다 — 가장 방어하기 쉬운 것을 앞에 둔다.

| # | 주장 | 근거 | 강도 |
|---|---|---|---|
| 1 | **표준 지표가 역방향을 가리킨다** | 붕괴하는 `baseline_v2`가 이상치 비율(153.1 대 168.0)도 낮고 INT8 SNR(31.17 대 23.10)도 높은데 붕괴율은 94.8% 대 0.0% | **가장 강함** — 논증이 아니라 산술 |
| 2 | **이산성** — 기능이 통째로 죽는다 | 붕괴가 이미지 단위로 0% ↔ 94.8%, 혼재 0건. **생성 단위로 재확인(2026-09-15, §7.3⑤)**: 708 키프레임 중 혼재 **0**, 붕괴형 출력의 정답 **0개**, 붕괴를 빼면 fp16·bf16 정확도 차이 소멸 | **매우 강함** |
| 3 | **예고하는 값은 순전파 한 번** | `format_max ÷ max\|activation\|` — 나눗셈 하나. 학생 순전파 1회 | 정의이므로 논쟁 여지 없음 |
| 4 | **움직이는 수단은 여럿** | `L_align`(교사 정렬) · **정렬 일반**(자기 정렬로 효과의 51%) · **fixA**(학습 없이 붕괴 94.4%→0.0%) · **형식 전환**(bf16) — 넷 | 강함 |

> **3번을 앞세우지 말 것 (2026-09-15).** `format_max ÷ max|activation|`은 **나눗셈 하나**다.
> "최대 활성값을 재라"가 기여냐는 질문에 답하기 어렵다. **기여의 실체는 지표 제안이 아니라
> 기존 지표가 틀렸다는 발견(1번)**이므로, 이전 판처럼 "예고 지표가 있다"를 앞에 놓으면
> 약한 쪽으로 논문을 소개하는 셈이 된다. 순서를 1↔3으로 바꿨다.
> 3번은 **"그래서 무엇을 대신 재야 하는가"라는 실무 답**으로만 쓴다.

> **2026-09-14 전면 재구성.** 이전 진술의 셋째 절은 "상실 여부는 학습 설계와 밀접하다"였다.
> **두 실험이 그 절을 무너뜨렸다.**
>
> - **fixA**: 학습을 전혀 하지 않고 마지막 블록 gate·up의 weight·bias를 1/√3로 나누는
>   것만으로 붕괴가 94.4% → **0.0%**가 되고 정확도는 유지된다. 학습 설계가 유일한 경로가 아니다.
> - **B (정렬 대상 ablation)**: 정렬 대상을 7B 교사가 아니라 **3B 학생 자기 자신**으로 바꿔도
>   효과의 **51%**가 재현된다(−24.2% 대 −47.3%). 효과의 절반은 `L_align`의 특정 설계가 아니라
>   **정렬 제약 일반**에서 온다.
>
> 여기서 두 갈래가 있었다. (i) 셋째 절을 계속 약화시켜 붙들거나, (ii) **논지의 축을 옮기는** 것.
> (i)은 "밀접하다" → "관련이 있다"로 계속 물러나다 결국 아무 말도 아니게 된다.
> (ii)를 택했다.
>
> **핵심은 이것이다.** 두 실험은 `L_align`의 지위를 낮추는 대신, **여유를 움직이는 것이
> 쉽다**는 사실을 보여준다. 교사가 특별할 필요도 없고(자기 정렬로 절반), 학습이 필요하지도
> 않다(fixA). 그렇다면 진짜 질문은 "왜 안 움직이는가"이고, 답은 **"재지 않기 때문"**이다.
>
> > **근거의 한계를 정확히 (2026-09-15).** "**아무도** 재지 않는다"는 쓰지 않는다.
> > 근거가 둘인데 둘 다 절대 주장을 받치지 못한다.
> > (i) 공개 저장소 197개 **config** 조사 — 그러나 **config는 애초에 활성 통계를 적는
> > 곳이 아니다.** 거기 여유가 없는 것은 당연하며 증거가 되지 못한다.
> > (ii) arXiv 전문 검색 몇 건이 0을 반환 — 검색어 몇 개의 부재는 문헌 전체의 부재가 아니다.
> >
> > **정확한 진술**: "우리가 조사한 범위(공개 저장소 197개의 config, arXiv 전문 검색)에서
> > 표현 범위 여유를 보고한 사례를 찾지 못했다." 절대 주장 대신 **조사 범위를 명시**한다.
> >
> > **미조사 위협**: TensorRT 등 배포 도구가 fp16 빌드에서 레이어별 정밀도를 자동
> > 선택하며 오버플로를 회피할 가능성이 있다. 그렇다면 "재지 않는다"는 문제 제기 자체가
> > 약해진다. **6장 6.3의 정밀도 3조건 비교가 이 질문에 답한다** — 그 전까지 이 위협을
> > 해소된 것으로 쓰지 않는다.
>
> 이 재구성으로 **B와 fixA가 약점에서 근거로 바뀐다.** 둘은 4번 절(수단은 여럿)의 직접
> 증거이며, 그 절이 있어야 "부족한 것은 측정"이라는 결론이 성립한다. 이전 구조에서는
> 두 결과가 논지를 갉아먹었다.

**무엇을 포기했는가.** "이 손실이 여유를 만든다"는 주장을 포기했다. 남는 것은
"`L_align`은 여유를 **부수적으로, 자동으로** 확보해 주는 여러 수단 중 하나"다.
정확도 이득이 작다는 약점(~1.4pp)이 여기서 오히려 정합적이 된다 — `L_align`의 가치를
정확도로도 여유로도 단독 주장하지 않고, **여유가 움직인다는 사실의 한 사례**로 쓴다.

**무엇을 얻었는가.** 기여가 인과 주장에서 **방법 제안**으로 바뀐다. 인과 주장은 반례
하나에 무너지지만(실제로 B가 절반을 무너뜨렸다), "이 두 스칼라를 재라"는 제안은 반례가
없다. 그리고 재는 비용이 순전파 한 번이라 실무자가 실제로 채택할 수 있다.

**정직한 단서 (2026-09-16 갱신).** 여유 역시 **사후에** 쟀다. 미리 재고 시작한 것이
아니다. 다만 그 값은 배포 **전에** 계산 가능하며, 쟀다면 5장의 붕괴를 학습 단계에서
미리 알 수 있었다. 논문에는 이 순서를 그대로 적는다 — 사후에 찾았고, 그래서 더
확실히 **"표준 절차에 없다"**는 증거가 된다.

**왜 지표가 필요한가 — 계열 간 편차가 근거다.** 3번 절(표준 평가에 지표가 없다)이
성립하려면, 지표 없이는 판단할 수 없다는 것을 보여야 한다. 같은 조건(원본 해상도,
동일 nuScenes CAM_FRONT)에서 잰 여유는 이렇게 갈린다.

| 모델 | fp16 여유 | 붕괴 |
|---|---|---|
| Qwen2.5-VL-3B | **0.95배** | 69.6% |
| Qwen2-VL-2B-AWQ | 3.88배 | — |
| SmolVLM-Instruct (Idefics3) | **50.86배** | — |

**이 편차가 처방을 필요하게 만든다.** 모두가 1.0배 근처였다면 처방은 "비전 인코더에 fp16을
쓰지 마라" 한 줄로 끝나고 지표가 필요 없다. 50배까지 갈리기 때문에 **모델마다 재야 한다**가
성립한다. 같은 Qwen 계열 안에서도 세대·크기에 따라 갈린다는 점이 이를 강화한다.

> **서술 위치에 대한 메모 (2026-09-14).** 이 결과는 이전 판에서 8장 한계 목록에
> "계열 일반성 없음"으로 들어가 있었다. 거기 두면 "우리 결과가 좁다"로 읽힌다.
> **한계가 아니라 근거이므로** 여기로 옮긴다. 8장(논의)에서는 "사례 연구 범위"로만 짧게
> 재확인한다.

**범위 — 한 실패 양식을 끝까지 판 사례 연구다 (2026-09-16 갱신).** 본 논문은 일반
법칙을 주장하지 않는다. **fp16 표현 범위 초과**라는 하나의 실패 양식에 대해
(i) 통제된 다중 변형에서 표준 지표가 역방향을 가리킴을 보이고, (ii) 그 실패가
이산적임을 생성 단위로 확인하며, (iii) 예고 지표와 (iv) 그것을 움직이는 네 가지
수단을 보이고, (v) 배포 런타임까지 검증하며 (vi) 도구로 묶는 것까지가 범위다.
**"모든 저정밀 실패가 이렇다"고 쓰지 않는다.**

> **이전 판은 두 사례로 "패턴"을 주장했다.** 2026-09-16에 사례 1(KD 온도)을
> 분리하기로 하면서(§3) 이 문단의 근거가 바뀌었다. 깊이로 방어하고 일반성은
> 주장하지 않는다.

자동차공학회 청중에게 이 구분은 익숙하다. **완만히 성능이 떨어지는 시스템과 갑자기
출력을 잃는 시스템**은 안전 논의에서 전혀 다른 대상이며, 후자에는 **사전 지표**가 있어야 한다.

---

## 3. 사례 1을 생략한다 — 2026-09-16 결정

**결정.** 4장(사례 1: 정지 기능의 상실 — KD 온도 × 어휘 크기)과 「두 사례의 공통
구조」 절을 **학위 논문에서 뺀다.** 논문은 **저정밀 배포 한 축**을 끝까지 파는
구성으로 간다.

**왜.** §2 논지가 2026-09-14 재구성에서 **저정밀 배포**로 범위가 잡혔는데, 사례 1은
**학습 하이퍼파라미터** 문제라 그 문장을 직접 예시하지 못한다. 논지와 장이 어긋난
채로 두면 심사에서 *"이 장이 왜 있는가"*가 나온다.

**무엇이 무너지지 않는지 먼저 확인했다.** 초기에는 "기여 2·3·4가 단일 사례가 되어
약해진다"고 보았으나, **2026-09-15~16 작업으로 사례 2의 근거가 크게 강해져** 넷 다
사례 2만으로 선다.

| 기여 | 사례 2 단독 근거 |
|---|---|
| 1 지표 역전 | 애초에 사례 2 근거 (5변형 ρ=−1.000, p=0.0083; 독립 집단은 방향만) |
| 2 이산성 | **708 프레임 혼재 0 · 붕괴형 정답 0개 · 붕괴 제외 시 dtype 차이 소멸** (§7.3⑤⑥) |
| 3 예고 값은 순전파 한 번 | 정의이지 경험적 일반화가 아니다 |
| 4 움직이는 수단은 여럿 | λ 스윕 · 자기 정렬(51%) · fixA · bf16 — 넷 |

**대신 지불하는 것.**
- **일반성을 주장할 수 없다.** §2에서 일반형 표현을 걷어낸다(아래).
- **온도 × 어휘 스윕이 이 논문에 실리지 않는다.** T=4에서 1,000클래스는 EOS에
  1.64%를 남기지만 152K 어휘는 0.053%만 남긴다(**31배**). 선행 연구 검색에서
  `"distillation temperature"` AND `"vocabulary"` **0건**이었다. **단독으로 새로운
  측정이므로 별도 논문으로 분리한다** — 버리는 것이 아니라 청중이 다른 주제다.
  재료는 모두 확보돼 있다(`eval_results/kd_temperature_vocab_sweep.json`,
  `scripts/probe_kd_temperature_vocab.py`, EOS 프로빙 로그, v3 반증 기록).
- **6장이 가장 생생한 예시(자기 사례)를 잃는다.** INT4 사례로 대체한다 — 그쪽이
  배포 축에 더 맞는다.

**기록으로 남기는 이유.** 심사에서 *"KD 온도 문제는 왜 다루지 않았나"*가 나올 수
있다. 답은 **"다루지 않은 것이 아니라 범위 밖으로 분리했고, 그 결과는 별도로 낸다"**다.

---

## 3.5 수치 정정 규칙 — 값만 고치면 절반만 고친 것이다 (2026-09-17 확정)

**왜 생겼나.** 요약문 최종 확인에서 오류 셋이 나왔는데 **둘이 같은 유형**이었다.

| 발견 | 본문 | 딸린 서술 |
|---|---|---|
| Fig. 2 캡션 (09-15 정정 누락) | 799장 출처로 통일 | 캡션은 **250장** 그대로 |
| Fig. 1 주석 (09-16 정정 누락) | 붕괴율 **3.2%**로 수정 | 주석은 **"모든 변형이 0%"** 그대로 |

둘 다 **값 대조로는 잡히지 않는다.** 250장도 0%도 각자의 원래 자리에서는 맞는 값이고,
틀린 것은 **어느 데이터에서 나왔느냐**뿐이기 때문이다. 같은 이유로 §7.4의
「1.91배(최악 기준)」와 「오탐 7건(p50 기준)」이 섞여 있던 것도, 「−0.900」의 원인을
표본으로 잘못 돌린 것도 값 검사를 통과했다.

**규칙.** 본문 수치를 고칠 때 **같은 수치가 사는 다른 자리를 함께 훑는다.**

1. 그림·표 **캡션** (표본 수·해상도·dtype·스텝)
2. 인용문(`>`)과 주석 — 인쇄물에 안 나가도 **다음 작업에서 다시 집어 온다**
3. 같은 값을 쓰는 **다른 장**과 발표 자료·스크립트
4. 그 값을 계산·표시하는 **스크립트와 그 주석**

**통계량·표본을 항상 병기한다.** 같은 "여유"가 자리마다 다른 통계량일 수 있다 —
Table 1은 p50(붕괴율과 짝지으므로), 도구 게이트는 최악(한 장이라도 터지는가).
어느 쪽인지 적지 않으면 두 값이 섞여도 검사를 통과한다.

> **활성값 자체도 마찬가지다 (2026-09-17 추가).** 발표 자료의 임계선 차트가
> *"마지막 블록의 **최대 활성값**"*이라고만 적고 있었는데, 실제 값 74,752는
> **이미지별 최대값의 중앙값**이다. 250장 중 진짜 최대는 88,576이다.
> 통계량을 적지 않으면 **"250장 중 최대"로 읽히고**, 그렇게 답하면 틀린 답이 된다.
> 요약문 Table 1 캡션에는 `median-image`를 넣었으면서 **발표 자료는 빠뜨렸다** —
> 같은 정정이 자료마다 따로 필요하다는 뜻이다.
>
> **감사 스크립트는 이런 것을 못 잡는다.** 74,752는 어느 쪽으로 읽어도 문서에
> 있는 값이고, 빠진 것은 숫자가 아니라 **그 숫자가 무엇의 요약인지**다.
> `audit_numbers.py`의 `watch`가 값에는 걸리지만 **캡션 문구에는 걸리지 않는다.**

**원인을 귀속할 때는 변수를 분리한다.** 「다른 표본에서 −0.900」은 표본과 통계량을
한꺼번에 바꾼 결과였다. 둘을 분리해 재니 원인은 **통계량**이었다(§7.4).

### 규칙의 확장 — 논거가 바뀌면 도구도 본다 (2026-09-17)

위 규칙은 **수치**를 대상으로 한다. 그런데 같은 결의 실패가 **코드**에서도 두 번 나왔다.

| 문서에서 결론이 바뀐 것 | 코드는 따라오지 않았다 |
|---|---|
| §7.2 — "게이트는 **최악** 통계량을 써야 한다"(λ=0.0 사례로 실증) | `plan_fix()`가 계속 **p50**으로 걸고 있었다 |
| §8 한계 ③ — "**DLA는 bf16을 지원하지 않는다**"(NVIDIA 문서 근거) | 도구가 대상을 묻지 않고 **모두에게 bf16을 권했다** |

둘 다 **문서 안에서는 일관됐고** 값 검사도 통과한다. 어긋난 것은 문서와 산출물 사이다.
**논문이 주장하는 바를 도구가 하지 않으면, 도구는 논문의 근거가 아니라 반례가 된다.**

**규칙.** 문서에서 판단·논거가 바뀌면 그 판단을 **구현한 코드가 있는지** 확인한다.
특히 §7의 도구는 4~6장의 결론을 실행하는 물건이므로, 4~6장이 바뀌면 7장 코드를 본다.

### 자동 감사 — `scripts/audit_numbers.py`

위 규칙을 사람이 매번 지키기 어려워 스크립트로 만들었다. 대장은 `number_registry.json`.

```
python scripts/audit_numbers.py            # 폐기값이 본문에 있으면 exit 1
python scripts/audit_numbers.py --all      # 현행값 출현 위치까지 전부
python scripts/audit_numbers.py --show 1.41배
```

**핵심은 정정 박스와 본문을 구분한다는 것이다.** 정정 박스가 옛 값을 인용하는 것은
정상이고 본문이 들고 있는 것만 오류다. 인용문(`>`), 코드 블록, 기록 구역
(작성 메모·사전 등록·제출본 수정 필요 항목 등), 그리고 정정 표식이 있는 줄
(`정정`·`철회`·`이전 판`·`조건 혼입`·`쓰지 않는다` 등)을 **기록**으로 본다.

대장은 세 종류를 담는다 — `canonical`(현행), `retired`(폐기, 본문에 있으면 오류),
`watch`(맞는 값이나 조건 병기가 필요한 값). **값을 정정할 때 대장에 먼저 적고 돌린다.**

**첫 실행(2026-09-17)에서 넷을 잡았다.**

| 발견 | 성격 |
|---|---|
| `seminar_20260917.html` Fig. 2 캡션 `250 images` | **진짜 버그** — 요약문만 고치고 발표 자료를 안 고쳤다 |
| `thesis_ch5_ch6_draft.md`의 미결 항목 둘 | **이미 끝난 일이 미결로** — TRT 정밀도 검사와 fixA McNemar |
| 4장 표의 `−0.59pp` 셀 | 철회 표시가 없어 본문처럼 읽혔다 → 셀에 명시 |
| v1/v2 활성 분해 표 | **해상도 조건 누락** → 「평가 해상도(200,704)」 병기 |

---

## 4. 장 구성

각 장은 **독립 기여가 아니라 §2 논지를 세우는 단계**다. 서론에서 이를 명시한다.

### 1장 서론
문제 제기(차량 탑재를 위한 경량화와 저정밀 연산) → §2의 논지 제시 →
**"본 논문의 기여는 하나이며, 하나의 실패 양식과 그 따름정리로 이를 입증한다"**를 명시.
**일반성을 주장하지 않는다는 점**도 여기서 밝힌다(§2 범위 명시, §3 분리 결정).

### 2장 관련연구
네 계열. 각 계열 끝에 본 연구와의 차이를 한 문장씩 (§`paper_positioning_ksae2026.md` §4.3 참조).

1. VLM 지식증류·주행 VLM 경량화 — Drive-KD, **SCPNet**, EM-VLM4AD, MiniDrive
2. 저정밀 배포와 활성 이상치 — CrAFT, Colinearity Decay, RoLoRA, RoSTE, QuantTune,
   Massive Activations 계열, Measuring Maximum Activations, RegCache, PASA
3. 자율주행 VLM 효율화 서베이 — 2608.30144, 2510.24795, 2506.24044, 2512.16760
4. 평가 프로토콜 — DriveLM, NuScenes-QA, CODA-LM

### 3장 실험 기반 — 통제된 다중 변형 증류 파이프라인
**기여 장이 아님을 서두에 명시한다.** 논지를 검증하려면 "손실만 다른 학생들"이
필요하고, 이 장은 그것을 만든다.

- 교사(7B)/학생(3B), LoRA 설정, 데이터 파이프라인
- `L_align`(특징 정렬, FitNets 계열)과 `L_atc`(비대칭 시간 맥락) 정의 —
  **`L_atc`는 SCPNet의 구조를 카메라 기반 생성형 VQA로 이식한 것**이며 참신성을
  주장하지 않는다고 명시
- 5개 변형과 공정성 통제(데이터·LoRA·optimizer·스텝·평가 프로토콜 고정) 및 로그 대조 감사
- NuScenes-QA val 5-way 결과 — **여기서 "정확도로는 변형을 구별할 수 없다"를 먼저 인정한다.**
  뒤 두 장이 "그런데 정확도가 못 보는 실패가 있다"로 이어지므로, 약점을 앞에 두는 것이
  논지를 강화한다.

  **통계 관련 서술 규칙 (2026-09-11 확정)**: 5개 변형을 각각 **시드 고정 없이 한 번씩만**
  학습했다. 따라서 Full / spatial / temporal의 전체 정확도(51.69 / 51.40 / 51.34%)
  **0.35%p 차이는 시드 노이즈와 구별되지 않는다.**
  - **금지**: "Full이 단일 손실 ablation보다 우위", "α가 β보다 낫다" 류의 순위 주장
  - **사용**: "세 변형은 전체 정확도에서 구별되지 않는다", "증류 미적용 기준선 대비
    차이도 1.4%p 이내로 작다"
  - **`object` 유형(43.87% 대 37.86~39.89%)도 같은 제약을 받는다 (2026-09-14 추가).**
    폭이 6pp로 0.35pp보다 훨씬 크지만 **반복 측정한 적이 없다** — n=1에서는 6pp에도
    구간을 붙일 수 없다. "뚜렷한 우위"라고 쓰지 말고 **"폭이 크며 방향이 `L_align`의
    목적과 일치한다"는 방향성 관찰**로만 적는다.
    (이 항목이 없어서 `ksae2026_manuscript_draft.md`에 "가장 높았으며 … 0.29~0.35%p
    높았다"는 금지 위반 문장이 들어가 있었다. 09-14 정정.
    `CLAUDE.md`의 "repeatable"이라는 표현도 근거가 없어 함께 고쳤다.)
  - 이 서술이 오히려 논지와 맞는다 — **정확도로는 안 보이는 차이를 다루는 논문**이기 때문이다.
  - 활성 크기 차이(62배)와 붕괴율 차이(94.8% 대 0%)는 규모가 커서 이 제약을 받지 않는다.

### 4장 수치 표현의 상실 — fp16 표현 범위 초과
- 활성 크기 프로파일: DriveLM val 799장 × 7모델, 층별 max|activation| (**그림 1**)
  - 중간층(blk16~28): `L_align`이 있는 둘만 약 62배 낮음 (8,096 대 130)
  - 마지막 블록: 과제 손실 단독이 베이스보다 **키움** (33,792 → 50,176)
  - 교사가 오히려 높다(9,024) — 코사인 손실은 방향만 제약하므로 크기 감소는 **부수 효과**
- 이산적 실패: fp16 표현 한계(65,504) 초과 → NaN → 토큰 0 반복.
  이미지 단위로 갈리고 같은 장면의 연속 키프레임에서 뭉친다
- 해상도 × 붕괴율 (**그림 2**, 완료)

  **해상도 × 붕괴율 (2026-09-10 측정, 250장, 그림 2)**

| 모델 | 128 | 256 | 512 | 1,024 | 1,836 tok | 원본 해상도 fp16 여유 |
|---|---|---|---|---|---|---|
| task CE only | 0.8% | 3.2% | 10.4% | 25.6% | **94.8%** | 0.88× (한계 초과) |
| 사전학습 (미세조정 없음) | 0 | 0 | 0 | 0 | **69.6%** | 0.95× |
| task + `L_atc` | 0 | 0.4% | 0 | 0 | **28.8%** | 1.06× |
| task + 출력 KD | 0 | 0 | 0 | 0 | **20.4%** | 1.10× |
| **task + `L_align`** | 0 | 0 | 0 | 0 | **0.0%** | **2.89×** |
| **task + `L_align` + `L_atc`** | 0 | 0 | 0 | 0 | **0.0%** | **2.92×** |

원본 해상도(1,836 토큰)에서 `L_align`을 쓴 둘만 붕괴하지 않는다. 따라서 "여유가 크다"가
아니라 **"이 조건에서는 `L_align`이 붕괴 회피의 필요조건"**으로 서술할 수 있다.
붕괴는 1,024 → 1,836 구간에서 급격히 일어나 **안전 해상도에 절벽이 존재**한다.

**서술 주의 — "미세조정이 안전한 모델을 망친다"고 쓰지 않는다.** 사전학습 모델
`base_3b`도 원본 해상도에서 69.6% 붕괴한다. 정확한 서술은 **여유 비율** 기준이다.

| 모델 | 원본 해상도 fp16 여유 | 해석 |
|---|---|---|
| base_3b (미세조정 없음) | **0.95×** | 사전학습 상태에서 이미 여유가 없다 |
| task CE only | 0.88× | 남은 여유마저 없앤다 |
| task + `L_atc` / 출력 KD | 1.06× / 1.10× | 증류 항이 하나라도 있으면 한계선 위로 올린다 |
| **task + `L_align`(±`L_atc`)** | **2.89× / 2.92×** | 3배 여유를 만든다 |

즉 논지는 "미세조정이 망친다"가 아니라 **"사전학습 모델부터 여유가 없는데 아무도 이를
측정하지 않으며, 손실 설계만이 그 여유를 만든다"**이다. 후자가 본 논문의 주장과 더
정확히 맞고, 반박 여지도 적다.

- **절대 크기(표현 범위) 대 이상치 비율(동적 범위)의 구분 — 독립 절로 승격 (09-10 확정)**

  | 층 | 모델 | max | max/rms | INT8 SNR (per-ch) |
  |---|---|---|---|---|
  | blk24 | task CE only | 8,128 | 316 | 31.5 dB |
  | blk24 | task+align+atc | 130 | 32 | **44.0 dB** |
  | blk31 | task CE only | 48,640 | **153** | **40.3 dB** |
  | blk31 | task+align+atc | 18,048 | **168** | 39.4 dB |

  중간층에서는 두 축이 함께 움직이나(기존 문헌과 일관), **붕괴를 결정하는 마지막
  블록에서는 절대 크기만 2.7배 다르고 이상치 비율·INT8 SNR은 사실상 동일**하다.
  기존 압축 지표가 이 차이를 구별하지 못한다는 것이 본 논문의 핵심 근거다.
  부수 결과로 중간층 per-channel INT8 SNR이 12.5 dB 개선된다.

- **학습 길이 축 (09-10 발견)**: blk31 활성은 학습이 진행될수록 자란다
  (spatial λ=1: step_5000에서 13,376 → 완주 18,432, +38%). 따라서 λ 스윕은 **동일
  5,000스텝 시점끼리만** 비교해야 하며, 완주 체크포인트와 직접 비교해서는 안 된다.
  동시에 "오래 학습할수록 배포 여유가 줄어든다"는 실무적 함의가 생긴다.

- **메커니즘 규명 — massive activation은 마지막 블록 MLP가 생성한다 (09-11)**

  `L_align`의 fp16 이득이 중간층 이상치 억제의 하류 결과인지(전파 가설) 별개 현상인지를
  개입 실험으로 가렸다 (`scripts/probe_blk31_intervention.py`,
  `scripts/probe_blk31_decompose.py`, 30장 × 5변형).

  **(a) 성분 분해** — 비전 블록은 `h = h + attn(norm1(h))` → `h = h + mlp(norm2(h))`.
  blk31 최댓값 좌표에서 각 성분의 부호 있는 값:

  | 변형 | 잔차 | attn | **mlp** | 합 |
  |---|---|---|---|---|
  | baseline_v2 | −88 | 7 | **−46,848** | −46,848 |
  | kd_only_v4 | −98 | −9 | **−28,544** | −28,672 |
  | temporal | −94 | −10 | **−33,536** | −33,536 |
  | spatial | −148 | −20 | **−17,024** | −17,280 |
  | full | −139 | −29 | **−17,152** | −17,408 |

  **잔차 기여가 전체의 0.2~0.9%에 불과하다.** massive activation은 앞 블록에서 운반된
  것이 아니라 **마지막 블록 MLP가 생성**하며, `L_align`을 쓴 둘은 그 **생성량 자체가
  절반 이하**다(−46,848 → −17,152). 이것이 fp16 여유의 직접적 원인이다.

  **(b) 잔차 개입** — blk30 출력에 스칼라를 곱해 잔차 운반분만 조절:

  | 변형 | 무개입 | 잔차 1/34배 | 잔차 34배 |
  |---|---|---|---|
  | baseline_v2 | 46,592 | 68,608 (147%) | 266,240 (5.7×) |
  | kd_only_v4 | 28,416 | 47,616 (168%) | 268,288 (9.4×) |
  | temporal | 33,280 | 54,016 (162%) | 276,480 (8.3×) |
  | spatial | 17,152 | 20,736 (121%) | 21,248 (1.2×) |
  | full | 16,320 | 12,544 (77%) | 22,016 (1.3×) |

  **잔차를 줄이면 blk31이 오히려 커진다.** 전파 가설이 예측하는 방향과 **반대**다.

  **(c) 간접 경로의 메커니즘 — RMSNorm은 크기는 고정하되 방향은 고정하지 않는다**
  (`scripts/probe_norm2_direction.py`, 20장)

  비전 블록은 `h1 = h + attn(norm1(h))`, `out = h1 + mlp(norm2(h1))` 구조다.
  `norm1`의 입력은 **순수 잔차**이므로 스케일이 완전히 흡수되지만, `norm2`의 입력은
  `잔차 + attn출력` 혼합이라 잔차만 스케일하면 **두 항의 비율이 바뀐다.**

  | 조건 | cos(norm1) | cos(norm2) | cos(mlp) |
  |---|---|---|---|
  | baseline_v2 × 1/34 | **1.00000** | **0.638** | 0.99936 |
  | baseline_v2 × 34 | **1.00000** | **0.914** | 0.99963 |
  | full × 1/34 | **1.00000** | **0.652** | 0.98720 |
  | full × 34 | **1.00000** | **0.918** | 0.99929 |

  `norm1`은 소수점 다섯 자리까지 1.00000 — 개입이 "잔차 운반분만 조절한다"는 전제가
  실증된다. `norm2`는 0.64까지 떨어져 **방향이 바뀌고**, 그래서 최댓값은 거의 그대로인데
  (31.00 → 31.25) MLP 생성량이 5.7배 변한다. `cos(mlp)`가 0.99 이상인 것도 일관된다 —
  **방향은 유지되고 크기만 바뀐다.**

  무개입 시 잔차/attn 최댓값 비가 **baseline 60.5, full 3.8**인 것이 민감도 차이를
  설명한다. baseline은 잔차가 압도적이라 축소 시 구성비가 급변하고(cos 0.638),
  full은 원래 둘이 비슷해 덜 민감하다.

  → **결론 (2026-09-14 정정). 전파 가설을 두 형태로 나눠야 한다.**

  | 형태 | 상태 |
  |---|---|
  | **크기 매개 전파** — "중간층 활성이 크므로 blk31도 크다" | **기각.** 잔차 기여 0.2~0.9%, 잔차를 34배 줄여도 blk31이 오히려 증가(부호 반대), 중간층 격차 33.6배가 blk31에서 2.8배로 축소(92% 소실) |
  | **방향 매개 전파** — "중간층 표현의 방향이 바뀌어 MLP 생성량이 준다" | **기각되지 않았다.** 오히려 (c)의 `norm2` 실험이 지지한다 — 방향이 바뀌면(cos 0.64) MLP 출력이 5.7배 달라진다 |

  **확실한 것**: blk31의 값은 앞 블록에서 **운반된 것이 아니라 마지막 블록 MLP가
  생성**한다(생성 위치). **주장하면 안 되는 것**: "중간층 변화가 blk31에 영향을 주지
  않는다". `L_align`의 개입이 코사인 손실이라 **방향을 바꾸므로**, 가장 그럴듯한 그림은
  **중간층 표현의 방향 변화 → 마지막 블록 MLP 생성량 감소**다.

  이 정정은 5장의 논지를 약화시키지 않는다 — 논지는 "표현 범위 여유가 기존 지표에
  보이지 않으며 학습 설계로 조절된다"이고, 그 조절이 어느 경로를 타는지와는 무관하다.

- **사후 개입 실험 — 어디서 줄이느냐가 전부다 (09-11)**

  `scripts/probe_mlp_overflow.py`, `scripts/probe_posthoc_rescale.py` (250장 + 300문항).
  비전 MLP는 `down_proj(act(gate_proj(x)) * up_proj(x))`이다. 아래 hook 개입은
  **weight와 bias를 함께 스케일하는 가중치 편집과 등가**다.

  > **정정 (2026-09-14)**: 이전 판은 "bias가 없어"라고 적었으나 **틀렸다**. `Qwen2_5_VLVisionBlock`은 `Qwen2_5_VLMLP(config, bias=True)`로 생성하므로 비전 MLP에는 bias가 있다(`gate_proj.bias` 등이 실제 가중치에 존재한다). 훅은 `gate_proj`의 **출력**(`Wx+b`)을 스케일하므로, 등가인 가중치 편집은 **weight와 bias를 함께** 나누는 것이다. weight만 나누면 bias가 남아 다른 연산이 된다. 실험 결과 자체는 훅으로 측정했으므로 영향이 없고, **처방 서술만 정정**한다.

  > **실측 후속 (2026-09-14 21:22, `scripts/probe_fixa_weight_equiv.py`, 250장 fp16)** —
  > 네 조건을 직접 재서 위 정정의 실제 영향을 확인했다.
  >
  > | 조건 | 붕괴율 |
  > |---|---|
  > | 무개입 | 95.2% |
  > | 훅 fixA (원래 실험) | **0.0%** |
  > | 가중치 fixA (weight+bias) | **0.0%** ← ONNX로 내보낸 판본 |
  > | 가중치 fixA (weight만) | **0.0%** |
  >
  > **`weight`만 편집해도 결과가 같다.** 이 스케일에서는 bias 기여가 붕괴 여부를 바꾸지
  > 않는다. 따라서 정정 전 서술의 **결론**("가중치를 1/√3로 나누면 된다")은 틀리지 않았고,
  > 틀린 것은 **근거**였다 — "bias가 없어서"가 아니라 "이 스케일에서 bias 기여가 작아서"다.
  > 논문에는 근거를 이렇게 정확히 쓰고, **일반적으로는 weight와 bias를 함께 나눠야 훅과
  > 등가**임을 함께 적는다(다른 스케일·다른 층에서는 갈릴 수 있다).
  >
  > 부수 소득: 이 실험이 **보드 측정의 전제를 닫았다.** Orin에 보낸 fixA ONNX가 A100에서
  > 잰 것과 같은 개입임이 확인됐다.

  | 개입 | 곱 | down 출력 | 붕괴율 (원본 해상도) | 정확도 (평가 해상도, 11,309문항) |
  |---|---|---|---|---|
  | 없음 | 4,588 | 63,200 | **94.4%** | 50.55% |
  | MLP 출력 1/3 (fixB) | 4,588 | 63,360 | 94.8% | — |
  | **gate·up 각 1/√3 (fixA)** | **1,530** | **24,800** | **0.0%** | **49.96%** (−0.59pp — 아래에서 철회) |

  최초 inf 발생 지점은 세 조건 모두 `down_proj`다. fixB는 **이미 inf가 된 값을 나누므로**
  실패하고, fixA는 **곱이 만들어지기 전에** 줄이므로 성공한다.

  > **정정 (2026-09-14, 전체 11,309문항 재측정).** 이전 판은 300문항 결과
  > (53.33% → 54.67%, **+1.34pp**)를 근거로 **"정확도 변화 없음"**이라 적었다.
  > **틀렸다 — 전체로 올리니 부호가 뒤집혔다**(50.55% → 49.96%, **−0.59pp**).
  > 300문항에서는 노이즈가 ±0.7pp라 1.34pp가 "개선"으로 보였던 것이다.
  > 전체 평가를 돌린 이유가 정확히 이것이고, 결과가 그 필요성을 입증했다.
  > 결과: `eval_results/posthoc_rescale_full.json`
  >
  > **이 0.59pp는 시드 노이즈가 아니다.** 5-way의 0.35pp와 성격이 다르다 — 같은
  > 가중치에 결정론적 편집만 가한 짝지은 비교라 **학습 무작위성이 전혀 없다.**
  > (엄밀히 하려면 문항별 덤프로 McNemar가 필요하나 이 실행에는 덤프가 없다.)
  >
  > **측정 조건을 정확히 읽을 것.** 정확도는 **평가 해상도(200,704)**에서 bf16으로 쟀다.
  > 그 해상도에서는 무개입도 거의 붕괴하지 않으므로(3.2%), 이 수치는 **개입의 순수
  > 비용**이다. 원본 해상도에서는 무개입이 94.4%의 이미지에서 출력을 내지 못하므로
  > 이 비용은 상쇄되고도 남는다.

> **McNemar 검정 결과 — "비용 0.59%p"를 철회한다 (2026-09-15).**
  > 문항별 덤프로 짝지어 검정하니 **p = 0.2503으로 유의하지 않다**
  > (불일치 681쌍: 무개입만 356 / fixA만 325, χ²(1)=1.32).
  > **평가 표본 잡음과 구별되지 않는다.**
  >
  > **더 중요한 것은 측정 자체가 흔들린다는 점이다.** 같은 무개입 조건을 두 번
  > 돌렸더니 50.55% → 50.30%로 **0.26%p 달랐다**(`do_sample=False`인데도).
  > cuDNN/cuBLAS 비결정성으로 보이며, 실행 중 `cudnnException` 폴백 경고가 있었다.
  > 즉 **관측된 효과(0.27%p)와 같은 크기로 측정이 흔들린다.**
  >
  > 이는 앞서 적은 "이 0.59pp는 시드 노이즈가 아니다 — 결정론적 편집이라 학습
  > 무작위성이 없다"는 서술을 **직접 반증한다.** 학습 무작위성이 없는 것은 맞지만
  > **추론 자체가 결정론적이 아니었다.**
  >
  > **정확한 진술**: "fixA는 원본 해상도에서 붕괴를 94.4% → 0.0%로 제거하며,
  > 정확도 변화는 **측정 한계 이하**다(11,309문항 McNemar p=0.25, 동일 조건
  > 재실행 변동 0.26%p)."
  >
  > **조건을 맞춘 재측정이 §7.3에 있다 (2026-09-17 상호참조).** 위 값은 **평가
  > 해상도·bf16**에서 잰 것이다. 배포 조건(원본 해상도·fp16) 짝으로 다시 재면
  > **+0.78%p, p=0.34**다. 부호가 뒤집혔으나 둘 다 비유의라 결론은 같고,
  > **근거의 조건이 맞아졌다.**
  >
  > 유형별로는 `count`만 −1.04%p에 p=0.0122였으나 **다중 비교 보정을 하지 않았고**
  > (5개 유형, Bonferroni 임계 0.01), `count`는 모든 변형에서 7~11%로 원래 약한
  > 유형이다. **유형별 주장은 하지 않는다.**
  > 결과: `eval_results/mcnemar_fixa.json`

  **서술 방침**: **"원본 해상도에서 붕괴를 94.4% → 0.0%로 제거하며, 정확도 변화는
  측정 한계 이하다"**로 쓴다. "공짜"라고 단정하지도, "0.59%p 비용"이라 쓰지도 않는다 —
  전자는 동등성을 증명한 것이 아니고(비유의는 동등성의 증거가 아니다), 후자는 반증됐다.

  **이 결과가 5장의 강조점을 바꾼다 (사전 등록 방침 적용).**
  - "손실 설계가 배포 가능성을 **결정한다**"는 표현을 **쓰지 않는다.**
  - 정확한 진술: **"이 활성값은 기능에 기여하지 않으면서 표현 범위 예산을 전부 소모한다.
    손실 설계는 그 여유를 자동으로 확보해 주고, 그렇지 않은 모델도 사후 수정이 가능하지만
    아무도 측정하지 않아 방치되어 있다."**
  - 실무 기여는 오히려 강해진다 — 6장의 위험 구성 27개에 **바로 적용 가능한 처방**이 생긴다.

- **가중치 분포 — 증폭기는 사전학습에 박혀 있다 (09-11)**

  `scripts/probe_weight_distribution.py` (LoRA 병합 후 행 노름, CPU).

  | 모델 | blk31 gate | up | **down** | down max/p50 | top 행 |
  |---|---|---|---|---|---|
  | base_3b ~ student_full (6개 전부) | 1.7 | 1.7 | **13.3** | **9.3** | **849** |

  **6개 변형이 완전히 동일하다.** LoRA가 비전 MLP에 걸려 있으나 델타가 base 대비
  무시할 수준이다(lora_A absmax 0.027, lora_B 0.007). 따라서 현상은 **가중치 이상치가
  아니라 활성**이며, 변형 간 차이는 `down_proj`에 **들어가는 입력**에서 온다.

  → 메커니즘 4단계: ① 사전학습 가중치의 `down_proj` 849행 이상치(중앙값의 9.3배)가
  증폭기다 → ② 미세조정은 이 증폭기를 바꾸지 않는다(가중치 이상치는 6개 변형 동일)
  → ③ 변형 간 차이는 증폭기로 가는
  입력(곱)의 크기다 → ④ `L_align`은 중간층 표현을 바꿔 그 입력을 줄인다.

  **활성 증가의 귀속 — "미세조정이 키운다"는 부정확하다 (2026-09-15 실측).**
  v1(LLM LoRA만)과 v2를 동일 250장에서 짝지어 재니 분해가 달라졌다.
  **모두 평가 해상도(200,704)·bf16 값이다** — 원본 해상도에서는 같은 두 모델이
  0.95배와 0.88배로 둘 다 한계 아래다(Table 1). 조건을 섞지 말 것.

  | 단계 | blk31 p50 (평가 해상도) | 여유 | 직전 대비 |
  |---|---|---|---|
  | 사전학습 `base_3b` | 33,312 | 1.97배 | — |
  | + 과제 손실 미세조정 (`baseline_v1`, LLM LoRA만) | 30,024 | **2.18배** | **0.90배** |
  | + 비전-어텐션 LoRA & hazard 오버샘플 (`baseline_v2`) | 47,296 | **1.38배** | **1.58배** |

  **과제 손실 미세조정 자체는 활성을 키우지 않는다 — 오히려 10% 줄인다.**
  `base_3b` → `baseline_v2`의 1.42배 증가는 `0.90배 × 1.58배`이고, 키운 쪽은
  두 번째 단계다. 붕괴도 v1은 0/250, v2는 8/250이다.

  > **다만 귀속을 확정할 수 없다 (2026-09-15).** v1 → v2는 `training_state.pt` 대조
  > 결과 **두 변수가 바뀐다** — `lora_target_vision_attn`(False→True)과
  > **`hazard_oversample`(False→True)**. 후자는 DriveLM 부분집합 내 샘플러 가중치를
  > 바꾸므로 **데이터 분포 변경**이다. 따라서 1.58배를 "비전-어텐션 LoRA 용량 때문"으로
  > 쓸 수 없다. (`CLAUDE.md`가 "exactly one variable changes"라고 적고 있었는데
  > 사실과 달라 함께 정정했다.)
  >
  > 분리하려면 `hazard_oversample=True` + `lora_target_vision_attn=False`인 세 번째
  > 실행이 필요하며 약 51.6시간이다.

  **그래도 5장 서술은 바꿔야 한다.** 지금까지 "과제 손실만으로 미세조정한 학생이 활성을
  1.5배 키웠다"고 써 왔는데, **그 귀속이 틀렸다.** 정확한 진술은 이렇다.

  > 사전학습 모델 대비 활성이 커진 것은 과제 손실 미세조정 때문이 아니다. LLM LoRA만
  > 쓴 변형은 오히려 활성이 10% 낮다. 증가는 **비전 인코더까지 LoRA를 확장하고 데이터
  > 샘플링을 바꾼 단계**에서 나타나며(1.58배), 두 요인은 본 연구에서 분리되지 않았다.

  **이것이 §2 넷째 절(수단은 여럿)의 세 번째 증거이자 방향이 반대인 첫 사례다** —
  `L_align`과 fixA는 여유를 늘리는 쪽, 이것은 줄이는 쪽이다. 실무적으로는
  **"LoRA를 비전 인코더까지 확장하는 흔한 선택이 fp16 여유를 2.18배 → 1.38배로 줄이고
  붕괴를 0/250 → 8/250으로 만드는데 아무도 재지 않는다"**가 된다.
  결과: `eval_results/v1_vs_v2_profile.json`

  **849는 잔차 스트림의 전용 채널이다 (2026-09-14 실측, `scripts/probe_channel849.py`).**

  두 가지를 함께 쟀다. 하나는 32개 블록 전부의 `down_proj` 849행 노름(가중치만),
  다른 하나는 마지막 블록 출력에서 **실제로 최대 활성이 나오는 채널**(원본 해상도 50장)이다.
  후자는 5장 서술의 전제 검증이다 — 오버플로가 다른 채널에서 일어난다면 "849행이
  증폭기"라는 서술이 틀리기 때문이다.

  | blk | 배율 | blk | 배율 | blk | 배율 |
  |---|---|---|---|---|---|
  | 0 | 2.79 | 16 | 2.21 | 28 | 1.99 |
  | 4 | 1.90 | 20 | 2.03 | 30 | 2.52 |
  | 8 | 1.58 | 23 | 2.42 | **31** | **9.33** |
  | 12 | 1.83 | 24 | 2.25 | | |

  - **849행이 32/32 블록 전부에서 최상위**다. 예외가 없다.
  - **점진적 증폭이 아니라 마지막 블록의 불연속이다.** blk0~30은 1.58~2.79배로 평탄하고
    blk31만 9.33배다(blk30 대비 3.7배 도약). **"깊이에 따라 증폭된다"고 쓰지 말 것** —
    "모든 블록이 쓰는 전용 채널인데 마지막 블록만 특별히 크게 쓴다"가 정확하다.
  - **활성 최대 채널이 50/50장에서 849번**이고, 849번/전역 최대 비율 중앙값이 1.0000이다.
    그 지점의 활성 중앙값은 **73,448**로 fp16 한계 65,504를 넘는다.
    → 가중치 이상치와 활성 이상치가 **같은 채널**임이 확정됐다. 5장 서술의 전제가 성립한다.

  결과: `eval_results/channel849.json`

  **이 행은 이 체크포인트 고유가 아니다 (2026-09-14 실측).**
  `scripts/probe_downproj_outlier_across_models.py`로 세 모델의 마지막 비전 블록
  출력 선형층 행 노름을 비교했다(가중치만, GPU 불필요).

  | 모델 | 최대 배율 | 행 번호 | 2위 |
  |---|---|---|---|
  | Qwen2.5-VL-3B (본 연구) | **9.32배** | **849** | 1.06배 |
  | Qwen2.5-VL-7B (교사) | **9.55배** | **849** | 1.25배 |
  | Qwen2-VL-2B (이전 세대, `fc2`) | 14.80배 | 759 | 1.12배 |

  세 모델 모두 **단 하나의 행만** 튄다 — 2~5위가 전부 1.05~1.25배에 몰려 있어
  점진적 분포의 꼬리가 아니라 별개의 것이다.

  **3B와 7B가 같은 849행인데 가중치는 다르다.** 두 모델은 비전 config가 완전히 동일해
  (depth 32 / hidden 1280 / intermediate 3420) "같은 ViT를 물려받았을 뿐"일 가능성이
  있었으므로, 노름 벡터를 직접 대조했다 — 최대 차이 0.949(중앙값 대비 66%)로 **다른
  가중치**다. 즉 독립적으로 학습된 두 모델이 같은 채널 번호에 이상치를 만들었다.
  이 대조를 넣지 않았다면 결과를 "같은 ViT를 썼을 뿐"으로 오독했을 것이다.

  → **현상은 Qwen 비전 인코더 계열 공통이고, 위치는 아키텍처가 같으면 같다.**
  결과: `eval_results/downproj_outlier_across_models.json`

  **여전히 답하지 못하는 것 — 왜 849인가.** 사전학습 데이터·과정에 접근해야 하므로 본
  연구 범위 밖이다(중간 체크포인트도 공개되지 않았고, 630M ViT를 직접 사전학습해 재현하는
  것은 학위논문 규모를 넘는다). 다만 **위 논증은 그 답을 필요로 하지 않는다**: 증폭기가
  존재하고, 6개 변형에서 동일하며, 달라지는 것은 거기 들어가는 입력이라는 세 사실만으로
  닫힌다. 표본이 셋뿐이라는 점도 함께 적는다.

- **인과 논증의 구조 — 2단계 사슬 (중요)**

  인과를 하나의 실험으로 세우려 하지 않는다. 두 링크를 각각 다른 실험이 담당한다.

  | 링크 | 담당 실험 | 결과 변수 | 상태 |
  |---|---|---|---|
  | ① **λ → 활성 크기** | λ 스윕 (0 / 0.5 / 1.0 / 2.0 × 5,000스텝, 시드 고정) | 연속 (max\|act\|) | 진행 중 |
  | ② **활성 크기 → 붕괴** | 해상도 스윕 (완주 모델 6개 × 5해상도) | 이산 (붕괴율) | **완료** |

  **두 링크가 같은 모델에서 동시에 측정될 필요가 없다.** ①은 손실 가중치가 활성 크기를
  좌우함을, ②는 활성 크기가 배포 성패를 좌우함을 각각 보인다.

  이 분리가 중요한 이유: λ 스윕은 5,000스텝이라 blk31 활성이 완주 모델보다 낮고,
  따라서 **붕괴율이 전 구간 0%로 나올 가능성이 높다.** 붕괴율을 ①의 결과 변수로 삼으면
  포화된 이진 변수를 쓰는 설계 오류가 된다(②에서도 30점 중 24점이 0%였다).
  ①은 반드시 **연속 변수인 활성 크기**로 측정한다.

- **λ 용량-반응 (그림 3) — 완료 (2026-09-12)**

  시드 1234·학습률 궤적 고정, 각 5,000스텝. 250장 프로파일.

  | λ | blk16 | blk24 | blk28 | **blk31** | **fp16 여유** | 붕괴율 |
  |---|---|---|---|---|---|---|
  | 0.0 | 896 | 8,512 | 8,512 | 36,608 | 1.79× | 0% |
  | 0.5 | 167 | 5,856 | 5,856 | 26,112 | 2.51× | 0% |
  | 1.0 | 55 | 124 | 187 | 19,072 | 3.43× | 0% |
  | 2.0 | 55 | 128 | 212 | **14,208** | **4.61×** | 0% |

  **층마다 거동이 다르다.**
  - **최종 블록(blk31)은 단조 감소하며 구간별 감소율이 28.7 / 27.0 / 25.5%로 일정하다.**
    fp16 여유가 1.79 → 4.61배로 늘어난다. **배포 여유를 결정하는 층에서 뚜렷한 용량-반응**이며,
    λ=2.0까지 포화 조짐이 없다. → **링크 ①(λ → 활성 크기)의 인과 근거**
  - 중간층(blk24·blk28)은 λ=1.0에서 급락한 뒤 **포화**한다(124 → 128). blk16도 55에서 멈춘다.

  이 층별 차이는 개입 실험에서 얻은 결론(중간층 억제와 최종 블록 생성량 억제가 독립)과
  일관된다.

  **붕괴율은 4점 모두 0%다.** 5,000스텝 모델은 blk31이 14,000~36,000이라 fp16 상한에 여유가
  있다. 예상된 결과이며, **결과 변수를 연속량(활성 크기)으로 잡은 2단계 사슬 설계가
  옳았음을 확인**한다 — 붕괴율로 쟀다면 4점 모두 0%라 아무것도 보이지 않았을 것이다.
- 배포 설계 지표 제안: `format_max / p99(max|act|)`
- 선행 연구 위치: CrAFT는 optimizer 축, 이상치 억제 정규화 계열은 설계된 처방,
  PASA·vLLM Issue #40290은 현상 선점이나 아키텍처 기인. **증류 손실 축은 미측정**
  (`"activation magnitude"` AND `"fp16"` 0건)

### 5장 따름정리 — 이산적 실패는 집계 지표를 무력화한다
별개 기여가 아니라 §2 논지의 직접적 귀결이다. 실패가 이산적·파국적이기 때문에
교란 변수 하나가 표 전체를 뒤집는다.

- INT4 정확도 1.39% 관측 → 원인은 양자화가 아니라 (i) 저장 dtype, (ii) 입력 해상도

  **두 변수를 고정한 재측정 (2026-09-15 완료).** 같은 AWQ 체크포인트를
  `--dtype bfloat16`으로 로드하고 processor를 base에서 만들어(max_pixels 200,704)
  전체 11,309문항을 다시 쟀다. 태그에 `_bfloat16`이 붙어 **교란값 파일을 덮지 않으므로
  둘을 나란히 실을 수 있다.**

  | 변형 | INT4 교란 | **INT4 정정** | bf16 원본 | 정정−원본 |
  |---|---|---|---|---|
  | `baseline_v2` | **1.39%** | **48.30%** | 50.30% | −2.00pp |
  | `full` | 51.60% | 51.86% | 51.69% | +0.17pp |
  | `spatial` | 50.82% | 50.64% | 51.40% | −0.76pp |
  | `temporal` | **30.49%** | **50.31%** | 51.34% | −1.03pp |
  | `kd_only_v4` | **32.99%** | **47.96%** | 46.74% | **+1.22pp** |

  **`baseline_v2` 1.39% → 48.30%, `temporal` 30.49% → 50.31%.** 두 변수만 고정했는데
  회복됐다. **양자화 때문이 아니었음이 수치로 확정된다** — 6장의 핵심 주장이 이제
  말이 아니라 표다.

  **양성 대조 — 교란값이 양자화 없는 fp16 붕괴값과 겹친다 (2026-09-17 추가).**
  위 논증은 소거법이다("교란을 걷으니 회복됐다 → 양자화 탓이 아니다"). 여기에
  **무엇 때문이었는지를 직접 가리키는** 대조가 붙는다.

  | 실행 | 양자화 | 비전 인코더 | 해상도 | 정확도 |
  |---|---|---|---|---|
  | INT4 교란 | **INT4 AWQ** | fp16 | 원본 | **1.39%** (157/11,309) |
  | `native_fp16` | **없음** (bf16 LoRA) | fp16 | 원본 | **1.36%** (14/1,029) |

  **0.03%p 차이다.** 양자화 여부가 다른 두 실행이 같은 자리에 온다. 우연이 아니라
  **같은 기전**이기 때문이다 — AWQ는 언어 모델의 linear 층만 INT4로 만들고
  **비전 인코더는 `--dtype`이 정하는 형식으로 그대로 돈다.** 두 실행 모두 비전 인코더가
  fp16으로 원본 해상도를 돌았고, 붕괴는 거기서 일어났다. **INT4는 붕괴 지점을
  건드리지도 않았다.**

  `limit30` 덤프에서 교란 실행의 출력이 `!` 반복(token 0)임을 확인했다 —
  `native_fp16`과 **같은 서명**이다.

  > **세 가지 단서를 함께 적는다.** (i) 두 값은 **표본이 다르다**(11,309 전체 대
  > stride-11 1,029). 짝지은 비교가 아니므로 "같다"가 아니라 **"겹친다"**로 쓴다.
  > (ii) 교란 실행의 **붕괴율은 직접 재지 않았다** — 전체 실행에 예측 덤프가 없고
  > `checkpoints_awq/`는 2026-09-15에 삭제해 재실행할 수 없다. 겹치는 것은 정확도이고,
  > 붕괴율 대조는 `limit30`의 서명 확인까지다. (iii) 그 `limit30`(30문항)의 느낌표
  > 비율 43.3%를 **붕괴율로 인용하지 않는다** — 앞 30문항은 장면 몇 개만 덮는데
  > 붕괴는 프레임 단위로 전부 아니면 전무다(⑤). `--limit`을 버리고 `--stride`를
  > 도입한 이유가 이것이며, 여기서도 같은 함정이 재현됐다.

  **서술 규칙.** 교란을 걷어내면 INT4 자체의 손실이 보이지만(정정−원본 열),
  **이것을 순위 주장으로 쓰지 않는다.** 각 변형이 n=1이고 `full`의 +0.17pp는 노이즈
  범위다. 쓸 수 있는 진술은 **"교란을 제거하면 다섯 변형이 모두 48~52%대로 모이며
  1.39% 같은 파국적 값은 사라진다"**까지다. `baseline_v2`의 −2.00pp가 다른 것보다
  크다는 점은 **관찰로만** 적고, "정렬 손실이 INT4 강건성을 준다"로 확대하지 않는다
  (3장 통계 서술 규칙 적용).

  **마지막 값이 이 규칙의 필요성을 입증했다.** `kd_only_v4`가 **+1.22pp**로 `full`
  (+0.17pp)보다 크게 올랐다 — **정렬 손실이 없는 변형이 가장 많이 회복한 것이다.**
  "정정−원본" 열에는 일관된 방향이 없다(−2.00 ~ +1.22pp). 만약 `kd_only_v4`를 재기 전에
  네 개만 보고 "정렬 손실이 INT4에서 덜 잃는다"로 썼다면 틀린 서술이 됐을 것이다.

  결과: `eval_results/*_4bit_awq_bfloat16_nuscenesqa_val.json`
- **일반성 검증 (2026-09-10 완료)**: HuggingFace 공개 양자화 VLM 체크포인트 **197개**의
  config를 수집해(`scripts/survey_quantized_vlm_configs.py`) 같은 패턴의 빈도를 셌다.

  | 패턴 | 비율 |
  |---|---|
  | `torch_dtype == float16` | 55/197 (28%) |
  | 비전 인코더를 양자화에서 제외 | 67/197 (34%) |
  | **둘 다 — 비전 인코더가 fp16으로 실행** | **27/197 (14%)** |
  | processor `max_pixels` > 4M | 56/197 (28%, 대부분 12,845,056) |

  위험 구성 27개에는 **모델 제작사 공식 저장소 3개**가 포함된다 —
  `Qwen/Qwen2-VL-{2B,7B,72B}-Instruct-AWQ`. 그중 7B는 `max_pixels`까지
  12,845,056으로, **본 연구가 겪은 것과 완전히 동일한 구성**이다.

  → 6장은 "우리가 실수했다"가 아니라 **"공개 양자화 VLM의 14%가 같은 조건에 놓여 있고,
  우리가 조사한 범위에서 그 여유를 보고한 저장소는 없었다"**가 된다.

  > ## ⚠️ 14%는 **노출의 하한**이다 — 정의를 고친다 (2026-09-18)
  >
  > 위 27개의 정의는 **`torch_dtype`이 fp16이라고 *적혀 있는* 것**이다. 그런데
  > **논문이 실제로 다루는 것은 "배포하려고 fp16으로 바꾸면"**이다. 둘은 다른
  > 모집단이고, 지금까지 전자만 세면서 **논지가 그쪽으로 끌려갔다.**
  >
  > | | 무엇 | 크기 |
  > |---|---|---|
  > | **A. 이미 fp16으로 적혀 있음** | 사용자가 아무것도 안 해도 fp16으로 돈다 | **27/197 (14%)** |
  > | **B. fp16으로 변환하면 위험** | 배포 시 변환하는 모든 경우 | **훨씬 크다** |
  >
  > **본 연구의 출발점이 A 밖에 있다.** `Qwen/Qwen2.5-VL-3B-Instruct`는
  > `torch_dtype: bfloat16`이라 27개에 들지 않는다. 그런데 배포를 위해 fp16으로
  > 바꾸자 **여유 0.95배, 붕괴 69.6%**다. **선언이 아니라 변환이 위험을 만든다.**
  >
  > 그리고 **fp16 변환은 엣지 배포의 표준 경로**다 — `trtexec --fp16`, ONNX fp16
  > 변환, `torch_dtype=torch.float16` 로드. **배포자가 선언을 덮어쓴다.**
  > 4·6·7장이 전부 이 경로를 다룬다.
  >
  > **그래서 두 문장으로 나눠 쓴다.**
  > > **이미 fp16으로 실행되도록 적혀 있는 것이 14%**다(27/197, 공식 저장소 셋 포함).
  > > **그러나 이는 노출의 하한이다** — fp16 변환은 엣지 배포의 표준 경로이고
  > > 선언된 dtype을 배포자가 덮어쓴다. 본 연구의 대상 모델도 `bfloat16`으로
  > > 선언돼 있으나 fp16으로 변환하자 69.6%가 붕괴했다.
  >
  > **왜 이렇게 쓰지 않았나 — 기록해 둔다.** 조사가 config 기반이라 **셀 수 있는 것이
  > "선언된 dtype"뿐**이었다. "누가 배포할 때 무엇을 할지"는 config로 셀 수 없다.
  > 셀 수 있는 것만 세다가 **측정 가능성이 주장의 범위를 정해 버렸다**(§3.5와 같은
  > 결의 문제). 세지 못하는 쪽이 더 크다면 **"세지 못했다"고 적어야지 없는 것처럼
  > 쓰면 안 된다.**
  >
  > **이 정정은 논문을 강하게 한다.** *"14%가 위험 구성"*은 **"86%는 괜찮다"**로
  > 읽힌다. 고치면 **"14%는 이미 그렇게 배포돼 있고, 나머지도 fp16으로 변환하는
  > 순간 같은 조건이 된다"**가 된다. 도구가 선언된 dtype이 아니라 **`--dtype`으로
  > 목표 형식을 받는 이유**가 정확히 이것이다.

  > **실측이 이 서술을 더 좁힌다 (2026-09-15).** 위험 구성 중 실제로 잰 셋이
  > **전부 안전했다** — Qwen2-VL-2B 3.88배, Qwen2-VL-7B 4.49배, SmolVLM 50.86배.
  > 따라서 **"공개 체크포인트가 위험하다"고 쓸 수 없다.** 실측으로 위험이 확인된 것은
  > **Qwen2.5-VL-3B 계열뿐**이며, 그중에는 미세조정 전 `base_3b`도 포함된다
  > (원본 해상도 여유 0.95배, 붕괴 69.6%).
  >
  > **그래서 실무 주장의 범위는 이렇다 (2026-09-18 갱신)**: "**이미 fp16으로
  > 적혀 있는 것이 13.7%**(읽을 수 있었던 197개 중)이고 **이는 하한이다** —
  > 배포 시 fp16으로 변환하면 선언과 무관하게 같은 조건이 된다(본 연구 대상이
  > 그 예로, `bfloat16` 선언에도 변환 후 69.6%가 붕괴했다). 그리고 구성만으로는
  > 알 수 없어 재봐야 한다 — 직접 잰 결과가 **양쪽으로 갈렸다.** Qwen2-VL 2B·7B와 SmolVLM은 3.88~50.86배로 안전했으나,
  > 본 연구 학생과 같은 아키텍처의 공개 INT4-AWQ 배포본은 **0.96배로 한계를 넘었다**."
  > 이전 판은 "잰 셋은 모두 안전했다"였다 — 더 재자 뒤집혔다. 범위는 좁지만
  > 이제 **양쪽 실례**가 실측으로 뒷받침된다.
  >
  > 분모도 정확히 적는다: 13.7%는 검색으로 얻은 후보 **281개**가 아니라 **config를
  > 읽을 수 있었던 197개** 위의 비율이다. 나머지 84개는 접근이 되지 않아 빠졌으므로
  > **자기선택 표본**이며, 편향이 없다고 말하지 않는다.
- **위험 구성의 실측 — 27개 중 4개만 잴 수 있었다 (2026-09-15 완료).**
  config 집계를 실측 표로 격상하려 했으나, **생태계의 버전 파편화 때문에 대부분
  로드되지 않았다.** 그 실패 자체가 6장의 논지를 보강한다.

  | 모델 | fp16 여유 | 비고 |
  |---|---|---|
  | **Qwen2.5-VL-3B** (본 연구 대상) | **0.95배** | 원본 해상도에서 69.6% 붕괴 |
  | Qwen2-VL-2B-AWQ | 3.88배 | 공식 위험 구성 |
  | Qwen2-VL-7B-AWQ | 4.49배 | 공식 위험 구성 |
  | SmolVLM-Instruct (Idefics3) | 50.86배 | 비-Qwen 계열 |

  > ## 제작사 공식 배포본도 한계 아래다 — 그리고 여유는 **상속된다** (2026-09-18)
  >
  > 27개 필터가 `torch_dtype == float16`을 요구하는데, **Qwen 공식 AWQ 저장소는
  > `bfloat16`으로 선언돼 있어 전부 빠져 있었다.** 논문의 전제가 *"배포하려고 fp16으로
  > 바꾸면"*이므로 선언은 무관하다. 그래서 직접 쟀다.
  >
  > | 저장소 | 활성 p50 | **여유 p50** | 최악 | 27개 포함? |
  > |---|---|---|---|---|
  > | `Qwen/Qwen2.5-VL-3B-Instruct` (기반) | 68,608 | **0.95** | 0.76 | — (양자화 아님) |
  > | **`Qwen/Qwen2.5-VL-3B-Instruct-AWQ`** (공식) | **68,608** | **0.95** | **0.76** | **✗ bf16 선언** |
  > | `AngelSlim/Qwen2.5-VL-3B-Instruct-INT4-AWQ` | 68,096 | 0.96 | 0.76 | ✓ fp16 선언 |
  > | **`Qwen/Qwen2.5-VL-7B-Instruct-AWQ`** (공식) | 37,632 | **1.74** | 1.26 | **✗ bf16 선언** |
  > | `AngelSlim/Qwen2.5-VL-7B-Instruct-AWQ` | 37,888 | 1.73 | 1.30 | ✓ fp16 선언 |
  >
  > **두 가지가 확정된다.**
  >
  > **① 제작사 공식 배포본이 fp16 한계 아래다.** `Qwen/Qwen2.5-VL-3B-Instruct-AWQ`가
  > **0.95배**다. 지금까지 실측 위험 사례가 비공식 저장소(AngelSlim) 하나였는데,
  > **제작사 공식 저장소**로 올라갔다. 그리고 **선언된 dtype만 보는 조사는 이것을
  > 놓친다** — 27개 필터에 안 걸린다.
  >
  > **② 여유는 기반 모델에서 상속된다.** 기반과 공식 AWQ가 **자릿수까지 같다**
  > (68,608 / 0.95 / 0.76). 당연하다 — **AWQ가 비전 인코더를 양자화에서 제외**하므로
  > 비전 가중치가 기반 그대로다. 서로 다른 두 양자화 주체(Qwen 공식 1.74 · AngelSlim
  > 1.73)가 같은 값을 내는 것도 같은 이유다.
  >
  > **②가 세는 방식을 바꾼다.** *"저장소 27개 중 몇 개가 위험한가"*가 아니라
  > **"기반 모델이 위험하면 그 파생본 전부가 위험하다"**이다. 조사 안의 미측정
  > Qwen2.5-VL 29개도 재 볼 필요가 없다 — **3B 파생은 0.95, 7B 파생은 1.74로
  > 결정돼 있다.** 조사 밖 파생본까지 합치면 규모는 더 크다.
  >
  > **그래서 실무 권고가 명확해진다.** 파생본마다 재는 것이 아니라 **기반 모델의
  > 비전 인코더를 한 번 재면**, 그것을 양자화에서 제외하는 **모든 배포본에 그대로
  > 적용된다.** 도구를 기반 모델에 돌리는 것으로 충분하다.
  >
  > 결과: `eval_results/vision_tower_direct_official.json`

  > ## Qwen3-VL을 쟀다 — 다리 측정으로 버전 장벽을 넘었다 (2026-09-18)
  >
  > 조사 197개 중 **최대 집단은 Qwen3-VL 57개**인데 `transformers` 4.49가
  > `qwen3_vl`을 몰라 못 쟀다. 그냥 새 버전에서 재면 **한 논문에 두 버전 수치가
  > 섞인다.** 그래서 **다리 측정**을 했다.
  >
  > **1단계 — 같은 모델을 두 버전에서 재고 일치를 확인한다.** 같은 이미지 100장을
  > 파일로 고정하고(`eval_results/bridge_image_paths.json`), 구 코드에 의존하지 않는
  > 독립 스크립트(`scripts/bridge_measure_newhf.py`)를 별도 venv에서 돌렸다.
  >
  > | 항목 | 4.49.0 | **5.17.0** | 차이 |
  > |---|---|---|---|
  > | 활성 **p50** | 68,608 | **68,608** | **0.0000%** |
  > | 여유 **p50** | 0.9548 | **0.9548** | **0.0000%** |
  > | 활성 최악 | 86,528 | 84,992 | −1.78% |
  > | 여유 최악 | 0.7570 | 0.7707 | +1.81% |
  >
  > **중앙값은 자릿수까지 같고 꼬리만 ~2% 다르다.** Table 1과 주요 주장이 p50
  > 기준이므로 **p50은 버전을 넘어 비교할 수 있고, 최악값에는 ~2% 단서를 단다.**
  > L6에 적은 *"올리면 측정 일관성을 잃는다"*는 **절반이 근거 없음으로 확인됐다.**
  >
  > **2단계 — Qwen3-VL 실측**
  >
  > | 세대 | 모델 | 활성 p50 | **여유 p50** | 최악 | HF |
  > |---|---|---|---|---|---|
  > | Qwen2-VL | 2B-AWQ | — | 3.88 | — | 4.49 |
  > | Qwen2-VL | 7B-AWQ | — | 4.49 | — | 4.49 |
  > | **Qwen2.5-VL** | **3B**(기반·공식AWQ·AngelSlim) | **68,608** | **0.95** | 0.76 | 4.49 |
  > | **Qwen2.5-VL** | **7B**(공식·AngelSlim) | 37,632 | **1.74** | 1.26 | 4.49 |
  > | Qwen3-VL | 32B-AWQ (dense) | 10,752 | **6.09** | 5.50 | 5.17 |
  > | Qwen3-VL | 30B-A3B-AWQ (MoE) | 1,552 | **42.21** | 34.69 | 5.17 |
  >
  > **사전 등록한 둘째 분기가 실현됐다 — Qwen3-VL에서 개선됐다.** 그대로 적는다.
  >
  > **Qwen2.5-VL 세대만 튄다.** 이전 세대(Qwen2-VL 3.88~4.49)도 안전했고 다음
  > 세대(Qwen3-VL 6.09~42.21)는 더 안전하다. **같은 제작사가 세대를 넘기며 실제로
  > 줄였다** — 기여 ④(여유를 움직이는 수단이 여럿)의 증거가 하나 늘었다.
  >
  > > **추측 하나가 반증됐다.** 두 Qwen3-VL 저장소의 비전 키가 351개로 같길래
  > > *"같은 인코더면 값이 재현될 것"*이라 적었는데, **dense 6.09 대 MoE 42.21로
  > > 7배 차이**다. **키 개수가 같다고 가중치가 같은 것이 아니다.** 상속은
  > > **같은 기반의 파생본** 사이에서만 성립한다(3B 기반 = 3B 공식 AWQ). §3.5.
  >
  > **논문에 불리한 면을 그대로 적는다.** *"최신 세대에서는 해결됐다"*면 실무
  > 관련성의 무게가 준다. 다만 **Qwen2.5-VL은 조사 197개 중 54개로 여전히 둘째로
  > 큰 집단**이고, 그 파생본 전부가 0.95~1.74를 상속한다.
  >
  > 결과: `eval_results/bridge_newhf.json`, `scripts/bridge_measure_newhf.py`

  > ## Qwen 밖 가변 시퀀스 셋을 더 쟀다 — Qwen2.5-VL은 **이상치**다 (2026-09-18)
  >
  > Pixtral 하나로는 "Qwen 때문인가"를 가리기 부족해, 조사 밖에서 **가변 시퀀스
  > 인코더를 더 찾았다.** 프로세서만 받아(가중치 없이) 크기가 다른 두 이미지의
  > 출력 형상을 비교해 선별했다 — MiniCPM-V 4.5와 Ovis2는 **고정**이라 대상 밖,
  > GLM-4.1V·Kimi-VL·LFM2-VL이 **가변**이었다.
  >
  > **전체 (단일 가변 시퀀스, 여유 오름차순)**
  >
  > | 제작사 | 모델 | 활성 p50 | **여유 p50** | 최악 |
  > |---|---|---|---|---|
  > | **Alibaba** | **Qwen2.5-VL-3B** (기반·공식AWQ) | **68,608** | **0.95** | 0.76 |
  > | Alibaba | Qwen2.5-VL-7B (공식AWQ) | 37,632 | **1.74** | 1.26 |
  > | Alibaba | Qwen2-VL 2B / 7B (공식AWQ) | — | 3.88 / 4.49 | — |
  > | Alibaba | Qwen3-VL-32B-AWQ | 10,752 | 6.09 | 5.50 |
  > | **Mistral** | **Pixtral-12B** | 1,824 | **35.91** | 28.63 |
  > | **Zhipu** | **GLM-4.1V-9B** | 1,712 | **38.26** | 36.72 |
  > | **Moonshot** | **Kimi-VL-A3B** (MoonViT) | 1,624 | **40.33** | 38.44 |
  > | Alibaba | Qwen3-VL-30B-A3B-AWQ | 1,552 | 42.21 | 34.69 |
  >
  > > **측정 범위를 고쳤다 — 블록 뒤까지 본다 (2026-09-18).**
  > >
  > > 위 표는 모두 **비전 블록의 마지막 출력**에서 잰 값이다. 그런데 **블록 뒤
  > > 연산이 크기를 키우는 계열이 실제로 있다** — vLLM #40290의 Gemma 4가
  > > SigLIP 표준화 `(h − std_bias) × std_scale`에서 `std_bias`가 53,760이라
  > > **뺄셈 중간값이 fp16을 넘는다**(여유 1.218배). 줄여 줄 `× std_scale`은
  > > 뺄셈 **뒤에** 온다. 그런 계열이라면 위 표의 "안전"은 **과대평가**다.
  > >
  > > 그래서 도구가 **두 지점을 다 재고 나쁜 쪽으로 판정**하도록 고치고(§3.5),
  > > 표의 계열을 다시 쟀다. **여섯 계열 전부 블록 뒤가 줄인다.**
  > >
  > > | 모델 | 블록 최악 | 블록 뒤 연산만 | 배율 | 블록 뒤 구조 |
  > > |---|---|---|---|---|
  > > | Qwen2.5-VL-3B (baseline_v2) | **0.76** | 885.19 | 0.0009 | merger |
  > > | Qwen2-VL-2B-AWQ | 3.66 | 675.30 | 0.0054 | merger |
  > > | Qwen3-VL-32B-AWQ | 5.50 | 1,488.73 | 0.0037 | merger + deepstack |
  > > | Qwen3-VL-30B-A3B-AWQ | 34.69 | 4,110.05 | 0.0084 | merger + deepstack |
  > > | GLM-4.1V-9B | 36.72 | 4,295.34 | 0.0085 | RMSNorm → Conv2d → merger |
  > > | Kimi-VL-A3B (MoonViT) | 38.44 | 406.86 | **0.0945** | LayerNorm **만** |
  > >
  > > **판정은 여섯 다 불변이다.** 설계가 달라도 방향이 같은데, 비전 타워의
  > > 마지막 단계가 하는 일이 **패치 표현을 LLM 임베딩 크기로 내리는 것**이라
  > > 구조적으로 감쇠하기 때문이다. **Kimi-VL만 배율이 한 자릿수 크다(0.0945)**는
  > > 점이 이 설명을 뒷받침한다 — MoonViT의 블록 뒤는 `final_layernorm` 하나뿐이라
  > > **차원을 줄이지 않는다.** 나머지 다섯은 merger가 차원까지 낮춘다.
  > >
  > > **Gemma 4가 예외인 이유는 그 자리에 정규화가 아니라 상수 뺄셈이 들어갔기
  > > 때문**이고, 이는 설계 선택이지 계열의 성질이 아니다.
  > >
  > > 나머지 계열은 **블록 뒤에 잴 것이 없다**(소스 확인) — Pixtral은 곧바로
  > > `return out`, CLIP 계열(LLaVA·Phi-3.5)과 InternViT는 `post_layernorm`을
  > > **CLS 토큰에만** 걸어 `last_hidden_state`가 블록 출력 그대로다.
  > >
  > > **함정 둘을 지나왔다.** ① GLM은 `last_hidden_state`(downsample 직후)와
  > > `pooler_output`(**merger 통과**)을 둘 다 돌려주는데 **LLM이 먹는 쪽은
  > > pooler_output**이다 — 관례와 반대라 첫 텐서만 집으면 merger를 놓친다.
  > > ② Qwen3-VL은 `last_hidden_state`가 **블록 출력 그 자체**라, 반환 텐서
  > > 전부의 최대를 잡으면 배율이 **항상 1.0000**이 되어 아무것도 말하지 못한다.
  > > 그래서 후킹한 블록 출력과 **같은 저장소를 가리키는 텐서는 빼고** 다시 센다.
  >
  > **독립 제작사 셋이 35.91 · 38.26 · 40.33으로 좁게 모인다.** Mistral·Zhipu·
  > Moonshot이 서로 무관하게 만든 가변 시퀀스 인코더인데 값이 10% 안에 들어온다.
  > Qwen3-VL-30B-A3B(42.21)도 같은 구간이다.
  >
  > **그리고 Qwen2.5-VL은 이들보다 약 40배 낮다.** GLM-4.1V는 프로세서 출력 형상이
  > Qwen2-VL과 **완전히 동일**한데(320×180 → `(264,1176)`, 1280×720 → `(4784,1176)`)
  > 여유는 38.26 대 3.88로 열 배 차이다. **같은 설계, 다른 학습.**
  >
  > **결론 — 설계가 아니라 그 세대의 학습이다.**
  > > 단일 가변 시퀀스 인코더 여덟을 네 제작사에서 쟀다. **Qwen2.5-VL 세대만
  > > fp16 한계 근처(0.95~1.74배)이고, 나머지는 모두 3.88~42.21배다.** 같은 설계를
  > > 쓰는 독립 제작사 셋(Mistral·Zhipu·Moonshot)이 35.91~40.33배로 좁게 모이므로,
  > > **설계가 원인이라고 볼 수 없다.** 원인은 그 세대의 학습이 만든 활성 크기이며,
  > > 무엇이 그렇게 만들었는지는 **공개 정보로 알 수 없다**(원장 L14).
  >
  > **논문에 불리한 면.** *"Qwen2.5-VL 한 세대의 이상치"*라면 일반성이 좁다.
  > 그대로 적는다. 다만 **세 가지가 남는다.**
  > - Qwen2.5-VL은 조사 197개 중 **54개로 둘째로 큰 집단**이고 파생본 전부가 상속한다
  > - **예측할 수 없다**는 것이 오히려 확증됐다 — config·설계·가중치 비·계열 넷 다 실패
  > - 같은 제작사가 다음 세대에서 줄였다는 것은 **여유가 움직인다는 증거**(기여 ④)
  >
  > **버전 파편화가 양방향임도 드러났다.** Kimi-VL의 원격 코드는
  > `is_torch_fx_available`를 쓰는데 **그 심볼이 5.17에서 제거**돼 새 환경에서 깨진다.
  > 4.49에서는 된다. 즉 **어느 한 버전으로도 전부 열 수 없다** — 6장이 적은
  > "27개 중 4개만 쟀다"에 이유가 하나 더 붙고, **4.49를 유지한 결정도 뒷받침된다**
  > (올렸다면 Kimi-VL을 못 쟀다).
  >
  > 결과: `eval_results/vision_tower_direct_kimi.json`, `bridge_newhf.json`

  **가중치가 실제로 로드됐는지 별도로 검증했다**(`scripts/verify_family_weights.py`,
  결과 `eval_results/family_weight_verification.json`). 세 모델 모두 비전 인코더의
  norm 파라미터 24개 중 정확히 1.0인 것이 **0개**이고 "newly initialized" 경고도
  없다 — 랜덤 초기화가 아니다.

  > **이 검증이 왜 필요했나.** 체크포인트 키 규약이 이 `transformers` 버전과 다르면
  > `from_pretrained`가 **경고만 찍고 랜덤 초기화 모델을 돌려준다.** 로드는 "성공"하고
  > 비전 인코더도 찾아지며 활성값도 나온다 — 그 값이 난수일 뿐이다. 실제로
  > `AngelSlim/Qwen2.5-VL-3B-INT4-AWQ`에서 이 일이 일어났고, 검증을 넣지 않았다면
  > **난수를 논문에 실었을 것이다.**

  **측정하지 못한 것과 그 이유** — "그냥 빠진 것"과 구분해 적는다.

  | 사유 | 해당 | 내용 |
  |---|---|---|
  | config 구조 불일치 | Qwen2.5-VL AWQ 4종 | 저장본이 **새 `transformers`의 중첩 config**(`text_config`)로 쓰여 4.49의 평면 구조와 충돌 |
  | 가중치 키 규약 불일치 | 위와 동일 | 저장본은 `model.visual.*`, 4.49는 `visual.*` → 전 가중치 랜덤 초기화 |
  | 아키텍처 미인식 | Qwen3-VL 30B·32B | 4.49가 `qwen3_vl` / `qwen3_vl_moe`를 모름 |
  | 커스텀 코드 의존성 | InternVL3-8B, Phi-3.5-vision | `timm` 등 미설치, config 클래스 미인식 |
  | 디스크 | Qwen2-VL-72B | 4bit로도 40GB |
  | 원인 미상 | LLaVA-1.5-7B-AWQ | 프로세서 경로에서 `TypeError` |

  **`transformers`를 올리지 않기로 했다.** 15개 스크립트가 4.49 내부 API에 묶여 있고
  (`visual.blocks`, `rot_pos_emb`, `get_window_index`, `fullatt_block_indexes`,
  `spatial_merge_unit`, ONNX export의 원숭이 패치 9곳), **지금까지의 모든 측정이
  4.49에서 나왔다.** 업그레이드하면 한 논문에 두 버전의 수치가 섞인다. 얻는 것(모델
  6~7개 추가)이 잃는 것(전체 측정의 일관성)보다 작다. autoawq 0.2.9도 공식 폐기됐고
  마지막 검증 조합이 transformers 4.51.3이다.

  → **6장에 이렇게 쓴다.** "공개 양자화 VLM 27개 중 실제로 여유를 측정할 수 있었던 것은
  4개뿐이었다. 나머지는 config 구조·가중치 키 규약·아키텍처 인식이 서로 달라 단일
  `transformers` 버전에서 로드되지 않았다. **'재면 된다'는 처방조차 생태계의 버전
  파편화 때문에 실행이 쉽지 않으며, 이는 아무도 재지 않는 이유의 일부다.**"
  이 서술은 논지를 약화시키지 않고 **"왜 안 재는가"에 대한 답을 추가한다.**

  > **예상 질문과 답 — "노력하면 열 수 있는 것 아닌가" (2026-09-17 추가).**
  > 그렇다. **최신 `transformers`로 올리면 상당수가 열린다.** 안 연 것은 뚫을 수
  > 없어서가 아니라 **측정 일관성 때문**이다 — 스크립트 15개가 4.49 내부 API에
  > 묶여 있고(`visual.blocks`, `rot_pos_emb`, `get_window_index`, ONNX export의
  > 원숭이 패치 9곳) **지금까지의 모든 측정이 4.49에서 나왔다.** 올리면 한 논문에
  > 두 버전의 수치가 섞인다.
  >
  > **두 논거를 섞지 않는다.** "파편화가 재는 일을 어렵게 만든다"는 **27개 중 4개**
  > 라는 비율이 뒷받침하고, 이는 어느 단일 버전을 고르든 성립한다. 반면 "개별
  > 저장소 여섯을 뚫지 않았다"는 **우리의 선택**이고, 앞 논거로 정당화하지 않는다.
  > 원장 L6에 그렇게 적었다.
  >
  > **뚫었을 때 무엇이 달라지는가**도 밝혀 둔다 — 표본이 4개에서 10개 안팎으로
  > 늘어 **"양쪽 실례가 있다"는 서술이 더 단단해진다.** 파편화 논거는 비율로
  > 이미 서 있으므로 약해지지 않는다. 즉 **닫을 수 있는 한계이며, 자원이 확보되면
  > 닫는 쪽이 낫다**(원장 「보류」 항목).

- 실무 권고: 정밀도 비교 시 고정해야 할 항목 체크리스트

### 6장 저정밀 배포 검증 파이프라인과 실기기 확인

> **2026-09-15 위상 변경.** 원래 "Orin에서 붕괴가 재현되는가"만 확인하는 장이었으나,
> **논문이 방법 기여가 약하다는 지적**에 따라 이 장을 **검증된 배포 절차(시스템 기여)**로
> 격상한다. 만든 것이 셋이다 — (i) 고정 형상 ONNX export 경로, (ii) 정밀도 조건별 엔진
> 빌드·측정 절차, (iii) 해상도 상한 결정 방법. 여기에 8장의 `headroom_guard` 도구가
> 붙으면 "진단 → 교정 → 배포 검증"이 하나의 경로로 닫힌다.

**이 장이 기여로 성립하는 근거.** VLM 비전 인코더를 엣지 보드에 fp16으로 올리는 절차는
공개된 것이 없다. 실제로 막히는 지점이 넷이었고 전부 해결했다.

| 막힌 지점 | 해결 | 근거 |
|---|---|---|
| `grid_thw` 의존 동적 로직(회전 위치 임베딩·윈도우 인덱스·`cu_seqlens`)이 ONNX로 안 나감 | 배포 해상도 고정 전제로 **상수화 래퍼** | 원본 대비 오차 0.000e+00 |
| 어텐션 마스크 채움값 `finfo.min`이 fp16에서 −inf가 되어 무관한 NaN 생성 | 채움값을 **−1e4**로 (exp(−1e4)는 fp32에서도 0) | 상수화 대조 통과 |
| 트레이서가 큰 상수를 블록마다 복제해 파일이 3배로 부풂 | 저장 전 **`dedup_constants()`** — 8.32 GiB → 2.69 GiB | 코사인 0.999920 동일 |
| 외부 데이터(`.onnx.data`) 그래프를 TensorRT가 파싱 못 함 | `parse(bytes)`가 아니라 **`parse_from_file`** | 9,177 레이어 정상 파싱 |

**정밀도 조건을 나눠 재는 절차**도 이 장의 산출물이다. `--fp16`은 fp16을 *허용*하는
것이지 강제가 아니므로, 관행·엄격·fp32 셋을 나눠 재야 "런타임이 회피하는가"에 답할 수
있다(6.3에서 실제로 답했다).


**왜 새 장인가.** 5장까지의 붕괴 측정은 전부 A100·PyTorch에서 이루어졌다. 「실차 배포 시
위험」을 주장하려면 **실제 엣지 하드웨어의 fp16 산술**에서 같은 일이 일어나는지 보여야
한다. 부수적으로 이 장은 현재 논문의 빈 자리 하나를 메운다 — **경량화를 표방하면서
지연시간·메모리 수치가 하나도 없다.**

**4장의 검증 장이다.** 표현 범위 초과는 **배포 시점의 수치 문제**이므로 런타임에서
재현되는지가 곧 주장의 성립 여부다. 학습 산물이라면 어떤 런타임에서도 같게 나타나
이 장이 필요 없었을 것이다.

#### 6.1 대상과 준비 (완료)

> **용어.** 본문은 이미지를 처리하는 모듈을 일관되게 **비전 인코더**로 부른다
> (`model.visual`, depth 32 / hidden 1280). 문헌에서는 *vision tower*라고도 하며
> **같은 것**이다. 본 저장소의 코드 식별자와 파일명은 그쪽 관례를 따르므로
> (`load_tower()`, `tower_forward()`, `tower_*.onnx`) 코드와 본문의 이름이 다르다 —
> 의도한 것이고, 코드 주석은 식별자와 맞추어 둔다.
>
> 붕괴가 일어나는 **"마지막 비전 블록"**은 비전 인코더 전체가 아니라 그 안의
> 31번 블록(`blocks[31]`)이다.

비전 인코더만 ONNX로 내보낸다. 검증 대상이 비전 인코더 마지막 블록의 활성 크기이므로
LLM은 필요 없고, 630M 파라미터(fp16 1.17 GiB)라 8GB 보드에 올라간다.
`grid_thw`에 의존하는 동적 로직(회전 위치 임베딩·윈도우 인덱스·`cu_seqlens`·어텐션 마스크)을
배포 해상도 기준으로 상수화한 래퍼를 쓴다. `scripts/export_vision_tower_onnx.py`.

| 파일 | `max_pixels` | 패치 | 상수화 대조 | ONNX 코사인 |
|---|---|---|---|---|
| `tower_baseline_v2_native.onnx` | 1,440,000 | 7,296 | 0.000e+00 | 0.999920 |
| `tower_full_native.onnx` | 1,440,000 | 7,296 | 0.000e+00 | 0.999999 |
| `tower_baseline_v2.onnx` | 200,704 | 924 | 0.000e+00 | 0.999978 |
| `tower_full.onnx` | 200,704 | 924 | 0.000e+00 | 1.000000 |

검증을 두 단계로 나눈 이유를 본문에 적는다. **절대오차로 판정하면 안 된다** — 블록 32개를
지나며 누적되는 fp32 재정렬 오차만으로 최대 절대오차가 수 단위까지 벌어지고, 이는 **같은
PyTorch 코드를 CPU와 CUDA에서 돌려도 동일하게 나타난다**(실측 0.8394). 따라서 토큰별
코사인 유사도로 판정하고 CPU/CUDA 차이를 기준선으로 함께 보고한다.

#### 6.2 측정 프로토콜

A100과 **같은 250장·같은 해상도**를 넣어 출력의 NaN/Inf 발생 이미지 비율을 센다.
A100 기준값(원본 해상도): `student_baseline_v2` 94.8% / `student_full` 0.0%.

**정밀도 조건을 둘로 나눈다.** `trtexec --fp16`은 "fp16을 **허용**"이지 "강제"가 아니며,
TensorRT는 레이어별로 fp32를 남길 수 있다. 두 조건 모두 의미가 있다.

| 조건 | 명령 | 무엇을 답하는가 |
|---|---|---|
| **관행** | `--fp16` | 실제 배포 관행에서 무슨 일이 일어나는가 |
| **엄격** | `--fp16 --precisionConstraints=obey --noTF32` | 우리 주장의 수치적 조건이 성립하는가 |
| 대조군 | (기본 fp32) | 붕괴가 정밀도 때문임을 확인 |

레이어별 실제 정밀도는 `--verbose` 빌드 로그로 확인해 함께 싣는다.

#### 6.3 실측 — TensorRT는 이 오버플로를 자동 회피하지 않는다 (2026-09-15, A100)

**보드 없이 답할 수 있는 질문이었다.** "런타임이 알아서 막아주지 않나"는 TensorRT의
정밀도 배치 정책에 대한 질문이지 Orin에 대한 질문이 아니다. A100에 TensorRT 10.13을
별도 가상환경(`/venv/trt`)에 설치해 확인했다 — 기존 파이프라인(transformers 4.49)은
건드리지 않았다.

| 조건 | 붕괴율 (50장, 원본 해상도) | 가중치 메모리 | 지연 p50 |
|---|---|---|---|
| fp32 대조군 | **0.0%** | 2,760 MiB | 441.0 ms |
| **fp16 (관행, `--fp16` 상당)** | **94.0%** | **1,385 MiB** | 139.7 ms |
| **fp16 + 도구 교정** | **0.0%** | 1,385 MiB | 149.5 ms |
| **bf16** (무교정 그래프) | **0.0%** | 1,385 MiB | 197.4 ms |
| fp16 엄격 | 빌드 실패 (아래) | — | — |
| *참고: A100 PyTorch fp16* | *94.8%* | — | — |

**형식 비용은 같은 그래프·같은 세션에서만 비교한다 (2026-09-16 정정).** 위 표는
그래프가 섞여 있다 — bf16만 무교정 그래프다. 교정 그래프 하나에서 두 형식을 모두
재면 다음과 같고, **넷 다 붕괴 0%**라 차이가 형식뿐이다.

| TRT | 교정 fp16 | 교정 bf16 | **bf16/fp16** |
|---|---|---|---|
| 10.13 | 149.5 ms | 205.5 ms | **1.375배** |
| **10.16** | 138.7 ms | 186.5 ms | **1.344배** |

이전 판이 적던 **1.32배는 무교정 그래프의 bf16을 교정 그래프의 fp16으로 나눈 값**이라
쓰지 않는다. **1.34~1.38배**로 적는다.

> **이 지시를 아래 문단들이 따르지 않고 있었다 (2026-09-17 정리).** 「bf16 대조」
> 문단과 그 아래 두 곳, 그리고 §7.1 말미가 **1.41배**를 그대로 들고 있었다. 정정을
> 위에 붙이기만 하고 **아래 서술을 훑지 않은 것**이다(§3.5 규칙). 넷 다 고쳤다.
> 139.7 ms·197.4 ms 같은 **원측정값은 그대로 두고 비율만** 바로잡았다.

> **지연의 기준점을 교정 fp16으로 바꿨다 (2026-09-16).** 이전 판은 무교정 fp16
> (139.7 ms)을 1.00배로 놓고 bf16을 1.41배라 적었는데, 그 엔진은 **50장 중 47장이
> NaN이다.** 붕괴하는 엔진을 정상 동작의 기준으로 삼은 셈이다. 교정 fp16은 붕괴 0%에
> 149.5 ms이므로 이쪽이 옳은 기준이다. **다만 그 1.32배도 그래프가 섞인 값이었다**
> (bf16만 무교정 그래프). 같은 그래프·같은 세션 짝은 **1.375배**(10.13)이고
> 10.16에서는 **1.344배**다 — 위 표 참조.
>
> **7.0%의 차이(139.7 → 149.5)는 설명되지 않는다.** 교정은 가중치 스케일링뿐이라
> 그래프 구조·레이어 수(9,177)·가중치 메모리(1,385 MiB)가 모두 같다.
>
> **두 가설 중 하나는 기각됐다 (2026-09-16).** 처음에는 (i) 측정/빌드 간 변동,
> (ii) NaN 연산이 빠른 경로를 타서 무교정 쪽이 **붕괴 덕에 빨라진 것**을 들었다.
> **(ii)는 틀렸다** — bf16은 무교정(197.4 ms)·교정(205.5 ms) 둘 다 붕괴가 0%인데
> 교정 쪽이 4.2% 느리다. NaN이 전혀 없는 쌍에서 같은 방향의 차이가 나므로 NaN
> 설명은 성립하지 않는다. 남는 것은 **빌드/세션 간 변동**이며 원인은 규명하지
> 않았다. **규명할 필요도 없다** — 비율은 같은 세션 짝으로만 낸다.

**6.5 처방의 런타임 검증 — 완료 (2026-09-16).** `headroom_guard fix`가 저장한
타워(PyTorch에서 1.36% → 50.73%를 만든 **바로 그 파일**)를 `--vision_state`로
내보내 fp16 엔진을 빌드하고 같은 50장을 넣으면 **붕괴 0.0%**다. 손으로 굳힌
1/√3이 아니라 **도구 산출물 자체**가 PyTorch → ONNX → TensorRT를 통과한다.
이로써 이 장의 파이프라인이 *진단 → 교정 → 내보내기 → 엔진 → 배포 조건 검증*으로
닫힌다. 결과: `eval_results/trt_fixa_native.json`

**관행 설정에서 그대로 붕괴한다.** 가중치 메모리가 정확히 절반(2,760 → 1,385 MiB)이므로
네트워크가 실제로 fp16으로 변환됐고, 빌드 로그의 경계 텐서도 `Half[7296,1280,…]`로
배치돼 있다. **TensorRT의 레이어별 정밀도 선택은 이 오버플로를 막지 못한다.**

> **근거를 좁혀 적는다 (2026-09-15 자기정정).** 이전 판은 "Engine Layer Information에서
> 텐서 **대부분**이 Half로 배치됐음이 확인된다"고 썼는데 **로그가 그걸 말해 주지 않는다.**
> 최종 엔진은 레이어가 **6개**뿐이고 그중 하나(`Myelin ForeignNode`)가 트랜스포머
> 32블록 전체를 삼켰다 — 내부 텐서의 정밀도는 로그에 나오지 않는다. 확인되는 것은
> (i) 가중치 메모리가 정확히 절반이라는 것과 (ii) Myelin 노드의 입출력 경계 텐서가
> `Half`라는 것 둘뿐이며, 그 둘로 충분하다.

PyTorch 94.8%와 TensorRT 94.0%가 거의 일치한다. **"PyTorch 특유의 아티팩트"라는 반론도
함께 막힌다.**

**bf16 대조 (2026-09-15 추가) — 형식 선택 자체가 교환이다.** "그냥 bf16으로 빌드하면
되지 않나"는 정당한 반론이므로 같은 그래프·같은 50장으로 재봤다. bf16 엔진은 가중치가
fp16과 동일한 1,385 MiB이면서 **붕괴가 0.0%**로 사라진다 — 표현 범위 가설의 독립적
확인이다(활성 크기는 그대로인데 형식 상한만 3.4e38로 올렸더니 붕괴가 없어졌다).
그러나 지연이 늘어난다 — 당시 잰 값은 139.7 ms → 197.4 ms였고, **같은 그래프·같은
세션으로 다시 짝지으면 1.34~1.38배**다(위 표). A100 기준이므로 Orin에서의 비율은 다를
수 있으나, **"공짜 해법"이 아니라는 점은 남는다.**

> **1.41배의 원인은 형식이 아니다 — 마이크로벤치로 확정했다 (2026-09-15).**
> 처음에는 사전 등록한 두 해석 중 "bf16이 느리다"로 읽고 *"형식 선택 자체가 교환"*이라고
> 적었다. **철회한다.** A100(SM80)은 규격상 fp16·bf16 텐서코어 처리량이 같고(둘 다
> 312 TFLOPS dense), 타워의 실제 GEMM 형상으로 순수 행렬곱을 재 보니 **기하평균
> 1.001배**로 완전히 동등했다.
>
> | GEMM | 형상 | fp16 | bf16 | 비 |
> |---|---|---|---|---|
> | attn.qkv | 7296×1280×3840 | 0.295 ms | 0.295 ms | 1.000 |
> | mlp.gate_proj | 7296×1280×3420 | 0.508 ms | 0.508 ms | 0.999 |
> | mlp.down_proj | 7296×3420×1280 | 0.517 ms | 0.517 ms | 0.999 |
> | attn.qkᵀ | 16×7296×80×7296 | 1.577 ms | 1.578 ms | 1.001 |
> | attn.av | 16×7296×7296×80 | 1.232 ms | 1.228 ms | 0.997 |
>
> 빌드 로그도 같은 곳을 가리킨다 — fp16은 tactic/autotuning 줄이 **707개**인데 bf16은
> **265개**뿐이고(2.7배 적다), fp16만 `Half(…1:2…)`·`Half(…1:8…)` 같은 벡터화
> 레이아웃을 시도하며, 활성 메모리는 bf16이 9% 더 크다(320 대 293 MB).
> 즉 1.41배는 **TensorRT 10.13 Myelin의 bf16 커널 커버리지 비용**이다.
> 결과: `eval_results/gemm_fp16_bf16.json`, 스크립트: `scripts/bench_fp16_bf16_gemm.py`

**이 정정은 본 논문에 불리하다 — 그대로 적는다.** 이 격차가 형식의 비용이 아니라
런타임의 미성숙이라면, **커널이 성숙하면 그 비용은 사라진다.** 그러면 "bf16으로
빌드하라"가 여유를 재지 않고도 통하는 처방이 되고, 본 연구 처방의 실용적 가치가
그만큼 줄어든다. 9장 한계에 **"처방의 수명"** 항목으로 명시한다.

**그래도 남는 것 둘.** (i) 지금 배포하는 사람에게 1.34~1.38배는 실재하는 비용이고,
어느 쪽을 고를지는 **여유를 재야 알 수 있다.** (ii) 더 근본적으로, 공개 저장소
상당수가 **이미 fp16으로 가중치를 저장해 배포**하고 있어 bf16 전환이 항상 선택지인
것도 아니다. 다만 (i)은 런타임 버전에 종속된 논거이므로 **주논거로 쓰지 않는다.**

**네 조건을 나란히 놓으면 선택지가 보인다.** fp32는 안전하지만 2.95배 느리고 메모리가
2배다. fp16 무교정은 94% 붕괴한다. **fp16 + 도구 교정**은 붕괴 0%에 가장 빠르고
메모리도 절반이며, 정확도 비용은 측정 한계 이하다(§7.3③). bf16은 가중치를 건드리지
않고 안전하지만 **이 런타임에서는** 1.34~1.38배 느리다(형식의 비용이 아니라 커널 커버리지).
**정확도로는 bf16과 교정이 구별되지 않으므로**(원본 해상도 49.95% 대 50.73%, p=0.34),
선택 기준은 **지연 대 가중치 편집 위험**으로 좁혀진다. 가중치 교정(fixA/`headroom_guard`)은 fp16의 속도를 유지한
채 붕괴를 없애지만 완전 등가가 아니다(§8). **어느 쪽을 고르든 먼저 여유를 재야 선택지가
생긴다는 것이 이 표의 요지다.**

> **사전 등록한 예상이 빗나갔다.** 6.4 분기표에서 "엄격에서만 재현, 관행에서는 안전"이
> 가장 가능성 높다고 적었으나, 실제로는 **관행에서 재현**됐다. 등록된 대로 결과를
> 그대로 적는다 — 이 경우 해당 분기의 서술("주장을 축소하지 말고 조건을 특정한다")은
> 적용되지 않으며, 주장이 실제 배포 도구에서 성립한다.

**엄격 조건 빌드 실패는 결론에 영향이 없다.** 원인은 모델이 아니라 스크립트다 —
모든 레이어에 일괄로 fp16을 강제했는데 인덱스를 계산하는 레이어에는 적용할 수 없다
(`cannot use precision Half for layer that computes indices`). 애초에 엄격 조건은
**관행에서 안전했을 때만** 필요했으므로 없어도 결론이 선다.

**부수 관찰 — 런타임이 대신 막아줄 여지가 줄고 있다.** TensorRT 10.12부터
`layer.precision`이 deprecated(강타입으로 대체)이고, **11.x는 `FP16`·
`OBEY_PRECISION_CONSTRAINTS` 플래그를 아예 제거**해 정밀도가 ONNX 그래프의 dtype으로
결정된다. 즉 "fp16을 허용하고 빌더가 고르는" 방식에서 **"그래프가 말하는 대로 하는"**
방식으로 이행 중이며, 이는 여유를 사용자가 직접 확인해야 할 이유를 강화한다.

결과: `eval_results/trt_precision_probe.json`, `trt_precision_bf16.json`,
빌드 로그 `logs/trt_build_*.log`

#### 6.4 해석 사전 등록 (결과 보기 전 작성)

| 결과 | 해석 | 논문 반영 |
|---|---|---|
| **엄격·관행 모두 재현** (baseline 높은 붕괴, full 0%) | 주장이 실기기까지 확장된다 | 5장을 "A100 시뮬레이션"에서 **"실제 배포 하드웨어에서 확인"**으로 승격. 7장이 논문의 실용적 정점이 된다 |
| **엄격에서만 재현, 관행에서는 안전** | TensorRT가 해당 레이어를 fp32로 남긴 것 | **가장 가능성 높은 결과.** 주장을 축소하지 말고 **조건을 특정**한다: "순수 fp16 경로에서 발생하며, TensorRT 기본 정책은 이를 우연히 회피한다." 실용적 함의는 **런타임 의존성**으로 옮겨간다 — 같은 가중치가 런타임 설정에 따라 붕괴하거나 안 하므로 **여유 측정의 필요성은 오히려 커진다** |
| **둘 다 안전** | fp16 경로가 아예 안 만들어졌거나 전처리 불일치 | 빌드 로그로 원인 확인. **결과가 아니라 디버깅 대상**이다. 끝내 재현 못 하면 "PyTorch fp16에서 관찰, TensorRT에서는 미재현"으로 **그대로 보고**하고 5장의 주장 범위를 런타임 한정으로 좁힌다 |
| **둘 다 붕괴** (full도) | 엔진 빌드·전처리 오류 | 결과 아님. 코사인 대조부터 다시 |
| **OOM으로 원본 해상도 불가** | 8GB 공유 메모리 한계 | 802,816으로 내린다(A100 기준 25.6% 대 0.0%). 판정력은 줄지만 유효하며, **그 사실 자체가 7.5의 배포 파라미터 결과가 된다** |

**유리하게 재해석하지 않는다.** 특히 "관행에서 안전"이 나오면 그것을 숨기지 않는다 —
그 결과는 **본 논문의 처방을 약화시키지 않고 정밀화한다.**

#### 6.5 처방의 실기기 검증 (fixA)

5장에서 fixA(gate·up 가중치를 각각 1/√3, 곱이 만들어지기 전에 축소)가 A100에서
붕괴를 94.4% → 0.0%로 없앴다. **가중치 편집이므로 그대로 export할 수 있다.**
fixA를 적용한 타워를 추가로 내보내 같은 엔진 절차를 밟으면, 처방이
**"A100에서 되는 것"에서 "배포 하드웨어에서 검증된 것"으로** 올라간다.
추가 비용은 export 약 10분과 엔진 하나뿐이다.

#### 6.6 이 기기에서 안전한 최대 해상도 — 새 배포 파라미터

해상도를 올리면 여유가 줄고(5장) 동시에 메모리가 는다. 두 곡선을 겹치면
**"이 보드에서 이 체크포인트가 안전하게 처리 가능한 최대 해상도"**라는 배포 파라미터가
나온다. 해상도별로 다음을 잰다.

- fp16 붕괴율 (A100 기준값과 대조)
- 지연시간 (중앙값·p95)
- 피크 메모리
- 최종 블록 여유 = 65,504 / max\|activation\|

**메모리 타당성 사전 계산.** 내보낸 그래프는 HF 원본과 같은 eager attention이라
어텐션 행렬 `(head 16, S, S)`를 통째로 만든다. 원본 해상도(S=7,296)에서 fp16 QK^T가
1.59 GiB, softmax가 fp32로 승격되면 3.17 GiB가 더 붙고, 가중치 1.17 GiB와 마스크
0.2 GiB를 더하면 피크가 **6 GiB 안팎**이다. 8GB를 OS와 공유하는 Orin Nano에서는
아슬아슬하다. 안 되면 (i) 802,816으로 내리거나 (ii) 윈도우 어텐션을 창 단위로
배치화한다(어텐션 3.17 GiB → 30 MiB).

#### 6.7 하드웨어를 확보하지 못할 경우

**실기기 없이도 절반은 메울 수 있다.** A100에 TensorRT를 설치해 fp16 엔진을 빌드하면
6.3의 정밀도 조건 비교와 6.5의 fixA 검증을 그대로 수행할 수 있다. Orin은 아니지만
**"PyTorch 특유의 아티팩트"라는 반론을 제거**하고 내보낸 ONNX를 끝까지 검증한다.
이 경우 장 제목을 「런타임 검증」으로 바꾸고 **엣지 보드 미검증을 한계로 명시**한다.
보드를 확보하면 같은 절차를 그대로 옮기면 된다.

---

### 7장 도구 — `headroom_guard`: 배포 전 진단·교정 패스

> **2026-09-15 신설.** 논문의 주장은 "부족한 것은 수단이 아니라 측정"인데, 그렇게
> 말하면서 **재는 도구를 주지 않으면 주장이 불완전하다.** 이 장은 앞 장들의 진단을
> 실행 가능한 하나의 패스로 묶는다. 공학 석사 논문에 기대되는 "만든 것"이 여기에 해당한다.

**입력** 모델(체크포인트 또는 저장소), 목표 dtype, 목표 해상도, 대표 이미지 N장
**출력** 층별 여유 표 · 위험 층 목록 · (선택) 교정된 가중치 · 교정 전후 붕괴율

| 단계 | 하는 일 |
|---|---|
| 1. 진단 | 목표 dtype·해상도에서 블록별 `max\|activation\|`을 재고 `format_max ÷ max`로 여유를 계산, **임계 미만 블록을 자동 검출** |
| 2. 계산 | 각 위험 블록에 필요한 축소 배수를 **목표 여유로부터 자동 산출** |
| 3. 교정 | 오버플로 지점 **이전에** 적용 — 게이트형 MLP면 곱 이전 선형층들을 `(1/scale)^(1/n)`로. **bias가 있으면 함께** 나눈다 |
| 4. 검증 | 재측정해 붕괴율·여유 변화를 보고 |

> **하위 절을 번호순으로 재배열했다 (2026-09-17).** 새로 쓴 절을 위에 붙이다 보니
> 7.4 → 7.3 → 7.2 역순으로 놓여 있었다. 작업 중에는 문제가 없었으나 본문 집필
> 순서와 어긋나므로 7.1 → 7.4로 고쳤고, 번호가 없던 첫머리를 **7.1**로 명명했다.

#### 7.1 구성·설계 판단과 계열 검증

**기본값이 중요하다.** `--max_pixels`를 지정하지 않으면 **모델 자신의 기본 설정**을
쓴다. Qwen2.5-VL-3B의 기본 `max_pixels`는 12,845,056으로 nuScenes 원본(1,440,000)보다
커서 축소가 일어나지 않는다. 즉 **"모델을 받아 그대로 쓰는 경로"에서의 위험을 기본으로
보여준다** — 인위적 고해상도 조건이 아니다.

**계열 일반화.** MLP 구조를 이름 패턴으로 찾는다 — `(gate_proj, up_proj) → down_proj`
(Qwen2.5-VL·LLaMA 계열)와 `(fc1) → fc2`(Qwen2-VL·CLIP 계열). 인식하지 못하면 해당 블록을
건너뛰고 그 사실을 기록한다.

**정직하게 적을 한계 — 교정은 완전 등가가 아니다.** 게이트형 MLP에서 곱이 만들어지기
전에 줄이면 오버플로는 사라지지만 **잔차 분기와 MLP 분기의 비율이 바뀐다.** 뒤따르는
정규화가 대부분 흡수하나 전부는 아니다. 배포 조건에서 짝지어 잰 비용은
**+0.78%p(McNemar p=0.34)로 측정 한계 이하**이며, **불일치 54쌍**은 교정이 답을 실제로
바꾼다는 뜻이다(§7.3③). 따라서 도구는 **교정을 권하지 않고 비용과 함께 보고**하며,
채택은 사용자가 정한다. 이 설계 선택 자체를 논문에 적는다 — "자동으로 고쳐 준다"고
주장하지 않는다.

> **낡은 값을 고쳤다 (2026-09-17).** 이 문단은 *"실측 비용은 11,309문항에서
> **−0.59%p**였다"*로 적혀 있었다. 그 값은 **평가 해상도·bf16**에서 잰 조건 혼입
> 값이고, §7.3이 배포 조건 짝으로 **+0.78%p(p=0.34)**를 내며 대체했는데 **같은 장
> 첫머리가 옛 값을 그대로 들고 있었다.** §3.5 규칙에 정확히 걸리는 사례다.

**설계 판단 — 측정 dtype과 배포 dtype을 분리한다.** 활성 **크기**는 넘치지 않는
dtype(bf16)에서 재고 **붕괴**는 목표 dtype(fp16)에서 잰다. 여유 정의
`format_max ÷ max|activation|`의 분모는 **참값**이어야 하는데, 목표 dtype에서 재면
이미 발산한 표본의 값을 알 수 없어 필요한 교정량을 **과소평가**한다.

> **이 원칙을 빠뜨렸다가 실측으로 잡았다 (2026-09-15).** 처음에는 두 측정을 모두
> fp16에서 했고, 그 결과 10장 중 8장이 발산한 상태로 풀려 **목표 2.0배에 1.74배밖에
> 도달하지 못했다.** bf16으로 크기를 재자 진단값 자체가 1.03배가 아니라 **0.90배**
> (이미 한계 초과)였고, 필요한 축소가 ×0.50에서 **×0.39**로 바뀌어 목표를 달성했다.
> 논문 본체는 처음부터 bf16/fp16 두 경로로 재고 있었는데 도구를 만들며 그 원칙을
> 놓친 것이다. **설계 판단으로 본문에 적는다.**

**잔차를 분리해 축소량을 푼다.** 블록 출력은 `잔차 + MLP(...)`이므로 MLP 분기만
f배로 줄여도 잔차는 남는다. 따라서 블록 출력의 최대 지점에서 두 분기를 분리해 재고
`|r + f·m| ≤ format_max / target`을 f에 대해 푼다. 모든 이미지에 대한 f 중 최솟값을
취해 보수적으로 잡으므로 **목표를 약간 넘겨 달성**한다(실측: 목표 2.0 → 2.28배).
잔차만으로 이미 목표를 넘는 표본이 있으면 **"MLP 축소만으로는 도달 불가"를 경고**한다.

**실측 (2026-09-15, `student_baseline_v2`, 기본 설정, 10장)**

| | 교정 전 | 교정 후 |
|---|---|---|
| blk31 여유 (bf16 참값 기준) | **0.90배** | **2.28배** |
| fp16 붕괴율 | **80.0%** | **0.0%** |
| MLP 분기 축소 | — | ×0.3901 (선형층 2개 각 ×0.6245, bias 포함) |

**검증 결과 (2026-09-15) — 3개 계열 4개 모델.** 도구를 우리 체크포인트와 외부 공개
모델에 그대로 적용했다. 각 모델의 **자기 기본 설정**(`--max_pixels` 미지정 또는 해당
저장소 설정)에서 잰 값이다.

| 모델 | 계열 | 블록 구조 | 마지막 블록 여유(fp16) | 판정 |
|---|---|---|---|---|
| `student_baseline_v2` | Qwen2.5-VL | `blocks`, gate·up→down | **0.90배** | **위험 → 교정** |
| Qwen2-VL-2B-Instruct-AWQ | Qwen2-VL | `blocks`, fc1→fc2 | 3.83배 | 교정 불필요 |
| Qwen2-VL-7B-Instruct-AWQ | Qwen2-VL | `blocks`, fc1→fc2 | 4.52배 | 교정 불필요 |
| SmolVLM-Instruct | Idefics3 | `encoder.layers`, fc1→fc2 | 51.34배 | 교정 불필요 |

세 안전 모델은 모두 "위험 블록 0개"로 판정되어 가중치를 건드리지 않았고(붕괴율 0.0%),
위험 모델 하나에서만 교정이 발동해 붕괴가 제거됐다. **오탐도 미탐도 없었다.**

> **6장 표와 소수점이 다른 이유를 적어 둔다.** 6장은 같은 세 모델을 3.88 / 4.49 / 50.86배로
> 적었는데 여기서는 3.83 / 4.52 / 51.34배다. **dtype 차이가 아니다** — 6장도 이미
> bf16으로 크기를 재고 fp16 상한으로 여유를 계산한다(`profile_other_vlm_families.py`
> L191·L246). 차이는 **표본**에서 온다: 6장은 100장, 도구 검증은 10~30장이고
> SmolVLM은 해상도 조건도 다르다(6장 `max_pixels=1,440,000`, 도구 모델 기본값).
> 1% 남짓의 차이이므로 결론에 영향이 없으나, **표를 나란히 둘 때 조건을 함께 적는다.**
>
> *(2026-09-15 자기정정: 이 주석의 이전 판은 "6장은 fp16에서 잰다"고 적었다. 틀렸다.
> 스크립트를 확인하지 않고 쓴 것이다.)*

> **교정 경로는 게이트형 MLP에서만 실행됐다 (2026-09-16 추가).** 검출은 3개 계열에서
> 확인됐지만, **교정이 실제로 발동한 것은 `gate·up→down`(Qwen2.5-VL) 뿐**이다.
> `fc1→fc2`(Qwen2-VL·Idefics3) 경로는 구현돼 있으나 **한 번도 실행된 적이 없다** —
> 그 계열 모델들이 전부 안전해 교정이 필요 없었기 때문이다. 따라서
> **"교정이 3계열에서 동작한다"고 쓰지 않는다.** 검출은 3계열, 교정은 1계열이다.
>
> **일반화의 범위를 정확히 적는다.** 3개 계열에서 돌아간 것은 (i) 블록 리스트 탐색과
> (ii) MLP 패턴 매칭이 계열 차이를 흡수한다는 뜻이지, **모든 VLM에서 동작한다는 뜻이
> 아니다.** 실제로 두 군데를 실측으로 고쳤다 — `tower.blocks` 하드코딩이 Idefics3에서
> 깨졌고(`encoder.layers`), SmolVLM 프로세서가 내놓는 5-D `pixel_values`
> (B, 타일, C, H, W)를 타워가 4-D로 기대해 깨졌다. 인식하지 못하는 구조는 건너뛰고
> 그 사실을 기록하는 것이 현재 설계이며, **"미인식=안전"이 아님을 출력에 명시**한다.

**형식 우선 권고가 실제로 값을 갖는다.** 도구는 목표 dtype만이 아니라 모든 형식의 여유를
함께 보고한다. 위 네 모델 모두 bf16 여유는 10³⁴배 이상으로, 표현 범위만 보면 bf16이
문제를 즉시 없앤다. 다만 그 선택에는 비용이 있다 — §6.3의 TensorRT 실측에서 bf16 엔진은
붕괴를 0.0%로 없애지만 지연이 fp16 대비 **1.34~1.38배**였다. 따라서 도구는 bf16을 **권고**하되
자동 선택하지 않고, 지연 비용은 사용자가 재도록 남긴다.

**대안이 쓸 수 있을 때만 대안이다 — `--available_formats` (2026-09-17 추가).**
위 권고에는 구멍이 있었다. **배포 대상이 그 형식을 지원하는지 묻지 않았다.**
Jetson Orin의 DLA는 **FP16과 INT8만 지원하고 BF16을 지원하지 않는다**(§8 한계 ③).
따라서 DLA 오프로드 배포에서 *"bf16으로 빌드하라"*는 **실행 불가능한 조언**이고,
도구가 그것을 1순위로 내놓으면 사용자를 막다른 길로 보낸다.

그런데 **바로 그런 대상에서 이 도구의 가치가 가장 크다.** 형식 전환이라는 탈출구가
없으므로 남는 수단이 (i) 가중치 교정, (ii) 해상도 하향, (iii) 대상 변경뿐이고,
셋 중 무엇을 고를지는 **여유를 재야 알 수 있기 때문**이다.

그래서 대상의 지원 형식을 선언받는다.

```
python scripts/headroom_guard.py diagnose --dtype float16 \
    --available_formats float16,int8        # Jetson DLA
```

출력이 이렇게 갈린다.

| 조건 | 도구의 1순위 권고 |
|---|---|
| 전부 지원(GPU 배포) | *"가중치를 고치기 전에 bfloat16 빌드를 먼저 검토할 것"* |
| **bf16 미지원(DLA)** | *"**형식 전환은 선택지가 아니다.** … 남는 수단은 교정·해상도 하향·대상 변경뿐"* |

미지원 형식은 표에 `(대상 미지원)`으로 표시하고, 결과 JSON에 `available_formats`와
`blocked_formats`(표현 범위로는 안전하나 대상이 못 쓰는 형식)를 남긴다.

**스케일 형식은 받아 두되 여유 비교에서 뺀다.** DLA를 적는 자연스러운 표기가
`float16,int8`인데 `int8`은 `FORMAT_MAX`에 없어 처음에는 *"모르는 형식"*으로 버려졌다.
`FORMAT_MAX`에 넣는 것은 **틀린 해법**이다 — INT8·INT4·fp8 추론은 거의 항상 스케일
인자와 함께 쓰므로 *"절대 크기가 상한을 넘느냐"*는 질문 자체가 성립하지 않는다.
§8이 fp8에 대해 그은 경계와 같은 논리다. 그래서 `SCALED_FORMATS`로 따로 받고
**"모른다"가 아니라 "알지만 내 소관이 아니다"**로 답한다.

```
· 스케일 형식 ['int8']은 여유 비교에서 뺀다 — 스케일 인자와 함께 쓰므로
  절대 크기 문제가 성립하지 않는다.
· int8도 대상이 지원하나 **이 도구가 답할 수 있는 질문이 아니다** — 스케일
  형식은 절대 크기가 아니라 동적 범위가 문제이므로 별도 평가가 필요하다.
```

**도구가 자기 적용 범위를 스스로 밝히는 셈**이고, 이는 §8의 경계 서술과 일관된다.

**실행 확인 (2026-09-17, `baseline_v2` @원본 해상도 30장, `--available_formats float16,int8`)**

```
[형식별 여유] 마지막 블록 max|act| = 86,016 (최악)
  float16            0.76배  **위험**  ← 목표 dtype
  bfloat16       3.94e+33배  안전  (대상 미지원)
  float32        3.96e+33배  안전  (대상 미지원)

  **형식 전환은 선택지가 아니다.** … 남는 수단은 (i) 가중치 교정(fix),
  (ii) 배포 해상도 하향, (iii) 대상 변경뿐이다.
```

JSON에 `available_formats: [float16]`, `blocked_formats: [bfloat16, float32]`,
`scaled_formats_declared: [int8]`이 남는다. 100장 실행(최악 0.73배·초과 95.0%)과
정합하며, 30장이라 도구가 **표본 부족을 스스로 경고**한다(참 초과율 95% 상한 9.5%).
결과: `eval_results/guard_dla_demo.json`

> **이 기능이 한계 ③의 서술을 바꾼다.** 한계 ③은 *"bf16 커널이 성숙하면 처방의 가치가
> 준다"*인데, **DLA처럼 bf16이 아예 없는 대상에서는 성숙과 무관하게 가치가 유지된다.**
> 이전에는 이 논거가 문서에만 있고 **도구는 그 구분을 하지 못했다.** 이제 도구가
> 대상별로 다른 답을 낸다 — 논문의 주장과 산출물이 일치한다.

#### 7.2 게이트 보정 — 임계값·통계량·표본 수를 데이터로 정한다 (2026-09-15)

> **왜 이 절이 필요했나.** 초판 도구는 "여유 p50 < 2.0이면 위험"으로 판정했다.
> **2.0의 출처가 없었다.** 관측된 (여유, 붕괴) 쌍은 1.03→94.8%부터 2.85→0%까지였고
> **1.15와 2.85 사이가 통째로 비어 있어**, 임계값을 놓은 바로 그 구간에 데이터가
> 하나도 없었다. 도구의 유일한 판단 기준이 매직 넘버였다.

**방법.** `baseline_v2`의 마지막 블록 MLP를 여러 배수로 줄여 **여유 연속체**를 만들고
(합성 20점, 100장), 각 지점에서 *도구가 보는 값*과 *실제 fp16 붕괴율*을 함께 쟀다.
공개 모델 5개(자연 점)를 같은 조건에서 재어 겹쳤다.
스크립트: `scripts/detection_curve.py`, `scripts/probe_natural_collapse.py`,
`scripts/analyze_gate_calibration.py`

**① 전이는 물리적 한계에서 일어난다.**

| 최악 여유 | 1.00 미만 구간 | | | | **1.00** | **1.05** | 1.09 이상 |
|---|---|---|---|---|---|---|---|
| (배수 f) | 0.92 | 0.88 | 0.85 | 0.82 | 0.78·0.75 | 0.72 | 0.69·0.66 |
| 붕괴율 | 71% | 57% | 42% | 23% | 6%·**2%** | **0%** | 0% |

최악 여유가 1.00일 때 붕괴 2%, 1.05일 때 0%다. 경계는 **형식 상한 그 자체**이며
따로 보정할 상수가 없다.

**② 현행 임계값 2.0은 1.91배 보수적이다.** 최초 무붕괴 지점의 최악 여유가
**1.049**이므로 `2.00 / 1.049 = 1.91`이다. 합성 20점에서 **오탐 8건, 미탐 0건**.
미탐이 없으니 위험하지는 않으나, 오탐은 불필요한 가중치 편집(§8 서두 — 완전 등가가
아니다)을 유발하므로 실제 비용이다. 자연 발생 모델에서도 확인됐다 —
`base_7b`는 여유 1.73배로 "위험" 판정을 받지만 100장 전부 멀쩡하다.

> **문서와 코드가 어긋나 있었다 — 코드를 문서에 맞췄다 (2026-09-17).**
> ③이 **최악 통계량을 써야 한다**고 결론냈는데 `plan_fix()`는 **p50 여유**로
> 게이트를 걸고 있었다. `detection_curve.py`의 `verdict_at_2.0`도 p50이었고,
> 그래서 이 항목의 「1.91배」(최악 기준 2.00/1.049)와 「오탐 7건」(p50 기준)이
> **서로 다른 통계량에서 나온 값**이었다. 값이 틀린 게 아니라 **섞여 있었다.**
>
> 게이트를 최악으로 통일했다(`headroom_guard.py` `plan_fix`·블록 표·형식별 여유,
> `detection_curve.py` 판정). 저장된 20점의 판정·혼동행렬은 **기존 행의
> `headroom_worst`로 재계산**했다 — GPU 재실행은 필요 없었다.
>
> | 기준 | TP | **FP** | TN | FN |
> |---|---|---|---|---|
> | p50 (이전 구현) | 8 | **7** | 5 | 0 |
> | **최악 (현행)** | 8 | **8** | 4 | 0 |
>
> 바뀐 것은 `f=0.42` 한 점뿐이다(최악 1.79 / p50 2.08). **미탐은 양쪽 모두 0**이라
> ①·③·④의 결론과 §7.3의 종단 검증 수치는 영향받지 않는다.
>
> **`baseline_v2`의 교정 배수도 바뀌지 않는다.** 두 기준 모두 blk31 하나만
> 걸리고(p50 0.86 / 최악 0.73, 나머지 최소가 5.30), 축소 배수는 `solve_factor()`가
> 이미 100장 전체에 대해 부등식을 풀어 정하므로 트리거 통계량과 무관하다
> (`factor_mlp` 0.3655 유지).

**③ 통계량은 이 데이터로 고를 수 없다 — 그리고 처음 결론은 틀렸다.**

| 통계량 | 위험 최대 | 안전 최소 | 절대 여백 | **여백 비율** |
|---|---|---|---|---|
| p50 | 1.17 | 1.22 | +0.05 | 1.043 |
| p95 | 1.04 | 1.09 | +0.04 | 1.043 |
| 최악 | 1.00 | 1.05 | +0.05 | **1.045** |

> **자기정정.** 처음에는 여백을 **절대 차이**로 재서 "p50이 가장 잘 가른다"는 결론을
> 냈다. 틀렸다 — 절대 차이는 **값의 크기가 큰 통계량을 자동으로 유리하게** 만든다.
> 무차원 비율로 고치자 셋이 1.043~1.045로 **사실상 동등**해졌다. 한 모델 안에서
> MLP를 줄이면 세 통계량이 비례해 움직이므로 당연한 결과다.
> **"p50은 틀렸고 최악이 맞다"는 본 연구의 초기 주장은 이 데이터가 지지하지 않는다.**
> 최악을 쓰는 이유는 여백이 커서가 아니라 **임계값이 물리적 의미(형식 상한 1.0)를
> 가져 모델 간에 옮겨 가기 때문**이다.
>
> **자연 발생 사례가 이를 뒷받침한다 (2026-09-16, §7.4).** λ=0.0 체크포인트는
> 여유 p50이 **1.14로 한계 위처럼 보이는데 최악이 0.87이라 11%가 붕괴**한다.
> p50만 보면 놓치는 사례가 합성 교란이 아니라 **학습된 모델에서** 나왔다. p50 임계값 1.22는 모델 내부 편차에 의존하는
> 경험 상수다. 이것은 측정 결과가 아니라 **설계 판단**이므로 그렇게 적는다.

**④ 임계값을 없애는 편이 낫다 — 초과 비율이 붕괴율을 직접 추정한다.**

bf16에서 이미지별 `max|activation|`이 형식 상한을 넘는 **비율**은 그 자체로 붕괴율의
추정치다. 250장·6개 모델에서 대조했다.

| 모델 | 초과 비율 | 실제 붕괴율 | 오차 |
|---|---|---|---|
| `base_3b` | 72.0% | 69.6% | +2.4%p |
| `baseline_v2` | 95.6% | 94.8% | +0.8%p |
| `kd_only_v4` | 23.2% | 20.4% | +2.8%p |

##### 두 값의 역할 분담 — 여유는 설명하고 초과 비율은 예측한다 (O3, 2026-09-17)

도구가 **여유(스칼라)**와 **초과 비율** 둘을 내놓으니 "어느 쪽을 봐야 하는가"가 남는다.
서술 규칙을 여기서 고정한다.

| | 여유 = `format_max ÷ max|activation|` | 초과 비율 = 상한을 넘는 이미지 비율 |
|---|---|---|
| 답하는 질문 | **왜** 무너지는가 | **얼마나** 무너지는가 |
| 성질 | 이미지 하나의 스칼라 통계량 | 표본 전체의 분포 요약 |
| 임계값 | 필요하다(1.0은 물리적, 2.0은 설계 판단) | **필요 없다** — 값 자체가 추정치다 |
| 정확도 | 통계량·표본 선택에 좌우된다 | 6모델 평균 오차 **2.1%p** |
| 쓰는 곳 | 기전 설명·교정량 산출·형식 비교 | **배포 가부 판정** |

**왜 여유를 버리지 않는가.** 초과 비율은 "지금 이 표본에서 몇 %가 터지는가"만 말하고
**왜 터지는지, 얼마나 줄여야 하는지는 말하지 못한다.** 교정 배수를 풀려면
`|r + f·m| ≤ format_max / target`이 필요하고 여기서 `target`은 여유다. 형식을 바꿨을 때
어떻게 되는지도 여유로만 답한다(bf16이면 10³⁴배). **기전과 처방은 여유 쪽 언어다.**

**왜 초과 비율을 1차 판정에 쓰는가.** 여유를 판정에 쓰려면 임계값과 통계량을 골라야
하는데, 게이트 보정이 보인 대로 **2.0은 1.91배 과보수**이고 p50/p95/최악의 여백 비율은
1.043~1.045로 **사실상 동등**하다. 즉 여유 기반 판정은 **고를 근거가 약한 선택을 둘**
요구한다. 초과 비율은 그 둘을 다 없애고 붕괴율을 직접 준다.

> **한 문장 규칙.** *"붕괴할 것인가"는 초과 비율로 답하고, "왜·얼마나 고쳐야 하는가"는
> 여유로 답한다.* 도구 출력도 이 순서다 — 초과 비율을 먼저 찍고 여유를 뒤에 둔다.

**둘은 불변식으로 묶여 있다.** `여유 < 1 ⟺ 초과율 > 0`이 성립해야 한다(정의상 그렇다).
어긋나면 **둘을 다른 통계량·dtype·표본에서 잰 것**이므로 값 하나가 아니라 측정을
의심해야 한다. `verify_claim_provenance.py`가 이 불변식을 자동 검사한다(O5).
| `temporal` | 35.2% | 28.8% | +6.4%p |
| `spatial` | 0.0% | 0.0% | 0.0%p |
| `full` | 0.0% | 0.0% | 0.0%p |

**평균 절대 오차 2.1%p, 최대 6.4%p.** 안전한 두 모델은 정확히 0.0% 대 0.0%다.
따라서 도구의 1차 출력을 스칼라 여유+임계값에서 **초과 비율**로 바꾼다 —
임계값도 통계량 선택도 필요 없고, 사용자가 받는 값이 곧 "몇 %의 이미지가 무너지는가"다.
여유는 보조 지표로 남긴다(초과가 0일 때 얼마나 얇은지 보여 준다).

**⑤ 표본 수는 이항 상한으로 답한다.** 게이트를 이미지 단위 사건으로 바꾸면
"몇 장을 봐야 하는가"가 곧바로 따라온다. N장에서 초과가 **0건**일 때 참 초과율의
95% 상한은 `1 − 0.05^(1/N) ≈ 3/N`이다.

| N | 10 | 20 | 30 | 50 | **100** | 250 |
|---|---|---|---|---|---|---|
| 95% 상한 | 25.9% | 13.9% | 9.5% | 5.8% | **3.0%** | 1.2% |

**10장으로 "안전"이라 말하면 참 초과율 26%까지 허용한다.** 도구 기본값을
50장에서 **100장**으로 올렸다. 부트스트랩도 함께 돌렸으나(worst-of-N 낙관 배율
N=5에서 1.06배, N=30에서 1.03배) 표본 **안**의 편향만 말해 주므로, 표본 **밖**을
다루는 이항 상한이 옳은 도구다.

**⑥ 측정은 결정론적이다.** 정밀 스윕에 배수 두 개(0.85, 0.72)를 본 스윕과 겹치게
넣었더니 여유·붕괴율이 **소수점까지 동일**했다. 같은 조건 재실행이 0.26%p 달랐던
정확도 측정과 대비된다 — 오버플로는 같은 가중치·같은 입력에 대해 같은 사건이고,
생성 정확도는 더 긴 파이프라인의 커널 비결정성을 탄다. **두 측정의 성격이 다르다.**

**⑦ 교정도 개발셋 밖에서 검증했다.**

| | `baseline_v2` (개발 대상) | **`base_3b` (독립)** |
|---|---|---|
| 교정 전 여유 | 0.90배 | **0.96배** |
| 교정 후 | 2.28배 | **2.44배** |
| 붕괴율 | 80% → **0%** | 69% → **0%** |
| MLP 축소 | ×0.3901 | ×0.3901 |

초판 검증은 양성이 개발 대상 체크포인트 하나뿐이었다(안전 3 + 위험 1이면 무조건
"안전"만 출력하는 도구도 3/4를 맞는다). 사전학습 `base_3b`는 우리가 학습하지 않은
모델이므로 **개발셋 밖 양성**이다. 도구는 여기서도 먼저 bf16/fp32를 권고한 뒤
교정을 적용했고 붕괴를 제거했다.

**도구에 반영한 변경 (`scripts/headroom_guard.py`)**
- `profile()`이 이미지별 최악(`max_worst`)과 **초과 비율**(`exceed_rate`)을 함께 낸다
- 1차 판정을 초과 비율로 바꾸고, 초과 0건이면 **표본 수에 따른 95% 상한**을 함께 찍는다
- 초과는 없으나 최악 여유가 `MARGINAL_HEADROOM`(1.25) 미만이면 **경계**로 보고한다
  — 실측 전이 1.00~1.05에 모델 내 이미지별 편차 15~20%를 더한 값이다

  > **1.25는 측정값이 아니다 — 숫자를 쓰는 자리마다 함께 적는다 (O4, 2026-09-17).**
  > 이 값은 위 두 실측에서 **유도한 설계 판단**이지 관측된 경계가 아니다. 관측된
  > 경계는 **1.00~1.05**(형식 상한 그 자체)뿐이고, 1.25는 *"표본 밖 이미지가 넘을 수
  > 있다"*는 여유분을 얹은 것이다. 초판의 2.0보다 논거가 낫지만 **종류는 같다 —
  > 매직 넘버를 더 작은 매직 넘버로 바꾼 것**이다.
  >
  > **§8 한계 6번에만 적어 두면 부족하다.** 독자는 이 숫자를 §7과 도구 출력에서
  > 먼저 만난다. 그래서 (i) 여기, (ii) 도구의 `--help`, (iii) 경계 판정 출력
  > 세 곳에 **"측정이 아니라 설계 판단"**을 명시한다. 1차 판정인 초과 비율에는
  > 임계값이 없으므로, 이 공시는 **보조 경고 한 줄에만** 걸린다.
- 기본 `--limit`을 50 → **100**으로 올렸다
- **판정부를 `print_verdict()`로 분리하고 `selftest` 명령을 넣었다 (2026-09-17).**
  세 분기 중 **「경계」는 여유가 1.00~1.25인 모델이 있어야 도는데 손에 있는 모델이
  전부 그 밖이라 한 번도 실행된 적이 없었다.** O4 공시를 그 분기에 넣고 나서야
  발견했다 — **문서에 "이렇게 출력된다"고 적은 코드가 미실행 상태**였던 것이다.
  `python scripts/headroom_guard.py selftest`가 모델 없이 세 분기와
  `solve_factor`(여유 충분 → f=1.0 / 잔차 초과 → infeasible)·
  `zero_obs_upper_bound`(100장 2.95% / 10장 25.89%)를 확인한다.

산출물: `scripts/headroom_guard.py`
결과: `eval_results/headroom_guard_baseline_v2.json`, `guard_qwen2vl_2b_awq.json`,
`guard_qwen2vl_7b_awq.json`, `headroom_guard_smolvlm.json`

---

#### 7.3 배포 조건 정확도 — 도구의 종단 검증 (2026-09-15)

> **왜 이 절이 필요했나.** 초판은 *"교정의 정확도 비용이 −0.59%p, McNemar p=0.25로
> 측정 한계 이하"*라고 적었다. 그 수치는 **평가 해상도(200,704)·bf16**에서 잰 것이다.
> 그 조건에서는 (i) 오버플로가 애초에 없고 (ii) 배포 dtype인 fp16을 쓰지도 않으며
> (iii) 축소 배수만 원본 해상도 통계로 계산해 놓고 적용·평가는 다른 해상도에서 했다.
> **교정이 필요 없는 조건에서 교정의 비용을 재고, 교정이 필요한 조건에서는 재지
> 않았다.** Table 1의 조건 혼입과 같은 계열의 오류다.

**측정.** NuScenes-QA val에서 `--stride 11`로 뽑은 **1,029문항·708 키프레임**.
앞쪽 N개를 자르면 799 키프레임 중 79장면만 덮어 독립 표본이 79개뿐이므로,
K개마다 하나씩 뽑아 전체에 고르게 퍼뜨렸다. 전체 11,309문항 대비 대표성은
`baseline_v2` bf16 기준 50.63% 대 50.30%로 **0.33%p** 차이다.

| 모델 | 해상도 | dtype | 교정 | 정확도 | 붕괴형 출력 |
|---|---|---|---|---|---|
| `baseline_v2` | 원본 | fp16 | — | **1.36%** | 1,001 / 1,029 |
| `baseline_v2` | 원본 | fp16 | **도구** | **50.73%** | **0** |
| `baseline_v2` | 원본 | bf16 | — | 49.95% | 0 |
| `baseline_v2` | 평가 | fp16 | — | 48.49% | 45 |
| `baseline_v2` | 평가 | bf16 | — | 50.63% | 0 |
| `full` | 원본 | fp16 | — | **52.38%** | **0** |
| `full` | 원본 | bf16 | — | 52.87% | 0 |
| `full` | 평가 | fp16 | — | 51.31% | 0 |
| `full` | 평가 | bf16 | — | 51.02% | 0 |

**① 붕괴는 정확도 0을 뜻한다.** 무교정 `baseline_v2`가 원본 해상도에서 1.36%다.
붕괴형 출력(`!` 반복 = token 0) 1,001문항의 정답은 **0개**다. 사전 등록한 네 번째
분기("붕괴율 94.8%에도 0에 가깝지 않다면 붕괴 정의를 재검토")는 실현되지 않았다.

**② 도구가 종단 검증됐다.** 도구가 배포 조건에서 진단(초과 비율 95.0%, 실측 붕괴율
95.0%와 **일치**)하고 배수를 산출(MLP 분기 ×0.3655)해 교정한 가중치가
**1.36% → 50.73%**를 만든다. 붕괴형 출력 0개.

**③ 교정의 비용은 배포 조건에서 측정 한계 이하다.** 같은 모델·같은 해상도·같은
문항에서 **교정 하나만 다른** 짝을 만들려면 오버플로가 없는 기준선이 필요하다 —
bf16은 상한이 3.4e38이라 그 역할을 한다.

| | 정확도 |
|---|---|
| 기준선: 무교정 bf16 @원본 | 49.95% |
| 교정 fp16 @원본 | 50.73% |
| 차이 | **+0.78%p** |
| McNemar (불일치 54쌍: 31 대 23) | **p=0.3408 비유의** |

**이전의 −0.59%p(평가 해상도·bf16)를 이 값으로 대체한다.** 부호까지 뒤집혔으나 둘 다
비유의이므로 결론은 같고 **근거의 조건이 맞아졌다.** **"공짜"라고 쓰지 않는다** —
비유의는 동등성의 증거가 아니며, 불일치 54쌍은 교정이 답을 실제로 바꾼다는 뜻이다.

**④ 처방이 완성된다.** `full`은 원본 해상도 fp16에서 52.38%, 붕괴형 출력 0개다.
정렬 손실을 쓴 모델은 배포 해상도에서 **"NaN이 안 난다"를 넘어 "제대로 답한다".**

**⑤ 이산성이 세 방향에서 지지된다 (§2 기여 2 보강).**
- **프레임 단위 혼재 0건** — 원본 해상도 708 키프레임 중 689개 전부 붕괴, 19개 전부
  정상, 혼재 **0**. 평가 해상도에서도 32개 붕괴 프레임 전부가 통째로 붕괴.
  기존 "혼재 0건"은 붕괴율 측정에서 나온 것이었는데 **생성 단위로 직접 확인**됐다.
- **붕괴형 정답 0개** — 중간 상태가 없다.
- **붕괴를 빼면 dtype 차이가 사라진다** — 아래 ⑥.

**⑥ fp16의 정확도 비용은 전부 오버플로에서 나온다 (짝지은 검정 2건).**

| 모델 | 해상도 | 여유 | fp16 | bf16 | 불일치 | McNemar | fp16 붕괴형 |
|---|---|---|---|---|---|---|---|
| `baseline_v2` | 평가 | 1.35배 | 48.49% | 50.63% | 14 대 36 | **p=0.0030 유의** | **45** |
| `baseline_v2` (붕괴 제외) | 평가 | — | **50.71%** | 50.30% | — | — | — |
| `full` | 평가 | 3.63배 | 51.31% | 51.02% | 7 대 4 | p=0.5465 비유의 | 0 |
| **`full`** | **원본** | **2.92배** | **52.38%** | **52.87%** | **4 대 9** | **p=0.267 비유의** | **0** |

`baseline_v2`의 −2.14%p는 **전부 45개 붕괴 문항에서 나온다.** 그것을 빼면 50.71% 대
50.30%로 차이가 없다. `full`은 붕괴형이 0개이고 차이도 비유의다.
**오버플로 이전의 연속적 열화는 관측되지 않았다.**

> **원본 해상도 짝을 추가했다 (2026-09-17).** 기존 두 짝은 모두 **평가 해상도**였다 —
> 즉 "붕괴가 없으면 dtype 차이가 없다"를 *배포 조건에서는* 보인 적이 없었다.
> `full`을 원본 해상도 bf16으로 재서(`student_full_epoch_1_native_bf16_*`) 짝을
> 채웠다. **52.38% 대 52.87%, 불일치 13쌍(4 대 9), p=0.267.** 더 강한 증거는
> 정확도가 아니라 **출력 문자열이 1,029개 중 961개(93.4%) 완전히 일치**한다는
> 것이다 — 두 형식이 같은 답을 내고 있고, 5문항 차이는 나머지 6.6%의 잔돌림이다.
> 이로써 ⑥의 주장이 **평가·배포 두 해상도 모두에서** 성립한다.

> **중간 해석의 자기정정.** 처음에는 "여유가 얇을수록 fp16에서 더 떨어진다"로 읽고
> 이산성 주장에 단서를 달 준비를 했다. 예측 덤프를 가르자 원인이 잔여 붕괴였다.
> **집계 수치만 보고 기전을 추정하지 않는다.**

**⑦ "평가 해상도에서는 붕괴가 없다"는 틀렸다.** `baseline_v2`는 평가 해상도에서도
**3.2%**(해상도 스윕 250장) / **4.5%**(여기, 708 키프레임 중 32개)가 붕괴하고,
`temporal`은 0.4%다. 0%인 것은 `base_3b`·`kd_only`·`spatial`·`full`이다.
데이터는 처음부터 `res_200704.json`에 있었으나 서술이 틀려 있었다.

> **2026-09-16 재정정.** 이 항목의 첫 판은 *"5-way 표의 50.30%는 그 손실을 포함한
> 값"*이라고 적었다. **틀렸다.** 5-way 표는 **bf16**으로 측정했다
> (`detect_precision` → `lora_bf16` → 기반 모델을 bfloat16으로 적재). bf16에서는
> 오버플로가 없으므로 그 표에는 붕괴 손실이 **들어 있지 않다.**
>
> 정확한 서술은 오히려 더 강하다 — **표준 평가(bf16·평가 해상도)는 이 손실을 전혀
> 보여 주지 않는다.** 같은 해상도라도 실제 배포 dtype인 fp16으로 돌리면 기준선의
> 3~4%가 무너진다(48.49% 대 50.63%). 즉 평가와 배포를 가르는 것은 해상도만이
> 아니라 **dtype이기도 하며**, 표준 평가는 둘 다에서 비켜 서 있다.

**⑧ 해상도 자체의 효과는 모델별로 다르다.** 무붕괴 기준으로 `baseline_v2`는
원본 49.95% 대 평가 50.63%(−0.68%p), `full`은 원본 52.38% 대 평가 51.31%(+1.07%p).
같은 dtype끼리 맞춘 bf16 짝도 같은 크기다 — `full` 원본 52.87% 대 평가 51.02%
(+1.85%p). 모두 n=1,029에서 비유의 수준이므로 **방향을 주장하지 않는다.**

**⑨ 측정이 조용히 실패하는 경로가 셋 있었다 — strict 검증이 전부 잡았다.**
(i) 공개 AWQ 저장소의 키 규약 불일치로 인한 랜덤 초기화(NaN), (ii) 교정 가중치를
LoRA 미병합 모델에 적재(643개 불일치), (iii) 타워 탐색이 언어 모델을 집음
(`embed_tokens.weight`). 셋 다 **조용히 틀린 숫자를 만들 수 있었고** 둘은 저자의
버그였다. **`strict` 검증을 도구에 내장하는 것은 선택이 아니라 필수**라는 근거다.
도구 사용법에 적재 절차를 명시한다 — 교정 가중치는 **LoRA 병합본 기준의 비전 인코더**다.

결과: `eval_results/student_*_{native,eval}_{fp16,bf16}*_nuscenesqa_val.json`,
`headroom_guard_fix_baseline_v2_native.json`

---

#### 7.4 한계 ①②의 극복 — 자연 발생 점과 독립 집단 (2026-09-16)

#### ② 검출 곡선을 자연 발생 모델로 검증한다

**문제였던 것.** 7.2의 검출 곡선 20점은 `baseline_v2`의 가중치를 직접 줄여 만든
**합성 연속체**다. *"자기가 만든 교란으로 자기 임계값을 보정했다"*는 반론이 성립한다.

**방법.** λ 스윕 체크포인트 넷(`sweep_align{0.0,0.5,1.0,2.0}/step_5000`)은 **학습으로**
서로 다른 여유에 놓인다. 가중치를 편집한 적이 없다. 이 넷의 배포 조건(원본 해상도·
fp16·100장) 붕괴율을 잰다.

| λ | 여유 p50 | **최악** | 초과 비율 | **실제 붕괴** |
|---|---|---|---|---|
| **0.0** | 1.14 | **0.87** | 14.0% | **11.0%** |
| 0.5 | 1.91 | 1.33 | 0.0% | 0.0% |
| 1.0 | 2.32 | 1.57 | 0.0% | 0.0% |
| 2.0 | 4.04 | 2.78 | 0.0% | 0.0% |

**결과 — 사전 등록한 첫 분기.** 최악 여유가 1.0 아래인 하나만 붕괴하고 넘는 셋은
전부 0%다. **합성 교란으로 얻은 경계(최악 1.00~1.05)가 학습으로 만들어진 모델에서
그대로 재현된다.** 한계 ②가 닫힌다.

**부수 소득 셋.**
- **초과 비율 추정기가 보정 밖 4모델에서도 맞았다** — 오차 3.0 / 0.0 / 0.0 / 0.0 %p.
  7.2의 보정에 쓴 6모델과 무관한 체크포인트다.
- **임계값 2.0의 자연 발생 오탐이 3건이 됐다** — `base_7b`(1.73배), λ=0.5(1.91배)가
  "위험" 판정을 받지만 100장 전부 멀쩡하다. λ=1.0(2.32배)이 겨우 통과한다.
  네 점이 임계값 양쪽에 걸쳐 놓여 **과보수 구간을 그대로 보여준다.**
- **최악 통계량의 근거가 자연 사례로 생겼다** — λ=0.0은 p50 1.14로 "한계 위"처럼
  보이지만 최악이 0.87이라 11%가 터진다. **p50만 보면 놓친다.**

> **조건을 반드시 병기할 것.** Fig. 2와 본문의 λ 여유 1.79~4.61배는 **평가 해상도**
> (200,704) 값이고, 위 표의 1.14~4.04배는 **원본 해상도**다. 같은 체크포인트인데
> 값이 다르다. 덤으로 이 대조는 **해상도만 올려도 여유가 깎인다**는 것을
> λ 축과 무관하게 보여준다(1.79→1.14, 4.61→4.04).

결과: `eval_results/guard_sweep_align{0.0,0.5,1.0,2.0}.json`

#### ① 지표 역전을 독립 집단에서 확인한다

**문제였던 것.** 한계 ①은 "단일 계열(Qwen2.5-VL)"이다. 지표 역전은 스케일 불변성의
귀결이므로 **수학적으로는 계열 독립**이어야 하지만, **측정은 한 계열뿐**이었다.

**방법.** 6장에서 여유를 이미 잰 공개 모델 셋(Qwen2-VL 2B·7B AWQ, SmolVLM)에
같은 INT8 지표를 잰다. 스크립트: `scripts/probe_int8_across_families.py`

| 집단 | n | Spearman(여유, SNR) | 우연일 확률 (1/n!) |
|---|---|---|---|
| 우리 6변형 (통제 실험, 200장) | 6 | **−1.000** | 0.0014 |
| **공개 3모델 (독립 집단, 100장)** | 3 | **−1.000** | **0.17** |
| 아홉 개를 섞으면 | 9 | **−0.517** | — |

공개 3모델: 여유 3.88 → 4.49 → 50.86으로 커지는 동안 SNR이 32.30 → 29.98 → 23.45로
**단조 감소**한다.

**결과 — 사전 등록한 세 번째 분기.** 역전의 **방향은 독립 집단에서도 관찰되는데
절대 수준은 옮겨 가지 않는다.** 우리 `full`(여유 2.92)이 22.88 dB인데 더 안전한
Qwen2-VL-2B(여유 3.88)는 32.30 dB다. 계열마다 SNR 기준선이 다르기 때문이다.

##### −1.000 두 개의 무게가 다르다 (2026-09-17 추가)

완전 역전이 **우연히** 나올 확률은 `1/n!`이다. 위 표의 마지막 열이 그것이다.

- **우리 6변형: 1/720 = 0.0014.** 통계적 근거가 된다.
- **공개 3모델: 1/6 = 0.17.** 세 점을 아무렇게나 늘어놓아도 여섯 번에 한 번은
  −1.000이 나온다. **이 값은 그 자체로 통계적 근거가 되지 못한다.**

따라서 **공개 3모델에 "재현됐다"를 쓰지 않는다.** 쓸 수 있는 것은 *"독립 집단에서도
같은 방향이 관찰됐다"*까지다. 계열 독립성의 근거는 여전히 **스케일 불변성이라는
수학적 논증**이고, 3모델은 그 논증과 모순되지 않는다는 확인이다.

**완벽한 −1.000은 인접한 한 쌍에 기대고 있다.** 6변형 안에서 `temporal`(여유 1.06배,
30.52 dB)과 `kd_only_v4`(1.10배, 30.41 dB)는 여유가 0.04배, SNR이 0.11 dB 차이다.
여유를 **중앙값 대신 최악값**으로 재면 이 쌍의 순서가 뒤집힌다.

| 여유 출처 | 통계량 | 5변형 ρ | 6모델 ρ |
|---|---|---|---|
| 250장 스윕 (**Table 1**) | p50 | **−1.000** | **−1.000** |
| 250장 스윕 | 최악 | −0.900 | −0.886 |
| 200장 INT8 파일 | p50 | **−1.000** | **−1.000** |
| 200장 INT8 파일 | 최악 | −0.900 | −0.943 |

> **뒤집는 변수는 표본이 아니라 통계량이다 (2026-09-17 정정).** 초판은 *"두 번째
> 표본에서 −0.900"*이라 적어 원인을 표본으로 돌렸다. 둘을 분리해 재니 **표본을
> 바꿔도 p50에서는 −1.000이 유지되고, 통계량을 최악으로 바꾸면 두 표본 모두
> −0.900**이다. `temporal`과 `kd_only_v4`의 최악 여유가 0.80으로 거의 같아 순서가
> 불안정하기 때문이다(p50에서는 1.06 대 1.10으로 갈린다).

**군 사이의 역전은 네 조합 모두에서 유지된다** — 여유 1배 미만 4개와 2.9배 군 2개
사이의 순서는 어느 통계량·어느 표본에서도 흔들리지 않는다. 흔들리는 것은 1배 근처에
몰린 인접 쌍뿐이다.

**Table 1의 여유는 p50이고 도구의 게이트는 최악이다 — 의도한 차이다.** Table 1은 여유를
**붕괴율**과 짝지으므로 분포의 중심이 맞다. 최악으로 바꾸면 `base_3b`(69.6%)와
`baseline_v2`(94.8%)가 둘 다 0.74로 같아져 **구별이 사라진다.** 반대로 배포 가부 판정은
"한 장이라도 터지는가"이므로 최악이 맞다(§7.2 게이트 보정). **어느 쪽을 쓰는지 표·본문·
도구 출력에 항상 병기한다.**

**서술 규칙 — 기여 A의 범위를 이렇게 쓴다.**
> 지표 역전은 **비교 가능한 모델 집단 안에서** 성립한다. 통제된 6변형에서
> ρ = −1.000이며(중앙값 기준, 우연일 확률 0.0014), 최악값으로 재면 −0.900으로
> **인접한 한 쌍의 순서만 흔들리고 군 사이의 역전은 유지된다.** 독립된 공개 3모델에서도 같은
> **방향**이 관찰되나 n=3이라 그 자체로는 통계적 근거가 아니며, 계열 독립성의
> 근거는 스케일 불변성이라는 정의상의 논증이다. 또한 **SNR의 절대 수준은 계열을
> 넘어 비교할 수 없다**(아홉 개 혼합 시 ρ = −0.517).

마지막 문장은 주장을 약화시키지 않는다 — **계열 간에는 순위조차 매길 수 없다**는 뜻이므로
*"SNR 30 dB면 안전"* 같은 절대 기준이 성립하지 않음을 오히려 강화한다.

**max/rms는 여기서도 무정보다** — 113.4 → 133.9 → **31.2**로 SmolVLM이 가장 낮은데
여유는 가장 크다.

> **측정 조건 차이를 적는다.** 공개 3모델은 100장, 우리 6개는 200장이다. SmolVLM은
> image_processor에 `max_pixels`가 없어(고정 타일 방식) **모델 기본 해상도**로 쟀다.
> 계열 간 절대 비교를 하지 말아야 할 이유가 하나 더 있는 셈이다.

**남는 것.** 기여 B(손실 설계가 여유를 움직인다)는 **여전히 단일 계열**이다. 다른
계열에서 같은 통제 실험을 하려면 학습이 필요해 범위 밖이며, **한계 ①은 절반만
닫힌다.**

결과: `eval_results/int8_across_families.json`

---

### 8장 종합 논의와 한계
- 4~7장을 §2 진술의 네 절에 다시 대응시켜 논지가 닫혔음을 보인다
- **일반성을 주장하지 않는다** — 이 논문은 fp16 표현 범위 초과라는 **한 실패 양식**을
  다룬다(§2 범위 명시, §3 사례 1 분리 결정)

**한계를 먼저 적되, 여덟으로 줄인다.** (09-14에 넷, 09-15에 셋이 실측으로 추가됐고, 09-16에 사례 1을 분리하며 **단일 실패 양식**이 0번으로 들어왔다. ②는 같은 날 해소돼 취소선으로 남긴다.)

> **2026-09-14 정리.** 이전 판은 여섯 항목을 나열했다. **한계 목록이 길수록 좋은 논문이
> 아니다** — 여섯을 늘어놓으면 그중 진짜 중요한 하나(단일 계열)가 상투구처럼 읽힌다.
> 판정 기준은 **"이걸 모르는 독자가 본문으로부터 틀린 결론을 내리는가"**이지
> "포함하면 불리한가"가 아니다. 후자를 기준으로 삼으면 은폐다.
>
> 이 기준으로 둘을 뺐고, 하나는 **한계가 아니라 근거**임을 확인해 위치를 옮겼다.

0. **단일 실패 양식 (2026-09-16 추가).** 본 논문은 **fp16 표현 범위 초과** 하나를
   다룬다. 이전 구성은 사례를 둘(KD 온도 종료 신호 소실 / 표현 범위 초과) 두어
   *"서로 다른 메커니즘에서 같은 구조가 나온다"*는 패턴을 주장했으나, §2 논지가
   저정밀 배포로 좁혀지면서 사례 1을 분리했다(§3). **따라서 "이산적 기능 상실이
   일반적 실패 양식"이라고 쓰지 않는다** — 한 양식을 깊게 판 사례 연구다.
   분리한 재료는 별도 논문으로 낸다(`thesis_ch4_removed_20260916.md`).
1. **단일 모델 계열 (Qwen2.5-VL) — 절반 해소 (2026-09-16).** 가장 중요한 한계다.
   - **기여 A(지표 역전)**: 공개 3모델(Qwen2-VL 2B·7B, SmolVLM)에서 **같은 방향이
     관찰**됐다(ρ = −1.000). **다만 n=3이라 우연일 확률이 1/6 = 0.17이므로 이것을
     "재현"이라 쓰지 않는다** — 계열 독립성의 근거는 스케일 불변성이라는 논증이고
     3모델은 그와 모순되지 않는다는 확인이다. 통계적 근거는 통제된 6변형 쪽이다
     (ρ = −1.000, p = 0.0014; 표본을 달리하면 −0.943이나 **군 사이의 역전은 유지**).
     또한 **절대 수준은 계열 간 비교 불가**(혼합 시 ρ = −0.517)이므로 "비교 가능한
     집단 안에서"로 범위를 적는다. §7.4 참조.

     **이 하향이 "절반 해소" 판정을 바꾸지는 않는다.** 근거가 원래 둘이었고 —
     지표 역전의 계열 독립성과, 현상 프로파일을 3계열에서 관측한 것 — 후자는
     그대로다. 다만 전자의 근거가 "측정으로 재현"에서 **"논증 + 모순 없음 확인"**
     으로 약해졌으므로, 발표·본문 어디서도 *"3계열에서 재현했다"*로 말하지 않는다.
   - **기여 B(손실 설계가 여유를 움직인다)**: **여전히 이 계열에서만 검증했다.**
     다른 계열에 `L_align`을 적용하려면 학습이 필요해 범위 밖이다. 현상의 프로파일은
     3계열에서 보였으나 **손실 설계 축은 단일 계열이다.**
2. ~~**게이트 보정의 근거가 합성 교란이다**~~ → **2026-09-16 해소.** λ 스윕
   체크포인트 넷(학습으로 여유 1.14~4.04배)의 배포 조건 붕괴율을 재니 **합성 경계가
   그대로 재현**됐다(최악 1.0 아래 하나만 11% 붕괴, 넘는 셋은 0%). §7.4 참조.
   아래는 해소 전 서술이며 기록으로 남긴다. 7.2의 검출 곡선은
   `baseline_v2`의 MLP 가중치를 직접 줄여 만든 **여유 연속체** 20점에서 얻었다.
   "자기가 만든 교란으로 자기 임계값을 보정했다"는 반론이 성립한다.
   **완화 근거**: 공개 모델 5개(여유 0.96~50.86배)를 같은 조건에서 재어 겹쳤더니
   합성 곡선 위에 얹혔다 — 합성 `f=0.92`(여유 0.95, 붕괴 71.0%)와 자연 `base_3b`
   (여유 0.96, 붕괴 70.0%)는 거의 같은 점이다. 그러나 자연 점은 **여유 1.73~50.86
   구간이 비어 있어** 경계 근처를 촘촘히 채운 것은 합성뿐이다. 그대로 적는다.
3. **처방의 수명 — 관측 범위에서는 유지됐다 (2026-09-16 갱신).** bf16 엔진은 붕괴를
   0.0%로 없애고, 그 지연 비용은 **형식의 산술 비용이 아니라 런타임의 bf16 커널
   커버리지** 탓이다(같은 형상 순수 GEMM에서 두 형식이 기하평균 1.001배로 동등, §6.3).
   따라서 **커널이 성숙하면 비용이 사라지고 "bf16으로 빌드하라"가 여유를 재지 않고도
   통하는 처방이 된다**는 우려가 성립한다.

   **이 예측을 실제로 시험했다.** 교정 그래프에서 같은 세션 짝으로 재면
   **TRT 10.13에서 1.375배, 10.16에서 1.344배**다. 두 버전에 걸쳐 **격차가 닫히지
   않았고**, 0.03 감소는 빌드 간 변동 폭(4~7%)보다 작다. 방향은 감소 쪽이므로
   **한계를 삭제하지 않되 "관측된 두 버전에서는 유지됐다"로 적는다.** 추측이
   측정으로 바뀌었다.

   **범위를 좁히는 근거 하나 — 다만 대상 보드를 정확히 적는다 (2026-09-18 정정).**
   NVIDIA TensorRT 문서의 DLA 레이어 제약 절은 지원 정밀도를 **"Both FP16 and INT8
   are supported"**로 적고 **BF16을 언급하지 않는다.** 즉 **DLA로 오프로드하는
   배포에서는 bf16이 선택지가 아니다.**

   > **그런데 본 연구의 대상인 Orin Nano에는 DLA가 없다.** NVIDIA 사양표상
   > Orin Nano 8GB는 *"1024-core Ampere GPU with 32 Tensor Cores"*뿐이고 **DLA 항목이
   > 없다**(4GB도 동일). DLA는 상위 모듈에만 들어간다 — AGX Orin 2×, Orin NX 1×.
   >
   > | 모듈 | DLA |
   > |---|---|
   > | AGX Orin | 2× DLA v2 |
   > | Orin NX | 1× DLA v2 |
   > | **Orin Nano 8GB / 4GB (본 연구 대상)** | **없음** |
   >
   > 이전 판은 *"Jetson Orin에서 DLA 오프로드 배포에서는"*이라고만 적어 **Orin Nano도
   > 포함되는 것처럼 읽혔다.** 자기 실험과 어긋나므로 범위를 좁힌다.
   >
   > **따라서 두 서술을 분리한다.**
   > - *"DLA는 bf16을 지원하지 않는다"* → **참**, 일반론. **AGX Orin·Orin NX** 해당
   > - *"따라서 본 연구 배포에서 bf16은 선택지가 아니다"* → **거짓**. Orin Nano는
   >   GPU만 쓰므로 bf16이 선택지에 있다
   >
   > **본 연구에서 fp16을 쓰는 이유는 정밀도(가수 10 대 7비트)와 현재 런타임의
   > 속도 차이(1.34~1.38배)다.** DLA 논거는 **상위 Orin 모듈로 확장할 때의 근거**
   > 로만 쓴다. 출처:
   > https://developer.nvidia.com/blog/solving-entry-level-edge-ai-challenges-with-nvidia-jetson-orin-nano/

   Orin 계열 전반으로 보면 DLA를 쓰는 배포가 드물지 않으므로 이 근거는 무게가 있으나,
   **GPU만 쓰는 배포에는 해당하지 않아 한계를 지우지는 못한다.**
   출처: https://docs.nvidia.com/deeplearning/tensorrt/latest/inference-library/dla-layer-restrictions.html

   **가장 단단한 반론 봉쇄 — fp16은 bf16보다 정밀하다 (2026-09-18 추가).**
   이 한계 논의는 *"bf16이 공짜가 되면 fp16을 쓸 이유가 없다"*를 전제한다.
   **그 전제가 틀렸다.** 두 형식은 같은 16비트를 다르게 쪼갠 것이다.

   | 형식 | 부호 | 지수 | **가수** | 최대값 | 상대 정밀도(eps) |
   |---|---|---|---|---|---|
   | **fp16** | 1 | 5 | **10** | 65,504 | **9.77e-04** |
   | **bf16** | 1 | 8 | **7** | 3.39e38 | 7.81e-03 |
   | fp32 | 1 | 8 | 23 | 3.40e38 | 1.19e-07 |

   bf16은 **fp32의 지수 범위를 그대로 가져오려고 가수를 깎은 형식**이다. 학습 중
   그래디언트의 언더플로/오버플로를 막으려는 설계다. **추론에서는 그 교환이 반대로
   작용할 수 있다** — 값이 범위 안에만 있으면 **fp16이 8배 정밀하다**(가수 10비트 대
   7비트 = 2³).

   따라서 진짜 교환은 이렇다.

   ```
   fp16   범위를 내주고 정밀도를 얻는다   (65,504 / 가수 10비트)
   bf16   정밀도를 내주고 범위를 얻는다   (3.4e38 / 가수 7비트)
   ```

   **커널이 성숙해도 이 교환은 사라지지 않는다 — 규격이기 때문이다.** 속도 논거
   (1.34~1.38배)는 런타임 버전에 종속되지만 정밀도 차이는 그렇지 않다. 그리고
   어느 쪽이 맞는지는 **활성값이 65,504의 어느 편에 있느냐**로 갈리므로,
   **재야만 답이 나온다.**

   > **이 논거를 넣기 전의 서술이 fp16 선택을 관성처럼 보이게 했다.** 이유를
   > 관행·속도·DLA로만 들면 *"그냥 bf16 쓰면 되는데 왜 고집하나"*로 읽힌다.
   > 실제로는 **정당한 공학적 판단**이다 — *"범위는 문제없을 테니 정밀도를 받자."*
   > 논문이 지적하는 것은 그 선택이 아니라 **그 전제를 아무도 확인하지 않는다**는
   > 점이다. 이렇게 적어야 논문이 겨냥하는 곳이 정확해진다.

   **측정의 목적이 바뀌는 것이지 사라지는 것이 아니다 (2026-09-17 추가).**
   bf16이 공짜가 된 세상을 가정해도 실무자에게는 질문이 남는다 — **"나는 fp16을 쓸
   수 있는가?"** fp16은 bf16보다 **1.34배 빠르고**(138.7 ms 대 186.5 ms, §6.3),
   **DLA에서 쓸 수 있는 유일한 부동소수 형식**이며(AGX Orin·Orin NX 한정 — 본 연구
   대상인 Orin Nano에는 DLA가 없다), **같은 크기에서 8배 정밀하다.**
   여유를 재면 그 답이 나온다.

   | 측정의 역할 | bf16이 비싼 지금 | bf16이 공짜인 미래 |
   |---|---|---|
   | 목적 | **재앙 회피** — 안 재면 94% 붕괴 | **최적화** — 재면 1.34배·DLA·정밀도 8배를 얻는다 |
   | 안 재면 | 배포가 망가진다 | **더 느리고 덜 정밀한 형식에 안주한다** |

   따라서 이 한계의 정확한 서술은 *"처방의 가치가 사라진다"*가 아니라
   **"처방의 목적이 안전에서 성능으로 옮겨 간다"**이다. 그래도 한계로 남긴다 —
   **"재앙을 막는 처방"과 "성능을 여는 처방"은 설득력의 무게가 다르다.**

   **경계를 정확히 긋는다 — fp8에는 그대로 적용되지 않는다 (2026-09-17 추가).**
   *"fp8로 가면 표현 범위 문제가 더 심해지니 처방이 살아남는다"*고 쓰고 싶어진다.
   숫자만 보면 그렇다.

   | 변형 | fp16 | fp8 E5M2 (상한 57,344) | fp8 E4M3 (상한 448) |
   |---|---|---|---|
   | `baseline_v2` | 0.876 | **0.767** | 0.006 |
   | `full` | 2.924 | **2.560** | 0.020 |

   **그러나 이 논거는 쓰지 않는다.** fp8 추론은 **거의 항상 스케일 인자와 함께**
   쓴다 — 활성을 최대값으로 나눈 뒤 캐스팅하므로 **절대 크기 문제가 애초에 사라지고**
   **동적 범위** 문제로 바뀐다. 그건 본 논문이 *"아무 말도 하지 않는다"*고 판정한
   `max/rms`가 보는 축이다.

   > **정확한 범위**: 이 처방은 **스케일 없이 그대로 쓰는 형식**(fp16, bf16)에 묶여
   > 있다. 필드가 **스케일된 fp8**로 이행하면 실패 양식 자체가 바뀌고 다른 지표가
   > 맞다. 이는 한계를 없애는 것이 아니라 **날카롭게 하는 것**이며, 심사에서
   > *"그럼 fp8은?"*에 대한 답을 미리 마련해 둔다.

   **기여 A는 이 한계의 영향을 받지 않는다.** bf16 커널이 아무리 좋아져도
   *"표준 압축 지표가 붕괴 위험을 역방향으로 가리킨다"*는 사실은 변하지 않는다.
   학위 논문·학술대회 제목을 모두 지표 축으로 옮긴 판단이 여기서 값을 한다 —
   **만료되는 주장을 제목에 걸지 않았다.**

   남는 논거는 공개 가중치 상당수가 이미 fp16으로 저장·배포된다는 점이다.
   이 한계는 본 논문에 불리하므로 **여전히 분명히 적는다.**
4. **실기기 검증의 범위 (한 줄).** 6장이 어디까지 갔는지에 따라 문장이 달라진다.
   보드에서 돌렸으면 "단일 보드(Orin Nano 8GB)·단일 런타임(TensorRT)", A100 TensorRT까지만
   갔으면 "엣지 보드 미검증". 어느 쪽이든 **"실차 배포 시 붕괴한다"고 단정하지 않는다.**
5. **논문이 내세운 양과 도구가 쓰는 양이 다르다 (2026-09-15 추가).** §2는 스칼라
   **표현 범위 여유**를 중심 개념으로 세우는데, §7.2의 게이트 보정은 배포 판정을
   **초과 비율**(이미지별 max가 형식 상한을 넘는 비율)로 하라고 결론냈다.
   논문이 이름 붙여 내세운 양을 같은 논문이 판정에서는 쓰지 말라고 하는 셈이다.
   **정리하면 역할이 다르다** — 여유는 **설계·진단 변수**다. 연속량이라 학습이
   움직이는 것을 볼 수 있고(λ 용량-반응은 크기에서 나오지 초과 비율에서 나오지
   않는다. 초과 비율은 0과 1에서 포화한다), 형식 간 비교가 되며, 한 장으로도
   정의된다. 초과 비율은 **배포 판정**이다. 표본이 있어야 하고 형식에 묶인다.
   그러나 이 구분은 **설계 판단이지 측정 결과가 아니므로** 한계로 적는다.
6. **도구의 보조 임계값 1.25는 측정값이 아니다 (2026-09-15 추가).**
   초과 비율 게이트에는 임계값이 없다. 다만 "초과 0건인데 여유가 얇은" 경우를
   **경계**로 경고하는 데 `MARGINAL_HEADROOM = 1.25`를 쓴다. 근거는 *실측 전이
   1.00~1.05 + 모델 내 이미지별 편차 15~20%*인데, 이는 **추론이지 측정이 아니다.**
   초판의 2.0보다 훨씬 나은 논거지만 종류는 같다. **매직 넘버를 더 작은 매직
   넘버로 바꾼 것**이므로 그대로 적는다.
7. **λ 스윕의 학습 길이 (5,000스텝).** 곡선의 **모양**(0 / 0.5 / 1.0 / 2.0 네 점)은
   5,000스텝에서만 측정했다. 다만 **두 끝점은 전체 에폭에서도 확인된다** — 아래.

   **끝점 대조 (2026-09-14, 추가 실험 없이 기존 데이터에서 확인).**
   `student_baseline_v2`(전체 에폭, L_align 없음)와 `student_spatial`(전체 에폭,
   λ_align=1.0)이 바로 λ=0과 λ=1.0 지점이다. 동일 250장 짝지어 비교(fp16, blk31 p50):

   | 조건 | 학습 길이 | λ_align | p50 |
   |---|---|---|---|
   | `sweep_align0.0` | 5,000 | 0 | 36,480 (250장) |
   | `sweep_align1.0` | 5,000 | 1.0 | 19,232 |
   | `student_baseline_v2` | **27,037** | 0 | 35,776 |
   | `student_spatial` | **27,037** | 1.0 | 18,304 |

   **학습 길이에 따른 효과 — 5개 지점 (`eval_results/training_trajectory_profile.json`,
   150장, max_pixels 200704).** 위 두 점 대조보다 이쪽이 정확하다. 같은 조건에서
   `student_baseline_v2`와 `student_spatial`의 중간 체크포인트를 모두 갖고 있다.

   | 스텝 | `baseline_v2` | `spatial` | 감소율 |
   |---|---|---|---|
   | 5,000 | 42,016 | 13,376 | **68.2%** |
   | 10,000 | 44,224 | 15,304 | 65.4% |
   | 15,000 | 47,392 | 17,024 | 64.1% |
   | 20,000 | 48,480 | 18,272 | 62.3% |
   | 25,000 | 48,288 | 18,256 | **62.2%** |

   > **정정 (2026-09-14).** 처음에는 위 두 점(5,000스텝 47.3% → 전체 에폭 48.8%)만 보고
   > "포화도 역전도 없고 오히려 크다"고 적었다. **틀렸다** — 그 둘은 해상도와 표본이
   > 다른 측정이라 직접 비교할 수 없었다. 같은 조건 5개 지점으로 보면 **완만히 감소하다
   > 20,000스텝 부근에서 62%로 포화**한다.

   따라서 한계 3의 정확한 진술은 이렇다.

   - **역전은 없다** — 5개 지점 전부 62~68%로 같은 방향이다.
   - **포화는 있다** — 20,000스텝 부근에서 62%로 수렴한다. 다만 **효과가 사라지는 것이
     아니라 안정화**되는 것이므로, 배포 관점에서는 학습을 길게 해도 여유가 유지된다는
     뜻이다.
   - **남는 한계는 곡선의 모양뿐이다** — λ 4점(0/0.5/1.0/2.0)의 형태는 5,000스텝에서만
     쟀다. 중간점을 전체 에폭에서 재려면 2회 × 51.6시간 = 약 4.3일이며, 끝점의 방향과
     크기가 이미 5개 지점에서 확인됐으므로 수행하지 않는다.
4. **데이터 축.** nuScenes 기반이 주축이고 CODA-LM으로 한 축을 더했다. CODA의 약 9%가
   nuScenes 유래이나, **학습 이미지와 실제로 겹치는 것은 없음을 실측으로 확인했다**
   (아래). 남는 한계는 "데이터 출처가 둘뿐"이라는 것이지 중첩 우려가 아니다.

**뺀 것 둘 — 무엇을, 왜.**

- **849번 행 이상치의 기원.** 논증에 구멍이 없다. 5장이 보이는 것은 "증폭기가 존재하고,
  6개 변형에서 동일하며, 달라지는 것은 거기 들어가는 입력"이다. 기원은 **더 나아간 질문**이지
  이 논증의 결손이 아니며, 몰라도 독자가 틀린 결론에 이르지 않는다.
  → **5장 말미에 한 문장**("왜 그 행인지는 사전학습 접근이 필요해 범위 밖")으로 옮기고
  한계 절에서 삭제.
- **다중 시드 5-way를 못 한 비용 사유.** 독자에게 필요한 것은 **결론**("정확도 순서를
  주장하지 않는다")이지 "15회 × 9.5시간이라 못 했다"가 아니다. 후자는 판단에 아무것도
  더하지 않는다. → 비용 사유 삭제, **결론은 반드시 유지**(안 쓰면 51.69 > 51.40을
  유의미한 차이로 읽는다).

**옮긴 것 하나 — 계열 일반성은 한계가 아니라 근거다.**

> SmolVLM 50.86배·Qwen2-VL-2B 3.88배라는 반례를 한계 목록에 두면 "우리 결과가 좁다"로
> 읽힌다. **그러나 이 반례들이야말로 처방의 필요성을 만든다.** 모든 모델이 여유 1.0배
> 근처였다면 처방은 "비전 인코더에 fp16을 쓰지 마라" 한 줄로 끝나고 지표가 필요 없다.
> 갈리기 때문에 **모델마다 재야 한다**가 성립한다.
> → **§2 진술 3번 절(표준 평가에 그 지표가 없다)의 근거로 이동**하고, 8장에서는
> "사례 연구 범위"로만 짧게 재확인한다.

**사례가 둘이라 패턴 제시 수준**이라는 항목도 한계가 아니라 **범위 설정**이므로
§2로 옮긴다.

**데이터 축 일반성 (2026-09-12 완료)** — 한계 4번을 좁히는 근거로 여기 둔다.

  | 조건 | 토큰 | baseline_v2 여유/붕괴 | spatial | full | 판정 |
  |---|---|---|---|---|---|
  | CAM_FRONT (기준) | 1,836 | 0.88× / 94.8% | 2.89× / 0% | 2.92× / 0% | — |
  | **②a CAM_BACK** | 1,824 | 0.86× / **96.8%** | 2.92× / **0%** | 2.99× / **0%** | **순서 유지** |
  | **②b CODA-LM** | 1,196 | 0.94× / **72.0%** | 2.64× / **0%** | 2.60× / **0%** | **순서 유지** |

  - **②a**: 토큰 수를 고정한 채 장면 내용만 바꿔도 순서·크기가 그대로다 →
    **활성 크기는 입력 내용이 아니라 모델 성질**이다.
  - **②b**: nuScenes 밖(CODA-LM)에서도 align 둘만 0%다. 토큰이 1,196개로 적어 활성이
    전반적으로 낮지만, nuScenes의 1,024토큰(25.6%)과 1,836토큰(94.8%) 사이에 72.0%로
    놓여 **같은 "토큰 수 → 붕괴율" 곡선 위**에 있다.

  **CODA-LM은 학습 이미지와 겹치지 않는다 (2026-09-14 실측).**
  CODA는 KITTI 309 / nuScenes 134 / ONCE 1,057에서 구성돼 약 9%가 nuScenes 유래이므로,
  그 장면이 학습에 쓴 696개 DriveLM 장면과 겹치는지 확인이 필요했다.
  **메타데이터로는 불가능했다** — CODA-LM llava-format의 id가 `Mini_general_0` 같은
  일련번호뿐이라 원출처가 남아 있지 않다. 그래서 **이미지 자체를 대조했다**
  (`scripts/probe_codalm_overlap.py`): CODA-LM 143장 × DriveLM 학습 **전 카메라
  24,432장**, dHash+aHash로 후보를 추리고 32×32 정규화 상관으로 확정.

  | 항목 | 값 |
  |---|---|
  | 확정 중복 | **0건** |
  | 후보 중 최고 상관 | 0.9077 (임계 0.92) |
  | 상관 중앙값 | 0.7880 |
  | **양성 대조**(720p 축소 + JPEG q95 재인코딩본을 원본과 대조) | 해밍 **0/0**, 상관 **1.0000**, 5/5 검출 |

  **양성 대조가 이 결과의 핵심이다.** 그것이 없으면 "0건"이 정말 없어서인지
  **검출기가 작동하지 않아서인지** 구별할 수 없다. CODA-LM이 가한 것과 같은 변환
  (720p 축소 + 재인코딩)을 거쳐도 검출기가 상관 1.0000으로 완벽히 잡으므로,
  0건은 실제로 겹치지 않는다는 뜻이다.

  상위 유사 쌍들은 `aHash` 거리 1~2(밝기 분포가 닮음)인데 `dHash` 거리는 21~26
  (구조가 다름)이다 — **도로 장면끼리 비슷한 것**이지 같은 사진이 아니다.
  진짜 중복(1.0000)과 최고 유사 쌍(0.9077) 사이에 뚜렷한 간격이 있다.

  → 서술을 **"largely domain-disjoint"에서 "학습 이미지와 겹치지 않음을 실측 확인"**으로
  올린다. 다만 "CODA 자체가 nuScenes를 일부 포함한다"는 사실은 그대로 적는다 —
  우리가 확인한 것은 **우리 학습 집합과의 중첩 부재**이지 데이터셋 간 독립성이 아니다.
  결과: `eval_results/codalm_overlap.json`

  **계열 간 편차(SmolVLM 50.86배 등)는 여기서 반복하지 않는다** — §2로 옮겼다.
  8장에서는 "본 논문이 사례 연구 범위임"을 한 줄로 재확인하는 데 그친다.
  이유: 그 결과는 한계가 아니라 **처방의 필요성 근거**이며, 한계 목록에 두면
  "우리 결과가 좁다"로 오독된다.

  **데이터 일반성 실험의 구분 (서술 주의)**
  - `CAM_BACK` / `CAM_FRONT_LEFT` 측정은 **데이터셋 일반성이 아니다.** 같은 nuScenes,
    같은 도시·센서이며 시점만 다르다. 이 측정이 보이는 것은 **활성 크기가 입력 내용에
    둔감한 모델 성질인지**(도메인 내 입력 분포 불변성)뿐이다.
  - nuScenes 밖 검증은 **CODA-LM 193장**이 담당한다. 단 CODA-LM 이미지는 720p로
    전처리되어 vision token 수가 nuScenes(1,836)와 다르므로(≈1,175) 직접 비교할 수 없다.
    nuScenes에서 이미 측정한 **"토큰 수 → 활성 크기" 곡선 위에 놓이는지**로 비교한다.
  - **CODA 출처 캐벗**: CODA는 KITTI·nuScenes·ONCE에서 구성된 데이터셋이라 nuScenes와
    완전히 분리되지 않는다. 받은 193장은 전부 720p로 재처리되어 원출처를 판별할 수
    없었다(해상도 1280~1355×720, nuScenes 원본 1600×900 없음). 따라서
    **"완전히 도메인 분리"라고 쓰지 않는다.**

  *(제외된 항목)* "간접 경로 미규명"은 5장 (c)에서 규명되어 한계가 아니다.
  "상위 K 절단이 시뮬레이션"도 한계가 아니라 **통제**다 — 검증 대상이 softmax 분모에
  꼬리가 기여하는 효과이므로 상위 K 절단이 그 효과만 정확히 분리하며, 다른 어휘 크기의
  실제 모델을 쓰면 사전학습·아키텍처가 전부 교란된다. 4장 본문에 그렇게 서술한다.

### 9장 결론
§5의 기여 진술을 결론형으로 다시 쓰고, 다음 셋을 명시한다.
- **범위**: fp16 표현 범위 초과라는 한 실패 양식. 일반 법칙을 주장하지 않는다
- **남는 것**: 손실 설계 축의 단일 계열(8장 한계 1), 실기기 범위(한계 4),
  처방의 수명(한계 3)
- **분리한 것**: 사례 1(KD 온도 × 어휘 크기)은 별도 논문으로 낸다(§3).
  *"다루지 않은 것이 아니라 범위 밖으로 분리했다"*를 결론에 한 줄 남긴다

---

## 5. 서론·결론 기여 진술 초안

> 본 논문의 기여는 하나다. 압축된 주행 시각-언어 모델을 저정밀로 배포할 때 표현 범위
> 초과로 **기능이 이산적으로 상실**되며, 그 상실에는 **순전파 한 번으로 계산되는 예고
> 지표**가 있으나 **표준 평가에는 그 지표가 없고 오히려 역방향을 가리키며**, 지표를
> 움직이는 수단은 여럿이므로 **부족한 것은 수단이 아니라 측정**임을 보인 것이다.
>
> **fp16 표현 범위 초과**라는 한 실패 양식으로 이를 입증한다. 미세조정이 비전 인코더
> 활성 크기를 키워 저정밀 표현 범위를 초과시키면, 예고 지표는
> **`format_max ÷ max|activation|`**이며 0.88배인 모델은 원본 해상도에서 94.8%의 이미지가
> 붕괴하고 2.92배인 모델은 0%다. 과제 성능으로는 같은 모델이 49.95%에서 **1.36%**로
> 떨어진다. 이 지표는 압축·배포 문헌의 표준 평가 항목에 없을 뿐 아니라, **표준 지표는
> 오히려 붕괴하는 모델을 더 안전하다고 순위 매긴다**(INT8 SNR 32.23 대 22.88 dB).
>
> 그리고 지표를 움직이는 수단이 하나가 아님을 함께 보인다. 특징 정렬 손실이 여유를
> 부수적으로 확보해 주고, 정렬 대상을 교사가 아닌 학생 자신으로 두어도 그 효과의 절반이
> 재현되며, 학습 없이 마지막 블록 가중치를 재조정하는 것만으로도 붕괴가 제거된다.
> **수단이 이렇게 여럿인데도 공개 양자화 체크포인트의 13.7%가 이미 fp16으로 실행되도록
> 적혀 있고, 여유를 보고한 저장소는 하나도 없다. 그리고 13.7%는 하한이다** — fp16
> 변환은 엣지 배포의 표준 경로이고 선언된 dtype을 배포자가 덮어쓰므로, **나머지도
> 변환하는 순간 같은 조건이 된다**(본 연구 대상이 그 예다. `bfloat16` 선언에도
> 변환 후 69.6%가 붕괴했다). — 이것이 본 논문이 지목하는 공백이다.

---

## 6. 진행 중 실험 결과에 따른 분기

논지(§2)는 **어느 결과에서도 유지된다.** 달라지는 것은 4장의 강도뿐이다.

| 실험 | 최선 | 그 외 | 4장 서술 조정 |
|---|---|---|---|
| ~~해상도 스윕 최종점~~ | **최선 확정 (09-10)** — align만 0%, 나머지 20~95% 붕괴 | — | **"필요조건"으로 서술 확정** |
| ~~INT8~~ | **최선 확정 (09-10)** — blk31에서 `max/rms`·SNR 모델 간 동일 | — | **독립 절로 승격 확정** |
| λ 스윕 | 0→0.5→1.0→2.0 단조 감소 | 계단 함수 | "조절 가능" → "존재 여부가 결정" |

**λ=0이 baseline 수준(5만대)으로 나오는지는 결과가 아니라 파이프라인 검증이다.**
gradient 상 동일해야 하므로, 벗어나면 결과 해석 전에 버그를 의심한다.

---

### 비-Qwen 계열 위험 구성 실측 — 해석 사전 등록 (2026-09-18, 결과 보기 전)

**문제.** 위험 구성 27개 중 넷을 쟀는데 **여유 1 미만은 하나뿐이고 그것이
`AngelSlim/Qwen2.5-VL-3B-INT4-AWQ`(0.96배)** — 본 연구 학생과 **같은 아키텍처**다.
게다가 직접 적재기를 적용한 것도 Qwen2.5-VL 두 저장소뿐이라 **선택 편향이 실재한다.**
프로젝트 전체에서 실측으로 확인된 위험 사례 셋(사전학습 원본 0.95 · `baseline_v2`
0.88 · AngelSlim 0.96)이 **전부 Qwen2.5-VL-3B의 변종**이다.

**실험.** 다른 계열 셋을 직접 적재기로 잰다 — `Isotr0py/Phi-3.5-vision-instruct-AWQ`
(Phi3V), `ELVISIO/SenseNova-SI-InternVL3-8B-AWQ`(InternVL), `ybelkada/llava-1.5-7b-hf-awq`
(LLaVA). 조건은 기존과 동일(원본 해상도 1,440,000 · 100장 · bf16 측정).

| 결과 | 해석 | 논문 반영 |
|---|---|---|
| **하나라도 여유 1 미만** | 위험이 Qwen2.5-VL에 국한되지 않는다 | **선택 편향 반박이 사라진다.** 6장의 실무 관련성을 "계열을 넘어 실측된 위험"으로 승격 |
| **셋 다 안전** | **가장 가능성 높다고 본다** — 앞서 잰 비-Qwen2.5-VL 셋(Qwen2-VL 2B·7B, SmolVLM)이 3.88~50.86배였다 | 주장을 **좁힌다**: "실측으로 위험이 확인된 범위는 Qwen2.5-VL 계열이다." **숨기지 않는다** — 안전 사례가 늘수록 *"구성만으로는 알 수 없으니 재야 한다"*는 처방이 오히려 정확해진다 |
| **적재 실패** | 파편화 사유 표에 추가 | 단, **저장소 접근 불가(429 등)는 파편화가 아니므로 따로 적는다** — 2026-09-18 확인 결과 `ybelkada/llava-1.5-7b-hf-awq`만 접근이 막혀 있고 다른 저장소는 익명으로도 0.2초에 받힌다 |
| **bf16에서 비유한** | 결과 아님 | 적재 검증부터 다시. 랜덤 초기화 사고(AngelSlim)의 재발 |

> **분기 하나를 뒤늦게 추가한다 (2026-09-18, 결과 보기 전).** 실행 중 알게 됐다 —
> **LLaVA-1.5와 Phi-3.5의 비전 인코더는 CLIP ViT-L/14-336**이고, 타워에 들어가는
> 입력이 **원본 해상도와 무관하게 고정**이다. `max_pixels`가 적용되지 않으므로
> **Qwen 계열과 같은 조건이 아니다.**
>
> **실측으로 확인했다 (같은 nuScenes 이미지를 해상도만 바꿔 넣음).**
>
> | 모델 | 1600×900 입력 시 타워 입력 | 해상도를 바꾸면 |
> |---|---|---|
> | Qwen2.5-VL | `(7296, 1176)` = 1,824 토큰 | **변한다** — 128~1,836 토큰 |
> | LLaVA-1.5 | `(1, 3, 336, 336)` | **안 변한다** — 640×360도 동일 |
> | Phi-3.5 | `(1, 5, 3, 336, 336)` 타일 5개 | **안 변한다** — 336×336~2560×1440 전부 5타일 |
>
> | | Qwen2-VL · Qwen2.5-VL · Qwen3-VL · InternVL | **LLaVA-1.5 · Phi-3.5** |
> |---|---|---|
> | 타워 입력 | **동적** | **고정** |
> | 논문의 붕괴 경로 | 해상도↑ → 토큰↑ → 활성↑ → 한계 초과 | **그 경로가 없다** |
>
> > **자기정정 — 내가 제기한 우려가 틀렸다.** 위 표를 쓰고 나서
> > *"Phi-3.5는 HD transform으로 타일 수가 이미지에 따라 변하니 '경로가 없다'는
> > 과하다"*고 적었다. **실측이 반증했다** — 이 프로세서 설정은 원본을 672×672로
> > 정규화한 뒤 2×2 서브타일 + 전역 1개 = **항상 5타일**을 만든다.
> > 336×336부터 2560×1440까지 전부 5개다. **추측으로 서술을 흔들지 않는다** — §3.5.
>
> **따라서 이 둘이 안전하게 나와도 "안전한 배포 사례"로 세지 않는다.** 정확한
> 해석은 *"고정 입력 타워에는 이 실패 양식이 적용되지 않는다"*이고, 이는
> **위험 범위를 좁히는 동시에 정확하게 만든다** — 논문의 현상은
> **동적 해상도 비전 인코더**에서 일어난다.
>
> **분류 기준을 바꾼다 — "동적/고정 해상도"가 아니라 "시퀀스가 자라는가"다.**
> 처음에는 InternVL을 동적 계열로 넣어 20 대 7로 세었다. 실측하며 알게 됐다 —
> **InternVL도 448×448 타일을 독립적으로 처리한다.** 타일 수는 늘지만
> **타일 하나의 시퀀스 길이는 고정**이다. Phi-3.5(336×336 × 5타일)·
> LLaVA-1.5(336×336 단일)와 같은 부류다.
>
> | | **단일 가변 시퀀스** | **고정 크기 타일 배치** |
> |---|---|---|
> | 계열 | Qwen2-VL · Qwen2.5-VL · Qwen3-VL | InternVL · Phi-3-V · LLaVA · Idefics3 |
> | 해상도를 올리면 | **한 시퀀스의 길이 S가 자란다**(128 → 1,836 토큰) | 타일 **개수**가 늘 뿐, 타일당 S는 고정 |
> | 어텐션 | S² 가 함께 자란다 | 타일마다 동일 |
> | 논문의 붕괴 경로 | **있다** | **없다** — 계산 자체가 변하지 않는다 |
> | 위험 구성 27개 중 | **19개** | **8개** |
>
> **관측된 붕괴는 전부 첫 열에서 나왔다.** `baseline_v2` 0.88 · 사전학습 원본
> 0.95 · AngelSlim 0.96 — 셋 다 Qwen2.5-VL이다. 그리고 지금까지 잰 둘째 열
> 여섯은 **전부 안전**하다.
>
> **실측 (2026-09-18) — 둘째 열 셋**
>
> | 모델 | 계열 | 타워 입력 | 활성 p50 | **여유(p50)** | 최악 |
> |---|---|---|---|---|---|
> | `ELVISIO/SenseNova-SI-InternVL3-8B-AWQ` | InternVL | 448² 단일 타일 | 155 | **422.61배** | 401.87배 |
> | `ybelkada/llava-1.5-7b-hf-awq` | LLaVA | 336² 단일 | 204 | **321.10배** | 291.13배 |
> | `Isotr0py/Phi-3.5-vision-instruct-AWQ` | Phi3V | 336² × 5타일 | 232 | **282.34배** | 257.89배 |
>
> 셋 다 fp16 한계에서 **세 자릿수** 떨어져 있다(활성 155~232 대 한계 65,504).
> 앞서 잰 SmolVLM(Idefics3, 50.86배)까지 합치면 **둘째 열은 4/4 안전**이다.
>
> > **InternVL3 측정의 범위를 정확히 적는다.** `AutoImageProcessor`가 준 것은
> > `CLIPImageProcessor`(448×448 고정)이고, InternVL의 배포 관행인
> > `dynamic_preprocess`(종횡비에 따라 1~12타일)를 **실행하지 않았다.** 따라서
> > 이 값은 **타일 하나의 활성**이다. 타일이 12개면 최대값이 순서통계량만큼
> > 오르지만, **여유가 422배라 12배가 되어도 35배**로 한계 근처에 가지 않는다.
> > 결론은 견고하나 **"동적 타일링 전체를 쟀다"고 쓰지 않는다.**
>
> **Qwen 밖에서 가변 시퀀스를 쟀다 — 기전 가설은 반쯤 기각됐다 (2026-09-18).**
> 조사 197개에는 Qwen 밖 가변 시퀀스 계열이 없어 **조사 밖에서 찾았다.**
> **Pixtral-12B**(Mistral)가 정확히 같은 설계다 — native resolution, 타일링 없음,
> RoPE-2D, 400M ViT를 **처음부터 따로 훈련**. 혈통이 완전히 다르다.
> 실측으로 가변임을 확인했다(320×180 → 240패치, 1280×720 → 2,304패치).
>
> **전체 측정 (여유 오름차순)**
>
> | 모델 | 기전 | 활성 p50 | **여유 p50** | 최악 |
> |---|---|---|---|---|
> | Qwen2.5-VL-3B `baseline_v2` | 가변 | 74,752 | **0.88** | 0.73 |
> | Qwen2.5-VL-3B 사전학습 | 가변 | 68,608 | **0.95** | 0.74 |
> | AngelSlim Qwen2.5-VL-3B-INT4-AWQ | 가변 | 68,096 | **0.96** | 0.76 |
> | AngelSlim Qwen2.5-VL-7B-AWQ | 가변 | 37,888 | 1.73 | 1.30 |
> | Qwen2-VL-2B-AWQ | 가변 | — | 3.88 | — |
> | Qwen2-VL-7B-AWQ | 가변 | — | 4.49 | — |
> | **Pixtral-12B** | **가변** | **1,824** | **35.91** | **28.63** |
> | SmolVLM-Instruct | 고정(타일) | — | 50.86 | — |
> | Phi-3.5-vision-AWQ | 고정 | 232 | 282.34 | 257.89 |
> | LLaVA-1.5-7B-AWQ | 고정 | 204 | 321.10 | 291.13 |
> | InternVL3-8B-AWQ | 고정 | 155 | 422.61 | 401.87 |
>
> **사전 등록한 세 분기 중 셋째가 실현됐다** — *"설계가 위험을 키우되 결정하지는
> 않는다."* 그대로 적는다.
>
> - **Pixtral은 안전하다**(35.91배). *"가변 시퀀스면 붕괴한다"*는 **성립하지 않는다.**
> - 다만 활성이 **1,824로 고정 입력 계열(155~232)의 약 8배**다. 설계가 크기를
>   **밀어 올리기는 한다.**
> - 그리고 Qwen2.5-VL(68,608~74,752)보다는 **약 40배 낮다.** 설계만으로 설명되지
>   않는 요인이 Qwen2.5-VL에 따로 있다.
>
> **그 요인은 이미 4장이 규명했다 — `down_proj` 849행이다.** 노름이 중앙값의
> **9.33배**이고 32개 블록 **전부에서 최상위**이며, 6변형에서 동일하다(미세조정이
> 바꾸지 않는다). **사전학습 가중치에 박힌 증폭기**다. Pixtral에는 그 증폭기가 없다.
>
> **따라서 서술을 이렇게 좁힌다.**
> > 가변 시퀀스 설계는 붕괴의 **전제**이지 원인이 아니다. 관측된 붕괴는 그 설계
> > **위에서 특정 채널이 증폭기로 자란** 경우에 일어났으며, 실측 범위에서 그것은
> > **Qwen2.5-VL 계열**이다. 설계만 같고 증폭기가 없는 Pixtral은 35.91배로 안전하다.
>
> 이 결론은 **4장의 기전 규명과 일관된다** — 논문은 처음부터 "849 채널이 증폭기"라고
> 말해 왔고, Pixtral이 그것을 **반대편에서 확인**한다. 두 열의 값이 겹치지 않는 것
> (가변 최대 35.91 < 고정 최소 50.86)은 흥미로우나 **n=11에 경계가 1.4배뿐이라
> 주장으로 쓰지 않는다.**

**미리 정해 두는 서술 규칙.** 셋 다 안전해도 **핵심 기여 넷(지표 역전·이산성·예고
값·움직이는 수단)은 영향을 받지 않는다** — 전부 우리 통제 실험에서 나온다. 약해지는
것은 **실무 관련성 하나**이고, 그것도 *"위험이 없다"*가 아니라 *"확인된 범위가 좁다"*다.
**이 구분을 흐리지 않는다.**

---

### 한계 ③(처방의 수명) 검증 — 해석 사전 등록 (2026-09-16, 결과 보기 전)

**왜 하는가.** 한계 ③은 *"bf16 커널이 성숙하면 1.32배 비용이 사라지고, 그러면
'그냥 bf16 쓰라'가 여유를 재지 않고도 통하는 처방이 된다"*이다. 이는 **검증 가능한
예측**이므로 추측으로 두지 않는다.

**측정 설계 — 이전 비교의 교란을 없앤다.** 10.13에서 잰 bf16 197.4 ms는 **무교정
그래프**였고 fp16 149.5 ms는 **교정 그래프**였다. 두 그래프를 섞어 비율을 냈다.
이번에는 **교정 그래프 하나에서 fp16·bf16을 모두** 재어 같은 조건으로 맞춘다.
그러면 둘 다 붕괴 0%이고 차이는 형식뿐이다.

| 조건 | TRT 10.13 | TRT 10.16 |
|---|---|---|
| 교정 그래프 · fp16 | 149.5 ms (기측정) | 측정 |
| 교정 그래프 · bf16 | **측정** | 측정 |

| 결과 | 해석 |
|---|---|
| 10.16에서 격차가 **줄었다** | **한계 ③이 실재함을 데이터로 보인다.** "커널 성숙도" 해석이 확정되고 처방의 수명 제약을 수치와 함께 적는다. 논문에 불리하지만 추측이 측정으로 승격된다 |
| 격차가 **유지됐다** | 두 버전에 걸쳐 유지됐다고 적을 수 있어 한계가 **약해진다.** 다만 "언젠가 닫힐 수 있다"는 가능성은 남으므로 **한계를 삭제하지 않는다** |
| 격차가 **커졌다** | 측정 잡음을 먼저 의심한다(앞서 7% 차이가 잡음 범위였다). 반복 측정 없이 주장하지 않는다 |
| 10.16에서 붕괴율이 달라졌다 | **그 자체가 결과다** — 런타임 버전에 따라 정밀도 배치가 바뀐다는 뜻이며, §6.3의 "런타임 의존성" 논거가 강해진다 |

**DLA 사양 확인 (2026-09-16 완료).** NVIDIA TensorRT 문서의 DLA 레이어 제약 절은
지원 정밀도를 **"Both FP16 and INT8 are supported"**로 적고 **BF16을 언급하지
않는다.** 즉 Jetson Orin에서 **DLA로 오프로드하는 배포에서는 bf16이 선택지가 아니다.**
(**2026-09-18 단서**: 본 연구 대상인 **Orin Nano에는 DLA가 없다** — AGX Orin·Orin NX
한정이다. §8 한계 ③ 참조.)
Orin을 쓰는 이유가 DLA 활용인 경우가 많으므로, **커널이 성숙해도 bf16이 만능
대안은 아니라는 근거**가 된다. 다만 GPU만 쓰는 배포에는 해당하지 않으므로
**한계 ③을 지우지는 못하고 범위를 좁힐 뿐이다.**
출처: https://docs.nvidia.com/deeplearning/tensorrt/latest/inference-library/dla-layer-restrictions.html

---

### 한계 ①②의 극복 — 해석 사전 등록 (2026-09-16, 결과 보기 전)

**실험 ② — λ 스윕 4점의 배포 조건 붕괴율 (한계 ② 합성 교란).**
검출 곡선 20점은 `baseline_v2`의 가중치를 직접 줄여 만든 **합성 연속체**이고,
자연 점은 여유 0.96/1.73/3.88/4.49/50.86배 다섯뿐이라 **1.73~50.86 구간이 비어
있다.** λ 스윕 체크포인트 넷은 **학습으로** 여유 1.79/2.51/3.43/4.61배에 놓여
있으므로 바로 그 구간을 자연 점으로 채운다. 원본 해상도 fp16 붕괴율만 재면 된다.

| 결과 | 해석 |
|---|---|
| 넷 다 붕괴 0% | 합성 곡선과 **일치**(여유 1.05 이상은 안전). 한계 ②가 사실상 닫힌다 |
| 일부가 붕괴 > 0 | **합성 곡선이 낙관적**이라는 뜻이다. 임계값을 자연 점 기준으로 올리고, 7.2의 보정을 다시 쓴다. 도구에 불리하지만 그대로 적는다 |
| 여유 순서와 붕괴 순서가 어긋남 | 여유가 붕괴의 충분한 예측자가 아니라는 뜻이다. **논문의 중심 주장에 직접 타격**이므로 원인부터 추적한다 |

**실험 ① — 공개 모델 3계열의 INT8 지표 (한계 ① 단일 계열).**
지표 역전은 지금까지 우리 여섯 모델에서만 쟀다. 스케일 불변성은 수학적 성질이라
계열 독립이어야 하지만, **측정은 한 계열뿐**이다.

| 결과 | 해석 |
|---|---|
| 안전한 공개 모델의 SNR이 우리 위험 모델보다 **낮다** | 역전이 3계열에서 성립. 기여 A를 계열 독립으로 쓴다 |
| **높다** | 역전이 우리 계열 안에서만 성립한다는 뜻. **기여 A의 범위를 "동일 아키텍처의 통제된 변형 간"으로 좁힌다.** 불리하지만 그대로 |
| 순서가 뒤섞임 | 계열 간 비교가 통제되지 않았다는 뜻(크기·데이터·구조가 모두 다르다). **"계열 간 비교는 통제 실험이 아니다"를 명시**하고 5변형 통제 실험만 근거로 남긴다 |

**미리 적어 둘 교란.** 공개 모델들은 크기·학습 데이터·아키텍처가 모두 다르므로
5변형처럼 **손실 항 하나만 다른 통제 실험이 아니다.** 결과가 유리해도 **일반성의
정황이지 인과가 아니라고** 본문에 적는다.

---

### 교정의 TensorRT 검증 — 사전 등록 (2026-09-16, 결과 보기 전)

**왜 하는가.** 7장은 「저정밀 배포 검증 파이프라인」인데 **마지막 칸이 비어 있다.**
현재 흐름은 *내보내기 → TRT fp16 엔진 → 붕괴 94.0% 재현*에서 끝나고,
**"그리고 처방은 그 런타임에서 작동한다"가 없다.** PyTorch에서는 교정이
1.36% → 50.73%를 만들었지만(§7.3), 그래프 내보내기와 엔진 빌드를 거쳐도 그런지는
확인되지 않았다. 가중치 스케일링뿐이라 위험은 낮으나, **낮은 위험을 근거로 검증을
생략하면 그 장이 파이프라인을 주장할 수 없다.**

**방법.** `headroom_guard fix`가 저장한 타워(`baseline_v2_fixa_native_tower.pt`,
PyTorch에서 50.73%를 만든 바로 그 파일)를 `--vision_state`로 내보내 원본 해상도
fp16 엔진을 빌드하고, 무교정 엔진과 **같은 50장**으로 붕괴율을 잰다. 손으로 굳히는
`--fixa`가 아니라 **도구 산출물 자체**를 런타임까지 옮기는 것이 요점이다.

| 결과 | 해석 |
|---|---|
| 붕괴 0% | **처방이 배포 런타임에서 검증된다.** 7장이 완결되고, 도구의 산출물이 PyTorch → ONNX → TensorRT를 통과함이 확인된다 |
| 붕괴 잔존 | **PyTorch와 TRT가 갈린다는 뜻이고 그 자체가 중요한 결과다.** 내보내기(그래프 상수 접힘)와 빌드(레이어 정밀도 배치) 중 무엇이 원인인지 추적한다. 7장의 결론을 "PyTorch에서만 검증"으로 좁힌다 |
| 빌드 실패 | 실패를 기록하고 7장을 "PyTorch에서 검증, 런타임 미검증"으로 범위 한정한다. 숨기지 않는다 |
| 무교정 대조가 94.0%에서 벗어남 | 같은 조건 재현이 안 된 것이므로 **결과가 아니라 디버깅 대상**이다 |

---

### 교정의 정확도 비용 — 배포 조건 기준선 · 사전 등록 (2026-09-15 23:10, 결과 보기 전)

**왜 하는가.** 도구의 교정이 배포 조건에서 작동함은 확인됐다(원본 해상도 fp16,
1.36% → **50.73%**, 붕괴형 출력 0개). 그러나 **비용을 말할 기준점이 없다.**
현재 가진 무붕괴 기준점은 전부 평가 해상도(bf16 50.63%, 붕괴 제외 fp16 50.71%)이고,
해상도 자체가 정확도를 움직인다(`full`: 원본 52.38% 대 평가 51.31%).
여기서 비교하면 **오늘 비판한 조건 혼입을 그대로 반복한다** — 8장의 "−0.59%p"가
평가 해상도·bf16 값이라는 이유로 이 실험을 걸었는데, 결과를 또 다른 교차 조건과
비교하면 같은 자리다.

**실험.** `baseline_v2` @ **원본 해상도 · bf16** · 같은 stride-11 부분집합 ·
예측 덤프. bf16은 상한이 3.4e38이라 오버플로가 없으므로, 같은 모델·같은 해상도·같은
문항에서 **교정 하나만 다른** 짝이 만들어진다.

| 결과 | 해석 |
|---|---|
| McNemar 비유의(p ≥ 0.05) | *"배포 조건에서 교정의 정확도 비용은 측정 한계 이하."* 조건이 통일된 최초의 근거. **"공짜"라고는 쓰지 않는다** — 비유의는 동등성의 증거가 아니다 |
| 교정이 유의하게 낮다 | **비용이 실재한다.** 이전의 −0.59%p(평가 해상도·bf16)를 이 값으로 **대체**하고, 도구 경고문도 바꾼다. 8장 결론을 약화시키지만 그대로 쓴다 |
| 교정이 유의하게 높다 | 측정 절차부터 의심한다 — 교정이 정확도를 올릴 기전이 없다 |
| bf16@원본이 fp16@평가보다 크게 낮다 | 해상도 효과가 모델별로 다르다는 뜻. **별도로 적고 교정 비용 논의와 섞지 않는다** |

---

### 오버플로 이전 fp16 비용 — 해석 사전 등록 (2026-09-15 22:00, 결과 보기 전)

**왜 하는가.** 평가 해상도(붕괴 0%)에서 `baseline_v2`가 fp16으로 48.49%인데 5-way 표는
50.30%(전체 11,309문항, bf16)다. **−1.81%p.** 같은 비교에서 `full`은 −0.38%p뿐이다.
평가 해상도의 여유는 `baseline_v2`가 1.31배로 얇고 `full`은 훨씬 크므로,
**여유가 얇을수록 더 떨어지는** 패턴으로 읽힌다.

사실이라면 §2 기여 2(이산성 — "혼재 0건, 중간 지대 없음")에 단서가 필요하다.
다만 현재 데이터는 **dtype(fp16 대 bf16)과 표본(stride-11 1,029문항 대 전체
11,309문항)이 섞여 있어** 판정할 수 없다.

**실험.** 두 체크포인트를 평가 해상도·**bf16**·같은 stride-11 부분집합에서 재측정한다.
표본이 동일해지므로 dtype 효과만 남는다. (각 8분)

| 결과 | 해석 |
|---|---|
| 둘 다 bf16 ≈ fp16 (CI ±3.1%p 이내) | 오버플로 이전 비용 없음. **이산성 주장 유지.** 1.81%p는 표본 효과였다 |
| **`baseline`만 격차, `full`은 없음** | **fp16은 오버플로 전에도 열화되며 그 크기가 여유를 따른다.** 이산성에 단서를 단다 — *"붕괴는 이산적이되 그 아래에 작은 연속 비용이 있다."* 직전 검토에서 이산성을 강화했으므로 **불리한 방향이지만 그대로 쓴다** |
| 둘 다 비슷한 격차 | 여유와 무관한 일반적 fp16 비용. **측정 주의사항으로만 적고** 여유 관련 발견으로 쓰지 않는다 |
| bf16이 더 낮음 | CI를 넘지 않으면 잡음. 넘으면 측정 절차부터 다시 본다 |

---

### 원본 해상도 정확도 · INT8 조건 통일 — 해석 사전 등록 (2026-09-15, 결과 보기 전)

**왜 하는가.** 두 구멍을 8차 비판 검토에서 찾았다.

1. **Table 1이 두 측정 조건을 한 표에 담고 있다.** 여유·붕괴율은 원본 해상도
   (1,440,000 픽셀, 250장), INT8 SNR·max/rms는 **평가 해상도**(200,704, 200장)에서
   쟀는데 표 제목은 "at native resolution (250 images)"다. 학술대회 제목이 이 표의
   **지표 역전**에 걸려 있으므로 조건을 통일해 다시 잰다.
2. **원본 해상도에서의 정확도를 한 번도 재지 않았다.** 정렬 모델이 원본 해상도
   fp16에서 NaN을 내지 않는다는 것은 알지만 **제대로 답하는지는 모른다.**
   오버플로 없이도 fp16 정밀도 손실로 열화될 수 있다.

**실험 1 — INT8 지표 원본 해상도 재측정** (`profile_int8_quant_error.py --max_pixels 1440000`)

| 결과 | 해석 |
|---|---|
| 역전이 유지된다 (여유 오름차순에 SNR 단조 감소) | 조건을 통일해 Table 1을 갱신한다. 제목의 주장이 같은 조건에서 성립한다 |
| 역전이 깨진다 | **초판 주장이 조건 혼입의 산물이었다는 뜻이다.** 그대로 보고하고 제목을 다시 검토한다. 유리하게 재해석하지 않는다 |
| max/rms가 여전히 비단조 | 예상된 결과다. 본문을 **"하나는 반대로 말하고 하나는 아무 말도 하지 않는다"**로 고친다 (현재 본문은 유리한 두 점만 골라 쓰고 있다 — 선택이다) |

**실험 1 결과 (2026-09-15 20:20) — 사전 등록한 첫 분기: 역전 유지.**

| 변형 | 여유 | 붕괴 | SNR(원본) | mr(원본) | SNR(평가) | mr(평가) |
|---|---|---|---|---|---|---|
| `baseline_v2` | 0.88 | 94.8% | **32.23** | 147.2 | 31.17 | 153.1 |
| `base_3b` | 0.95 | 69.6% | 31.56 | 173.2 | 29.05 | 163.0 |
| `temporal` | 1.06 | 28.8% | 30.52 | 205.4 | 29.34 | 162.5 |
| `kd_only_v4` | 1.10 | 20.4% | 30.41 | 208.3 | 28.90 | 160.8 |
| `spatial` | 2.89 | 0.0% | 25.72 | 173.7 | 24.89 | 137.2 |
| `full` | 2.92 | 0.0% | **22.88** | 238.2 | 23.10 | 168.0 |

- **SNR은 원본 해상도에서도 5변형 완전 단조 역전**이고, 낙차가 8.07 → **9.35 dB**로
  커졌다. 조건을 통일하니 주장이 강해졌다.
- **조건 통일은 실제로 필요했다.** 지표가 해상도에 따라 뚜렷이 움직인다
  (`temporal`의 max/rms 162.5 → 205.4, 26%). "스케일 불변량이라 해상도에 둔감할
  것"이라는 예상은 **활성 분포 자체가 바뀌면 성립하지 않는다.**
- **max/rms는 원본에서도 비단조**다(147.2 → 205.4 → 208.3 → **173.7** → 238.2).
  깨지는 지점(`spatial`)이 평가 해상도와 같으므로 조건 탓이 아니라 성질이다.
  사전 등록한 대로 본문을 **"하나는 반대로 말하고 하나는 아무 말도 하지 않는다"**로
  고쳤다.
- **중간 관찰의 자기정정.** 세 모델까지 보고 "원본에서는 max/rms도 단조"라고
  추측했으나 네 번째·다섯 번째에서 깨졌다. **부분 데이터로 추세를 말하지 않는다.**

결과: `eval_results/int8_quant_error_native.json`

---

**실험 2 — 원본 해상도 정확도** (NuScenes-QA val 1,000문항, fp16, 4조건)
`baseline_v2`·`full` × 원본(1,440,000)·평가(200,704) 해상도.

| 결과 | 해석 |
|---|---|
| `full`@원본 ≈ `full`@평가 (수 %p 이내) | **처방이 완성된다** — 정렬 모델이 배포 해상도에서 "NaN이 안 난다"를 넘어 "쓸 만하다"까지 간다 |
| `full`@원본이 뚜렷이 낮으나 `baseline`@원본보다는 훨씬 높다 | **붕괴는 없으나 열화가 있다.** 처방을 "오버플로 회피는 필요조건이지 충분조건이 아니다"로 고쳐 쓴다. 논문에 불리하지만 그대로 적는다 |
| `full`@원본이 더 높다 | 해상도가 도움이 된 것. **관찰로만 적고 주장으로 쓰지 않는다** — 1,000문항 부분집합이고 모델은 평가 해상도에서 학습됐다 |
| `baseline`@원본이 붕괴율 94.8%에도 0에 가깝지 않다 | **붕괴 → 오답 연결이 약하다는 뜻이다.** 논문 전체가 쓰는 "붕괴" 정의를 재검토해야 한다. 가장 불리한 분기이며, 나오면 숨기지 않고 5장 정의부터 다시 쓴다 |

표본은 1,000문항이므로 50% 부근에서 95% 신뢰구간이 약 ±3.1%p다. **그보다 작은 차이는
차이로 읽지 않는다.**

---

### 사후 리스케일 실험 — 해석 사전 등록 (2026-09-11, 결과 보기 전 작성)

**동기.** Qwen2.5-VL의 patch merger는 앞에 RMSNorm(`ln_q`)이 있어 blk31 출력이 토큰별로
정규화된 뒤 LLM에 전달된다. **균일 스케일은 정규화가 흡수하므로** blk31 MLP 출력을 일정
배수로 나눠도 표현이 수학적으로 동일할 수 있다. 사실이면 fp16 여유를 **학습 없이** 얻을 수
있다는 뜻이라, range 정규화 학습 실험(5.6 GPU-시간)을 하기 전에 먼저 가려야 한다.

**방법.** `student_baseline_v2`의 blk31 MLP 출력에 1/3을 곱하는 forward hook만 걸고
(i) 원본 해상도 fp16 붕괴율, (ii) NuScenes-QA 정확도(bf16 기준 대조)를 잰다. 학습 없음.

**예상.** 결과 1이 가장 유력하다고 본다(주관적 55%). 근거: align 모델이 blk31 18,000에서
정확도가 오히려 높고(51.69% 대 50.30%), 비전 MLP에 이미 LoRA가 걸려 스케일 조정 용량이 있다.

| 결과 | 서술 방침 (**지금 확정**) |
|---|---|
| **1. 붕괴 0% + 정확도 유지** | 여유는 **사후에 무료로 얻어진다.** 5장의 강조점을 "손실 설계가 결정한다"에서 **"기능적으로 무의미한 스케일이 배포 위험을 만들고, 아무도 측정하지 않아 방치된다"**로 옮긴다. 실무 기여는 오히려 강해진다(위험 구성 27개를 가중치 나누기로 수정 가능). `L_align`은 "그것을 자동으로 주는 손실"로 서술 수위를 낮춘다. **range 정규화 학습 실험은 불필요해지므로 취소한다.** |
| **2. 붕괴 0% + 정확도 하락** | 스케일이 기능적으로 유의미하다 → 여유는 학습 중에 벌어야 한다. **5장의 현재 서술을 그대로 유지**하고, `L_align`을 "정확도 대가 없이 여유를 얻는 방법"으로 정량 비교한다. range 정규화 실험을 예정대로 진행한다. |
| **3. 붕괴 유지** | 흡수 논리가 틀렸다 → 오버플로가 MLP 출력이 아니라 내부 계산(gate×up)이나 merger 내부에서 발생한다는 뜻. 5장 (a) 성분 분해를 MLP 내부까지 확장해 재규명한다. range 실험은 보류. |

**주의.** 예상(1번)과 논문에 유리한 결과(2번)가 어긋난다. 그래서 해석을 결과 전에 고정한다.
1번이 나와도 5장과 관통 논지는 영향을 받지 않는다 — 흔들리는 것은 4장의 강조점 하나다.

#### 실행 결과 (2026-09-11) — **3번**. 예상(1번) 빗나감

`scripts/probe_posthoc_rescale.py`, 원본 해상도 250장 + NuScenes-QA val 300문항.

| 조건 | fp16 붕괴율 | blk31 p50 | 정확도(bf16) |
|---|---|---|---|
| 무개입 | **94.4%** | 63,264 | 53.33% |
| 1/3 리스케일 | **94.8%** | 21,184 | **54.00%** (+0.67pp) |

무개입 94.4%는 해상도 스윕의 94.8%와 일치해 **재현성이 확인**된다.

**두 가지가 동시에 드러났다.**

1. **스케일은 기능적으로 대체로 무의미하다** — 3배 줄여도 정확도가 크게 흔들리지 않는다.
   merger의 RMSNorm이 흡수한다는 예측은 **맞았다.**
   (2026-09-14: 여기 적힌 +0.67pp는 300문항 값이다. fixA를 전체 11,309문항으로 재측정하니
   −0.59pp였으므로 **"무손실"이라는 표현은 쓰지 않는다.** fixB는 전체 재측정을 하지 않았다.)
2. **그럼에도 붕괴는 전혀 고쳐지지 않는다** — 오버플로가 hook보다 **먼저** 일어나기 때문이다.
   원본 해상도에서 baseline_v2의 blk31은 bf16에서 이미 74,752로 fp16 상한(65,504)을 넘는다.
   즉 MLP **내부 계산**에서 inf가 되고, NaN을 3으로 나눠도 NaN이다.
   (리스케일 조건의 21,184는 붕괴하지 않은 5%만 집계한 값이라 착시다.)

**서술 방침 (사전 등록 3번에 따름)**
- 5장의 "손실 설계가 결정한다"를 **그대로 유지**한다.
- **"그냥 사후 리스케일하면 되지 않나"라는 반론이 실험으로 막혔다** — §6 예상 질문에 추가.
- 동시에 **"이 크기는 배포 위험에 비해 기능 기여가 작다"**는 진술은 살아남았다
  (3배 축소의 대가가 0.59pp). 5장의 논지를 강화하되 **"무의미"·"무손실"로 쓰지 않는다**
  — 2026-09-14 전체 재측정에서 비용이 확인됐다.
- 사전 등록대로 **성분 분해를 MLP 내부까지 확장**해 오버플로 발생 지점을 특정한다(후속).
- range 정규화 학습 실험은 **보류**한다(사전 등록 3번 방침).

#### 후속: MLP 내부 개입(fixA) — 해석 사전 등록 (2026-09-11, 결과 보기 전 작성)

**동기.** 위 실험이 막은 것은 blk31 **출력** 스케일링뿐이다. 심사에서 나올 다음 수는
**"가중치를 줄이면 되지 않나"**이므로, 우리가 먼저 시험한다.

**방법** (`scripts/probe_mlp_overflow.py`, 원본 해상도 250장, fp16).
비전 MLP는 `down_proj( act_fn(gate_proj(x)) * up_proj(x) )`이다. gate/up 출력을
스케일하는 hook은 **weight와 bias를 함께 나누는** 가중치 편집과 등가이므로,
hook 개입이 가중치 편집을 대신한다. (bias 관련 정정은 위 §5 각주 참조.)
- **진단**: norm2 / gate / up / 곱 / down / 블록출력 중 inf가 **처음** 나타나는 지점
- **fixA**: gate·up 출력을 각각 1/√3 → 곱이 1/3 (down_proj 계산 **전에** 축소)
- **fixB**: down 출력만 1/3 (이미 실패한 조건, 대조군)

| 결과 | 서술 방침 (**지금 확정**) |
|---|---|
| **fixA 실패** (붕괴 유지) | **사후 수정 불가가 확정**된다. 5장의 "여유는 학습 단계에서만 얻을 수 있다"가 완성되고, "그냥 리스케일하면 되지 않나"라는 반론이 두 층위(출력·가중치) 모두에서 막힌다. 논문에 가장 유리하지만 **그래서 더 조심해서 서술한다** — "우리가 시도한 두 가지 사후 개입이 실패했다"이지 "사후 수정이 원리상 불가능하다"가 아니다. |
| **fixA 성공** (붕괴 0% + 정확도 유지) | **사후 가중치 편집으로 고쳐진다.** 5장의 강조점을 옮긴다: "손실 설계가 **결정한다**" → "손실 설계가 여유를 **자동으로** 주며, 그렇지 않은 모델도 사후 수정이 가능하다 — 다만 **아무도 측정하지 않아 방치되어 있다**". 실무 기여는 오히려 강해진다(위험 구성 27개에 적용 가능한 구체적 처방). **이 결과가 나오면 5장 제목·초록의 '결정한다'를 약화된 표현으로 고친다.** 유리하게 재해석하지 않는다. |
| **fixA 성공 + 정확도 하락** | 여유와 정확도의 교환 관계가 사후 개입에도 존재. `L_align`이 그 대가 없이 얻는다는 비교 우위가 정량화된다. 5장 현행 서술 유지. |

**주의.** fixA 성공은 논문의 현재 프레이밍에 **불리**하다. 그럼에도 시험하는 이유는
심사에서 반드시 제기될 반론이고, 우리가 먼저 재지 않으면 심사위원이 재기 때문이다.
결과가 어느 쪽이든 **그대로 보고한다.**

**실행 결과 (2026-09-11 → 2026-09-14 정정): fixA 성공, 다만 정확도 비용이 있다.**
붕괴 94.4% → **0.0%**. 정확도는 300문항에서 53.33% → 54.67%(+1.34pp)로 보였으나,
**전체 11,309문항에서 50.55% → 49.96%(−0.59pp)로 부호가 뒤집혔다.**
사전 등록표의 세 번째 분기("fixA 성공 + 정확도 하락")가 실제 결과다 —
"여유와 정확도의 교환 관계가 사후 개입에도 존재"한다.
최초 inf 지점은 세 조건 모두 `down_proj`.
등록된 방침대로 5장 강조점을 이동하고 §3·초록의 "결정한다"를 약화했다
(`paper_positioning_ksae2026.md` §3 상단 수정 표시 참조). 유리하게 재해석하지 않았다.

> **이 절의 −0.59pp는 이후 두 번 대체됐다 — 기록으로만 읽을 것 (2026-09-17 추가).**
> 이 절은 2026-09-11~14 시점의 기록이다. 그 뒤 두 가지가 밝혀졌다.
> (i) **2026-09-15 McNemar** — 같은 11,309문항을 짝지어 검정하니 p=0.2503으로
> 비유의였고, 같은 조건 재실행이 0.26%p 흔들려 **관측된 효과와 측정 잡음이 같은
> 크기**였다(4장). (ii) **§7.3** — 그 −0.59pp 자체가 **평가 해상도·bf16**에서 잰
> 조건 혼입 값이라, 배포 조건(원본 해상도·fp16, 오버플로 없는 bf16 기준선) 짝으로
> 다시 재어 **+0.78%p(p=0.34)**로 대체했다. **부호까지 뒤집혔으나 둘 다 비유의라
> 결론은 같다.**
>
> 따라서 아래 두 문장의 숫자는 **폐기된 값**이다 — *"fixA를 전체 11,309문항으로
> 재측정하니 −0.59pp"*, *"3배 축소의 대가가 0.59pp"*. 서술 방침(**"무의미"·"무손실"로
> 쓰지 않는다**)은 그대로 유효하되, 근거는 **"측정 한계 이하이나 불일치 54쌍이
> 있으므로 동등하지 않다"**로 바뀐다. 절 자체는 사전 등록 기록이므로 고치지 않는다.

---

## 6.5 디스크 정리 기록 (2026-09-17)

여유가 9.5 GB까지 줄어 **재생성 가능한 산출물만** 지웠다. 결과는 전부
`eval_results/`에 기록돼 있다. **9.5 GB → 21 GB.**

| 지운 것 | 크기 | 결과 기록 위치 | 재생성 |
|---|---|---|---|
| `/venv/trt2` (TRT 10.16) | 6.4 GB | `trt_fixa_native_1016.json` | `pip install tensorrt-cu12==10.16.1.11 cuda-python ml_dtypes numpy onnx`, 3분 |
| `trt_inputs.npz` (50장 전처리) | 1.6 GB | — (입력 캐시) | `scripts/prep_trt_inputs.py`, 2분 |
| `sweep_align*/step_{1000~4000}` | 3.3 GB | 미참조 (λ 스윕은 `step_5000`만 씀) | 재학습 필요 — **다만 어디서도 참조하지 않는다** |

**지우지 않은 것과 이유.**
- `onnx/*.onnx(.data)` — **Orin 보드 작업에 필요하다**(`orin_pkg/README.md`가 ONNX
  세 개를 보드로 옮기라고 지시). 정리 중 `tower_full_native`가 빠져 있음을 발견해
  **2026-09-17에 다시 내보냈다**(원본 대비 최대 오차 0.000e+00, ONNX 런타임 코사인
  중앙값 0.999999). 이제 셋이 모두 있고, 세 그래프의 입력 형상이 동일하다
  (`pixel_values=(7296, 1176)`, `grid=[[1, 64, 114]]`, 비전 토큰 1,824).

  | ONNX | 역할 | A100 fp16 기준값 |
  |---|---|---|
  | `tower_baseline_v2_native` | 붕괴하는 쪽 | 94.0% 붕괴 |
  | `tower_full_native` | 붕괴 안 하는 쪽 | 0.0% |
  | `tower_baseline_v2_native_fixa` | 처방 검증 | 0.0% (교정 후) |

  보드로 옮길 때 **`.onnx`와 `.onnx.data`를 같은 디렉터리에** 둘 것. 셋 합쳐 8.1 GB.
- `checkpoints/student_*` 전부 — 학습에 각 9.5~51시간이 들어간 대체 불가 자산.
- `checkpoints/student_{baseline,kd_only}`(v1, 1.9 GB) — 측정은 끝났으나
  (`eval_results/v1_vs_v2_profile.json`) 재학습 비용이 커서 남긴다.
- `checkpoints/baseline_v2_fixa_native_tower.pt` 1.3 GB — 도구가 산출한 교정
  가중치. Orin에서 처방을 검증할 때 그대로 쓴다.

---

## 7. 남은 할 일

### 보류 — 자원이 확보되면 진행 (2026-09-14 결정)

큐에 걸지 **않았다.** 지금 자원으로는 돌릴 수 없거나, 돌려도 앞 작업을 밀어내기만 한다.
조건이 충족되면 아래 그대로 실행한다.

| 항목 | 조건 | 명령 | 얻는 것 |
|---|---|---|---|
| **C3 대형 위험 구성** | 여유 디스크 **25GB 이상** | `python scripts/profile_other_vlm_families.py --limit 100 --cleanup --min_free_gb 25` | 위험 구성 27개 중 30B·32B·72B의 실측 여유. 목록과 디스크 가드는 이미 스크립트에 있다 — 가드가 부족분을 "디스크 부족"으로 결과에 기록하므로 **그냥 빠진 것과 구분된다** |
| ~~C4 시드 잡음~~ | — | — | **2026-09-14 생략 결정.** 아래 참조 |

**디스크를 25GB로 만드는 법** — 6장 INT4 재측정(큐에 있음)이 끝나면
`checkpoints_awq/`(16GB)를 지울 수 있다. 그것이 그 체크포인트의 유일한 용도였다.
**단, 지우기 전에 재측정 결과가 실제로 저장됐는지 확인할 것.**

**C5의 잔여(849행 기원)에는 큐를 만들지 않는다.** 사전학습 데이터·과정 접근이 필요해
자원 확보로 풀리는 종류가 아니다. 구조성 확인만 별도로 실행 중이다
(`scripts/probe_downproj_outlier_across_models.py`).

### C4(시드 잡음)를 생략한다 — 2026-09-14 결정

**완전 해소 비용을 이전 판에서 5배 낮게 적었다.** "15회 × 9.5시간 ≈ 6일"이라 썼는데,
9.5시간은 **5,000스텝** 기준이고 5-way 표는 **27,037스텝**(전체 에폭)이다.
실측 속도(0.1145분/스텝)로 다시 계산하면:

| | |
|---|---|
| 전체 에폭 1회 | **51.6시간** |
| 5변형 × 3시드 = 15회 | **774시간 ≈ 32일** |
| 시드 2개만 추가 (10회) | 516시간 ≈ 21일 |

**부분 측정(두 시드)도 하지 않는다.** 이유 셋:

1. 두 시드는 **차이값 하나**일 뿐 분산 추정이 아니다. 구간을 붙일 수 없다.
2. **운용점이 다르다.** 5,000스텝 대 27,037스텝, 단일 변형(`--variant spatial`,
   λ_align=1.0) 대 5변형. 학습이 길수록 시드 민감도가 줄어드는 것이 통념이나
   그건 측정이 아니므로, 유리한 방향(차이 ≥ 0.35pp)이 나와도 단서를 달아야 한다.
3. **논문의 결론이 바뀌지 않는다.** 처음부터 "정확도 순서를 주장하지 않는다"이고,
   C4는 그 주장을 뒷받침할 뿐 필요조건이 아니다.

**논문에 쓸 문장은 이것뿐이다** — "5-way 정확도 차이(0.35pp)는 단일 시드 관측이므로
순서를 주장하지 않는다." 시드 분산을 수치로 제시하지 않는다.


**2026-09-14 결정 — Orin Nano 배포를 논문에 포함한다.** 7장을 신설했고 기존 7·8장을
8·9장으로 옮겼다. 이로써 「실기기 검증」이 선택 항목에서 **필수 항목**이 되었으므로,
아래 목록의 우선순위가 바뀐다.

- [x] 4장 보강 — 어휘 크기 × 온도 스윕 **완료** (T=4에서 1K 대 152K가 31배 차이)
      결과: `eval_results/kd_temperature_vocab_sweep.json`, 스크립트: `scripts/probe_kd_temperature_vocab.py`
- [x] 6장 보강 — 공개 양자화 VLM 체크포인트 config 조사 **완료** (후보 281개 중 config를 읽을 수 있었던 197개, 그중 위험 구성 27개=13.7%, 공식 저장소 3개 포함)
      결과: `eval_results/quantized_vlm_config_survey.json`, 스크립트: `scripts/survey_quantized_vlm_configs.py`
- [x] 그림 2 생성 **완료** — `figures/fig2_resolution_collapse.{png,pdf}` (+`_single`)
- [x] INT8 결과 반영 **완료** — 5장 독립 절로 승격 (`eval_results/int8_quant_error.json`)
- [x] blk31 메커니즘 규명 **완료 (09-11)** — 전파 가설 기각. 개입 실험 + 성분 분해
      `scripts/probe_blk31_intervention.py`, `scripts/probe_blk31_decompose.py`,
      `eval_results/blk31_{intervention,decompose}.json`
- [x] λ 스윕 **완료 (09-12)** — 4점 전부. 최종 블록 단조 감소(여유 1.79→4.61배),
      중간층은 λ=1.0에서 포화. `eval_results/lambda_sweep_activation_profile.json`
- [x] 일반성 실험 **완료 (09-12)** — ②a CAM_BACK / ②b CODA-LM 순서 유지,
      ① SmolVLM은 여유 50.86배로 **계열 일반이 아님**
- [x] 그림 3 **완료 (09-12)** — `figures/fig3_lambda_dose_response.{png,pdf}` (+`_single`)
- [x] `Qwen2-VL-2B-AWQ` 재측정 **완료 (09-14)** — fp16 여유 **3.88배로 안전**.
      SmolVLM은 50.86배. → **위험 구성은 필요조건이지 충분조건이 아니다.**
      6장 서술을 "14%가 위험하다"가 아니라 **"위험 구성이 드물지 않은데 실제 위험 여부는
      측정해야만 알 수 있고, 아무도 측정하지 않는다"**로 쓴다. 논문의 처방과 일치한다.
      (같은 Qwen 계열에서도 세대·크기에 따라 base_3b 0.95배 대 Qwen2-VL-2B 3.88배로 갈린다.)
- [x] B 정렬 대상 ablation **완료 (09-14 19:28 KST)** — 사전 등록한 두 분기 **모두 빗나갔다**.
      `L_align`의 정렬 대상을 7B 교사 대신 **3B 자기 자신**으로 바꿔 5,000 step·λ=1.0으로
      학습(`checkpoints/aligntgt_base3b/step_5000`). 동일 250장 짝지어 비교(fp16, blk31 p50):

      | 변형 | 정렬 대상 | p50 | λ=0 대비 |
      |---|---|---|---|
      | `sweep_align0.0` | — | 36,480 | — |
      | **`aligntgt_base3b`** | **3B 자기 자신** | **27,664** | **−24.2%** |
      | `sweep_align0.5` | 7B 교사 | 25,560 | −29.9% |
      | `sweep_align1.0` | 7B 교사 | 19,232 | −47.3% |
      | `sweep_align2.0` | 7B 교사 | 14,464 | −60.4% |

      같은 λ=1.0에서 **자기 정렬이 교사 정렬 효과의 51%를 재현**한다. 즉 "정렬 제약 자체가
      원인"도 "교사 표현이 필요"도 아니고 **두 요인이 대략 절반씩 기여**한다. 어느 한쪽
      분기로 밀지 말 것. 부수 관찰: 3B 자기 정렬 λ=1.0이 7B 교사 정렬 λ=0.5와 거의 같은
      자리에 온다 — 교사 표현이 "특별한 무언가"라기보다 **더 강한 정렬 신호**로 작동함을
      시사하나, 두 지점의 일치라 근거가 약하므로 **관찰로만 적고 주장으로 쓰지 않는다**.
      결과: `eval_results/align_target_profile.json`, `eval_results/align_target_per_image.jsonl`
      **제목에 미치는 영향**: fixA(사후 재스케일만으로 붕괴 94.4%→0.0%)와 함께,
      "`L_align`이 여유를 **결정한다**"는 과하다. "좌우한다/영향을 준다" 수준까지 낮출 것.
- [x] 두 제목 **확정 (09-14)** — fixA·B 결과를 반영해 09-11 안에서 어감을 낮췄다.
      - 학술대회: ~~「지식 증류가 비전-언어 모델의 fp16 표현 범위 여유에 미치는
        영향: 자율주행 VLM 사례 연구」~~ → **2026-09-15 교체**:
        「자율주행 비전-언어 모델의 fp16 표현 범위 여유와 표준 압축 지표의 역전」
        (콜론 없음, "포착하지 못한다"보다 강한 "역전"을 씀). 요약문을 핵심 기여 중심으로 재구성하니 구
        제목이 **단일 시드 기여**(증류 손실 → 여유)를 논지 자리에 올리는 반면
        가장 단단한 기여는 **지표 역전**(산술, 시드 무관)이었다. 근거:
        `paper_positioning_ksae2026.md` §1
      - 학위 논문: ~~*Feature Alignment Shapes Representable-Range Headroom in
        Distilled Vision-Language Models*~~ → **2026-09-17 교체**:
        「자율주행 비전-언어 모델의 저정밀 배포를 위한 표현 범위 여유 측정과 진단」
        *Measuring and Diagnosing Representable-Range Headroom for Low-Precision Deployment of Autonomous-Driving Vision-Language Models*
        이유: `Shapes`가 §2에서 4순위로 강등된 기여(움직이는 수단)를 가리키고,
        A안 채택(§3)으로 범위가 측정·진단 축으로 좁혀졌다. 학술대회는 "발견",
        학위 논문은 "체계"로 역할을 나눈다. 근거: `paper_positioning_ksae2026.md` §1
      폐기: *Distillation Loss Design **Governs** …* — `Governs`는 (i) fixA(사후
      재스케일로 붕괴 94.4%→0.0%), (ii) B(자기 정렬이 효과의 51% 재현), (iii) 통계적으로
      뒷받침되지 않는 정확도 순서와 충돌한다. `Shapes`는 "형태를 만든다"이지 "혼자
      결정한다"가 아니라 셋 모두와 양립한다. 근거: `paper_positioning_ksae2026.md` §1
- [x] 코드/문서 명칭 통일 **완료 (09-14)** — `train_distillation.py`의 클래스·변수·
      CONFIG 키·CLI·로그 키까지. 구 이름은 alias와 `_normalize_config()`로 호환 유지.
      `--variant spatial`과 `spatial_proj.pt`는 기존 산출물 연결 때문에 의도적으로 유지.
      상세: `RENAME_PLAN.md`
- [x] **`headroom_guard` 3계열 검증 완료 (09-15)** — Qwen2.5-VL(`student_baseline_v2`,
      0.90배·위험) / Qwen2-VL(2B·7B AWQ, 3.83·4.52배·안전) / Idefics3(SmolVLM, 51.34배·안전).
      오탐 0, 미탐 0. 도구를 만들며 두 군데를 실측으로 고쳤다 — `tower.blocks` 하드코딩이
      Idefics3(`encoder.layers`)에서, 5-D `pixel_values`(B,타일,C,H,W)가 4-D를 기대하는
      타워에서. **"3계열에서 동작"이지 "모든 VLM에서 동작"이 아니라고 적을 것.** §8장.
      결과: `eval_results/headroom_guard_{baseline_v2,smolvlm}.json`,
      `guard_qwen2vl_{2b,7b}_awq.json`
- [x] **bf16 TensorRT 대조 완료 (09-15)** — fp32 0.0%/441.0 ms, fp16 94.0%/139.7 ms,
      **bf16 0.0%/197.4 ms(1.41배)**. 사전 등록한 두 해석 중 **"bf16이 느리다"** 쪽.
      등록 문장 그대로: *형식 선택 자체가 교환이며, 그 판단도 이 값을 재야 내릴 수 있다.*
      부수 소득: 활성 크기는 그대로인데 형식 상한만 올려 붕괴가 사라졌으므로 **표현 범위
      가설의 독립적 확인**이 된다. §6.3. 결과: `eval_results/trt_precision_bf16.json`
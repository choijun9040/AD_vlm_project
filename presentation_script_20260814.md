# 발표 스크립트 — 20260814.pptx (LAB Seminar, AVLAB 최준)

> **⚠ 2026-08-14 시점의 스냅샷이다. 최신 판단은 여기를 따르지 말 것.**
> 이후 검증에서 이 문서의 서술 몇 개가 뒤집혔다.
>
> | 이 문서 | 이후 |
> |---|---|
> | `object`에서 Full이 **"확실히 우세"** | **순위 주장 불가.** 5개 변형 모두 시드 고정 없이 1회 학습이라 43.87% 대 37~40%도 n=1 관측이다. "방향성 관찰"로만 쓴다 |
> | 전체 정확도에서 Full 우위 | **세 변형은 구별되지 않는다** (0.35pp는 시드 노이즈와 구별 불가) |
> | INT4 표(baseline 1.39% 등)를 정밀도 비교로 읽음 | **교란된 값.** dtype(fp16)과 해상도가 원인이며 정정 재측정이 큐에 있다 |
> | 증류 손실이 활성 크기를 **결정**한다 | **절반만 참.** fixA(학습 없이 붕괴 94.4%→0.0%)와 B(자기 정렬이 효과의 51% 재현)가 반증 |
>
> **최신 판단**: `thesis_outline_20260910.md`(장 구성·서술 규칙·실험 대장),
> `paper_positioning_ksae2026.md`(제목·초록). 이 파일은 **판단이 어떻게 바뀌었는지
> 추적하기 위해** 고치지 않고 남긴다.

실제 pptx(14슬라이드)를 그대로 읽어와서 슬라이드 내용에 맞춰 작성한 스크립트입니다.
슬라이드 2, 3에 이미 있는 노트는 유지하고 자연스럽게 이어지도록 구성했습니다.

---

## 슬라이드 1 — 표지 (LAB Seminar / 2026.08.14 / AVLAB 최준)

> 안녕하십니까, AVLAB 최준입니다. 이번 세미나에서는 자율주행 특화 VLM 증류 파이프라인
> 진행 상황을 보고드리겠습니다. 지난 세미나 이후로 손실 함수 설계를 크게 바꿨고,
> 5가지 방법을 전부 학습·평가까지 완료했습니다. 오늘은 그 변경 배경과 최종 결과,
> 그리고 그 과정에서 있었던 디버깅 사례 하나를 공유드리겠습니다.

---

## 슬라이드 2 — Driving-Aware Distillation: 기존 (L_task + L_hazard)

> 원래는 위험도 가중 증류(hazard-weighted KD)를 쓰고 있었습니다. 모든 샘플을
> 동일한 비중으로 증류하는 대신, 위험도가 높은 장면일수록 teacher 출력을 student가
> 더 정확히 모방하도록 가중치를 주는 방식이었습니다.
>
> 그런데 이 방식을 학습에 쓰기 전에, 위험도 라벨 자체의 분포가 1~5점 사이에서
> 심하게 불균형하다는 문제가 있었습니다. 외부 데이터셋을 섞는 것도 검토해봤고,
> 라벨링 프롬프트를 여러 번 재보정해보기도 했는데, 근본적으로 만족스러운
> 해결이 안 됐습니다. 그래서 접근 자체를 바꾸기로 했습니다.

---

## 슬라이드 3 — Driving-Aware Distillation: 변경 (L_task + L_spatial + L_temporal)

> 위험도 가중치라는 데이터 레이블링 문제를 우회해서, 아예 손실 함수 설계를
> 바꿨습니다. 출력 로짓을 모방시키는 게 아니라, teacher의 중간 표현(feature)을
> 직접 전이하는 방향입니다.
>
> **L_spatial(공간 정보 보존 증류)**: teacher와 student의 vision encoder 중간층에서
> patch 단위 hidden state를 뽑아서, 학습 가능한 projection과 cosine 손실로 정렬시킵니다.
>
> **L_temporal(시간적 맥락 압축 증류)**: teacher는 현재+과거 K개 프레임을 보고 얻은
> 답변 직전 hidden state를 갖는데, student는 항상 단일 프레임 1장만 보면서 이걸
> 모방하도록 학습시킵니다.
>
> (노트) **student의 추론 비용이 배포 시에도 늘지 않는 게 핵심입니다** — teacher만
> 여러 프레임을 보고, student는 배포 환경에서도 프레임당 1회 추론만 하면 됩니다.

---

## 슬라이드 4 — 학습 설정: 공통

**표**: Teacher(Qwen2.5-VL-7B+LoRA) / Student base(Qwen2.5-VL-3B) / lr(2e-5) /
num_epochs(1) / effective_batch(16) / total_steps(27,037) / GPU(단일 GPU)

> 5개 변형 전부 이 설정을 동일하게 유지했습니다. teacher, student 베이스 모델,
> learning rate, 총 스텝 수까지 전부 같고, 단일 GPU에서 순차적으로 학습을
> 진행했습니다. 다른 건 다음 슬라이드에서 보여드릴 손실 가중치와 배치 구성뿐입니다.

---

## 슬라이드 5 — 학습 설정: 변형

> 5개 변형 간 실제로 다른 값은 네 가지입니다.
>
> - **Full**: λ_spatial=1.0, λ_temporal=1.0, temporal_k=2 — 두 손실을 전부 사용
> - **Spatial only**: λ_spatial=1.0, λ_temporal=0, temporal_k=1 — L_temporal이
>   꺼져 있으니 teacher 멀티프레임 입력 자체를 생략해서 데이터 비용도 같이 절약했습니다
> - **Temporal only**: λ_spatial=0, λ_temporal=1.0, temporal_k=2
> - **Baseline**: teacher 관여 없이 task loss만, micro-batch만 4×4로 다르지만
>   effective batch는 16으로 동일하게 맞췄습니다
> - **KD-only**: teacher 출력 로짓만 모방하는 균등 가중 KD — 이 부분은 뒤에서
>   따로 자세히 말씀드리겠습니다

---

## 슬라이드 6 — 학습: Baseline (task CE only) — 30시간

> Baseline은 teacher 없이 student 단독으로 task loss만 학습한 결과입니다. teacher
> forward pass가 없어서 5개 변형 중 가장 빨리 끝났습니다. loss curve를 보시면
> 발산이나 정체 없이 깔끔하게 수렴하는 걸 확인할 수 있습니다.

---

## 슬라이드 7 — 학습: KD-only (L_task + L_kd) — 51.7시간

> 이 슬라이드에서 이번 주 있었던 가장 흥미로운 디버깅 사례를 공유드리겠습니다.
>
> KD-only를 처음 학습했을 때(v2), 학습 loss 자체는 정상적으로 보였는데 나중에
> 평가해보니 정확도가 1.78%로 완전히 무너져 있었습니다. 원인을 들여다보니, 정답
> 토큰 자체는 높은 확신도로 맞히는데 **"언제 답변을 멈춰야 하는지"를 전혀 학습하지
> 못해서** 정답 뒤에 숫자를 끝없이 이어붙이고 있었습니다.
>
> 처음엔 KD 손실의 가중치가 너무 커서 그런가 싶어 낮춰봤는데(v3), 문제가 그대로였습니다.
> 그래서 teacher 모델 자체의 확률을 직접 찍어봤습니다. teacher는 원래 온도 1
> 기준으로 멈춰야 할 위치에서 EOS 확률이 0.97까지 나올 정도로 정상이었는데,
> KD에서 쓰는 temperature 스케일링(T=4)을 거치면 이 확률이 0.0006 수준까지
> 뭉개진다는 걸 확인했습니다. 152,000개짜리 거대한 vocabulary에 일반적인 KD
> temperature(1,000개 클래스 기준으로 설계된 값)를 그대로 적용한 게 문제였던
> 겁니다.
>
> temperature를 1.5로 낮추자(v4) 바로 해결됐고, 최종적으로 지금 보시는 51.7시간짜리
> 결과를 얻었습니다. 여기 있는 loss curve는 이 v4, 즉 수정된 버전의 곡선입니다.

---

## 슬라이드 8 — 학습: Spatial only (L_task + L_spatial, λ_temporal=0) — 52시간

> L_spatial만 켠 결과입니다. loss curve에서 task loss와 spatial loss 둘 다
> 안정적으로 수렴하는 걸 볼 수 있고, 꺼져 있는 temporal loss는 로그만 되고
> 학습에는 반영되지 않아서 평탄한 값으로 남아 있는 게 정상입니다.

---

## 슬라이드 9 — 학습: Temporal only (L_task + L_temporal, λ_spatial=0) — 62.6시간

> L_temporal만 켠 결과입니다. teacher가 K=2 프레임(현재+과거 1장)을 계속
> 봐야 해서 spatial-only보다 오래 걸렸습니다. spatial loss는 마찬가지로 꺼진
> 채 평탄하게 로그만 됩니다.

---

## 슬라이드 10 — 학습: Full (L_task + L_spatial + L_temporal) — 63.2시간

> 두 손실을 전부 켠 최종 버전입니다. temporal-only와 비슷하게 teacher가 K=2
> 프레임을 봐야 해서 소요 시간이 비슷합니다. task/spatial/temporal 세 손실
> 전부 안정적으로 수렴하는 걸 확인했습니다.

---

## 슬라이드 11 — 평가: NuScenes-QA val (11,309 샘플, exact-match)

**표**: Full 51.69% (1위) / +L_spatial 51.40% / +L_temporal 51.34% /
Baseline 50.30% / KD-only 46.74%

**카테고리 설명**
- **exist**: 특정 대상의 존재 여부 (yes/no)
- **count**: 대상 개수 세기
- **object**: 지목된 대상이 무엇인지 식별
- **status**: 대상의 상태 (이동 중/정차 등)
- **comparison**: 두 대상 간 상대적 위치·관계 비교

> 5개를 전부 동일한 held-out 셋 11,309개로 평가한 결과입니다. 카테고리는 다섯
> 가지인데요, exist는 대상이 있는지 없는지 yes/no로 묻는 질문, count는 개수를
> 세는 질문, object는 지목된 대상이 무엇인지 맞히는 질문, status는 이동
> 중인지 정차 중인지 같은 상태를 묻는 질문, comparison은 두 대상 간 상대적
> 위치나 관계를 비교하는 질문입니다.
>
> Full이 단일 손실만 쓴 경우보다도 근소하게 앞섭니다 — 두 손실을 함께 쓰는 게
> 각각 따로 쓰는 것보다 낫다는 근거입니다. object 카테고리에서는 Full이 확실히
> 우세한데(43.87%), 이건 L_spatial의 패치 정렬 효과와 일치합니다. Baseline도
> 이미 50.30%로 준수해서, feature-KD의 순이득은 실재하지만 크지는 않습니다
> (약 1~1.4pp).
>
> KD-only가 최하위인데, 특히 exist 카테고리(67.68%)에서 크게 처집니다. 실제
> 생성 결과를 보면 판단 자체는 맞히는데 정답 뒤에 "yes, there are many"처럼
> 부연설명이 붙어서 exact-match에서 떨어지는 패턴이었습니다 — teacher의 DriveLM
> 혼합 학습 스타일이 KD를 통해 일부 전이된 잔여효과로 진단하고 있습니다.
>
> count는 5개 전부 7~11%대로 공통 약점입니다. 모델 규모나 데이터 쪽에서 오는
> 구조적 한계로 보고 있습니다.

---

## 슬라이드 12 — CODA-LM

**표**: Mini(50장) ~$4 / Test(500장) ~$40 / Val(4,384장) ~$350

> 다음 정량 평가축으로 CODA-LM을 검토했는데, 공식 채점 방식이 GPT-4o를
> judge로 쓰는 OpenAI API 기반이라 로컬이나 무료 대안이 없습니다. 표준
> API 가격 기준으로 스플릿별 예상 비용을 계산해봤고, 진행 여부와 예산은
> 아직 결정이 필요한 상태입니다.

---

## 슬라이드 13 — 차주 계획

- 종합시험 준비
- 산학프로젝트 챌린지 보고서 작성
- CODA-LM 평가 or 양자화+배포

> 다음 주는 종합시험 준비와 산학프로젝트 챌린지 보고서 작성이 우선순위입니다.
> 그 사이에 여유가 되면 CODA-LM 진행 여부를 정하거나, 양자화·배포 쪽으로
> 넘어가는 걸 시작해보겠습니다.

---

## 슬라이드 14 — 감사합니다

> 이상으로 발표 마치겠습니다. 질문 있으시면 편하게 해주세요.

---

## 부록 — 예상 질문 대비

**Q. KD-only 하이퍼파라미터를 검증셋 보고 조정한 게 공정한가요?**
> 정확도 수치를 비교해서 고른 게 아니라 "EOS가 제대로 나오는가"라는 정성적
> 진단이었습니다. 게다가 이 디버깅 이점을 감안해도 KD-only가 5개 중
> 최하위였다는 점이 오히려 결론을 더 보수적으로 뒷받침합니다.

**Q. λ_spatial, λ_temporal 값은 왜 1.0/1.0으로 고정했나요?**
> 현재는 on/off 형태(1.0 vs 0)로만 검증했고, 0.1~2.0 사이 중간값 스윕은
> 아직 안 했습니다. 시간 관계상 다음 ablation으로 남겨뒀습니다.

**Q. count 카테고리가 5개 전부 낮은 이유가 뭔가요?**
> 정확한 개수 세기는 이 모델 규모·데이터 조건에서 공통적으로 어려운 태스크로
> 보고 있습니다. 손실 설계보다는 모델 스케일이나 카운팅 전용 supervision이
> 필요한 구조적 한계로 판단하고 있습니다.

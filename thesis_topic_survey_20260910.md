# 최신 서베이 분석 및 석사 학위논문 주제 제안 (2026-09-10)

자율주행 VLM 최적화 분야의 2025–2026 서베이 6편을 "저자들이 스스로 명시한 미해결 문제"
중심으로 읽고, 본 프로젝트가 이미 확보한 자산과 대조해 학위논문 주제를 도출한다.

---

## 1. 읽은 서베이와 그들이 말한 공백

| 서베이 | 시점 | 명시한 미해결 문제 (원문 취지) |
|---|---|---|
| Rethinking Language's Role in Efficient VLA for AVs (arXiv:2608.30144) | 2026-08 | "효율–안전 프론티어가 정량화되어 있지 않다", "벤치마크 파편화로 계층 간 비교 불가", "L3(조건부 호출) 트리거 설계에 원칙적 해법이 없다", "실제 배포 근거가 희박하다" |
| A Survey on Efficient Vision-Language-Action Models (arXiv:2510.24795) | 2025-10 | 압축성 vs 표현력 균형, 효율 기법의 수렴 안정성 보장 부재, **효율 이득 대비 성능 저하를 비교하는 평가 지표가 미성숙** |
| A Survey on VLA Models for Autonomous Driving (arXiv:2506.24044) | 2025-06 | 실시간성(30Hz 제약), **"모델이 커질수록 증류·희소화가 필수"**, 시간적 상태가 부분적으로만 융합됨, 표준 평가·규제 시험 부재 |
| VLA for AD: Past, Present, and Future (arXiv:2512.16760) | 2025-12 | **"증류와 양자화는 주행 맥락에서 거의 탐구되지 않았다"**, 장기 시평 추론 취약, 폐루프 벤치마크 부재 |
| Rethinking Small VLM Quantization (arXiv:2607.08029) | 2026-07 | 비전 인코더가 양자화에 더 민감, **멀티모달 정렬에 양자화가 미치는 영향 이해 부족** (주행·증류 학생 모델은 다루지 않음) |
| When Flat Minima Fail: INT4 Quantization Collapse (arXiv:2604.15167) | 2026-04 | 평탄한 최소점이 INT4 강건성을 보장하지 않음. **순수 LLM(Pythia, next-token)만 다룸 — VLM·LoRA·증류 목적함수·생성/EOS 거동은 범위 밖** |

### 교차 독해 결론

세 가지가 반복해서 등장한다.

1. **압축 기법 간 "상호작용"을 아무도 측정하지 않는다.** 증류 논문은 FP16에서만, 양자화
   논문은 사전학습/지시학습 모델에서만 평가한다. "어떤 목적함수로 증류한 학생이 이후
   양자화에 강한가"를 통제 실험으로 답한 연구가 없다.
2. **효율 지표와 주행 성능 지표가 같은 논문에 등장하지 않는다** (2608.30144의 직접 표현).
   따라서 효율–정확도 파레토 프론티어가 그려진 적이 없다.
3. **조건부 호출(L3)의 트리거 설계에 원칙적 해법이 없다.** 언제 무거운 언어 추론을
   부를지 결정하는 신호를 아무도 제대로 정의하지 못했다.

---

## 2. 본 프로젝트가 이미 가진 자산

- **완전 통제된 5-way ablation**: 데이터·LoRA·optimizer·스텝 수·평가 프로토콜이 전부 동일하고
  손실 함수만 다른 학생 체크포인트 5개 (약 260 GPU-시간).
- **동일 5개에 대한 INT4(AWQ) 양자화 및 재평가 완료** (`checkpoints_awq/`,
  `eval_results/*_int4_nuscenesqa_val.json`, 2026-09-09~10 실행).
- **대형 vocabulary에서의 KD 온도 붕괴 진단 기록** (T=4 → EOS 확률 1e-4 붕괴, v2→v3→v4)과
  이를 위해 만든 forced-position EOS 로짓 프로브.
- **통제된 EM-VLM4AD 동일 데이터 비교** (single-camera / max_length=16).
- 미착수 상태로 남아 있는 **CODA-LM 도메인 이동 평가 축**.

> `data/hazard_labels.json`은 클래스 불균형(2~3점이 88.6%, 5점이 0.3%) 때문에 **연구 축으로
> 사용하지 않기로 이미 결정**되어 있다. 아래 주제 제안은 이 라벨에 의존하지 않는다.

---

## 3. 핵심 관측 — INT4 전후 순위 역전, 그리고 그 원인

FP16 결과와 INT4(AWQ) 결과를 나란히 놓으면 다음과 같다.

| 모델 | 손실 구성 | FP16 | INT4 | Δ |
|---|---|---|---|---|
| student_full | task + **align** + atc | 51.69% | **51.60%** | **−0.09pp** |
| student_spatial | task + **align** | 51.40% | **50.82%** | **−0.58pp** |
| student_temporal | task + atc | 51.34% | 30.49% | −20.85pp |
| student_kd_only_v4 | task + 출력 KD (T=1.5) | 46.74% | 32.99% | −13.75pp |
| student_baseline_v2 | task only | 50.30% | **1.39%** | **−48.91pp** |

이 표가 무엇을 재고 있는지 2026-09-10에 직접 검증했다. 결론부터: **변형 간 차이는 실재하지만,
원인은 INT4 가중치 양자화가 아니다.**

### 3.1 검증 절차와 결과

`scripts/eval_nuscenesqa_val.py`에 `--dump-predictions`를 추가해 샘플별 생성 결과·생성 길이·
EOS 방출 여부를 기록하고, 동일 30샘플(고유 이미지 6장)에 대해 설정을 하나씩 바꿔가며 비교했다.
평가 프로토콜(해상도 `min/max_pixels`, `max_new_tokens=16`, greedy)은 전 조건에서 고정했다.

| 조건 | 정확 | `!!!!` 붕괴 | EOS 미방출 |
|---|---|---|---|
| baseline_v2 · FP16 LoRA (bf16) | 19/30 | 0 | 2 |
| baseline_v2 · INT4 + **fp16 로드** | 10/30 | **13** | 14 |
| baseline_v2 · INT4 + **bf16 로드** | 16/30 | **0** | 1 |
| student_full · INT4 + fp16 로드 | 18/30 | **0** | 0 |

**(a) 붕괴의 형태**: 오답이 아니라 `!!!!!!!!!!!!!!!!`(토큰 id 0의 반복)이다. 정답 내용은 맞히되
멈추지 못하던 `kd_only_v2`의 EOS 붕괴와는 다른 현상이다 — **"압축이 EOS 신호를 먼저 파괴한다"는
초기 가설은 이 검증으로 기각됐다.**

**(b) 이미지 단위로 갈린다**: 6장 중 3장은 그 이미지에 딸린 질문이 전부 붕괴하고, 나머지 3장은
전부 정상이다. 질문 단위 혼재가 0건 — 텍스트가 아니라 이미지가 트리거다.

**(c) 붕괴 지점**: `scripts/probe_int4_nan.py`로 층별 활성값을 추적한 결과, NaN이 처음 나타나는
곳은 **vision tower의 출력(patch merger)** 이다. 그런데 vision tower는 애초에 양자화 대상이
아니었다 — 5개 체크포인트 전부 `modules_to_not_convert: ["visual"]`로 동일하다.

**(d) 진짜 원인**: AWQ 저장본의 config가 `torch_dtype: float16`이라, 양자화되지 않은 vision
tower가 학습 때의 bf16이 아니라 **fp16으로 실행**된다. Qwen2.5-VL vision tower의 중간 활성값은
fp16 상한(65,504)에 근접할 수 있고, 넘어가면 NaN → 이후 모든 층이 NaN → argmax가 토큰 0으로
고정된다. 로드 dtype만 bf16으로 바꾸자 붕괴가 13/30 → **0/30**으로 사라졌다.

### 3.2 그런데 왜 변형마다 다른가 — 여기가 진짜 발견이다

같은 이미지, 같은 층에서 측정한 vision tower 활성값 최댓값:

| 체크포인트 | `visual.blocks[16]` | `visual.blocks[24]` | `visual(out)` |
|---|---|---|---|
| student_baseline_v2 (task CE only) | 912 | **7,368** | NaN (fp16) |
| student_full (task + align + atc) | 55.5 | **128.5** | 61.0 |

**약 57배 차이다.** 그리고 이 차이는 INT4 체크포인트뿐 아니라 FP16 LoRA 체크포인트에서도
동일하게 관측된다(7,392 vs 128) — 즉 양자화의 산물이 아니라 **학습된 가중치 자체의 성질**이다.

`L_align`(교사 vision 특징과의 코사인 정렬)이 학생의 vision 인코더 활성 크기를 교사 수준으로
끌어내리는 반면, task CE만으로 학습한 기준선은 활성이 수천 단위까지 자란다. 그 상태로 축소
정밀도에 올리면 특정 이미지에서 표현 범위를 넘어 터진다.

> **주의**: 위 30샘플/6이미지는 스모크 검증이지 측정이 아니다. 정량 수치는 §5의 재평가로
> 다시 뽑아야 한다. 확정된 것은 메커니즘과 방향이다.

### 3.3 전면 측정 — DriveLM val 799장 × 7모델

`scripts/profile_vision_activations.py`로 vision tower만 forward하며 층별
max|activation|을 측정했다(bf16 = 참값, fp16 = 실제 배포 dtype의 NaN 발생률).

**bf16 통과, 층별 max|activation| 중앙값 (799장)**

| 모델 | 손실 | blk8 | blk16 | blk24 | blk28 | blk31 |
|---|---|---|---|---|---|---|
| base_3b | 미세조정 없음 | 42 | 840 | 8,256 | 8,256 | 33,792 |
| student_baseline_v2 | task CE only | 42 | 876 | 8,096 | 8,096 | **50,176** |
| student_kd_only_v4 | task + 출력 KD | 42 | 604 | 7,712 | 7,712 | 30,336 |
| student_temporal | task + atc | 45 | 932 | 7,872 | 7,872 | 34,304 |
| student_spatial | task + **align** | 38 | **56** | **124** | **187** | 18,432 |
| student_full | task + **align** + atc | 37 | **58** | **130** | **177** | 17,920 |
| teacher_7b | 교사 | 36 | 458 | 9,024 | 9,024 | 27,904 |

**서로 다른 두 현상이 층 깊이별로 나뉜다.**

- **중간 깊이(blk16~28) — `L_align`의 효과.** align이 있는 둘만 blk24가 124/130이고
  나머지 넷은 전부 7,700~8,300이다. **약 63배** 차이이며 경계가 깔끔하다.
- **마지막 블록(blk31) — massive activation과 fp16 한계.** 여기선 모든 모델이 수만 단위다.
  중요한 건 순서다: `base_3b` 33,792 대비 **task CE only 파인튜닝만 50,176으로 1.5배 키웠고**,
  증류 항이 하나라도 있으면 베이스 이하에 머문다(kd_only 30,336, temporal 34,304).
  align이 있으면 절반 수준(17,920~18,432)까지 내려간다.

**교사가 오히려 높다(blk24 = 9,024)** — 이 점은 정직하게 짚어야 한다. `L_align`은 코사인
손실이라 **방향만** 맞추고 크기는 제약하지 않는다. 따라서 활성 크기 감소는 "교사에 맞춘 결과"가
아니라 정렬 학습의 **부수 효과**다. 논문에서 "교사 수준으로 맞춰진다"고 쓰면 틀린 서술이 된다.

**fp16 통과 — 한계(65,504) 대비 여유와 NaN 발생률 (799장)**

| 모델 | blk31 p50 | p95 | max | 한계 초과 | NaN 이미지 |
|---|---|---|---|---|---|
| base_3b | 33,792 | 47,872 | 65,536 | 0.0% | 0/799 |
| student_baseline_v2 | 50,176 | **65,280** | **86,016** | **4.8%** | **38/799** |
| student_kd_only_v4 | 30,336 | 43,520 | 59,136 | 0.0% | 0/799 |
| student_temporal | 34,304 | 47,872 | 68,096 | 0.1% | 1/799 |
| student_spatial | 18,432 | 23,552 | 32,128 | 0.0% | 0/799 |
| student_full | 17,920 | 23,424 | 29,184 | 0.0% | 0/799 |
| teacher_7b | 27,904 | 36,096 | 46,848 | 0.0% | 0/799 |

NaN이 난 38장은 전부 bf16 blk31이 61,440~86,016인 이미지 — 한계선에 걸친 것들이다.
최초 NaN 발생 위치도 38장 전부 `block31`. §3.1에서 붕괴했던 그 이미지도 이 38장 안에 있다.

실제 배포 형태(LoRA 병합 후 fp16 저장 + LLM만 AWQ INT4)로도 같은 값이 나온다:
`awq_baseline_v2` 5.0%(40/799), `awq_full` 0.0%. 즉 §3.1의 30샘플에서 3/6이 붕괴했던 건
같은 scene의 연속 키프레임이라 뭉친 것이고, **이미지 단위 기본 붕괴율은 5% 수준이되
scene 단위로 상관**된다 — 주행 시스템 관점에선 흩어진 5%보다 오히려 나쁜 성질이다.

### 3.4 원래의 1.39%가 완전히 설명된다

남은 격차(5% 붕괴로는 1.39%가 안 나온다)는 **해상도**였다. 과거 INT4 평가는 AWQ 저장본에
딸린 processor를 그대로 썼다. 그 기본값 `max_pixels=12,845,056`은 명목상 학습·평가에서
쓴 `256*28*28=200,704`의 64배지만, **nuScenes CAM_FRONT 원본이 1600×900(1,440,000px)이라
실제로는 원본 해상도에서 포화한다** — 즉 유효 배수는 약 7.2배(vision token 256개 → 1,836개)다.
해상도를 그 상태로 올려 다시 재면:

**초기 2점 측정 (AWQ 배포본, 50장)**

| 조건 | baseline_v2 blk31 p50 | baseline_v2 NaN | full blk31 p50 | full NaN |
|---|---|---|---|---|
| 평가 해상도 (`max_pixels` 200,704) | 50,176 | 5.0% | 18,048 | 0.0% |
| 원본 해상도 (`max_pixels` 12,845,056 → 유효 1,440,000) | **72,704** | **92.0%** | 21,888 | **0.0%** |

**전면 스윕 (2026-09-10 완료, 250장 × 5해상도 × 6모델 — 그림 2)**

| 모델 | 128 | 256 | 512 | 1,024 | 1,836 tok |
|---|---|---|---|---|---|
| task CE only | 0.8% | 3.2% | 10.4% | 25.6% | **94.8%** |
| 사전학습 (미세조정 없음) | 0 | 0 | 0 | 0 | **69.6%** |
| task + `L_atc` | 0 | 0.4% | 0 | 0 | **28.8%** |
| task + 출력 KD | 0 | 0 | 0 | 0 | **20.4%** |
| **task + `L_align`** | 0 | 0 | 0 | 0 | **0.0%** |
| **task + `L_align` + `L_atc`** | 0 | 0 | 0 | 0 | **0.0%** |

원본 해상도에서 `L_align`을 쓴 둘만 살아남는다. 순서도 해석 가능하다 — **과제 손실 단독
미세조정이 사전학습 모델보다 나쁘다**(94.8% 대 69.6%). 붕괴는 1,024 → 1,836 구간에
집중되어 **안전 해상도에 절벽이 존재**한다.

패치가 많아질수록 활성이 커지고, 여유가 없던 `baseline_v2`는 92%의 이미지에서 터진다 →
**1.39%**. 여유가 충분한 `student_full`은 같은 조건에서도 0% → **51.60%**(FP16 51.69%와 거의 동일).
INT4 평가에 걸린 3.3~5.6시간의 실행 시간도 이 해상도로 설명된다.

**정리하면 과거 INT4 표는 "정밀도의 효과"가 아니라 "활성 여유가 얼마나 되는가"를 재고 있었다.**
그 양은 목적함수가 결정하며, 양자화 없이도 직접 측정할 수 있다.


---

## 4. 주제 제안

### [1순위] 특징 정렬 증류가 저정밀 배포 강건성에 미치는 영향 — 주행 VLM의 vision 활성 크기 관점

*Feature-Alignment Distillation Controls Vision-Encoder Activation Magnitude:
Implications for Reduced-Precision Deployment of Driving VLMs*

**주장할 명제**
1. 동일 아키텍처·데이터·하이퍼파라미터에서 **학습 목적함수만 바꾸면 vision 인코더의 활성
   크기가 약 두 자릿수 배 달라진다** (7,368 vs 128.5). 양자화와 무관하게 학습된 가중치의
   성질이며, 직접 측정 가능하다.
2. 그 크기가 **축소 정밀도 배포에서 생사를 가른다.** 활성이 큰 기준선은 fp16 vision tower에서
   이미지 단위로 NaN 붕괴를 일으키고, `L_align`으로 학습한 변형은 같은 이미지에서 무손실이다.
3. 따라서 배포 강건성은 양자화 알고리즘만의 문제가 아니라 **그 앞 단계인 증류 설계의 문제**다.
   기존 PTQ 연구가 전제하는 "잘 학습된 FP 모델"이라는 출발점 자체가 목적함수에 따라 다르다.

**왜 지금 이 주제인가**
- 서베이가 문자 그대로 요구한 공백이다: "증류와 양자화는 주행 맥락에서 거의 탐구되지 않았다"
  (2512.16760), "효율 이득 대비 성능 저하 평가 지표 미성숙"(2510.24795),
  "양자화가 멀티모달 정렬에 미치는 영향 이해 부족"(2607.08029).
- 선행 연구의 사각지대가 정확히 맞물린다. 2604.15167은 양자화 붕괴를 다루지만 순수 LLM만,
  2607.08029는 소형 VLM 양자화를 다루지만 증류 학생도 주행도 아니다. 활성 이상치를 다루는
  outlier-robust PTQ 계열(예: 2602.07596)은 **양자화 시점의 처방**이지, 애초에 이상치가 덜
  생기게 학습하는 축은 다루지 않는다.
- **추가 학습이 0시간이다.** 5개 학생(260 GPU-시간)과 INT4 변환이 이미 끝났다. 남은 것은
  재평가와 활성 측정이며 추론 비용만 든다(§5의 4번만 예외).
- 기존 포지셔닝을 버리지 않는다. 비대칭 시간 맥락 증류는 3장 그대로, 학위논문은
  "압축 방법 + 그 압축이 배포 정밀도에서 어떻게 버티는가"로 확장된다.

**논문 구성안**
- 3장: 비대칭 시간 맥락 증류(`L_atc`) + 특징 정렬(`L_align`) — 기존 파이프라인, FP16 5-way 결과
- 4장: 대형 vocabulary에서의 KD 온도 붕괴 — EOS 확률 프로빙, v2→v3→v4 반증 실험
- **5장(신규·핵심)**: 목적함수별 vision 활성 크기 측정과 저정밀 배포 강건성 —
  5-way × {bf16, fp16, INT4} 재평가, 층별 활성 프로파일, 이미지 단위 붕괴율
- **6장(신규)**: 압축 평가의 함정 — 정밀도 비교에서 dtype 같은 숨은 변수가 결론을 뒤집는
  사례로 본 검증 과정 자체를 보고

---

### [2순위] 불확실성 기반 조건부 호출 — L3 트리거 설계

*Uncertainty-Gated Invocation of Language Reasoning for Efficient Driving VLMs*

2608.30144이 "원칙적 해법이 없다"고 명시한 문제. 장면마다 무거운 언어 추론을 호출할지
결정하는 트리거를, **모델 자신의 예측 불확실성**(첫 생성 토큰의 엔트로피 / 상위 후보 간
확률 격차)으로 정의한다. 외부 라벨이 필요 없어 `hazard_labels.json`의 불균형 문제를
피해 가고, 트리거 임계값 하나로 효율–정확도 파레토를 직접 그릴 수 있다.

- 장점: 서베이가 직접 지목한 무주공산, 라벨 의존 없음, 기존 5개 체크포인트에 그대로 적용 가능
- 단점: 캐스케이드 추론 인프라와 지연시간 측정 환경을 새로 만들어야 함. 1순위보다 리스크 큼

### [3순위] 효율–정확도 파레토 벤치마크 구축

5개 변형 × {bf16, fp16, INT4} × {지연시간, 메모리, 정확도}를 한 장의 프론티어로 제시하는 것
자체가 서베이 두 편이 공통 지적한 공백을 메운다. 다만 방법론적 신규성이 약해
**1순위의 5장에 흡수시키는 편이 낫다.**

---

## 5. 남은 실험

§3의 검증으로 초기 체크리스트 1~3번은 끝났다 — 붕괴 원인(dtype+해상도 기인 활성 오버플로),
양자화 범위(vision tower 제외, 5개 동일), 활성 프로파일 전면 측정과 fp16 붕괴율 정량화.
결과는 `eval_results/vision_activation_profile.json`,
`eval_results/vision_activation_per_image.jsonl`. 남은 것:

1. **인과 방향 확인 (최우선).** `λ_align`을 0.5 / 1.0 / 2.0으로 짧게(예: 5,000 스텝) 학습해
   blk24·blk31 활성이 단조 감소하는지 본다. 지금까지는 "align이 있는 두 변형이 낮다"는
   **상관**이며, 이 실험이 있어야 인과를 주장할 수 있다. 유일하게 GPU 학습이 필요한 항목이고
   5장의 핵심 근거가 된다.
2. **5-way 정밀도 재평가 (bf16 로드, 고정 해상도).** 과거 INT4 표는 dtype·해상도 교란이 섞여
   무효다. 5개 × 11,309 샘플을 조건을 고정해 다시 측정해야 "정밀도 자체의 손실"이 나온다.
   교란이 제거되면 격차는 크게 줄어들 것으로 예상되며, 그래도 남는 차이가 진짜 결과다.
3. **해상도 축을 정식 실험 변수로 승격.** §3.4에서 해상도가 활성 크기를 좌우함이 드러났으므로,
   `max_pixels`를 3~4단계로 바꿔가며 "여유 → 붕괴율" 곡선을 모델별로 그린다. 배포 관점에서
   "이 체크포인트는 어느 해상도까지 fp16으로 안전한가"라는 실용적 질문에 답한다.
4. **활성이 큰 이미지의 공통 성질.** 붕괴한 38장이 어떤 장면인지(야간·역광·터널 등) 확인.
   안전 관련 장면에 몰려 있다면 서베이가 말한 "효율–안전 프론티어"에 직접 닿는다.
5. **AWQ 커널 복구.** `awq_ext` 부재 + torch 2.3의 triton에 `tl.interleave` 없음 →
   현재 순수 PyTorch 참조 구현으로 우회 중(`scripts/awq_compat.py`). 수치는 정확하나 느려서
   2번의 소요 시간을 지배한다.

## 6. 이미 학습된 5개 체크포인트에 대한 주석

완료된 5개 학습은 `hazard_oversample=True`(beta=0.5)로 실행되었다 —
`train_distillation.py` / `train_kd_only.py` / `train_baseline.py` CONFIG에 남아 있는 그대로다.
다만 이 값은 **5개 변형 전부에 동일하게 적용**되었으므로 ablation 공정성에는 영향이 없다.
따라서 재학습은 불필요하고, 논문에서는 이를 **기여로 서술하지 않고 고정된 데이터 샘플링
설정으로만 한 줄 기재**하면 된다. 이후 신규 실행부터는 CONFIG에서 꺼도 무방하다.

## 7. 명명 정리 (기존 결정 유지)

`paper_positioning_ksae2026.md`의 결정에 따라 `L_spatial → L_align`, `L_temporal → L_atc`.
본 문서도 그 명칭을 따랐다. 코드 반영은 아직 미완이므로 5장 실험을 시작하기 전에
일괄 변경하는 것이 혼선이 적다.

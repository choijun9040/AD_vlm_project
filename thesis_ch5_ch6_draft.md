# 학위 논문 5·6장 초안 (2026-09-15)

> **원래 KSAE 학회 원고로 쓴 것인데, 학회 양식이 「2페이지 이내 요약문」임이 확인돼
> 용도를 바꿨다.** 학회용은 `ksae2026_abstract.md`에 따로 작성한다.
> 이 문서는 분량 제약이 없는 **학위 논문 5장(사례 2)·6장(따름정리)**의 초안으로 쓴다.
> 구성은 개요(`thesis_outline_20260910.md`)의 장 구성에 맞춰 나중에 재배치할 것.

## 제목

*Feature Alignment Shapes Representable-Range Headroom
in Distilled Vision-Language Models*

> **제목 정리 (2026-09-15).** 이 문서는 학회 원고를 재구성하며 떼어 낸 **학위 논문
> 5·6장 초안**이므로 학위 논문 제목을 단다. 이전에는 학술대회 제목이 붙어 있었는데,
> 그 제목은 같은 날 교체되었다(구: 「지식 증류가 …여유에 미치는 영향」 →
> 신: 「자율주행 비전-언어 모델의 fp16 표현 범위 여유와 표준 압축 지표의
> 역전」). 학술대회와 학위
> 논문의 무게중심이 다른 것은 의도된 것이다 — 근거는 `paper_positioning_ksae2026.md` §1.

저자: ○○○¹ · ○○○¹  |  ¹○○대학교 ○○공학과

---

## 초록

자율주행용 시각-언어 모델(VLM)의 차량 탑재에는 경량화와 저정밀 연산이 함께 요구된다.
본 연구는 저정밀 배포의 성패가 **순전파 한 번으로 계산되는 값 하나 — 표현 범위 여유
(format_max ÷ max|activation|) — 로 사전에 갈리는데도, 압축 평가의 표준 지표에는 그 값이
없다**는 점을 보인다. Qwen2.5-VL-7B 교사를 3B 학생으로 증류하면서 데이터·LoRA 구성·최적화
설정·학습 스텝을 모두 고정하고 손실 항만 달리한 다섯 변형을 DriveLM 검증 키프레임에서
계층별로 측정하였다. 원본 해상도에서 특징 정렬 손실을 사용하지 않은 학생은 비전 인코더
마지막 블록의 여유가 **0.88배로 이미 한계를 넘어** 94.8%의 이미지에서 수치 발산과 생성
붕괴를 일으킨 반면, 정렬 손실을 사용한 학생은 2.92배로 모든 해상도에서 붕괴가 관찰되지
않았다. 활성 크기는 넘치지 않는 bf16에서 재고 붕괴는 fp16에서 관측한다 — 목표 dtype에서
크기를 재면 발산한 표본의 값을 잃어 여유를 과대평가한다(같은 데이터에서 0.88배가
1.03배로 보인다). **같은 두
모델에 대해 INT8 양자화 신호대잡음비는 32.23 dB 대 22.88 dB로, 표준 압축 지표는
붕괴하는 모델을 오히려 더 안전하다고 순위 매긴다.** 이상치 비율(147.2 대 238.2)은
방향이 같아 보이나 다섯 변형 전체로는 순서가 없다(§5.3). 오버플로
지점을 마지막 블록 down_proj의 단일 채널로 특정하였으며, 해당 채널은 32개 블록 전부에서
최상위 노름을 가지고 마지막 블록에서만 불연속적으로 증폭된다. 여유를 움직이는 수단은
하나가 아니어서, 정렬 손실의 가중치에 따라 단조 변화하고, 정렬 대상을 교사가 아닌 학생
자신으로 두어도 효과의 51%가 재현되며, 학습 없이 가중치를 재조정하는 것만으로도 붕괴가
제거된다. 조사한 범위에서 이 값을 보고한 공개 저장소는 없었다.

**주요어**: 표현 범위 여유(Representable-Range Headroom), 저정밀 추론(Low-Precision
Inference), 지식증류(Knowledge Distillation), 시각-언어 모델(Vision-Language Model),
자율주행(Autonomous Driving)

---

## 1. 서론

자율주행 인지에 시각-언어 모델을 도입하면 장면 이해를 자연어로 설명할 수 있어 사고 원인
규명과 신뢰 확보에 유리하다. 그러나 실용 성능을 내는 VLM은 수십억 파라미터를 가져 차량
탑재를 위해서는 경량화와 저정밀 연산이 함께 필요하다. 이때 통상 두 단계를 거친다 —
지식 증류로 모델을 줄이고, 양자화·반정밀도로 연산을 줄인다.

두 단계는 보통 독립적으로 평가된다. 증류는 정확도로, 양자화는 이상치 비율이나 양자화
신호대잡음비 같은 지표로 평가한다. **본 연구는 이 평가 체계가 놓치는 축이 있음을 보인다.**

fp16의 표현 한계는 65,504다. 어떤 층의 활성값이 이를 넘으면 그 이미지의 출력은 통째로
무너진다 — 정확도가 조금 떨어지는 것이 아니라 생성 자체가 실패한다. 이 실패는 **활성값의
절대 크기**에만 의존한다. 그런데 압축 문헌이 사용하는 지표는 대부분 **스케일 불변량**이다.
이상치 비율(max/rms)이나 양자화 SNR은 텐서 전체에 상수를 곱해도 변하지 않는다. 즉 두 축은
수학적으로 직교하며, 한쪽으로 다른 쪽을 판단할 수 없다.

본 논문은 이 직교성이 실제로 문제를 일으킴을 같은 모델에서 동시에 측정해 보인다. 기여는
다음과 같다.

1. **표준 압축 지표가 저정밀 배포 안전성에 대해 역방향 신호를 준다**는 것을 동일 모델·동일
   이미지에서 측정해 보였다. 붕괴율 94.8% 대 0.0%인 두 모델이 이상치 비율과 양자화 SNR
   에서는 반대 순서로 평가된다.
2. 오버플로 지점을 **마지막 블록 down_proj의 단일 채널**로 특정하고, 개입 위치에 따른
   성패 차이로 그 인과를 확인하였다.
3. 여유를 움직이는 수단이 여럿임을 보였다 — 정렬 손실 가중치, 정렬 대상, 사후 가중치 편집,
   그리고 반대 방향으로 LoRA 적용 범위.

---

## 2. 관련 연구

**압축 평가 지표.** 양자화 연구는 활성 이상치를 다루기 위해 이상치 비율, 채널별 스케일링,
양자화 SNR 등을 사용한다. 이 지표들은 모두 스케일 불변이며, 이는 정수 양자화에서 스케일이
양자화 파라미터로 흡수되기 때문에 타당하다. 그러나 **부동소수점 형식의 표현 한계는 절대
크기에 의존**하므로 같은 지표로 판단할 수 없다.

**대형 활성.** 트랜스포머의 특정 채널에 매우 큰 활성이 나타나는 현상은 언어 모델에서
보고되어 왔으며, 암묵적 편향으로 기능한다는 해석이 제시되었다. 본 연구가 관찰한 채널도
같은 부류로 보이나, 본 논문은 그 기원을 규명하지 않으며 **배포 시 표현 범위에 미치는 영향**만
다룬다.

**미세조정과 압축 친화성.** 미세조정 방식이 후속 압축의 난이도를 바꾼다는 관찰은 최적화기
축에서 보고된 바 있다. 본 연구는 손실 설계 축에서 같은 종류의 상호작용을 다루되,
**정확도가 아니라 표현 범위 여유를 결과 변수로** 삼는다는 점에서 구별된다.

**특징 정렬 증류.** 중간층 특징을 학습 가능한 정렬 계층과 함께 정합시키는 설계는 FitNets
이래 확립된 방법이다. 본 연구의 정렬 손실은 이 계열에 속하며 **새로운 기여로 주장하지
않는다.**

---

## 3. 측정 대상과 실험 설정

### 3.1 표현 범위 여유

층 ℓ의 출력에 대해 여유를 다음으로 정의한다.

> **여유(ℓ) = format_max / max|activation(ℓ)|**

fp16의 경우 format_max = 65,504이다. 여유가 1에 가까우면 그 층은 표현 한계에 걸쳐 있고,
1 미만이면 이미 넘어섰다. **계산 비용은 순전파 한 번이다.**

### 3.2 통제된 다섯 변형

Qwen2.5-VL-7B를 LoRA 미세조정한 교사에서 Qwen2.5-VL-3B 학생으로 증류하되, 데이터
(DriveLM + NuScenes-QA 통합, 샘플러 설정 동일), LoRA 구성(rank·alpha·대상 모듈),
최적화 설정(학습률·워밍업·유효 배치), 학습 스텝(27,037), 평가 프로토콜을 전부 고정하고
**손실 항만 달리하였다.**

| 변형 | 손실 구성 |
|---|---|
| Full | 과제 손실 + 특징 정렬 + 시간 맥락 |
| 정렬만 | 과제 손실 + 특징 정렬 |
| 시간만 | 과제 손실 + 시간 맥락 |
| 기준선 | 과제 손실만 |
| 출력 증류 | 과제 손실 + 출력 수준 KD |

### 3.3 평가

활성 프로파일은 DriveLM 검증 키프레임의 전방 카메라 이미지에서 계층별 max|activation|을
bf16·fp16 두 경로로 측정하였다. 정확도는 NuScenes-QA 검증셋 중 해당 키프레임에 대응하는
11,309개 샘플의 정확 일치로 평가하였다.

> **통계 관련 서술 원칙.** 다섯 변형을 각각 **시드 고정 없이 한 번씩만** 학습하였다.
> 따라서 전체 정확도의 0.35%p 내외 차이는 시드 노이즈와 구별되지 않으며, **본 논문은
> 변형 간 정확도 순위를 주장하지 않는다.** 활성 크기 차이(수십 배)와 붕괴율 차이
> (94.8% 대 0.0%)는 규모가 커서 이 제약을 받지 않는다.

---

## 4. 결과 (1) — 표준 지표가 역방향을 가리킨다

원본 해상도(1,440,000 픽셀 상한, 이미지 250장)에서 측정한 마지막 블록 여유와 fp16 붕괴율,
그리고 같은 모델의 INT8 양자화 지표를 함께 제시한다.

| 변형 | 여유 | 붕괴율 | max/rms | INT8 SNR (텐서) |
|---|---|---|---|---|
| 사전학습 (미세조정 전) | 0.95배 | 69.6% | 173.2 | 31.56 dB |
| 기준선 (과제 손실만) | **0.88배** | **94.8%** | 147.2 | **32.23 dB** |
| 출력 증류 | 1.10배 | 20.4% | 208.3 | 30.41 dB |
| 시간만 | 1.06배 | 28.8% | 205.4 | 30.52 dB |
| 정렬만 | 2.89배 | **0.0%** | 173.7 | 25.72 dB |
| Full | **2.92배** | **0.0%** | 238.2 | **22.88 dB** |

> **INT8 열도 원본 해상도로 통일했다 (2026-09-15).** 이전 판은 이 두 열만 평가
> 해상도(200,704)에서 잰 값이었다. 같은 조건으로 다시 재니 SNR 역전이 유지되고
> 낙차가 8.07 → **9.35 dB**로 커졌다. **max/rms는 원본에서도 비단조**이므로
> (`spatial`에서 깨진다) "두 지표가 역전한다"고 쓰지 않는다 — SNR은 완전 단조
> 역전, max/rms는 순서 없음. 결과: `eval_results/int8_quant_error_native.json`

> **여유 열 정정 (2026-09-15).** 이전 판의 1.05 / 1.03 / 1.15 / 1.12 / 2.85 / 2.90은
> **fp16에서 잰 크기**로 계산한 값이었다(`res_1440000.json`의 float16 pass와 여섯 개
> 모두 일치). 붕괴율 열은 bf16 실행에서 왔으므로 한 표 안에서 출처가 갈렸고,
> "여유 1.03배(한계 미초과)인데 94.8% 붕괴"라는 모순이 생겼다. bf16(참값) 기준으로
> 고쳤다. 8장이 설계 판단으로 지적한 바로 그 함정에 이 표가 빠져 있었다.

**핵심은 오른쪽 두 열이다 — 다만 둘이 같은 방식으로 실패하지는 않는다.**

**텐서 단위 INT8 SNR은 다섯 변형에서 완벽하게 역전한다.** 여유가 작을수록 SNR이 높다
(0.88배 → 32.23 dB, 2.92배 → 22.88 dB, 사이 세 점도 단조). 붕괴율 94.8%인 기준선이
붕괴 0%인 Full보다 9.35 dB 높게 평가된다. **구별하지 못하는 것이 아니라 순서가 반대다.**

**단, 완벽한 단조성은 양자화 입도에 걸려 있다.** 채널 단위로 재면
40.56 / 37.67 / 37.64 / **39.15** / 36.41 dB로 `spatial`에서 깨진다 — 이상치 비율이
깨지는 지점과 같다. **두 끝점의 역전은 남는다**(94.8% 붕괴 40.56 dB 대 0% 붕괴 36.41 dB).
실무 INT8 배포는 채널 단위가 흔하므로 **입도를 밝히지 않으면 유리한 쪽을 고른 것으로
읽힌다.** 스케일 불변성 논증 자체는 입도와 무관하므로 영향받지 않는다.

**이상치 비율은 역전조차 하지 않는다.** 두 끝점만 보면 같은 방향으로 보이지만
(147.2 대 238.2), 다섯 변형 전체로는 147.2 → 205.4 → 208.3 → **173.7** → 238.2로
`spatial`에서 단조가 깨진다. 붕괴율과 어떤 순서 관계도 없다.

> **서술 정정 (2026-09-15).** 이전 판은 두 끝점만 인용해 "두 지표 모두에서 붕괴하는
> 모델이 더 안전하게 평가된다"고 썼다. 유리한 두 점을 고른 것이다. 다섯 변형을 모두
> 보면 SNR만 역전하고 이상치 비율은 무정보다. **하나는 반대로 말하고 하나는 아무
> 말도 하지 않는다** — 정직한 서술이 오히려 강하다.

이는 우연이 아니라 정의상의 귀결이다. 두 지표는 스케일 불변이므로 절대 크기를 볼 수 없고,
fp16 오버플로는 절대 크기에만 의존한다.

**사전학습 모델이 이미 위험선에 있다**는 점도 주목할 만하다. 미세조정 전 Qwen2.5-VL-3B의
여유가 0.95배이고 원본 해상도에서 69.6%의 이미지가 붕괴한다. 이는 본 연구가 만들어낸 현상이
아니라 공개 모델이 이미 갖고 있는 성질이다.

### 4.1 해상도 의존성

붕괴는 입력 해상도에 강하게 의존한다(250장 기준).

| 입력 픽셀 상한 | 패치 수 | 기준선 | Full |
|---|---|---|---|
| 1,440,000 | 7,296 | **94.8%** | 0.0% |
| 802,816 | 3,864 | 25.6% | 0.0% |
| 401,408 | 1,920 | 10.4% | 0.0% |
| 200,704 | 924 | 3.2% | 0.0% |
| 100,352 | 462 | 0.8% | 0.0% |

정렬 손실을 사용한 변형은 모든 해상도에서 0.0%다. 기준선은 해상도가 높아질수록 급격히
악화된다. **동적 해상도를 지원하는 최신 VLM에서 이 축이 특히 중요함**을 뜻한다.

---

## 5. 결과 (2) — 오버플로 지점과 인과

### 5.1 단일 채널

마지막 블록의 down_proj 가중치에서 849번 행의 노름이 중앙값의 9.33배로, 2위(1.06배)와
현격한 차이를 보인다. 이 채널은 **32개 블록 전부에서 최상위**이며, 0~30번 블록에서는
1.58~2.79배로 평탄하다가 **마지막 블록에서만 9.33배로 불연속적으로 증폭된다.**

원본 해상도 50장에서 마지막 블록 출력의 최대 활성 채널을 세면 **50장 전부가 849번**이고,
그 값의 중앙값은 73,448로 fp16 한계 65,504를 넘는다. 즉 가중치 이상치와 활성 이상치가
같은 채널이다.

같은 구조는 다른 모델에도 있다. Qwen2.5-VL-7B도 849번(9.55배), 이전 세대인 Qwen2-VL-2B는
759번(14.80배)에서 같은 형태의 단일 행 이상치를 보인다. 3B와 7B는 비전 설정이 동일하나
가중치 값은 서로 달라(노름 벡터 최대 차이 0.949) 같은 가중치를 공유한 것이 아니다.

### 5.2 개입 위치가 성패를 가른다

마지막 블록 MLP는 `down_proj(act(gate(x)) ⊙ up(x))` 형태다. 오버플로는 down_proj의 출력에서
발생한다(곱 4,588 → 출력 63,200).

| 개입 | 곱 | down 출력 | 붕괴율 |
|---|---|---|---|
| 없음 | 4,588 | 63,200 | 94.4% |
| MLP **출력**을 1/3 | 4,588 | 63,360 | **94.8%** |
| **gate·up를 각각 1/√3** (곱이 1/3) | **1,530** | **24,800** | **0.0%** |

출력 스케일링은 실패한다. 오버플로가 down_proj 계산 **내부에서 이미** 일어나므로 무한대를
나누는 셈이다. 반면 곱이 만들어지기 **전에** 줄이면 붕괴가 사라진다. **이 비대칭이 오버플로
지점에 대한 가설을 검증한다** — 위치가 틀렸다면 두 개입이 같은 결과를 냈어야 한다.

정확도는 평가 해상도에서 50.55%에서 49.96%로 0.59%p 낮아졌다. 이 해상도에서는 무개입도
거의 붕괴하지 않으므로(3.2%), 이 값은 **개입의 순수 비용**이다. 원본 해상도에서는 무개입이
94.4%의 이미지에서 출력을 내지 못하므로 비용은 상쇄되고도 남는다.

---

## 6. 결과 (3) — 여유를 움직이는 수단은 여럿이다

### 6.1 정렬 손실 가중치

정렬 손실 가중치 λ를 0 / 0.5 / 1.0 / 2.0으로 두고 시드와 학습률 궤적을 고정한 채 5,000
스텝씩 학습한 결과, 마지막 블록 활성이 단조 감소하였다(**799장**, max_pixels 200,704).

| λ | 마지막 블록 p50 | λ=0 대비 |
|---|---|---|
| 0 | 36,608 | — |
| 0.5 | 26,112 | −28.7% |
| 1.0 | 19,072 | −47.9% |
| 2.0 | 14,208 | −61.2% |

> **표본 수를 명시하는 이유 (2026-09-15).** 같은 체크포인트인데 §6.2의 표는 다른 값을
> 쓴다(36,480 / 25,560 / 19,232 / 14,464). §6.1은 **λ 스윕 전용 실행(799장,
> `lambda_sweep_activation_profile.json`)**이고 §6.2는 `aligntgt_base3b`와 **짝지어
> 비교하기 위한 실행(250장, `align_target_profile.json`)**이다. 짝지은 비교는 같은
> 실행 안에서만 유효하므로 둘을 합칠 수 없다. 차이는 0.3~1.8%로 결론에 영향이 없으나,
> **표본 수를 적지 않으면 같은 값이어야 할 것이 달라 보인다.**
> 요약문의 Fig. 2는 799장 실행으로 그려지므로 본문 수치도 그쪽으로 통일했다.

전체 에폭 학습에서도 같은 방향이 유지되며, 감소율은 20,000 스텝 부근에서 62% 내외로
포화한다.

### 6.2 정렬 대상 — 교사가 필요한가

정렬 대상을 7B 교사가 아니라 **3B 학생 자기 자신**(동결 사본)으로 바꾸고 나머지 조건을
동일하게 학습하였다(이하 **250장** 짝지은 비교 — §6.1의 799장 실행과 값이 다른 이유는
위 주석 참조). 그 결과 λ=0 대비 24.2% 감소로, 교사 정렬(47.3%)의 **51%가 재현**되었다.
즉 효과의 절반은 교사 표현의 특수성이 아니라 **정렬 제약 일반**에서 온다.

### 6.3 반대 방향의 수단

LoRA 적용 범위를 LLM 디코더에서 비전 인코더 어텐션까지 확장한 단계에서는 활성이 **1.58배
증가**하고 여유가 2.18배에서 1.38배로 줄었다(붕괴 0/250 → 8/250). 다만 이 비교에는 데이터
샘플링 설정 변경이 함께 포함되어 있어 두 요인이 분리되지 않는다. **방향이 반대인 수단이
존재한다**는 것까지가 확인된 사실이다.

한편 **과제 손실 미세조정 자체는 활성을 키우지 않는다.** 사전학습 대비 오히려 10% 낮다
(33,312 → 30,024).

---

## 7. 논의

### 7.1 무엇이 부족한가

여유를 움직이는 수단은 넷이 확인되었다 — 정렬 손실, 정렬 제약 일반, 사후 가중치 편집,
LoRA 적용 범위. **부족한 것은 수단이 아니라 측정이다.**

검색으로 얻은 공개 양자화 VLM 저장소 후보 281개 중 **설정을 읽을 수 있었던 197개**를
조사한 결과, 27개(13.7%)가 저장 dtype이 fp16이면서 비전 인코더를 양자화에서 제외하는
구성이었다. 여기에는 모델 제작사 공식 저장소도 포함된다.
**조사한 범위에서 표현 범위 여유를 보고한 저장소는 없었다.**

> **분모를 정확히 적는다 (2026-09-15).** 13.7%의 분모는 281개가 아니라 **config를
> 읽을 수 있었던 197개**다. 나머지 84개는 config 접근 자체가 되지 않아 빠졌으므로
> 이 비율은 **자기선택 표본 위의 값**이다. 읽히지 않는 저장소가 체계적으로 다른
> 구성을 쓸 이유는 떠오르지 않으나, 확인하지 않았으므로 **편향이 없다고 말하지
> 않는다.** 이 논문이 다른 곳에서 "못 쟀다"를 결과로 다루는 것과 같은 취급이다.

**위험 구성이 곧 위험을 뜻하지는 않는다 — 그러나 위험한 실례가 있다.**
직접 측정한 결과는 양쪽으로 갈렸다.

| 저장소 | 여유(p50) | 판정 |
|---|---|---|
| Qwen2-VL-2B-Instruct-AWQ | 3.88배 | 안전 |
| Qwen2-VL-7B-Instruct-AWQ | 4.49배 | 안전 |
| SmolVLM-Instruct | 50.86배 | 안전 |
| **AngelSlim/Qwen2.5-VL-3B-Instruct-INT4-AWQ** | **0.96배** | **한계 초과** |
| **AngelSlim/Qwen2.5-VL-7B-Instruct-AWQ** | 1.73배 | 경계 |

> **2026-09-15 갱신.** 이전 판은 "측정한 세 모델은 모두 안전했다"였다. 같은 위험
> 구성의 공개 저장소를 더 재자 **본 연구 학생과 같은 아키텍처의 INT4-AWQ 배포본이
> 0.96배로 한계를 넘었다**(100장 p50 68,096 > 65,504). 그 저장소의 비전 키 390개에는
> 양자화 흔적이 없어, 비전을 제외한 양자화가 사전학습 원본의 성질을 배포본에 그대로
> 옮긴 것이다(원본 `base_3b`도 0.95배).
>
> **그리고 그 저장소는 표준 경로로는 잴 수 없었다.** `from_pretrained`가 키 규약
> 불일치(`model.visual.*` 대 `visual.*`)로 비전 타워를 조용히 랜덤 초기화해 NaN을
> 냈다. 비전 샤드만 받아 strict로 적재하는 별도 경로를 만들어야 도달했다.
> 시도한 13개 저장소 중 6개가 서로 다른 이유로 적재에 실패했다.

계열과 세대에 따라 여유가 두 자릿수 배 차이 나고 같은 구성에서도 양쪽이 나오므로,
**구성만으로는 알 수 없고 재봐야 한다**는 것이 정확한 결론이다. 달라진 것은 이제
**위험 쪽 실례가 있다**는 점이다.

### 7.2 한계

- **단일 모델 계열.** 정렬 손실의 효과는 Qwen2.5-VL에서만 검증하였다. 다른 계열에 적용해도
  같은 효과가 나는지는 학습이 필요해 본 연구 범위 밖이다.
- **정확도 순위 미주장.** 다섯 변형을 시드 고정 없이 한 번씩만 학습했으므로 전체 정확도
  차이는 순위 근거가 되지 못한다.
- **런타임 정책.** 본 연구의 붕괴 측정은 PyTorch fp16 경로에서 이루어졌다. 배포 런타임이
  레이어별 정밀도를 자동 선택하는 경우의 거동은 별도 검증이 필요하다.
- **849번 채널의 기원.** 왜 그 채널인지는 사전학습 과정에 접근해야 답할 수 있어 범위 밖이다.

---

## 8. 결론

저정밀 배포의 성패를 가르는 값이 순전파 한 번으로 계산되는데도, 압축 평가의 표준 지표에는
그 값이 없다. 더욱이 그 지표들은 붕괴하는 모델을 더 안전하다고 **역방향으로** 평가한다.
본 연구는 이를 동일 모델·동일 이미지에서 측정해 보이고, 오버플로 지점을 단일 채널로 특정
하였으며, 여유를 움직이는 수단이 여럿임을 확인하였다. 실무적으로는 저정밀 배포 전에
**대상 해상도에서 마지막 블록의 여유를 한 번 재는 것**으로 충분하다.

---

## 참고문헌

1. Qwen Team, "Qwen2.5-VL Technical Report," arXiv:2502.13923, 2025.
2. M. Sun, X. Chen, J. Z. Kolter, Z. Liu, "Massive Activations in Large Language
   Models," Conference on Language Modeling (COLM), 2024. (arXiv:2402.17762)
3. A. Romero, N. Ballas, S. E. Kahou, A. Chassang, C. Gatta, Y. Bengio,
   "FitNets: Hints for Thin Deep Nets," ICLR, 2015.
4. C. Sima, K. Renz, K. Chitta, L. Chen, H. Zhang, C. Xie, J. Beisswenger,
   P. Luo, A. Geiger, H. Li, "DriveLM: Driving with Graph Visual Question
   Answering," ECCV, 2024. (Oral)
5. T. Qian, J. Chen, L. Zhuo, Y. Jiao, Y.-G. Jiang, "NuScenes-QA: A Multi-Modal
   Visual Question Answering Benchmark for Autonomous Driving Scenario,"
   Proc. AAAI Conf. on Artificial Intelligence, Vol.38, No.5, pp.4542-4550, 2024.
6. J. Lin, J. Tang, H. Tang, S. Yang, W.-M. Chen, W.-C. Wang, G. Xiao, X. Dang,
   C. Gan, S. Han, "AWQ: Activation-aware Weight Quantization for On-Device LLM
   Compression and Acceleration," MLSys, 2024. (Best Paper Award)
7. J. H. Heo, S. Azizi, A. Fayyazi, M. Pedram, "CrAFT: Compression-Aware
   Fine-Tuning for Efficient Visual Task Adaptation," arXiv:2305.04526, 2023.
8. L. Cheng et al., "Online Pseudo-average Shifting Attention (PASA) for Robust
   Low-precision LLM Inference: Algorithms and Numerical Analysis,"
   arXiv:2503.01873, 2025.
9. vLLM Project, "[Bug]: Gemma 4 (31B/26B-A4B) vision outputs only \<pad\> under
   fp16 — vision_tower standardize overflows," GitHub Issue #40290, 2026-04-19.
   https://github.com/vllm-project/vllm/issues/40290

> **서지 확인 완료 (2026-09-16).** 1~9 전부 원문·게재처를 조회해 확인했다.
> 고친 것 넷:
> - **6 (AWQ)** — 가장 큰 오류였다. **arXiv판 제목과 MLSys 게재처를 섞어** 놓았다.
>   MLSys 2024판 제목은 *"…for **On-Device** LLM Compression and Acceleration"*이고
>   저자도 10인인데 6인만 적혀 있었다(arXiv v1 저자 목록). MLSys 2024 Best Paper.
> - **5 (NuScenes-QA)** — 권·호·쪽을 보강(Vol.38, No.5, pp.4542-4550). AAAI
>   수록본 제목은 "Multi-**M**odal"로 대문자다.
> - **2 (Massive Activations)** — arXiv만 적었으나 **COLM 2024** 정식 게재본이 있다.
> - **4 (DriveLM)** — "et al."을 전체 저자 10인으로 펴고 ECCV 2024 **Oral** 표기.
>
> 확인만 하고 그대로 둔 것:
> - **1 (Qwen2.5-VL)** — arXiv:2502.13923, 2025-02-19 제출. 다만 arXiv 저자는
>   **S. Bai 외 27인**이다. 기술 보고서를 "Qwen Team"으로 인용하는 것은 관행이므로
>   유지하되, 학위 논문 심사 규정이 개인 저자를 요구하면 "S. Bai et al."로 바꾼다.
> - **3 (FitNets)** — 저자 6인·ICLR 2015 모두 일치(arXiv:1412.6550).
>
> 인용 위치: 7은 §2 "미세조정과 압축 친화성", 8·9는 §2 각주 및 §7.2 "런타임 정책",
> 2는 §2 "대형 활성", 3은 §2 "특징 정렬 증류".

---

## 제출 전 체크리스트

- [ ] 저자·소속·교신저자 기입
- [ ] 학회 템플릿(한글/워드)에 이식, 분량 규정 확인 (전문연구발표 통상 2~10쪽)
- [x] 참고문헌 1~9 서지 확인 완료 (2026-09-16) — 6(AWQ)에서 arXiv 제목과 MLSys 게재처 혼입을 바로잡음
- [ ] **그림 1** — 계층별 활성 프로파일 (`figures/fig1_activation_profile_single.png`)
- [ ] **그림 2** — 해상도 × 붕괴율 (`figures/fig2_resolution_collapse_single.png`)
- [ ] **그림 3** — λ 용량-반응 (`figures/fig3_lambda_dose_response_single.png`)
- [ ] 초록 분량이 학회 규정에 맞는지 확인
- [ ] 코드의 `lambda_spatial`/`L_spatial` 명칭은 미변경 상태 — 원고의 `L_align`과
      대응 관계를 각주에 한 줄 명시할지 결정
- [ ] **7.2 "런타임 정책" 항목** — TensorRT 정밀도 검사 결과가 나오면 갱신할 것
      (자동 회피가 확인되면 한계가 아니라 본문 결과로 승격)
- [ ] fixA 정확도 −0.59%p에 대한 McNemar 검정 결과 반영

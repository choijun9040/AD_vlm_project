# 현재 연구의 기여점 정리 (2026-09-10)

> **⚠ 이 문서는 2026-09-10 시점의 스냅샷이다. 이후 판단은 여기를 따르지 말 것.**
> 09-11~09-14에 이 문서의 핵심 주장 몇 개가 바뀌었다.
>
> | 이 문서 | 이후 |
> |---|---|
> | C3 "증류 목적함수가 활성 크기를 **결정한다**" (인과 검증 중) | **절반만 참.** fixA(학습 없이 붕괴 94.4%→0.0%)와 B(자기 정렬이 효과의 51% 재현)가 반증. 논지를 "측정되지 않는 축 + 그 축을 다루는 여러 수단"으로 **재구성**했다 |
> | 세 갈래(C1 시간 증류 / C2 KD 온도 / C3 활성) 구조 | **「기여 하나 + 사례 둘」**로 통합 |
> | "849행 기원 미상"이 한계 | **해결.** 32/32 블록에서 최상위, 활성 50/50장 일치, Qwen 계열 공통 |
> | 해상도 교란 "64배" | **약 7.2배**로 정정 (max_pixels가 1,440,000에서 포화) |
> | 시드 분산을 측정할 예정 | **생략 결정** (C4). 5변형 × 3시드 = 약 32일 |
>
> **최신 판단은 다음을 따른다**: `thesis_outline_20260910.md`(장 구성·실험 대장·해석
> 사전 등록), `paper_positioning_ksae2026.md`(제목·초록·관련연구).
> 이 파일은 **판단이 어떻게 바뀌었는지 추적하기 위해** 고치지 않고 남긴다.

`paper_positioning_ksae2026.md`의 등급 체계를 잇되, 2026-09-10 검증에서 드러난 내용
(INT4 표 무효화, vision 활성 크기 발견)을 반영해 다시 정리한다. 등급은 **근거의 강도**이지
중요도가 아니다.

| 등급 | 뜻 |
|---|---|
| **[확립]** | 측정·반증·재현이 끝났다. 지금 논문에 그대로 쓸 수 있다. |
| **[측정 완료·인과 검증 중]** | 현상은 측정됐으나 인과 주장에는 실행 중인 실험이 필요하다. |
| **[보조]** | 그 자체로 기여라기보다 다른 기여의 신뢰도를 받치는 설계·통제. |
| **[주장 안 함]** | 우리 것이 아니다. 선행 연구에 귀속시킨다. |

---

## C1. 비대칭 시간 맥락 증류 (`L_atc`) — [확립: 방법론]

교사는 현재+과거 K프레임을 보고, 학생은 **현재 1프레임만** 보면서 교사의 시간 맥락 표현을
모방한다. 배포 시 학생의 프레임당 추론 횟수가 늘지 않는 것이 설계 제약이자 목적이다.

**선행 연구와의 차별점** — 시간 증류의 *방향*이 반대다.
- Patera et al. (ICLR 2026): 단일 프레임 교사(MobileCLIP)로부터 **학생이 미래(t+1)를 예측** →
  학생이 교사보다 시간적으로 풍부하다. 시간 정보를 **생성**한다.
- 본 연구: **교사가 멀티프레임, 학생이 단일 프레임** → 시간 정보를 **압축**한다.
  대상 과제도 이진 사고 예측이 아닌 자연어 생성 VQA이고, 압축 대상이 소형 이미지 인코더가
  아닌 범용 대형 VLM이다.
- Drive-KD (arXiv:2601.21288): 같은 Qwen2.5-VL 7B→3B 조합이지만 텍스트–비전 attention 행렬을
  MSE로 정렬하며, **시간 정보 미활용을 스스로 한계로 명시**한다. 본 연구가 그 지점을 다룬다.

**근거**: `student_temporal` 51.34% vs `student_baseline_v2` 50.30% (NuScenes-QA val 11,309).
**한계**: 단독 이득이 약 1pp로 작다. 기여는 성능 폭이 아니라 방향성 설계에 있다.

---

## C2. 대형 vocabulary에서의 KD 온도 붕괴 — [확립: 진단→반증→근본원인→수정→검증]

Hinton식 KD 온도(T=2~4)는 ~1,000 클래스 기준으로 설계됐다. 152K 토큰 vocabulary에 그대로
적용하면 교사의 EOS 목표 확률이 균등분포 수준으로 뭉개져 **생성이 붕괴**한다.

이 기여가 강한 이유는 결론이 아니라 **경로**에 있다. 네 단계를 전부 밟았다.

1. **현상**: `kd_only_v2`(T=4.0)가 val 1.78%. 다른 넷은 50%대. 내용은 맞히는데(`yes` p=0.99)
   EOS가 rank 수백~수십만 위에 머물러 멈추지 못함.
2. **첫 가설 반증**: "손실 크기 불균형"이라 보고 λ를 0.3으로 낮춘 v3 — 손실 비율은 개선됐으나
   `baseline_v2`가 이미 EOS를 깔끔히 학습한 동일 지점(step_5000)에서 여전히 미학습. **가설 기각.**
3. **근본 원인**: 교사 자신의 forced-position 로짓을 직접 프로빙. T=1에서 EOS는 0.24~0.97
   (대부분 1위)로 정상인데, T=4로 나눠 152K vocab에 softmax하면 **0.0004~0.0016까지 붕괴**
   (순위는 남아도 2·3위와 사실상 동률). "여기서 멈춰라"는 신호 자체가 사라진다.
4. **수정·검증**: T를 1.5로 낮춤. step_5000~epoch_1 각 지점에서 EOS가 rank 1(확률 0.4~1.0)로
   회복됨을 재확인.

**미개척 확인**: 2026년 *Rethinking the Role of Temperature in LLM Distillation*(arXiv:2606.00306)
조차 vocabulary 크기 민감도나 EOS 붕괴를 다루지 않는다.
**성격**: 재현 가능하고 실무적으로 즉시 쓸모 있는 발견. 대형 vocab VLM에 KD를 적용하는
누구에게나 해당된다.

---

## C3. 증류 목적함수가 vision 인코더 활성 크기를 결정한다 — [측정 완료·인과 검증 중]

**가장 새롭고, 가장 확장성 있는 축.** DriveLM val 799장 × 7모델 × 2 dtype 전면 측정
(`scripts/profile_vision_activations.py`).

**깊이별로 서로 다른 두 현상이 나뉜다.**

*중간 깊이(blk16~28) — `L_align`의 효과.* bf16 참값 중앙값:

| | base_3b | baseline_v2 | kd_only_v4 | temporal | **spatial** | **full** | teacher_7b |
|---|---|---|---|---|---|---|---|
| blk24 | 8,256 | 8,096 | 7,712 | 7,872 | **124** | **130** | 9,024 |

align이 있는 둘만 두 자릿수, 나머지 넷은 전부 7,700~8,300. **약 63배 차이이고 경계가 깔끔하다.**

*마지막 블록(blk31) — fp16 표현 한계(65,504).* 여기서는 모든 모델이 수만 단위(널리 알려진
massive activation)지만 **순서가 결정적이다**: 베이스 33,792 대비 **task CE only 파인튜닝만
50,176으로 1.5배 키웠고**, 증류 항이 하나라도 있으면 베이스 이하에 머문다. align이 있으면
17,920~18,432로 절반까지 내려가 한계선까지 여유가 2배 이상 남는다.

**여유가 배포 결과를 가른다.**

| 조건 | baseline_v2 | student_full |
|---|---|---|
| fp16 NaN율 (평가 해상도, 799장) | 4.8% | 0.0% |
| fp16 NaN율 (AWQ 배포본, 799장) | 5.0% | 0.0% |
| fp16 NaN율 (저장본 기본 해상도, 50장) | **92.0%** | **0.0%** |

**정직하게 짚어야 할 점**: 교사가 오히려 높다(blk24 = 9,024). `L_align`은 코사인 손실이라
**방향만** 맞추고 크기는 제약하지 않는다. 활성 감소는 "교사에 맞춘 결과"가 아니라 정렬 학습의
**부수 효과**다. 논문에 "교사 수준으로 맞춰진다"고 쓰면 틀린 서술이 된다.

**남은 검증**: 지금까지는 "align이 있는 두 변형이 낮다"는 **상관**이다. `λ_align`을
0 / 0.5 / 1.0 / 2.0으로 5,000 스텝씩 학습하는 dose-response 스윕이 실행 중이며
(`_orchestrate_lambda_sweep.sh`, 시드 1234 고정, 약 39시간), 단조 감소가 나오면 인과를
주장할 수 있다. 나오지 않으면 63배 차이는 align 자체가 아닌 다른 요인이며, 그 역시 답이다.

---

## C4. 압축 평가의 숨은 변수 — 정밀도 비교가 통째로 뒤집힌 사례 — [확립: 방법론적 경고]

INT4 평가에서 `student_baseline_v2`가 1.39%, `student_full`이 51.60%로 나와 "증류 목적함수가
양자화 강건성을 결정한다"로 읽혔다. **직접 검증한 결과 그 표는 정밀도를 재고 있지 않았다.**

원인 사슬을 실측으로 완전히 닫았다.
1. 붕괴 형태는 오답이 아니라 `!!!!!!!!!!!!!!!!`(토큰 id 0 반복) = 내부 NaN.
2. **이미지 단위**로 갈린다 — 한 이미지의 질문은 전부 붕괴하거나 전부 정상. 혼재 0건.
3. NaN 최초 발생 지점은 **vision tower 출력**. 그런데 vision tower는 애초에 양자화 대상이
   아니다(5개 전부 `modules_to_not_convert: ["visual"]`).
4. 진짜 원인 (a): AWQ 저장본 config가 `torch_dtype: float16`이라 양자화되지 않은 vision tower가
   학습 때의 bf16이 아닌 **fp16으로 실행**된다. 로드 dtype만 bf16으로 바꾸자 붕괴 13/30 → **0/30**.
5. 진짜 원인 (b): 저장본에 딸린 processor 기본 해상도가 학습·평가 값의 **64배**
   (`max_pixels` 12,845,056 vs 200,704). 그 해상도에서 baseline은 92%의 이미지에서 터진다 →
   **1.39%**. INT4 평가에 걸린 3.3~5.6시간의 실행 시간도 이것으로 설명된다.

**기여의 성격**: 서베이들이 "효율–안전 프론티어가 정량화되어 있지 않다"(arXiv:2608.30144),
"효율 이득 대비 성능 저하 평가 지표가 미성숙"(arXiv:2510.24795)이라고 지적한 자리에,
**정밀도 비교에서 dtype·해상도라는 숨은 변수가 결론을 통째로 뒤집는 구체적 사례**를 제공한다.
동시에 이 사건은 C3의 발견을 낳은 경로이기도 하다 — 무효화된 결과가 더 나은 관측량을 지목했다.

---

## C5. 통제된 다중 변형 비교 설계 — [보조]

데이터(`drivelm_ratio`, `hazard_oversample`), LoRA(rank/alpha/dropout,
`lora_target_vision_attn=True`), optimizer(lr, warmup), `effective_batch=16`,
`total_steps=27,037`, 평가 프로토콜(동일 11,309 샘플·동일 스크립트)을 **5개 변형 전부에 대해
로그 대조로 직접 확인**했다. 손실 함수만 변수다.

진행 중 발견한 공정성 결함도 스스로 고쳤다: v1 baseline/kd_only는 `lora_target_vision_attn`
추가 이전이라 학습 가능 모듈 집합이 달랐다(37,152,768 vs 41,124,224). v2로 재학습하고
**v1은 지우지 않고 부록 대조군으로 보존** — "vision-attention LoRA 용량 단독 효과"를 분리하는
공짜 통제 실험이 된다.

**남은 캐벗 2건** (논문에 명시할 것):
- `kd_only`의 `kd_temperature`/`lambda_kd`만 val 소수 샘플을 보며 조정됐다. 다만 조정 기준이
  정확도가 아니라 "EOS가 rank 1인가"라는 정성 진단이었고, 그럼에도 최하위라 결론을 보수적으로
  지지한다.
- **시드가 고정되어 있지 않았다.** 기존 5-way는 데이터 순서·LoRA 초기화가 실행마다 다르다.
  run-to-run 분산을 측정한 적이 없으므로 1pp 내외 차이는 그 폭 안일 수 있다. (이번 λ 스윕부터
  `--seed`를 도입해 고정한다.)

---

## C6. SOTA 비교의 공정성 통제와 불리한 결과의 그대로 보고 — [보조]

EM-VLM4AD 비교를 진행하던 중 두 가지 구조적 비대칭을 발견하고 **진행 중이던 평가를 중단**한 뒤
통제했다: 6뷰 입력 → `--single_camera`(CAM_FRONT 1장을 6슬롯에 복제), 생성 길이 캡 512 →
`--max_length 16`. 부수적으로 이미지 6회 중복 read를 제거해 eval을 8배 가속했다.

**결과는 우리에게 불리하다**: EM-VLM4AD 54.08% > `student_full` 51.69%, `count`에서는 18.35% vs
7~11%로 명확히 앞선다. **그대로 보고한다.** 본 연구의 기여는 특정 벤치마크 최고 성능이 아니라
압축 방법론이며, EM-VLM4AD에는 압축 대상이 되는 대형 교사 자체가 없고 고정 해상도 6뷰 CNN
구조라 QA 형식 밖 확장이 제한적이다.

---

## 기여로 주장하지 않는 것

- **`L_align`(특징 정렬 손실) 자체의 신규성.** 중간층 특징을 학습 가능한 projection과 함께
  정렬하는 설계는 FitNets 계열의 확립된 방법이고, ICLR 2026 논문의 `L_spatial`(projection + L2)과
  사실상 동일 구조다. 주장 가능한 것은 **코사인 거리 채택의 실증적 근거**뿐이다 — attention map
  MSE는 스케일이 0.0004까지 죽었고, projection 없이 hidden state를 직접 정렬하면 450스텝 동안
  loss가 0.437→0.438로 정체했다.
- **벤치마크 최고 성능.** C6 참조.
- **위험도(hazard) 관련 어떤 축도.** 클래스 불균형(2~3점 88.6%, 5점 0.3%)으로 연구 축에서
  제외했다. 완료된 5개 학습이 `hazard_oversample=True`로 실행됐으나 **5개 전부 동일**하므로
  공정성에는 영향이 없고, 논문에는 고정된 데이터 샘플링 설정으로 한 줄만 기재한다.

---

## 예상 반론과 현재의 답

| 반론 | 답 |
|---|---|
| feature-KD 순이득이 1.0~1.4pp로 작다 | 인정한다. 다만 C3이 성립하면 이득의 축이 정확도가 아니라 **배포 강건성**으로 옮겨간다 — 같은 정확도에서 fp16 붕괴율 4.8% vs 0%는 안전 관점에서 다른 크기의 차이다. |
| `count`가 전 변형 7~11%로 약하다 | 5개 전부 동일하게 약해 손실 설계로 해결되는 문제가 아니다. 모델 규모/데이터 체제의 한계로 보고한다. |
| EM-VLM4AD가 더 높다 | 통제까지 해놓고 그대로 보고한다. 기여는 압축 방법론이며 EM-VLM4AD에는 압축할 교사가 없다. |
| 시드 미고정, 1 epoch, 단일 데이터셋 | 실재하는 한계다. λ 스윕부터 시드를 고정했고, CODA-LM(도메인 분리)이 남은 일반화 축이다. |
| `L_align`은 FitNets 아닌가 | 맞다. 신규성을 주장하지 않는다. 위 "주장하지 않는 것" 참조. |

---

## 증거 대장

| 기여 | 산출물 |
|---|---|
| C1 | `checkpoints/student_temporal/`, `student_full/`, `eval_results/*_nuscenesqa_val.json` |
| C2 | `logs/` 내 kd_only v2/v3/v4 학습 로그, `eval_results/kd_vocab_temperature_analysis.json` |
| C3 | `eval_results/vision_activation_profile.json`, `vision_activation_per_image.jsonl`(11,186행), `scripts/profile_vision_activations.py`, `scripts/probe_int4_nan.py` |
| C4 | `logs/dump_baseline_v2_{int4,int4_bf16,fp16}.log`, `logs/probe_int4_nan*.log`, `eval_results/*_predictions.jsonl` |
| C5 | `progress_report_20260812.md` §6 공정성 감사, 각 학습 로그의 `print_trainable_parameters()` |
| C6 | `eval_results/em_vlm4ad_*_finetuned_singlecam_nuscenesqa_val.json`, `scripts/{finetune,eval}_em_vlm4ad_nuscenesqa.py` |
| 진행 중 | `_orchestrate_lambda_sweep.sh` → `eval_results/lambda_sweep_activation_profile.json` |
